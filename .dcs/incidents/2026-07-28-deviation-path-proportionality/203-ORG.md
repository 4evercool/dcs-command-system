<!--
203-ORG.md -- written by the IC after the chief(s) return, during
/dcs-plan. Records which positions are activated THIS period -- doctrine
principle 4, scalable activation. Re-written each period if activation
changes.

Written this period (Type 3) because activation differs from the
Type-3 default in one respect: execution mode is staged, not plain
parallel (plan.md step 5's own trigger for writing this file).
-->

# 203 — Organization (Operational Period 1)

**Incident:** deviation-path-proportionality
**Type:** 3
**Period:** 1

## Activated positions

| Position | Activated? | Who / model |
|---|---|---|
| Incident Commander | always | dcs-commander (Fable), transfer of command -- this session runs Sonnet 5, not Fable |
| Dispatcher | always | main session (Sonnet 5) -- intake, spawning, transcription, Owner relay; no command judgment |
| Planning Chief | yes | dcs-planning-chief, opus |
| Logistics Chief | no | Type 3, not activated |
| Safety Officer | yes | dcs-safety-officer, opus |
| Ops Specialists | 3 | dcs-ops-specialist x3, sonnet |

## Specialist roster this period

| ID | Territory (summary) | Tasking file |
|---|---|---|
| S1 | `dcs/workflows/execute.md`, `dcs/workflows/plan.md`, `agents/dcs-commander.md` -- the proportionate amendment path itself | `204-TASKING/S1.md` |
| S2 | `dcs/references/doctrine.md`, `dcs/references/doctrine-appendix.md`, `dcs/references/forms.md` -- the doctrine statement + provenance, inside the hot-path budget | `204-TASKING/S2.md` |
| S3 | `dcs/VERSION`, `package.json`, `CHANGELOG.md` -- release prep, citation-verified against S1/S2's output | `204-TASKING/S3.md` |

## Execution mode

**Staged, not plain parallel** — this is why this file exists this period.
S1 and S2 run in parallel (territories disjoint, and the Planning Chief
verified neither depends on the other's output: S2 does not rename any
doctrine heading S1's references depend on, does not touch
`schemas.md` which S1's citation checks depend on). S3 runs
**sequentially, after both S1 and S2 return** — its `CHANGELOG.md` entry
cites labels S1 and S2 create (the new `plan.md` step number, the amended
doctrine principle number), and S3's own tasking requires it to `grep`-verify
each cited label actually exists in the tree before writing the sentence
that cites it, per the incident's own Rec-2 discipline applied to its own
paperwork.
