# 204 — Tasking S2

**Incident:** deviation-path-proportionality
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

State the rule in doctrine and its provenance in the appendix, inside a
measured byte budget.

(a) **CRITERION 6:** amend `dcs/references/doctrine.md` principle 15 with
a dated clause stating the by-reference-not-retype rule generally — a
fact a prior seat established moves to a later artifact by file reference
plus regenerating command; the Dispatcher copies bytes or cites paths,
never retypes a value. Prefer the clause on principle 15: it is the same
rule — write the derivation, not the result — extended from durable
artifacts to transfer between seats, and 15 already carries dated
sub-clauses. A NEW principle 16 is the fallback only if the clause cannot
be stated inside 15 without distorting it; if you take the fallback,
re-measure the budget BEFORE writing and say so in your return.

(b) Optionally, and only if the budget still allows after (a), add a
short clause to principle 8 pointing at the proportionate path —
principle 8's line 59 is the sentence the field read as "a deviation
costs a full replan." If it does not fit, DROP IT and report which item
you dropped; never trim unrelated doctrine prose to make room.

(c) **CRITERION 8** — this is the binding constraint. Measured before any
edit: `doctrine.md` 23,387 B + `schemas.md` 13,296 B = 36,683 B
normalised, ceiling 37,888 B, headroom 1,205 B. Measure before and after
with the command in evidence required and report both actual numbers.

(d) **CRITERION 6 provenance** in `dcs/references/doctrine-appendix.md`:
follow the file's own idiom — a `**Principle N — <topic> (field lesson
<date>, v<version>).**` paragraph. Cite NO path into `vault/` or
`.dcs/`: neither ships (`package.json`'s files whitelist), so such a
citation is a dead pointer downstream and collides with `CLAUDE.md`'s
"Ship no project facts" rule — name the incident instead. Annotate the
"9 of `prod-tools-drift`'s 10 halts were not about the code" figure "as
of the incident's own close — it moves", exactly as the file's existing
historical figures are already annotated, and reconcile with the
existing nearby "eight of ten" figure so two unreconciled censuses do
not end up adjacent.

(e) `dcs/references/forms.md`: the `IAP-APPROVED` row's "Written during"
cell says "`/dcs-plan`, after `AskUserQuestion` approval", which the
amendment path makes incomplete; touch up that cell only.

**Hard constraints, each guarding a check that will otherwise go red:**
do NOT rename or delete any existing `##`/`###` heading in `doctrine.md`
(check 6 resolves workflow references against those headings, and those
workflows are S1's territory, not yours); do NOT add any line containing
`--halt-count` to `doctrine-appendix.md` ABOVE the existing one at line
209 — `tests/test_dcs_gate.py` takes the FIRST such line, strips
backticks and EXECUTES it across nine fixtures, and that test is
untouchable under criterion 4; do NOT reword or remove the verbatim
`GRAMMAR_LINE` quotation in any of your three files (check 12(c) names
one case per file); do NOT remove the real-hex stamp witnesses at
`forms.md:35` and `:37`; write NO Cyrillic anywhere — field measurement
1's log is in Russian and quoting the Owner's sentence verbatim would
turn check 9 red on a shipped file.

## File territory (may edit only within these globs)

- `dcs/references/doctrine.md`
- `dcs/references/doctrine-appendix.md`
- `dcs/references/forms.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/references/schemas.md`
- `dcs/references/typing.md`
- `dcs/workflows/**`
- `agents/**`
- `skills/**`
- `dcs/hooks/**`
- `tests/**`
- `dcs/templates/**`
- `dcs/VERSION`
- `package.json`
- `CHANGELOG.md`
- `.dcs/**`
- `vault/**`

## Evidence required in the return

- **CRITERION 8**, before and after, real numbers from:
  `python -c "d=open('dcs/references/doctrine.md','rb').read().replace(b'\r\n', b'\n'); s=open('dcs/references/schemas.md','rb').read().replace(b'\r\n', b'\n'); print(len(d), len(s), len(d)+len(s), 37*1024-(len(d)+len(s)))"`
  — last field is remaining headroom and must stay ≥ 0.
- `python tests/test_doctrine_integrity.py` — paste the tail plus every
  FAIL line; baseline 82/82. Named cases that guard your work:
  "principles: no duplicate numbers", "principles: contiguous from 1",
  "principles: heading count matches the list", "doctrine sections
  referenced by name exist", "hot-path budget: doctrine+schemas <= 37
  kB", "no Cyrillic anywhere in the shipped package", and the three "log
  grammar: `dcs/references/<file>` quotes GRAMMAR_LINE verbatim" cases.
- `python tests/test_dcs_gate.py` — baseline 100/100. This suite
  EXECUTES `doctrine-appendix.md`'s first `--halt-count` line against
  nine fixtures, so it is your specific regression signal, not a
  formality. Also paste: `grep -n -- '--halt-count'
  dcs/references/doctrine-appendix.md`, which must show the original
  line still first.
- `python tests/test_dcs_intake.py` — baseline 10/10.
- `git diff --stat -- dcs/references/schemas.md dcs/hooks/dcs_gate.py
  tests/` — must print nothing (`schemas.md` is your forbidden
  hot-path sibling; the other two are criterion 4).

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
