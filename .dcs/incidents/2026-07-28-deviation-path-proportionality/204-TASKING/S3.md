# 204 — Tasking S3

**Incident:** deviation-path-proportionality
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Prepare the release, and verify your own citations rather than retyping
them.

**CRITERION 9:** bump `dcs/VERSION` and the `version` field of
`package.json` from `0.6.10` to `0.6.11` — both files, one atomic change,
per `CLAUDE.md`'s version-sync rule and `test_doctrine_integrity.py`
check 1. Measured at plan time and re-confirmed by both the Planning
Chief and the IC: `npm view dcs-command-system version` returns
`0.6.10`, `dcs/VERSION` reads `0.6.10`, `package.json` reads `0.6.10` —
tree and registry are in sync, so this is an ordinary next release.

Add a new dated section to `CHANGELOG.md` describing this incident's
change under `0.6.11`.

Do NOT run `npm publish` — Owner-only with a 2FA OTP, explicitly out of
scope this period.

Write the changelog entry in English (`CHANGELOG.md` ships via
`package.json`'s files whitelist, though check 9's Cyrillic scan does not
reach it — so the guard will not catch a slip here and you must not make
one).

Every label your entry cites — the new `plan.md` step, the amended
doctrine principle — must be verified present by the greps in evidence
required BEFORE you write the sentence citing it. An empty grep means the
sentence is false: report a deviation, do not soften the wording. That
verification is the point of this tasking as much as the bump is. Run
this tasking only after S1 and S2 have both returned `done` — your
citations depend on their output existing in the tree.

## File territory (may edit only within these globs)

- `dcs/VERSION`
- `package.json`
- `CHANGELOG.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/references/**`
- `dcs/workflows/**`
- `dcs/templates/**`
- `dcs/hooks/**`
- `agents/**`
- `skills/**`
- `tests/**`
- `bin/**`
- `install.ps1`
- `install.sh`
- `.dcs/**`
- `vault/**`

## Evidence required in the return

- `cat dcs/VERSION` and
  `python -c "import json;print(json.load(open('package.json'))['version'])"`
  — both must print `0.6.11`.
- `python tests/test_doctrine_integrity.py` — the named case "version
  sync: `dcs/VERSION` == `package.json`" must be PASS; paste the tail
  plus any FAIL line.
- `grep -n '## 6c' dcs/workflows/plan.md` and
  `grep -nE '^1?5[a-z]?\.|^16\.' dcs/references/doctrine.md` — paste real
  output proving each label your CHANGELOG entry cites actually exists
  in the tree. Read-only on those files; they are S1's and S2's
  territory.
- `npm pack --dry-run` — paste the file list and confirm `vault/` and
  `.dcs/` are absent from it.
- `python tests/test_dcs_gate.py` and `python tests/test_dcs_intake.py`
  — baselines 100/100 and 10/10.
- `git diff --stat -- dcs/hooks/dcs_gate.py tests/` — must print nothing
  (criterion 4).

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
