# 204 — Tasking S1

**Incident:** deviation-path-proportionality
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Author the proportionate amendment path and its entry points.

(a) **CRITERIA 1 + 2:** add `## 6c.` to `dcs/workflows/plan.md` — the
single declaring site — stating: who may enter it (only from
`execute.md` step 6 or step 9, and only with an already-logged `command:
deviation -> amend_tasking|replan|escalate_owner` or `command: verdict ->
fix_taskings` entry, cited by its timestamp); its boundary conditions
(single-tasking or `.dcs/**`-only scope, no change to 202 criteria, no
change to any 204 territory, no premise change); the SYMMETRIC fallback
sentence, stating plainly that anything outside those conditions runs the
full steps-1-9 path unchanged, so the cheap route cannot be reached for a
genuine re-plan; which acts of steps 1-6 are skipped and which lint 4a
checks still run (checks 1/4/8 are degenerate when no territory changed —
say so rather than implying the lint is waived); and the explicit
ceremony count (full pass: ≥2 agent spawns + up to 2 Owner round-trips;
6c: 0 agent spawns + at most 1 Owner round-trip). End 6c by routing into
steps 7 and 8 UNCHANGED — do not restate or fork their mechanics, because
that is what keeps criterion 1's `marker_valid()` and sentinel-tally
clauses true by construction.

(b) Add the bounded exception to `plan.md` step 7's pre-stamp checklist:
on an amendment the ratifying entry is the command-point-3/4 decision
cited by timestamp, NOT a fresh `command: iap_review`. State why: the
checklist as written is satisfied by the period's first-pass entry and
would ratify nothing.

(c) **CRITERION 1 reachability:** in `dcs/workflows/execute.md` step 6,
replace the terminal "Tell the Owner to run `/dcs-plan` again." (line
173) with a routing line citing `plan.md` step 6c and naming which
dispositions may take it; do the same in step 9's `halt` → fix-taskings
branch and in the post-pass advisories paragraph (lines 289-302), whose
"the IC fixes them itself, now" instruction currently collides with the
gate — see the measured contradiction in the IAP's risks section. Keep
lines 168-170's statement that the edit voids the hash: it stays TRUE and
is the doctrine, not the defect.

(d) **CRITERION 5:** rewrite the two command-point spawn instructions in
`execute.md` — step 6's deviation-arbitration spawn and step 9's
verdict-disposition spawn — so each instructs the Dispatcher to pass the
SOURCE (file path plus line range, or the command that regenerates the
value) for any fact a prior seat already established, instead of
summarizing or retyping it. These two satisfy criterion 5's minimum of
two on their own; do NOT touch `plan.md` step 3's chief spawn, whose
inline-text mandate is a deliberate information-diet requirement, not a
retyping defect (see the IAP's risks section).

(e) Amend `agents/dcs-commander.md`'s disposition descriptions (lines
55-58 and 66-71) so `amend_tasking` and `fix_taskings` name the
proportionate route; add NO new enum token.

(f) **CRITERION 3:** produce the side-by-side ceremony count for both
cited field measurements against the steps you just wrote, as described
in evidence required below.

## File territory (may edit only within these globs)

- `dcs/workflows/execute.md`
- `dcs/workflows/plan.md`
- `agents/dcs-commander.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/hooks/**`
- `tests/**`
- `dcs/references/**`
- `dcs/templates/**`
- `dcs/workflows/close.md`
- `dcs/workflows/deploy.md`
- `dcs/workflows/esg.md`
- `dcs/workflows/init.md`
- `dcs/workflows/loop.md`
- `dcs/workflows/new.md`
- `dcs/workflows/run.md`
- `dcs/workflows/status.md`
- `agents/dcs-safety-officer.md`
- `agents/dcs-planning-chief.md`
- `agents/dcs-logistics-chief.md`
- `agents/dcs-ops-specialist.md`
- `agents/dcs-situation-analyst.md`
- `skills/**`
- `dcs/VERSION`
- `package.json`
- `CHANGELOG.md`
- `.dcs/**`
- `vault/**`

## Evidence required in the return

- `python tests/test_doctrine_integrity.py` — paste the tail plus every
  FAIL line. Baseline before your edit is 82/82; the total may
  legitimately GROW because checks 12(c)/13(d)/14(d) emit one named case
  per discovered population file, so compare WHICH cases fail, not the
  count. Named cases that guard your three files specifically: "log
  grammar: `dcs/workflows/execute.md` quotes GRAMMAR_LINE verbatim", the
  same for `plan.md`, "schema citation: `agents/dcs-commander.md` —
  every `schemas.md #N` carries N's real title".
- `python tests/test_dcs_gate.py` — baseline 100/100. `python
  tests/test_dcs_intake.py` — baseline 10/10.
- `git diff --stat -- dcs/hooks/dcs_gate.py tests/` — must print nothing
  (criterion 4 self-check; if it prints anything, stop and report a
  deviation).
- `grep -c 'IAP-APPROVED:' dcs/workflows/plan.md` and confirm any fenced
  example log line you ADD is accepted at the entry boundary: bracketed
  timestamp at column zero, then the token — verify with
  `python -c "import importlib.util;s=importlib.util.spec_from_file_location('g','dcs/hooks/dcs_gate.py');g=importlib.util.module_from_spec(s);s.loader.exec_module(g);print([l for l in open('dcs/workflows/plan.md',encoding='utf-8').read().splitlines() if g.sentinel_of(l)=='stamp'])"`
  — must still print the real-hex witness line (currently
  `dcs/workflows/plan.md:402`).
- **CRITERION 3 side-by-side**, as a table in your return: for each of
  the two cited logs, the ceremony that ACTUALLY happened versus what
  your step 6c would instruct, counted as agent spawns and Owner
  `AskUserQuestion` round-trips separately. Regenerate the "actual"
  column yourself with an ANCHORED pattern, never a bare grep (a bare
  grep over-counts on narrative lines):
  `grep -nE '^\[[^]]*\] (command:|ESCALATION:|IAP-APPROVED:|SAFETY-)' .dcs/incidents/2026-07-25-halt-loop-unbounded/214-LOG.md`
  and the same against
  `.dcs/incidents/2026-07-27-register-field-repair-path/214-LOG.md`.
  Read-only: `.dcs/**` is outside your territory. State any saving that
  is contingent on the Delegation as contingent — field measurement 1
  saves one unconditional agent spawn; field measurement 2's saving is
  one Owner round-trip, but only where Delegation bounds hold. Do not
  average the two into one headline number.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
