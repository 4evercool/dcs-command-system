# 204 — Tasking S1

**Incident:** direct-resolution-lane
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)
**Stage:** FIRST. S2 runs after you return and will read your landed work.

## Task

Add the seventh state **`RESOLVED`** to the register template's state enum, and
reconcile every other claim inside that one file. Satisfies **202 criteria 1 and
2**, and the two in-file carriers of **criterion 3**.

The literal is fixed by the plan as `RESOLVED` — it is a cross-tasking contract,
not your choice. It was checked collision-free across the tree, and `CLOSED` was
considered and rejected (it collides with `/dcs-close`'s vocabulary and with
`dcs_gate.py`'s `.dcs/CLOSED` zombie-rule filename). If you find `RESOLVED`
unworkable, report a **deviation**; do not substitute a name.

In `dcs/templates/REGISTER.md` and nowhere else:

1. Add `RESOLVED` to the value list in the `<!-- State values (v0.3): ... -->`
   comment, in that line's existing pipe-separated style, placed **last**.
2. Add its description **last** in the state-description block, in the block's
   existing `NAME    -- text` indentation style. It must be **scenario-neutral**:
   terminal for *any* incident whose work completed without ever opening a
   worktree or entering a deploy lifecycle. It must not contain `Type 5`,
   `step 7a`, or `express lane`, and must name no workflow, type, or lane. The
   type-specific knowledge belongs in the writer (`new.md` step 7a, S2's job),
   never in the enum. *(This is an IC bound from command point 1: two queued
   incidents — `register-field-repair-path` and `trivial-work-inline-lane` — must
   be able to consume this state without reopening the enum.)*
3. Add `RESOLVED` to the row template's state cell (currently line 71,
   `{{QUEUED\|ACTIVE\|...}}` — note the backslash-escaped pipes) in that cell's
   existing style.
4. That same row template's **Closed** column reads *"date the row left
   `ACTIVE`"* and **Outcome** reads *"or `—` until MERGED/DEPLOYED"*. Both are
   now false for a `RESOLVED` row that was never `ACTIVE` and never ships.
   Reword both minimally so they cover the new state.
5. The header comment's writer map (line 8, *"/dcs-new (territory check +
   QUEUED -> ACTIVE with worktree/branch)"*) no longer describes everything
   `/dcs-new` does to this file once S2 lands criterion 4. Extend it, **without
   naming a type or a step number.**

## Construction rules — the whole safety margin, do not improvise around them

`tests/test_doctrine_integrity.py` **check 15** holds this file's `DEPLOYED`
description as the **sole** declaring paragraph in its entire population, pinned
by name as *"the halt-2 anti-erasure floor"*. Rule C caps a file at **one**
declaring paragraph. A paragraph declares iff it has **all three** of: the
literal token `DEPLOYED`; a proof word
(`verify|verifies|verified|read|confirm|confirms|confirmed|proof|ancestor|ancestors|witness|witnesses|evidence`);
**and** a rule shape (``DEPLOYED`?\s*(--|:)`` · `DEPLOYED only after|once|when|if`
· `U+2192 →` immediately before `DEPLOYED`).

1. **Introduce NO blank line anywhere inside the state-description block.** It is
   one blank-line-delimited paragraph today and must stay one. Your new
   description lands *inside* the declaring paragraph — that is intended.
2. **Do not use the literal token `DEPLOYED` in `RESOLVED`'s own sentences.**
   Express the contrast as *"never enters the deploy lifecycle"* or similar. This
   makes a second declaring paragraph impossible regardless of placement.
3. **Anywhere in this file outside the existing `DEPLOYED` description: never
   place `--` or `:` immediately after the token `DEPLOYED`, and never place the
   Unicode arrow `→` (U+2192) immediately before it.**

**Rule 3 is not hypothetical, and it is why item (5) is dangerous.** Two
paragraphs in this file already carry `DEPLOYED` **plus** a proof word and are
non-declaring **only** because their arrows are ASCII `->` rather than `→`:

| paragraph | why it is currently safe |
|---|---|
| the header comment (lines 1-12) — **which item (5) edits** | contains `DEPLOYED` and the proof word *"read"*; its `QUEUED -> ACTIVE` arrow is ASCII |
| the `FACTS-ONLY` block | contains `DEPLOYED` and proof words; its arrows are ASCII |

The check's arrow is `chr(0x2192)`, verified at
`tests/test_doctrine_integrity.py:922`. So "tidying" an ASCII `->` into a
typographic `→` while editing line 8 would silently create a **second** declaring
paragraph and redden Rule C. **Leave every existing `->` exactly as it is.**

## File territory (may edit only within these globs)

- `dcs/templates/REGISTER.md`

## Forbidden zones (explicitly, even if it seems related)

- `tests/test_doctrine_integrity.py` and `tests/**` — **you may READ check 15's
  source, and should. You may never edit it.** If check 15 reddens, the remedy is
  rewording *this* file. Editing the check is a deviation, full stop (IC bound,
  command point 1).
- `dcs/workflows/**` and `dcs/references/**` — S2's territory and the hot path
- `dcs/references/doctrine.md`, `dcs/references/typing.md` — **forbidden, not
  merely out of scope.** No doctrine *rule* changes here; a bookkeeping substep
  is not a rule. The hot path has 1,205 B of slack against a ratchet that must
  not move, so this is made structurally impossible rather than left to
  judgement. If you conclude the convention is unintelligible without a hot-path
  carrier, **stop and report a deviation** — that is the IC's call.
- `dcs/hooks/**`, `dcs/VERSION`, `agents/**`, `skills/**`, `bin/**`,
  `install.ps1`, `install.sh`, `package.json`, `CHANGELOG.md`, `README.md`,
  `CLAUDE.md`
- every other file under `dcs/templates/` (`201-BRIEF.md`, `202-OBJECTIVES.md`,
  `203-ORG.md`, `204-TASKING.md`, `209-SITREP.md`, `214-LOG.md`, `AAR.md`,
  `DELEGATION.md`, `IAP.md`, `STRATEGY.md`, `config.json`)

Write with the Write/Edit tools only — **never** PowerShell
`Set-Content`/`Out-File`, which emit a UTF-8 BOM that has broken a hash
comparison and a parse in this repo before.

## Evidence required in the return

Paste real output, not descriptions.

- `python tests/test_doctrine_integrity.py | tail -1` → must print `82/82 passed`.
  **A different total is a tripwire, not a counting quibble** — check 15 emits one
  check per declaring file, so the total moves if and only if the declaring
  population changed. Stop and report; do not adjust the check.
- `python tests/test_doctrine_integrity.py | grep 'declaring population is non-empty'`
  → must still read `... 1 paragraphs across 1 files: ['dcs/templates/REGISTER.md']`
- `python tests/test_doctrine_integrity.py | grep -cE '^FAIL'` → `0`
- `sed -n '/<!-- State values/,/^$/p' dcs/templates/REGISTER.md | tr -d '\n' | sed 's/.*State values[^:]*: *//' | tr '|' '\n' | sed 's/^ *//;s/ *$//' | grep -c .`
  → `7` (prints `6` at HEAD) — criterion 1, without a line number
- `sed -n '/^ *RESOLVED /,/^$/p' dcs/templates/REGISTER.md` → the new state's full
  description and nothing from another state (this requires it be placed **last**)
- `sed -n '/^ *RESOLVED /,/^$/p' dcs/templates/REGISTER.md | grep -inE 'type ?5|step 7a|express lane'`
  → no matches — criterion 2
- `grep -n 'RESOLVED' dcs/templates/REGISTER.md` → at least three hits: value
  list, description, row-template cell
- `python -c "import re,pathlib;t=pathlib.Path('dcs/templates/REGISTER.md').read_text(encoding='utf-8');print(sum(1 for p in re.split(r'\n\s*\n',t) if re.search(r'\bDEPLOYED\b',p)))"`
  → **`5`, exactly as at HEAD.** A `6` means a blank line split the description
  block — the check-15 hazard — **even if the suite happens to stay green.**
- `grep -c -- '->' dcs/templates/REGISTER.md` and
  `python -c "import pathlib;print(pathlib.Path('dcs/templates/REGISTER.md').read_text(encoding='utf-8').count(chr(0x2192)))"`
  → report both. The Unicode count must **not** increase (rule 3).
- `git status --porcelain` → the only modified tracked file is
  `dcs/templates/REGISTER.md`
- `python -c "print(open('dcs/templates/REGISTER.md','rb').read()[:3])"` → must
  **not** print `b'\xef\xbb\xbf'`

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` — a recommendation, not an action. The IC
re-enters planning around your finding.

**Return the schema #4 JSON block itself, not a prose summary of it.** A return
without `status` / `files_touched` / `tests_run` / `evidence` / `deviation` is
treated as a failed spawn. If you find a semantic divergence from this tasking,
that is `status: "deviation"` — not a remark inside `evidence`.
