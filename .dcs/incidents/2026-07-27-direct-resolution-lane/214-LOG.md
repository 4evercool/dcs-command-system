<!--
214-LOG.md -- append-only. Never edit or delete a past entry (see
references/forms.md's note on why). Every workflow appends here on every
phase transition. This is what /dcs-status reads the tail of, and what a
fresh session reads in full to resume losslessly after a context reset.

Format: [ISO8601 local timestamp] free-text description of the transition.
One entry, one line: a continuation line is never a new entry, even one
that opens with a sentinel-shaped token at column zero.

Three tokens carry a mechanical sentinel -- `IAP-APPROVED:`, `SAFETY-HALT:`,
`SAFETY-PASS:` -- which dcs_gate.py's halt-ceiling counter parses (doctrine
principle 13), per dcs_gate.py's own published grammar (GRAMMAR_LINE): "An
entry begins at column zero with a mandatory bracketed timestamp; any other
line is a continuation, never a sentinel, and quoting a whole prior entry
inside a body requires indenting it off column zero." Keep the token
immediately after the bracketed timestamp; a paraphrase, a missing
timestamp, or a token merely mentioned mid-line is never counted.
-->

# 214 — Operational Log

**Incident:** direct-resolution-lane

[2026-07-27T15:26:00+11:00] incident opened, type 1, phase=planning -- worktree C:\DCS-wt\direct-resolution-lane, branch dcs/direct-resolution-lane, opened by /dcs-new from REGISTER.md rank 1. Stem NOT re-derived: this incident's brief was already worked 2026-07-27 by a third-party DCS review and parked at .dcs/esg/QUEUED-201/2026-07-27-direct-resolution-lane.md when new.md step 7b refused to open on a territory conflict with deploy-marker-blind. That conflict cleared when deploy-marker-blind reached DEPLOYED.
[2026-07-27T15:26:00+11:00] two dcs-situation-analyst spawns, scoped to re-verification only -- the queue note's own reopen condition ("unless the blast radius changed") was met, because deploy-marker-blind merged (0843816, integration 916bebc) after the brief was written and edited a file it cites. Result: every original evidence claim HOLDS except one line-range citation (dcs/templates/REGISTER.md 25-49 -> 25-53, enum values textually unchanged) and one SUPERSEDED claim. Three carriers new to the blast radius: dcs/references/forms.md:22 (literal six-state enum, in no check's population), dcs/workflows/close.md:239 (asserts DEPLOYED is THE terminal state), dcs/workflows/status.md:102-103 (already contradicts the enum today, before this incident touches anything). Baseline at HEAD 69be722, each count read from its own line: 82/82, 100/100, 10/10.
[2026-07-27T15:26:00+11:00] the SUPERSEDED claim is the one that matters and it is a hard constraint, not a note: tests/test_doctrine_integrity.py grew to checks 1-16, and check 15 now holds dcs/templates/REGISTER.md's DEPLOYED paragraph (lines 39-45) as the SOLE member of its declaring population, pinned by name as "the halt-2 anti-erasure floor" -- immediately adjacent to the enum block this incident must edit. Rule A requires that paragraph to cite deploy.md step 7 by live step number; Rule C allows at most one declaring paragraph per file. PASS line verbatim: declaring population is non-empty, spans ['dcs/'], and has 1 paragraphs across 1 files: ['dcs/templates/REGISTER.md'].
[2026-07-27T15:26:00+11:00] analyst disagreement recorded rather than smoothed: Analyst A put tests/test_doctrine_integrity.py inside the blast radius, Analyst B held it is not edited but only satisfied. Settled by the IC on B's side and made a hard bound -- see the command entry below.
[2026-07-27T15:26:00+11:00] command: typed 1 -- confirmed rather than re-litigated (decided at the original stem by an IC running fable and Owner-confirmed 2026-07-27; the register forbids re-litigating it unless the blast radius changed, and it changed UPWARD, which cannot move a type already at the ceiling), and independently re-earned for the narrowed scope: a new terminal state in the shipped portfolio state machine is a new cross-cutting concern, typing.md's Type 1 trigger by name (IC=dcs-commander, fable)
[2026-07-27T15:26:00+11:00] command: DECOMPOSITION RULED -- SPLIT (new.md step 4a, doctrine principle 4). The parked brief carried three manifestations under one asserted root cause; the IC ruled that root cause is a MODEL, not a defect ("the register state machine and every workflow that writes it were designed exclusively for full-lifecycle incidents"), and principle 4 disposes of models by name: open the first concrete defect, route the model to STRATEGY.md. It also dismantled the keep-together argument on its own premise -- a split does NOT mean three enum edits, because one incident creates the state and the others consume it. This incident keeps manifestation (a) only: a Type 5 resolved off the queue leaves its row QUEUED forever. (a) is the critical path, being the one actively causing harm -- 3 of 6 Type-5 rows stuck QUEUED in the field census (IC=dcs-commander, fable)
[2026-07-27T15:26:00+11:00] command: two IC bounds attached to the ruling, both inherited by the 202. (1) The new state's definition MUST be scenario-neutral -- terminal for ANY resolution that never opened a worktree/deploy lifecycle -- so the split-out manifestations later consume it without reopening the enum. (2) tests/test_doctrine_integrity.py stays OUT of territory: if check 15 goes red the remedy is rewording REGISTER.md, and editing the check is a deviation, full stop (IC=dcs-commander, fable)
[2026-07-27T15:26:00+11:00] escalation trigger (e) raised at command point 1 -- the IC attached esg_activation (doctrine principle 14), on three grounds: the contested root cause is a model belonging in STRATEGY.md, the (c)-vs-rank-9 territory overlap needs a sweep adjudication, and the split adds three QUEUED rows to a queue the Owner ranked hours earlier. 209 filed at .dcs/esg/SITREPS/direct-resolution-lane-p0.md before pausing, per principle 14.
[2026-07-27T15:26:00+11:00] Owner decision on the 209: CONTINUE -- split accepted in full, ESG activation DECLINED FOR NOW and not refused on the merits (the fifth /dcs-esg ran an hour before this stem). Consequence recorded rather than left implicit: the three new rows and the model behind them are OWED to the next sweep, which must not discover them as unranked debris -- the fourth ESG session had to clean up exactly that.
[2026-07-27T15:26:00+11:00] Owner confirmed typing at step 6 as part of the same decision; no override offered or taken. Type 1 stands.
[2026-07-27T15:26:00+11:00] territory check against REGISTER.md passed CLEAN: zero ACTIVE rows portfolio-wide at open (7 DEPLOYED, 15 QUEUED, 2 PARKED, 4 KILLED), so no overlap was possible with a live incident. While this row is ACTIVE it blocks ranks 5, 6, 7, 8, 9 and 12 -- derived from the Territory column at this stem, CORRECTING the list the fifth /dcs-esg first wrote, which omitted rank 6 (prompt-vs-schema-drift claims dcs/workflows/**) and wrongly included rank 10 (doctor-version-only-check claims bin/dcs.js alone). Both register and STRATEGY.md corrected.
[2026-07-27T15:26:00+11:00] three rows registered from the split, each carrying this brief's evidence: register-field-repair-path (b), trivial-work-inline-lane (c), status-md-enum-drift (the pre-existing status.md:102-103 drift). All QUEUED, unranked, owed to the next /dcs-esg.
[2026-07-27T15:30:00+11:00] brief size smell recorded rather than hidden: this 201 measures 12,628 B against new.md step 4a's ~7-11 KB well-scoped range (`wc -c` on the file). It is 1.15x the top of the range, not the 2-3x the rule flags as probable over-scope, and the excess is provenance -- per-claim HOLDS/MOVED/SUPERSEDED dispositions from the re-verification, plus the record of the split. The parked brief it replaces was 8,067 B and carried THREE manifestations; this one is longer while describing ONE, which is the opposite direction from the smell's meaning. Named here so the Planning Chief and the Safety Officer judge it rather than inherit it silently.
[2026-07-27T15:30:00+11:00] phase=planning, gate ARMED (.dcs/ACTIVE written, per-worktree and gitignored; dcs_gate.py --halt-count returns 0). No source edit is possible in this worktree until /dcs-plan stamps an approved IAP. Next command: /dcs-plan.
[2026-07-27T15:40:00+11:00] 202 period 1 drafted and Owner-confirmed at plan.md step 2 (Type 1, so no delegation skip applies -- Delegation v3's auto_approve_type3 reaches Type 3 only). Ten criteria, criterion 10 tagged [Owner]. Plan-time measurements recorded WITH their commands: enumeration census 3 in-territory carriers; suites 82/82, 100/100, 10/10; hot path 36683 1205; `npm view dcs-command-system version` -> 0.6.9 against dcs/VERSION 0.6.10, so 0.6.10 is unpublished -- MEASURED at 15:33+11:00, not asserted, and criterion 9 carries the command to re-measure at close. That last one is deliberate: asserting an unmeasured external fact is the defect register row criterion-unmeasured-fact (rank 2) exists for, and it cost a version published twice with different contents.
[2026-07-27T15:40:00+11:00] Planning Chief and Logistics Chief spawned in parallel, each fed the full 201 + 202 text inline per their charters (no file paths). Both returned structured. Planning Chief: 2 taskings, partition_ok true, 6 tactics, 7 risks. Logistics Chief: install path, payload membership verified against install.ps1/install.sh line by line, rollback, and the live-register compatibility question.
[2026-07-27T15:40:00+11:00] tasking lint: 4 defects fixed pre-review, ALL FOUR MINE and none the chiefs' -- (1) criterion 3's word "agrees" was ambiguous to the point of being RED before any edit: the three carriers already disagree textually at HEAD (`REGISTER.md:25` says `MERGED (deploy pending)`, `:71` says `MERGED`, `forms.md:22` uses slashes) -- flagged by the Planning Chief, confirmed by me mechanically, rewritten as "the same set of VALUES, each site in its own existing style" with normalisation explicitly out of scope; (2) criteria 1 and 2 cited line ranges (`:25-53`) that this very change moves -- replaced with extraction commands, principle 15; (3) criterion 9 had no owner for its registry half -- split and tagged [IC] at close, since `npm view` needs network a specialist harness may lack; (4) the Logistics Chief named a post-deploy live-register legend touch-up with no owner -- tagged [IC, post-deploy] in the 202 rather than left dangling.
[2026-07-27T15:40:00+11:00] tasking lint checks 1, 3, 3a, 4, 5, 6, 7, 8 verified mechanically rather than read: territory-forbidden intersection empty for both taskings and each forbids all of the other's territory (fnmatch, printed per path); territories disjoint; criterion 3's population enumerated by command and every in-territory occurrence falls in exactly one territory (`REGISTER.md:25` and `:71` -> S1, `forms.md:22` -> S2, `status.md:102` out of territory by design); S2's awk anchors `## 7a.` and `## 7b.` confirmed present at new.md:132 and :154; check-15 DEPLOYED-bearing paragraph baseline confirmed at 5, matching the plan's pin; the state literal `RESOLVED` confirmed collision-free across dcs/ agents/ skills/ tests/ CLAUDE.md README.md independently of the chief's claim.
[2026-07-27T15:52:00+11:00] command: iap_review ACCEPT (IC=dcs-commander, fable) -- five forks ruled, each on evidence the IC read itself rather than on the chiefs' claims. (1) ACCEPT the close.md:243-244 scope addition the Planning Chief flagged rather than smuggled: that file instructs ACTIVE -> CLOSED, a state never in the enum, and shipping a seven-state enum four lines above an instruction naming a phantom eighth is what a Safety Officer should catch. (2) SEQUENTIAL S1 -> S2, overriding the chief's parallel default: territories are disjoint but the VERIFICATION surface is shared, and the state literal is a cross-tasking contract nothing enforces -- running S2 second lets it read S1's landed literal. On a Type 1 beside a forbidden guard, latency loses to margin. (3) RESOLVED accepted; the CLOSED rejection is doubly right now that ruling 1 retires the package's only CLOSED reference. (4) construction rules sufficient WITH two hardenings. (5) the live-register touch-up stays [IC, post-deploy] -- writing the main checkout's register mid-incident would break single-writer.
[2026-07-27T15:52:00+11:00] the IC found a hazard both chiefs missed, and I VERIFIED it rather than transcribing it: check 15's arrow rule shape is `_DEP_ARROW = chr(0x2192)` (tests/test_doctrine_integrity.py:922), NOT ASCII "->". Consequence: dcs/templates/REGISTER.md's header comment paragraph -- WHICH S1 EDITS at item 5 -- already carries the token DEPLOYED and the proof word "read", and is non-declaring ONLY because its `QUEUED -> ACTIVE` arrow is ASCII. Tidying that one arrow into a typographic one would silently create a second declaring paragraph and redden Rule C, with a non-obvious remedy. Confirmed by extracting all five DEPLOYED-bearing paragraphs and testing each predicate limb separately.
[2026-07-27T15:52:00+11:00] I found a SECOND paragraph in the same one-character margin that the IC did not name: the FACTS-ONLY block also carries DEPLOYED plus proof words and is likewise saved only by its ASCII arrows. Folded into S1's construction rule 3, which now covers the whole file rather than the two sites the chief listed, and S1's evidence now reports the U+2192 count so a rise is visible even if the suite stays green.
[2026-07-27T15:52:00+11:00] all four IC required_changes folded into the 204s and the 202 as stamping amendments -- no Planning Chief re-spawn, which the IC judged would burn a round over three one-liners it could supply verbatim. 203-ORG.md written (Type 1 always, and activation departs from default twice: Logistics Chief activated, execution sequential). 204-TASKING/S1.md and S2.md written. IAP.md integrated.
[2026-07-27T15:58:00+11:00] register territory REFINED at plan.md step 5a to the IAP partition union -- five files, NARROWER than the 201 estimate and narrower than the command-point-1 narrowing, because the IC made doctrine.md and typing.md forbidden rather than conditional. Consequence recomputed from the Territory column rather than carried forward: this row blocks ranks 6, 8 and 9 plus two of its own split-out rows; ranks 5, 7 and 12 are FREED, having collided only through doctrine.md. REGISTER-LOCK taken and released around the write.
[2026-07-27T15:58:00+11:00] delegation check: NOT APPLICABLE. Delegation v3 has auto_approve_type3 true, but no auto-approve path reaches Type 1 -- typing.md: "no auto_approve escape valve exists for Type 1, ever". Owner approval was mandatory and was given via AskUserQuestion at plan.md step 6b.
[2026-07-27T15:58:00+11:00] pre-stamp checklist PASSED: `command: typed` present (command point 1) and `command: iap_review` present (command point 2), both read from this log by count before any hash was computed.
[2026-07-27T15:58:00+11:00] IAP-APPROVED: a9c65a20cd71 -- phase: planning -> execution (period 1)
[2026-07-27T16:35:00+11:00] execute.md preflight passed on all four checks before any spawn: ACTIVE phase=execution; command chain has both `command: typed` and `command: iap_review`; approval marker VALID against the digest set (set size 2 -- the file is pure LF, so raw and lf coincide); trigger (c) 1 stamped attempt against esg.max_periods_before_review 3; trigger (d) no Delegation v3 forbidden_glob intersects the IAP territory. Channel A preflight: `grep -c halt_cycles .claude/hooks/dcs_gate.py` = 8, so the halt ceiling is REAL and enforced here, not a phantom.
[2026-07-27T16:35:00+11:00] Type 1 step 3.5 offered to the Owner: standard Agent fan-out chosen over a deterministic Workflow script. Reason recorded: both taskings require wording judgement under constraints (a scenario-neutral definition, a "minimal" reword, a ruling on whether close.md:239 stays true), which a script cannot specify exactly.
[2026-07-27T16:35:00+11:00] S1 spawned (stage 1 of the sequential mode). Returned status=done, schema #4 block present, files_touched = dcs/templates/REGISTER.md only -- inside territory. VALIDATED INDEPENDENTLY rather than accepted: git diff shows 9 insertions / 3 deletions in that file alone; suite 82/82; check 15 still reports 1 declaring paragraph in dcs/templates/REGISTER.md; the structural tripwire holds at 5 DEPLOYED-bearing paragraphs; U+2192 count still 0 and ASCII arrows preserved, so construction rule 3 held; the DEPLOYED description paragraph itself is untouched (the two DEPLOYED-bearing lines in the diff are the value list and the row template, neither of which is the declaring paragraph) -- which matters because IAP risk 4 records that nothing checks this mechanically.
[2026-07-27T16:35:00+11:00] S2 spawned (stage 2, after S1 landed). Returned status=done, schema #4 block present, files_touched = new.md, close.md, forms.md, CHANGELOG.md -- all inside territory. It ran the cross-tasking grep first as instructed and matched S1's literal exactly. VALIDATED INDEPENDENTLY: git status shows exactly the five expected modified tracked files and nothing else; new.md 234 -> 242 lines, inside the ~250 ceiling; the 7a substep resolves esg_root by cross-reference rather than restatement and is update-only-and-silent as planned; forms.md carries seven values in its own slash style with no normalisation attempted; close.md's phantom `ACTIVE -> CLOSED` is reconciled to RESOLVED and :239 gained a minimal qualifier rather than a rewrite.
[2026-07-27T16:35:00+11:00] ONE DEFECT FOUND BY THE IC AT STEP 5 AND DELIBERATELY NOT FIXED -- it goes to the Safety Officer rather than being quietly corrected, because hiding a finding from the verifier is worse than the finding. CHANGELOG.md:41-42 states "the one live declaring paragraph is still dcs/templates/REGISTER.md's FACTS-ONLY note." That is FALSE. Measured by evaluating check 15's three predicate limbs separately over every DEPLOYED-bearing paragraph: the declaring paragraph is the STATE-DESCRIPTION block (proof=1, shape=1); the FACTS-ONLY note has proof words but NO rule shape (proof=1, shape=0) and does not declare. The claim is in a SHIPPED artifact. Passed to the officer as a claim to verify, with the verdict on advisory-vs-refutation left to it.
[2026-07-27T16:35:00+11:00] step 7: work is an uncommitted working-tree diff across five files, which is correct -- the integration commit is a post-pass IC step (execute.md 9b), and the absence of a commit is never a refutation.
[2026-07-27T16:50:00+11:00] Safety Officer spawned (opus), one spawn. Verdict: pass, ZERO refutations, five advisories. Its checked[] ran to 26 items and it did not trust a single specialist self-report: it re-implemented check 15's declaring predicate from source, ran criterion 3's census against HEAD blobs as well as the worktree, walked all three 201 repro traces forward, and proved the forbidden territory untouched by `git diff --stat -- tests/`.
[2026-07-27T16:50:00+11:00] the officer independently CONFIRMED the defect the IC found at step 5 and refused to take the IC's word for it -- it re-measured the predicate limb by limb and then triple-confirmed against 201-BRIEF.md:74 and against tests/test_doctrine_integrity.py:1010-1015, whose own source comment names the FACTS-ONLY paragraph as the ARCHETYPAL FALSE POSITIVE that limb (iii) was narrowed to exclude. Ruled advisory, reasoned against all three step-6 bars by name, decisive one being bar 2: the guard prints its own declaring population on every run, so the truth is one command away at the moment of use.
[2026-07-27T16:50:00+11:00] the officer found THREE things the IC had not. (1) S1's arrow claim was itself false: it reported ASCII arrows 4 -> 4, but the genuine transition count (excluding HTML comment closers) went 3 -> 4 -- the post-state reported as its own baseline. Independently re-verified by the Dispatcher: HEAD 5 total / 2 closers / 3 genuine, worktree 6 / 2 / 4. The substantive tripwire was never at risk (U+2192 stayed 0 and ASCII is not in the rule-shape regex), so IAP risk 3b did not materialise. (2) The 202's criterion 3 UNDERCOUNTS its own enumerated population: the census returns 7 hits at HEAD, not 4 -- three transition-prose sites (REGISTER.md:8, loop.md:121, new.md:208) matched before any edit. (3) A cross-specialist gap neither tasking's isolated verification could catch: close.md 6a now makes /dcs-close a writer of RESOLVED while REGISTER.md's header writer-map still lists it as producing only ACTIVE -> MERGED -- and the officer established the map was ALREADY partial at HEAD (no /dcs-loop as PARKED writer, no KILLED writer at all), so it is long-standing partiality rather than a contradiction this period introduced.
[2026-07-27T16:50:00+11:00] escalation triggers checked after the verdict, both negative: trigger (b) -- zero SAFETY-HALT entries in this log, so no second halt on the objective; trigger (a) -- combined files_touched is five files, every one inside the IAP partition table and NARROWER than the 201's declared blast radius, so the radius was not exceeded.
[2026-07-27T16:50:00+11:00] SAFETY-PASS: period 1 complete
[2026-07-27T16:50:00+11:00] SAFETY.md written with verdict 1 transcribed VERBATIM as returned, plus the officer's two non-mechanical reads quoted rather than summarised (scenario-neutrality: both split-out incidents consume the definition unchanged, with the one place it might not hold named and resolved; the Rule-B hole: REGISTER.md's DEPLOYED bullet byte-identical to HEAD and deploy.md step 7 still numbered 7).
[2026-07-27T16:50:00+11:00] command: verdict -> close (IC=dcs-commander, fable). Grounds: pass with zero refutations on ONE attempt, all nine specialist-ownable criteria independently green, and the delivered work is provably self-contained -- the officer verified both split-out incidents consume the definition unchanged, so nothing is left half-migrated and close-and-requeue is execute.md 9b's default rather than an argued exception. No next objective the 201 asked for exists; the remainder is advisory fixes, a register row, and Owner acts.
[2026-07-27T16:50:00+11:00] command: advisory triage (IC=dcs-commander, fable) -- 1 and 5 FIXED in the integration commit; 2 is a commit-message note only (no file change, the arrow was added on purpose); 3 goes to the AAR at close with its command; 4 DEFERRED to a new register row `register-writer-map-completeness`, because fixing only the /dcs-close entry would be a partial repair of an out-of-scope pre-existing defect.
[2026-07-27T16:50:00+11:00] criterion 9's [IC] half verified AT CLOSE rather than carried from plan time, which is the whole point of how it was written: `npm view dcs-command-system version` -> 0.6.9, `cat dcs/VERSION` -> 0.6.10. Repo ahead of registry is the expected pre-publish state; 0.6.10 remains unpublished, so the CHANGELOG entry stays in that open section and no bump was needed. npm publish is an Owner act with a 2FA OTP and is not a close blocker.
[2026-07-27T16:50:00+11:00] advisories 1 and 5 fixed by the IC, then ALL guards re-run because the fix touched the very file check 15 pins: 82/82 exit 0 with zero FAIL lines, check 15 still reporting 1 declaring paragraph in dcs/templates/REGISTER.md, 100/100, 10/10, structural tripwire still 5 DEPLOYED-bearing paragraphs, U+2192 still 0, territory still exactly five modified tracked files, `git diff --stat -- tests/` empty, and all five files no-BOM with zero CRLF.
[2026-07-27T16:58:00+11:00] integration commit 13f557d (5 files) -- staged explicitly by path, never git add -A. 46 insertions, 13 deletions. The incident directory is deliberately NOT in this commit; incident artifacts belong to the close commit, matching this repo's own precedent (916bebc carried nine source files, 91fd75e carried the artifacts).
[2026-07-27T16:58:00+11:00] objectives assessed against 202-OBJECTIVES.md: goal FULLY MET for the narrowed scope. Criteria 1-9 all green and independently verified by the officer; criterion 9's [IC] registry half verified at close; criterion 10 is Owner-UAT and belongs to /dcs-close. execute.md 9b's default (close and requeue) applies without needing to be argued -- there is no remaining work that cannot ship separately, which is exactly the test that branch sets.
[2026-07-27T17:10:00+11:00] Owner-UAT APPROVED (criterion 10), 2026-07-27, against the concrete question of whether RESOLVED is the shape wanted in the portfolio's state machine -- offered with the state's full definition, the enum line and the new 7a substep shown side by side, and with the note that this is the first state added since v0.3 established all six together. Accepted without qualification. All ten criteria now met.
[2026-07-27T17:10:00+11:00] AAR.md written. Deviation history taken FROM THIS LOG, not from memory: 0 `deviation reported by` entries, 0 SAFETY-HALT sentinels, 1 IAP-APPROVED, 1 SAFETY-PASS, four command points, one escalation (trigger (e), answered `continue`). Every past-tense claim in the AAR cites an artifact read at write time; deploy status is written as NOT DEPLOYED, which is the honest state.
[2026-07-27T17:10:00+11:00] memory routed per CLAUDE.md's three-store rule. No doctrine RULE changed this period (a bookkeeping substep is not a rule, and doctrine.md was forbidden territory), so nothing went to doctrine.md or doctrine-appendix.md -- that is the routing rule working, not a gap. Two meta-lessons appended to vault/Meta/building-dcs-lessons.md: §16 (narrowing scope at the stem widened what could run beside it -- the anti-split argument does not follow, and territory width is a PORTFOLIO cost invisible unless the collision is recomputed after the IAP narrows) and §17 (hand the verifier what you already found -- the IC passed a known-false claim to the officer unfixed, and the officer then found three defects the IC had missed). Nothing written to vault/Backlog.md: this incident's four follow-ups are register ROWS, and ranking them is an ESG act.
[2026-07-27T17:10:00+11:00] intake source closure: the intake is REGISTER.md rank 1 (third-party DCS review, 2026-07-27), not an external system. Closure is the register's own ACTIVE -> MERGED transition at step 5a.3. Nothing external to flag; no production system touched.
