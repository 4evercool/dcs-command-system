# 204 — Tasking S2

**Incident:** deploy-marker-blind
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Rewrite `dcs/workflows/deploy.md` steps 4 and 7 onto a **content witness**,
satisfying **202 acceptance criteria 3 and 4**, and clear `deploy.md`'s 8
hits in criterion 5's population.

### Step 7 (`deploy.md:172-183`) — criterion 3

Before and after the step-6 deploy command, run the project's
deployed-content witness, and record the integration-branch sha at both
points. **Three named outcomes** replace the single advance-or-stop:

- **All identical** → `DEPLOYED`. A version string that did not move is
  explicitly **not** a stop condition, and before-green/after-green is a
  legitimate no-op ship, not a discrepancy.
- **Differing or repo-only** → **stop**, naming the files.
- **Installed-only only** → **`DEPLOYED`, with a mandatory flag.** The
  merged payload is fully live; the extras are pre-existing debris of
  installers that never delete, not something this train caused. **IC
  ruling at command point 2, binding:** step 7 must name the stale files
  in the deploy report so the Owner can delete them — a stale
  old-named workflow is still readable by anything citing the old name.
  Stopping here would recreate the stop-fires-on-a-correct-ship defect in a
  new shape, which is the whole reason this incident exists. A stop is
  reserved for the differing/repo-only case.

State plainly **why the working checkout is a faithful stand-in for "what
was merged"**: the content witness compares against the working checkout,
not against a named merge commit, and that gap is closed by `deploy.md`'s
existing step 3 (payload-clean) plus the recorded sha. Write the
equivalence down rather than leaving it assumed.

### Step 4 (`deploy.md:77-112`) — criterion 4

Give the reconciliation a **marker-shape trichotomy**:

- **Commit-ish marker:** unchanged `git merge-base --is-ancestor`
  behaviour.
- **Content witness:** run it against the integration-branch tip C. Green
  means the deployed tree equals C's payload, so every `MERGED` row whose
  merge commit is an ancestor of C is already live and reconciles away.
  Red means the deployed tree differs from C and the difference **cannot be
  attributed to individual rows** — so treat every `MERGED` row as
  unshipped, include them all, and name the differing files. State the
  semantics plainly: a content witness attests to **payload bytes**, so a
  merge that touched no payload file (`vault/`, `.dcs/`, `docs/`) is
  already "shipped" in payload terms.
- **Readable but neither a commit-ish nor a content witness:** say so,
  treat all rows as unshipped, flag the possible re-ship.

**Keep the existing "marker unreadable" branch (`deploy.md:101-103`) and
widen it** — do not replace it. It degrades in the safe direction
(over-ship rather than silently record unshipped work as live). Map a
witness environment error (exit 2) onto it, which is precisely what
distinguishes "cannot check" from "differs".

### Criterion 5 — the other four hits in this file

Beyond steps 4 and 7, fix `deploy.md:4`, `:190` and `:211`, where the
purpose line, step 8 and step 10 still present the version marker as the
proof of a ship.

## Hard constraints

1. **`deploy.md` stays project-agnostic.** Never name `tests/payload_check.py`,
   `~/.claude`, or `dcs/VERSION`. The witness is **discovered from the
   project's own `CLAUDE.md`**, exactly as the deploy command already is at
   step 6 — `CLAUDE.md`'s "Ship no project facts" rule. S3 owns naming the
   concrete command; you own the generic contract.
2. **Preserve the step numbering 1-10 and step 5's identity.**
   `run.md:135-147` and `schemas.md:146` both cite "`deploy.md` step 5" by
   number.
3. **Preserve the `schemas.md #7, delegation bounds` citation** in step 5's
   preamble (`deploy.md:119`). Integrity **check 13** resolves every
   `schemas.md #N` citation against the section's real title within 80
   characters; a rewording near step 5 that drops or separates the anchor
   turns the merge guard red. Verified present at plan time.
4. **File size.** 215 lines at plan time against `CLAUDE.md`'s ~250-line
   workflow budget. Both steps grow. **Compress step 4's existing prose if
   you need the room rather than overrunning** — and note that this budget
   is a `CLAUDE.md` rule, **not machine-enforced**: nothing will fail red if
   you overrun, which is exactly why you must report `wc -l` explicitly.

## File territory (may edit only within these globs)

- `dcs/workflows/deploy.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/close.md`, `dcs/workflows/run.md`, `dcs/workflows/loop.md`,
  `dcs/workflows/status.md`, `dcs/workflows/plan.md`,
  `dcs/workflows/execute.md`, `dcs/workflows/new.md`,
  `dcs/workflows/esg.md`, `dcs/workflows/init.md`
- `dcs/references/**`
- `dcs/templates/**`
- `dcs/hooks/**`
- `agents/**`
- `skills/**`
- `tests/**`
- `bin/**`
- `docs/**`
- `install.ps1`, `install.sh`
- `package.json`
- `CLAUDE.md`
- `CHANGELOG.md`
- `README.md`
- `vault/**`

## Evidence required in the return

1. `wc -l dcs/workflows/deploy.md` — must be ≤ 250 (215 at plan time).
2. `grep -n '^## ' dcs/workflows/deploy.md` — proving steps 1-10 still
   exist under those numbers and step 5 is still the Owner/Delegation gate.
3. `grep -n 'schemas.md #7' dcs/workflows/deploy.md` — proving constraint 3
   held.
4. **The money evidence — that the new step 4 is actually runnable in this
   repo where the old one never was.** Paste the old failure:
   ```
   git merge-base --is-ancestor aab9f06 0.6.10
   ```
   → `fatal: Not a valid object name 0.6.10`, exit 128. Then paste the
   **exact commands your new step 4 prescribes, RUN against this repo
   today**, with their real output and exit codes. Read-only only: no
   deploy, no install.
5. **Same for step 7:** paste the exact before/after command sequence the
   new step prescribes, and run the "after" half read-only against the
   current tree, showing which of the three outcomes it lands in and what
   the step then tells the operator to do.
6. `grep -rniE 'deployed[- ]version marker|deployed_sha|marker (actually )?advanc|VERSION.*after deploy' dcs/workflows/deploy.md`
   — paste the **full** output and, **line by line, state for every
   remaining hit** whether it was changed or is deliberately correct as
   written. Several are generic advice about *any* project's marker and are
   not defects. 8 hits at plan time. A hit with no disposition is a gap the
   Safety Officer will find.
7. `python tests/test_doctrine_integrity.py` — the suite's own `N/M passed`
   line (checks 3-6 and 10-13 all read workflow prose).
8. `python tests/test_dcs_gate.py` and `python tests/test_dcs_intake.py` —
   each suite's own `N/M passed` line.

## Context you need but must not edit

The witness S1 is building in parallel, whose **generic** contract you are
describing (you will not see S1's return — write against this, not against
guesses):

- Four output classes: identical / differing / repo-only / installed-only,
  with counts and, for the three non-identical classes, paths.
- Exit codes: `0` all identical · `1` differing or repo-only · `3`
  installed-only only · `2` environment error.
- It accepts overrides for both the repo root and the installed root.

Describe those **properties** generically ("the project's deployed-content
witness reports … and exits …"), never the concrete path.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.

Note in particular: `install.ps1`, `install.sh`, `package.json` and
`dcs/VERSION` are in **no** tasking's territory and in **every** tasking's
forbidden list. If you find criteria 3-5 genuinely cannot be met without an
installer edit, that is a deviation and an IAP re-approval — those files are
Type 1 by name in `CLAUDE.md:146-148` and sit in the live Delegation's
deploy `forbidden_globs`.
