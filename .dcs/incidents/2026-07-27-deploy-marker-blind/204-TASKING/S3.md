# 204 — Tasking S3

**Incident:** deploy-marker-blind
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Bring every remaining surface onto the new contract, satisfying **202
acceptance criteria 5, 6 and 7**.

### (a) `CLAUDE.md` — criterion 6

Rewrite the Deploy table's **Deployed-version marker** row (line 30) and
the sentence at line 34, so they state the contract step 7 now actually
enforces. Today they read:

> `| **Deployed-version marker** | ~/.claude/dcs/VERSION — must equal dcs/VERSION after deploy |`
> `/dcs-deploy runs the deploy command and verifies the marker advanced.`

**This is the one place the concrete command is named.** `deploy.md` stays
project-agnostic by design (`CLAUDE.md`'s own "Ship no project facts"
rule), and this table is where a session discovers DCS's actual mechanism —
exactly as it already discovers the deploy command here. Name:

- the witness command `python tests/payload_check.py`,
- its four output classes (identical / differing / repo-only /
  installed-only),
- its exit codes: `0` all identical · `1` differing or repo-only · `3`
  installed-only only · `2` environment error,
- and what `~/.claude/dcs/VERSION` is now **for**: a version label, not
  proof of a ship.

### (b) Criterion 5 — the four remaining population files

Walk every hit end to end and **either change it or state in your return
that it is deliberately correct as written.** Do not silently leave any —
a hit with no disposition is a gap the Safety Officer will find.

- `dcs/workflows/close.md:76-79` — the AAR's deploy-status field.
- `dcs/templates/REGISTER.md:40` — the `DEPLOYED` status definition, which
  says the marker *"advanced past this incident's merge commit"*.
- `skills/dcs-deploy/SKILL.md:3,20` — the frontmatter `description` and the
  objective. **Keep the frontmatter valid** and the description within its
  existing shape; integrity checks read it.
- `docs/spec-v0.3-parallel.md:127`.

**Several of these may be fine.** `close.md`'s and `REGISTER.md`'s are
generic advice about *any* project's marker and may well be correct once
they stop implying a version string is sufficient; the v0.3 spec is a
historical snapshot and may legitimately keep its wording. **Name your
judgement per hit** — that is the deliverable, not a blanket rewrite.

### (c) `CHANGELOG.md` — criterion 7

Add an entry under the **EXISTING** `## 0.6.10` heading. **Never a new
version heading**, and **never touch `dcs/VERSION` or `package.json`** —
0.6.10 is unpublished and a version bump is out of scope this period.

**Write the entry from this tasking and the IAP's tactics, not from the
other specialists' outputs — you will not see them.** Cover: the witness
command and its contract; the step 4 and step 7 changes; and the rejected
alternative (an installer-written hash marker) with one line on why it was
rejected. **Verify 0.6.10 is still unpublished before you write** — that is
a claim about an external registry and it moves.

## File territory (may edit only within these globs)

- `CLAUDE.md`
- `CHANGELOG.md`
- `dcs/workflows/close.md`
- `dcs/templates/REGISTER.md`
- `skills/dcs-deploy/SKILL.md`
- `docs/spec-v0.3-parallel.md`

## Forbidden zones (explicitly, even if it seems related)

Enumerated rather than globbed, because four of your six territory files
live inside directories that are otherwise off limits.

- **Workflows other than `close.md`:** `dcs/workflows/deploy.md` (S2's
  territory), `run.md`, `loop.md`, `status.md`, `plan.md`, `execute.md`,
  `new.md`, `esg.md`, `init.md`
- **Templates other than `REGISTER.md`:** `dcs/templates/201-BRIEF.md`,
  `202-OBJECTIVES.md`, `203-ORG.md`, `204-TASKING.md`, `209-SITREP.md`,
  `214-LOG.md`, `AAR.md`, `DELEGATION.md`, `IAP.md`, `STRATEGY.md`,
  `config.json`
- **Docs other than `spec-v0.3-parallel.md`:** `docs/design-v0.1.md`,
  `docs/publishing.md`, `docs/spec-v0.2-esg.md`
- **Skills other than `dcs-deploy`:** `skills/dcs-close/**`,
  `skills/dcs-esg/**`, `skills/dcs-execute/**`, `skills/dcs-init/**`,
  `skills/dcs-loop/**`, `skills/dcs-new/**`, `skills/dcs-plan/**`,
  `skills/dcs-run/**`, `skills/dcs-status/**`
- `dcs/references/**`
- `dcs/hooks/**`
- `dcs/VERSION`
- `agents/**`
- `tests/**` (S1's territory)
- `bin/**`
- `install.ps1`, `install.sh`
- `package.json`
- `README.md`
- `vault/**`

## Evidence required in the return

1. `grep -rniE 'deployed[- ]version marker|deployed_sha|marker (actually )?advanc|VERSION.*after deploy' dcs/workflows/close.md dcs/templates/REGISTER.md skills/dcs-deploy/SKILL.md CLAUDE.md docs/spec-v0.3-parallel.md`
   — paste the **full** output and give a **per-hit disposition**: changed,
   or deliberately correct as written and why. 8 hits across these five
   files at plan time (close.md 2, SKILL.md 2, CLAUDE.md 2, REGISTER.md 1,
   spec 1).
2. `sed -n '25,40p' CLAUDE.md` — the rewritten Deploy table and the
   sentence beneath it.
3. `npm view dcs-command-system version` — real output, **re-run by you**,
   confirming 0.6.10 is still unpublished before the CHANGELOG entry is
   written. It read `0.6.9` at plan time.
4. `grep -n '^## ' CHANGELOG.md | head -3` — proving no new version heading
   was created and the entry sits under the existing 0.6.10 section.
5. `git diff --stat` — proving `dcs/VERSION` and `package.json` are
   untouched.
6. `python tests/test_doctrine_integrity.py` — the suite's own `N/M passed`
   line (check 5 validates templates named in workflows; checks 8-11 read
   `SKILL.md` frontmatter and the shipped set).
7. `python tests/test_dcs_gate.py` and `python tests/test_dcs_intake.py` —
   each suite's own `N/M passed` line.

## Context you need but must not edit

The witness S1 is building in parallel, whose concrete command you are
naming in `CLAUDE.md` (you will not see S1's return — write against this):

- Path `tests/payload_check.py`, invoked
  `python tests/payload_check.py [--repo PATH] [--installed PATH]`,
  defaulting to the repo root and `~/.claude`.
- Payload set derived by walking `dcs/`, `agents/dcs-*.md` and each
  `skills/dcs-*/`, the three roots the installers walk.
- Four output classes and exit codes `0` / `1` / `3` / `2` as above.

An **IC ruling at command point 2, binding and worth reflecting where you
describe the outcome**: exit 3 (installed-only only) resolves to
`DEPLOYED` **with a mandatory flag** naming the stale files — never a stop.
The extras are debris of installers that never delete
(`robocopy /E` has no `/PURGE`; `rsync -a` has no `--delete`), not
something the train caused.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.

In particular, if you conclude that satisfying criterion 6 requires
changing `dcs/VERSION`, `package.json` or either installer — all forbidden
here and in every other tasking — that is a deviation and an IAP
re-approval, not a local decision.
