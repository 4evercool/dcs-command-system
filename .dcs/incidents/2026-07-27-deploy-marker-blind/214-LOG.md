<!--
214-LOG.md -- append-only. Never edit or delete a past entry (see
references/forms.md's note on why). Every workflow appends here on every
phase transition. This is what /dcs-status reads the tail of, and what a
fresh session reads in full to resume losslessly after a context reset.

Format: [ISO8601 local timestamp] free-text description of the transition.
One entry, one line: a continuation line is never a new entry, even one
that opens with a sentinel-shaped token at column zero.

Three tokens carry a mechanical sentinel -- `IAP-APPROVED:`, `SAFETY-HALT:`,
`SAFETY-PASS:` -- which dcs_gate.py's halt-ceiling counter parses (doctrine
principle 13), per dcs_gate.py's own published grammar (GRAMMAR_LINE): "An
entry begins at column zero with a mandatory bracketed timestamp; any other
line is a continuation, never a sentinel, and quoting a whole prior entry
inside a body requires indenting it off column zero." Keep the token
immediately after the bracketed timestamp; a paraphrase, a missing
timestamp, or a token merely mentioned mid-line is never counted.
-->

# 214 — Operational Log

**Incident:** deploy-marker-blind

[2026-07-27T09:09:27+11:00] incident opened, type 1, phase=planning -- worktree C:\DCS-wt\deploy-marker-blind, branch dcs/deploy-marker-blind, driven by /dcs-run (attended auto-chain)
[2026-07-27T09:09:27+11:00] stem: 2 dcs-situation-analyst spawned in parallel (A: deploy.md mechanics + repro; B: prior art + blast radius). No material disagreement between returns; B widened the blast radius beyond the register's estimate with bin/dcs.js:121-146 and CLAUDE.md:30.
[2026-07-27T09:09:27+11:00] decomposition (new.md 4a): one defect opened here (the deploy train has no witness for an unbumped ship -- deploy.md steps 4 and 7 share one root in the marker's identity). One defect split out and to be registered QUEUED, not absorbed: doctor-version-only-check (bin/dcs.js:121-146's doctor() reports "current" on version-string equality; independent consumer, ships alone, bin/** collides with the queued version-bump-command row).
[2026-07-27T09:09:27+11:00] command: typed 1 -- blast radius forks on an undecided approach; Backlog fix (1) edits install.ps1/install.sh, which CLAUDE.md:146-148 types Type 1 by name, and typing is fixed at the stem, so Type 3 would foreclose that branch by ceremony selection; under either approach the incident rewrites CLAUDE.md:30's proof-of-deployment contract, so typing.md's "type up, not down" resolves upward (IC=dcs-commander, fable)
[2026-07-27T09:09:27+11:00] Owner confirmed typing as proposed (Type 1). IC's open question relayed at the same gate: Owner declined to constrain the approach at the stem -- the Planning Chief proposes, the Owner decides between backlog fix (1) and (2) at the mandatory Type 1 IAP approval.
[2026-07-27T09:22:00+11:00] period 1 planning: 202 drafted and confirmed by the Owner as drafted (Type 1 -- no Delegation skip applies). Owner extended territory at the same gate: CHANGELOG.md IS in territory this period (criterion 7), and NO version bump (criterion 7 rationale).
[2026-07-27T09:22:00+11:00] lint 3a enumerator run at plan time for criterion 5: `grep -rniE 'deployed[- ]version marker|deployed_sha|marker (actually )?advanc|VERSION.*after deploy' dcs/ agents/ skills/ CLAUDE.md README.md docs/` -> 16 hits in 6 files (deploy.md 8, close.md 2, skills/dcs-deploy/SKILL.md 2, CLAUDE.md 2, templates/REGISTER.md 1, docs/spec-v0.3-parallel.md 1).
[2026-07-27T09:22:00+11:00] external fact measured rather than assumed (the criterion-unmeasured-fact class): `npm view dcs-command-system version` -> 0.6.9, while dcs/VERSION and package.json both read 0.6.10 -- 0.6.10 is genuinely unpublished, so criterion 7's entry lands in that open CHANGELOG section and no bump is needed.
[2026-07-27T09:52:00+11:00] chiefs returned (dcs-planning-chief + dcs-logistics-chief, spawned in parallel, Type 1). Both independently recommend backlog fix (2) -- the permanent content gate -- over fix (1), the installer-written hash marker. Planning Chief's decisive argument: fix (1) cannot satisfy criterion 1 on its own (an aggregate hash says "differs" and nothing more, strictly less than the per-file report the Owner authorised by hand three times), so (1) is (2) plus installer edits plus a redundant marker. Logistics Chief's: a marker the installer writes testifies to what the installer believes it copied, not to what is on disk; and putting the witness in tests/ (outside the payload) means no package.json change, no install-time dependency, no residue a rollback cannot undo, and no self-reference on the first train.
[2026-07-27T09:52:00+11:00] tasking lint: 6 defects fixed pre-review -- (1) chiefs disagreed on the witness filename (payload_check.py vs payload_witness.py), resolved to tests/payload_check.py, the name the register already cites; (2) S1 would report every foreign gsd-* agent and non-DCS skill as installed-only and go permanently red, so installed-only is now scoped to the dcs-* globs; (3) __pycache__ is treated inconsistently by the two installers (install.ps1 excludes it for dcs/ but not skills/, install.sh's cp -R fallback excludes nothing), so S1 now excludes it unconditionally on both sides reusing test_doctrine_integrity.py's existing exclusion set; (4) S1's "expect green" baseline was a race against S2/S3's concurrent payload edits, now pinned to --repo C:/DCS, the clean main checkout, with ambient worktree red declared expected; (5) S2 could redden integrity check 13 by dropping deploy.md:119's `schemas.md #7, delegation bounds` citation during a step-5 rewording -- named in its tasking (lint check 7); (6) criterion 5 said "no surviving site in the shipped package" while its enumerator never searched bin/, where bin/dcs.js:121-135 is such a site -- 202 amended so the enumerator's file set is the binding population and bin/** is explicitly outside it, already carried as register row doctor-version-only-check.
[2026-07-27T09:52:00+11:00] lint checks 1-8 otherwise clean: territory and forbidden disjoint per tasking; no orphaned deliverables; criterion 5's 16-hit population falls entirely inside exactly one territory (deploy.md 8 -> S2, the other 8 -> S3); the three territories are literal file paths with no glob intersection; every evidence command is runnable in a specialist harness; all 9 criteria map to a tasking or carry an [Owner] tag (criterion 9); no territory escapes the incident's own project root.
[2026-07-27T10:05:00+11:00] command: iap_review accept -- IC verified the partition and criterion 5's population itself rather than accepting the returns' claims (16 hits in the 6 named files; deploy.md 215 lines with steps 1-10 and the check-13 citation intact; installer citations line-exact; the __pycache__ asymmetry real). Endorsed fix (2) on three grounds: criterion 1's per-file report is unproducible by an aggregate marker so fix (1) is additive not alternative; an installer-written marker attests to intent not disk state; (2) has zero rollback residue and zero bootstrap. Three rulings issued, all binding -- (i) exit 3 (installed-only only) resolves to DEPLOYED with a mandatory flag naming the stale files, never a stop, since stopping would recreate the stop-on-a-correct-ship defect in a new shape; (ii) the fourth Owner substitution on this incident's own deploy train is acceptable and is the LAST, because that train installs the content-shaped step 7 every later train reads; (iii) the durable derivation guard is a registered follow-up, not this period -- planning it now would widen S1 into a deliberately forbidden file, and its trigger can only arise in a future installer-touching incident. Zero required_changes. (IC=dcs-commander, fable)
[2026-07-27T10:05:00+11:00] planning artifacts written: 203-ORG.md (Type 1, always), 204-TASKING/S1.md + S2.md + S3.md, IAP.md. Dispatcher applied the IC's transcription note -- S3's forbidden list is ENUMERATED rather than globbed, because four of its six territory files live inside directories that are otherwise off limits; a dcs/workflows/** or docs/** glob there would have put S3's own territory in its forbidden list.
[2026-07-27T10:05:00+11:00] 5a: register territory refined to the IAP partition union -- 8 files, 3 disjoint territories, parallel. The estimate SHRANK: install.ps1, install.sh, package.json and dcs/VERSION are in no territory and in every forbidden list. One conflict appeared that the stem could not see: dcs/templates/REGISTER.md is claimed by the queued esg-artifact-bloat row (rank 8) via dcs/templates/ -- recorded in REGISTER.md's Notes; that row must not open until this closes. tests/payload_check.py is a NEW file, not tests/test_doctrine_integrity.py, so the three-row cluster on that file is untouched.
[2026-07-27T10:14:00+11:00] Owner APPROVED the IAP (Type 1, mandatory -- no auto-approve path exists for Type 1, so no Delegation bound was evaluated). At the same gate the Owner GRANTED the IC's ruling (ii) authorisation in advance: the fourth and final version-marker substitution, bounded to this incident's own deploy train. The grant was recorded in IAP.md BEFORE the hash was computed, so the stamped artifact is the one the Owner approved and /dcs-deploy inherits the authorisation instead of raising a fresh prompt.
[2026-07-27T10:14:00+11:00] pre-stamp checklist passed: `command: typed` present (command point 1), `command: iap_review` present (command point 2).
[2026-07-27T10:14:00+11:00] IAP-APPROVED: b27f7200e4cd -- phase: planning -> execution (period 1)
[2026-07-27T10:20:00+11:00] period-boundary escalation checks clear: trigger (c) attempt 1 of 3 (one IAP-APPROVED sentinel; esg.max_periods_before_review = 3); trigger (d) no Delegation forbidden_glob is inside the IAP territory (install.*, package.json, bin/**, dcs/hooks/** all excluded by construction). Channel A preflight: `grep -c halt_cycles .claude/hooks/dcs_gate.py` = 8, so the halt ceiling is REAL here, not a phantom; `--halt-count` = 0.
[2026-07-27T10:20:00+11:00] Owner chose Agent-tool fan-out over the deterministic Workflow script (execute.md step 3.5, Type 1): two of the three taskings are prose judgment -- S2 rewrites workflow reasoning, S3 must decide per-hit which of 8 grep results are real defects and which are correct generic advice -- and a script cannot make those calls.
[2026-07-27T10:20:00+11:00] fan-out: 3 dcs-ops-specialist spawned in parallel on disjoint territories. All three returned status=done, zero deviations, zero blocked. files_touched validated against each declared territory: every file in-bounds, nothing outside. S3 touched 4 of its 6 territory files and left close.md and docs/spec-v0.3-parallel.md unmodified with a stated per-hit reason, which is the tasking's own carve-out, not an omission.
[2026-07-27T10:20:00+11:00] combined working-tree diff: 5 modified (CHANGELOG.md, CLAUDE.md, dcs/templates/REGISTER.md, dcs/workflows/deploy.md, skills/dcs-deploy/SKILL.md) + 1 new untracked (tests/payload_check.py); 141 insertions, 58 deletions. dcs/VERSION, package.json, install.ps1, install.sh and bin/** untouched, as the plan required. Uncommitted by design -- the integration commit is an IC step AFTER the Safety pass (execute.md 7).
[2026-07-27T10:20:00+11:00] two specialist self-reports the IC is passing to Safety as CLAIMS TO PROBE, not as findings: (1) S2 reports deploy.md at exactly 250 lines, the ~250 budget's own number, having "compressed repeatedly to land exactly at the budget" -- a number that lands exactly on its ceiling is worth checking for content lost to trimming, and the budget is a CLAUDE.md rule the suites do not enforce; (2) S1 copied test_doctrine_integrity.py's EXCLUDED_DIRS/BYTECODE_SUFFIXES verbatim rather than importing them, because that module runs its checks and calls sys.exit() at import time -- a defensible call, but it leaves two copies of one rule that can drift, which is the tasking's "do not invent a third rule" concern in a second form.
[2026-07-27T10:52:00+11:00] SAFETY-HALT: two refutations, both functional, both introduced by the fix rather than surviving it. (1) deploy.md step 7 lost the shape-awareness step 4 kept: step 4 handles four marker shapes and preserves the commit-ish ancestry check, while the rewritten step 7 mandates one mechanism and its outcomes are literally payload_check.py's four classes -- "repo-only" and "installed-only" are meaningless for a .deployed_sha file or a /version endpoint, and the old shape-agnostic text naming both was deleted with no replacement path. deploy.md is a SHIPPED deliverable and tests/payload_check.py is not in what the installers copy, so this converts a working verification into a hard stop for every consuming project. (2) Criterion 6 is not met: CLAUDE.md:40 says exit 2 "blocks nothing it can't explain" while deploy.md:217-218 says "report it and stop" -- and step 7 grounds that stop in "step 4's same 'cannot check' treatment", which is itself wrong, because step 4's cannot-check branch explicitly does NOT stop. Three normative statements of one disposition, written this period, disagreeing. Verdict also carries 8 advisories.
[2026-07-27T10:58:00+11:00] command: verdict -> fix_taskings (IC=dcs-commander, fable). Both refutations verified by the IC against the tree, not accepted on the officer's word. Rationale: the plan's premise is proven sound by the officer's own experiments (witness proven red four ways; derivation proven by adding files the script had never seen; criteria 1, 2, 3, 5, 7, 8 met on independent evidence), and both refutations are narrow execution defects in files already assigned. Halt count 1 of 3, so the fix-tasking path is available and this needs one cycle. Ceiling ruling: exceeding deploy.md's ~250-line budget by up to 15 lines is authorised for step 7's trichotomy -- the budget is approximate and unenforced, the officer proved clause-by-clause that the current 250 lines lost no rule, so there is no safe fat to trim; trimming prose that costs no normative clause is preferred, deleting a clause to fit is forbidden. Advisory dispositions: 2, 3 folded into S3; 4 folded in (reassigned, see below); 1, 5, 6 folded into S1; 8 left for the IC's integration commit (docs/** is unguarded, optional); 7 resolved by the ceiling ruling.
[2026-07-27T10:58:00+11:00] two Dispatcher transcription corrections to the directives, both mechanical rather than judgment, both recorded rather than silent. (1) The close.md directive was labelled [S2]; close.md is S3's territory in the approved partition, and assigning it to S2 would put one file in two territories -- lint check 4. Kept with S3, content unchanged. (2) The directive adding a constants-equality check to tests/test_doctrine_integrity.py is HELD, not applied: that file is in S1's forbidden list and outside every territory in the approved partition, S1 writing a guard that validates S1's own file is the section verifying its own work (principle 7), and three QUEUED register rows claim that file (check-14-hardening rank 3, schemas-contract-format rank 4, json-examples-unparsed rank 11). It also closes an ADVISORY, not a refutation, and an advisory never justifies widening an approved partition. Surfaced to the Owner at the UAT gate as an explicit choice rather than decided here; the IC was not given the portfolio-territory facts when it wrote "register no follow-ups from this set".
[2026-07-27T10:58:00+11:00] fix round: 3 FRESH dcs-ops-specialist spawns (doctrine principle 9b -- never a resumed agent; a resumed specialist still holds its OLD tasking). Amended taskings written to 204-TASKING/S1-FIX.md, S2-FIX.md, S3-FIX.md. Territories unchanged from the approved partition and still disjoint.
[2026-07-27T11:35:00+11:00] fix round 1 complete, all three fresh specialists returned done with zero deviations. Staged rather than fully parallel: S1-FIX and S2-FIX ran together, S3-FIX after, because making CLAUDE.md agree with step 7 word for word is a read dependency on S2-FIX's final text -- S3-FIX was given that text verbatim and told to read the file anyway. S2-FIX: step 7 is now shape-aware with four branches mirroring step 4's own (commit-ish -> ancestry check; content witness -> the four classes; readable-but-neither -> stop; unreadable-or-witness-error -> stop), and the false cross-reference is replaced by an explicit statement that step 7 is DELIBERATELY stricter than step 4 -- before a deploy over-shipping is the safe direction, after it recording an unproven ship is not. deploy.md 250 -> 265 lines, exactly the authorised ceiling. S3-FIX: CLAUDE.md now agrees with step 7 on all four exit classes, proven side by side class by class. S1-FIX: the installed-side guard mirrors the repo-side one, so an existing-but-empty installed root returns 2 (not a DCS install) where it returned 1 before, and the docstring census carries its regenerating command.
[2026-07-27T11:52:00+11:00] SAFETY-HALT: one refutation. Both of halt 1's refutations are confirmed CLOSED by the second officer's own traces -- a .deployed_sha project and a sha-returning /version endpoint both reach a definite outcome through the new step 7, and all four exit classes agree between CLAUDE.md and deploy.md. The new refutation is in dcs/templates/REGISTER.md: S3-FIX widened the DEPLOYED definition at lines 39-46 to admit two routes (commit-ish ancestry OR a green/stale-extras-only witness run) and left the FACTS-ONLY block 13 lines below it verbatim, which states the commit-ish route as a NECESSARY condition ("DEPLOYED only after the project's deployed marker was read and the merge commit confirmed an ancestor of it"). A content-witness ship produces no commit-ish to be an ancestor of, so the witness route is unreachable under the rule -- this incident's own target defect recreated inside the file that defines what DEPLOYED means, in a shipped template every DCS project instantiates. Confirmed newly introduced, not pre-existing: git show HEAD:dcs/templates/REGISTER.md is internally consistent, both halves commit-ish-shaped. Second instance same block: deploy.md:121 cites that facts-only rule as authority for "naming the sha or witness result", which the rule as written forbids. Verdict also carries 7 advisories.
[2026-07-27T11:58:00+11:00] ESCALATION: trigger b -- second Safety halt on the same objective, convergence read SAME CLASS (three refutations, one form: two statements of one contract disagreeing). Mechanism measured rather than asserted: criterion 5's enumerator defines its population by VOCABULARY while the defect is defined by ROLE -- the halting line reads "deployed marker was read" and matches none of the four patterns. Binding population returns 4 hits; a role-shaped sweep returns 22. Criterion 5 was MET at both halts and would keep being met while the class kept shipping. 209 filed at <esg_root>/.dcs/esg/SITREPS/deploy-marker-blind-p1.md -- Owner: PIVOT, raise the altitude, re-plan.
[2026-07-27T11:58:00+11:00] safety valve (run.md step 6) checked before looping back to planning, and it does NOT fire: IAP-APPROVED stamp count is 1, so the re-plan makes attempt 2, under the cap of 3; halt count is 2, under dcs_gate.py's ceiling of 3 rather than sitting at it. Both read from the log and the gate, not from a turn-local tally, so the valve holds across a session boundary.
[2026-07-27T11:58:00+11:00] phase: execution -> planning (period 1, revision 2). plan.md step 1's execution-phase question is already answered by the Owner's pivot decision: this is a deliberate re-plan, and it voids the current approval mechanically the moment IAP.md changes -- IAP-APPROVED's hash b27f7200e4cd will no longer match. That is the deviation doctrine working, not a bug to route around. The specialists' work stays on disk and is NOT reverted; what is voided is the plan's authority, not the diff.
[2026-07-27T12:05:00+11:00] 202 revision 2 drafted and confirmed by the Owner. Criteria 1-4 and 6-8 carried unchanged and explicitly NOT re-litigated -- both officers verified them independently and the second ran 14 perturbation scenarios itself. Criterion 5 redefined by ROLE rather than vocabulary; new enumerator measured at plan time: 18 hits in 6 files (deploy.md 9, templates/REGISTER.md 3, SKILL.md 2, close.md 1, CLAUDE.md 1, docs/spec-v0.3-parallel.md 1). New criterion 5a is a NEGATIVE CONTROL: the new population must be SHOWN to return templates/REGISTER.md:59-60 where the old one does not -- this incident has already shipped one thing that looked like new capability and was a relabel, and a population definition that cannot demonstrate new reach is the same mistake at a higher altitude. New criterion 10 (no file states the contract two ways) puts halt 2 at class level rather than as a REGISTER.md special case.
[2026-07-27T12:05:00+11:00] Owner chose to ADD THE DURABLE GUARD to this period rather than register it as a follow-up (new criterion 11), which widens territory to include tests/test_doctrine_integrity.py. Consequences accepted and recorded: three QUEUED rows claim that file (check-14-hardening rank 3, schemas-contract-format rank 4, json-examples-unparsed rank 11) and stay queued while this runs -- they already collide with each other regardless. The guard follows the pattern checks 13 and 14 already establish: population derived by walking, the only file literal is the source of truth's own path, exit-class tokens parsed out of deploy.md step 7 at run time, a structural-degeneracy guard so an emptied population is red rather than vacuously green, and a behavioural proof (forge a disagreement -> red naming the file; the identical forgery against git archive ba6019e -> green, proving new capability rather than a relabel).
[2026-07-27T12:05:00+11:00] the directive held at command point 4 is RELEASED into this revision as criterion 12: the constants-equality check between payload_check.py and test_doctrine_integrity.py. It was held because it needed a file outside the then-approved partition; criterion 11 brings that file into territory, so it lands here rather than as a follow-up. The hold was the right call at the time and the Owner has now made it moot rather than overruled it.
[2026-07-27T12:40:00+11:00] chiefs returned for revision 2 (both fresh spawns). Planning Chief proposes 3 specialists run SEQUENTIALLY, S1-CONTRACT -> S2-GUARD -> S3-RECORD, and spends 3 of the permitted 4 deliberately: revision 1 split the contract surfaces across two heads and the halt landed exactly on that seam, so one hand owns all six declaring files. Its structural answer to the same-class recurrence is one-source-plus-citation -- only deploy.md step 7 states dispositions, every other site cites it in a fixed form, and AT MOST ONE declaring paragraph per non-source file, which is what catches halt 2 phrasing-independently: REGISTER.md's second declaring paragraph is red for EXISTING, whatever it says. Logistics Chief measured the reach split with npm pack --dry-run (75 files; tests/ and CHANGELOG.md ride the tarball but NEVER reach ~/.claude; CLAUDE.md is in neither the payload nor the files whitelist) and verified `git archive ba6019e` emits all 206 entries and runs the suite 73/73 green from a scratch extraction.
[2026-07-27T12:40:00+11:00] tasking lint: 2 defects fixed pre-review. (1) Criterion 5's per-file breakdown was wrong -- the Planning Chief caught it and re-measurement confirms deploy.md is 10 hits, not 9; total 18 in 6 files holds. 202 corrected, with a note that the criterion binds to the command's live output rather than to the list. (2) Criteria 1 and 2 mapped to no tasking and carried no ownership tag (lint check 6), which reads as unsatisfiable-as-written; they are already delivered and tests/payload_check.py is forbidden to every tasking deliberately, so both are now tagged explicitly with the reason. Criterion 5a's negative control re-verified at lint: old enumerator 4 hits, new 18, and only the new one returns dcs/templates/REGISTER.md:59-60.
[2026-07-27T12:40:00+11:00] lint checks 1-8 otherwise clean: territory and forbidden disjoint per tasking; three territories (6 prose files / 1 test file / 1 changelog) with no path in two; all 18 population hits inside S1-CONTRACT's territory; criterion 5 is enumeration-shaped with the command run at plan time and its output recorded; every evidence command runnable in a specialist harness; no territory escapes the project root.
[2026-07-27T13:05:00+11:00] command: iap_review accept, revision 2 (IC=dcs-commander, fable), zero required_changes beyond six binding directives. IC verified the enumerator (18 hits in 6 files, all inside S1-CONTRACT's territory), the ceiling (deploy.md exactly 265), the partition (disjoint by inspection of explicit file lists), and found SHIPPED PRECEDENT settling directive (i): tests/test_doctrine_integrity.py:549-555, check 13, already pins a named anti-erasure floor on a walked population. It also established a fact that frees directive (ii) from the merge gate: the hot-path budget (check 7) covers only doctrine.md + schemas.md, so a deploy.md overrun cannot touch the 1,205 B spare -- the line ceiling is a doctrine discipline, not a byte tripwire, and no integrity check counts workflow lines.
[2026-07-27T13:05:00+11:00] six binding IC directives recorded in IAP.md: (i) criterion 11's literal rule takes the NARROW reading -- one named anti-erasure floor permitted, dcs/templates/REGISTER.md, per check 13's precedent, because the hazard the no-literal rule guards against is a FITTED LIST and a floor assertion is not one; (ii) the 265-line ceiling holds as the target with a pre-authorised band to 275, every extra line inside step 7's disposition section, and the step-4/step-7 asymmetry named as PROTECTED -- rejected both raising the ceiling wholesale and holding it hard, because a specialist squeezing under a hard ceiling deletes the asymmetry silently; (iii) rule C holds TREE-WIDE, the chief's fallback rejected because scoping to dcs/ and skills/ drops CLAUDE.md, which is both a halt-1 refutation site and where the flawed contract was normatively stated; (iv) the fourth specialist slot IS used, read-only, as a pre-Safety reconciliation pass -- the one fourth use that cannot recreate the seam; (v) the self-blocking-guard escape is pre-defined NOW rather than discovered under a red gate, since a too-strict check 15/16 makes the incident that fixes it red by construction; (vi) P3's non-emptiness condition is binding, and the baseline is git archive of the LITERAL sha ba6019e, never HEAD -- verified: HEAD currently EQUALS ba6019e and will silently diverge at the first commit.
[2026-07-27T13:05:00+11:00] 5a: register territory refined to the revision-2 partition union -- nine files held, four specialists, three disjoint territories, SEQUENTIAL. tests/test_doctrine_integrity.py enters territory, so check-14-hardening (rank 3), schemas-contract-format (rank 4) and json-examples-unparsed (rank 11) must not open until this closes. tests/payload_check.py moves from owned to FORBIDDEN TO EVERY TASKING -- delivered and Safety-proven, and criterion 12 asks for a check that its constants stay aligned, not for an edit.
[2026-07-27T13:12:00+11:00] Owner APPROVED IAP revision 2 (Type 1, mandatory -- no auto-approve path reaches Type 1). Declined the offered fallback of dropping criterion 11's guard, so the durable mechanism stays in this period. Pre-stamp checklist passed: `command: typed` present, `command: iap_review` present (three of them now -- rev 1's accept, and rev 2's).
[2026-07-27T13:12:00+11:00] IAP-APPROVED: 215939852854 -- phase: planning -> execution (period 1, revision 2 / attempt 2)
[2026-07-27T13:55:00+11:00] S1-CONTRACT returned done, no deviation. deploy.md step 7 is now the declared single source of every disposition; every other declaring paragraph in the territory cites it verbatim. Both criterion-10 instances fixed: REGISTER.md's facts-only block no longer restates the DEPLOYED condition (it points at the definition above, keeping the branch-deleted/worktree-removed clauses and the 2026-07-23 field lesson intact), and deploy.md step 4's out-of-band paragraph now cites step 7 for the disposition and the facts-only rule only for the discipline. All three step-7 advisories folded in. deploy.md 265 -> 275 lines, exactly the pre-authorised band, paid for by compressing step 4's shape branches into cross-references to step 7 -- which is the one-statement-per-file fix, not a tax. The protected step-4/step-7 asymmetry is untouched in substance. Population moved 18 hits in 6 files -> 11 in 5: skills/dcs-deploy/SKILL.md dropped out of the vocabulary population entirely.
[2026-07-27T13:55:00+11:00] S1-CONTRACT flagged one thing against itself rather than hiding it, and it is the exact case the IAP's risk list anticipated: skills/dcs-deploy/SKILL.md's frontmatter description and <objective> BOTH still carry the literal token DEPLOYED, so a strict word-co-occurrence predicate would count two declaring paragraphs in one file and rule C would go red. Its argument is that neither restates step 7's disposition logic, which is what rule C protects against. That is a judgement about the guard's predicate, not about the prose, so it is S2-GUARD's to resolve -- passed to it explicitly as a named test case rather than left to be discovered. Per the IAP, if the guard reddens here S2 raises a DEVIATION naming the file and the rule; it does NOT edit prose to make its own guard green.
[2026-07-27T14:30:00+11:00] S2-GUARD returned done, no deviation. Checks 15 and 16 added; integrity suite 73 -> 82 (7 for check 15, 2 for check 16). It resolved S1's handed-over SKILL.md question as Option 2 -- the co-occurrence predicate was too coarse -- and TIGHTENED it: a declaring paragraph must bind DEPLOYED in a RULE SHAPE (definitional dash, an only-after/once/when/if conditional, or an arrow resolution), not merely narrate a workflow ACTION ("marks rows DEPLOYED"). The burden of proof for a tightening was discharged three ways rather than asserted: P2's halt-2 sentence still reds (it is a rule, not an action); the tightening independently excluded a SECOND false positive S1 never flagged (REGISTER.md's facts-only paragraph, which defers with "only per" rather than asserting a competing rule); and, decisively, running the tightened check's code against ba6019e's REGISTER.md content finds 2 declaring paragraphs and reds rule C -- so had this check existed before the incident it would have caught the actual defect.
[2026-07-27T14:30:00+11:00] P3 satisfied IC directive (vi) in both directions: the old tree's OWN suite is green at 73/73 against the semantic-equivalent forgery (it contains no check reading deploy.md's dispositions at all -- that is the new reach), AND the new check's derived population on that tree is non-empty and printed (45 files walked, 2 declaring paragraphs), so the green is not vacuous. The "exactly one source section: found 0" red on the old tree is the correct signal that ba6019e predates the disposition contract.
[2026-07-27T14:30:00+11:00] IC flags one thing forward to S4-RECONCILE and the Safety Officer rather than accepting it: at end state the DECLARING population is 1 paragraph across 1 file (dcs/templates/REGISTER.md). The two-surface degeneracy requirement was applied to the WALKED population (45 files, 3 surfaces), not the declaring one. That reading is defensible and the anti-erasure floor pins the one member -- but a guard whose live population is a single paragraph is close to vacuous in practice, and the question of whether the tightening went one notch too far is exactly what a read-only reconciliation pass and an adversarial officer exist to answer. Not a finding; a probe.
[2026-07-27T15:10:00+11:00] S3-RECORD returned done, no deviation. CHANGELOG surface list rebuilt from git diff --stat at end state, closing the omitted-close.md advisory. It also caught a real error in the earlier draft by reading the actual deploy.md diff rather than the summary it was given: the draft claimed readable-but-neither AND unreadable both stop, but step 7 gives readable-but-neither step 6's harness-refusal treatment (stays MERGED (deploy pending), named remedy, never an override) and only unreadable stops. Corrected, and labelled as a correction rather than silently rewritten. npm view re-measured: still 0.6.9, so 0.6.10 remains unpublished and the entry stays in that section.
[2026-07-27T15:10:00+11:00] S4-RECONCILE returned done, files_touched EMPTY, and it earned the fourth slot. Both manual reads land on ONE statement. It re-implemented the declaring predicate itself and enumerated the 9 paragraphs the loose version would have caught and the tightened one excludes, judging each by role -- all 9 are action narration or REGISTER.md's deferring clause, none a real second statement, so the tightening did not go a notch too far. It REPRODUCED rather than read S2's load-bearing claim: extracting ba6019e's REGISTER.md into scratch and running the tight predicate finds 2 declaring paragraphs, so the check would have caught the actual historical defect. It confirmed the guard's narrowness limit is exactly close.md:66 and nothing wider. One honest correction to the IC's OWN ruling text: "every line past 265 inside step 7's disposition section" is true as delta attribution but not of the file's physical tail -- step 7 ends at 244 and lines 266-275 are steps 9 and 10. A wording imprecision in the ruling, not a specialist defect.
[2026-07-27T16:20:00+11:00] SAFETY-HALT: two refutations, both against criterion 11's guard rather than against the prose. Officer 3 confirmed the DELIVERABLE is sound -- deploy.md read end to end, step-4/step-7 asymmetry present and named deliberate, no file states the contract two ways at end state, criteria 1-8 and 10 re-derived green, the witness proven to find a file it had never seen. What it refuted is the guard's SENSITIVITY, by forging four contradictions that stayed GREEN at 82/82. (1) Rule B's disposition comparator is defeated by MARKUP: `_states_deployed` requires a BACKTICKED DEPLOYED in the window, and REGISTER.md's legend writes it unbackticked as a table label, so flipping "Differing or repo-only" onto a DEPLOYED route passes; worse, on the unforged tree none of step 7's four class names appears in the one live declaring paragraph at all, so the comparator currently contributes ZERO binding. The officer also rewrote REGISTER.md's legend as the ORIGINAL 201 defect, correctly cited -- green. (2) CLAUDE.md can NEVER enter the declaring population: `grep -n DEPLOYED CLAUDE.md` returns zero hits, because it states dispositions in exit-code vocabulary. So IC directive (iii), which held rule C tree-wide expressly to cover CLAUDE.md, is defeated not by the population but by the predicate -- the officer reintroduced halt 1's exact defect there (exit 2 "a warning only, the train continues") and got 82/82 green.
[2026-07-27T16:30:00+11:00] ESCALATION: trigger b -- THIRD SAFETY-HALT on the same objective, convergence read SAME CLASS for the third time, but with a material difference recorded rather than glossed: halts 1 and 2 were defects in the DELIVERABLE; halt 3 is not. The shipped prose is correct and officer-verified. What failed is the mechanism claiming to hold it. Three attempts at a role-shaped detector, each defeated by a narrower surface assumption than the last -- vocabulary, then token, then markup. 209 appended to <esg_root>/.dcs/esg/SITREPS/deploy-marker-blind-p1.md -- Owner: CONTINUE, narrow the guard's claim to what it demonstrably enforces. Routes to a fix-tasking; the IAP stands and is NOT voided, so the halt count does NOT reset -- it stays at 1 of 3 and the next iteration is the last before a mechanical denial.
[2026-07-27T16:30:00+11:00] command point 4 disposition: fix_taskings, DECIDED BY THE OWNER at the escalation rather than by the IC at the command point. Recorded that way deliberately -- the Owner's escalation answer named the disposition and the fix shape precisely, and spawning a commander to re-decide what the Owner had just decided would be ceremony without judgment. Doctrine's escalation path says "proceed per the decision", and this is that.
[2026-07-27T17:15:00+11:00] S2-FIX2 returned done, no deviation. Rule B REMOVED rather than repaired, which is what the Owner's decision preferred rather than a fallback. Its diagnosis of why repair was impossible is the valuable part: F2 cites step 7 CORRECTLY and names NONE of the four classes, using superseded vocabulary instead, so a class-name comparator cannot catch it by construction -- the same root failure (prose using vocabulary a recognizer does not expect) reproduced INSIDE the recognizer. It also confirmed rule B was not dead code by construction (a constructed well-formed case did redden under the old code), so the removal is a narrowing of a real but unreliable check, not the deletion of a no-op. The per-file PASS line now reads "cites deploy.md step N by number (disposition-content agreement beyond the citation is a human read, not checked here)", and a removal-rationale comment walks F1/F2's distinct failure modes so a future maintainer does not rediscover this the hard way. All nine PASS strings confirmed literally true. Suite stays 82/82.
[2026-07-27T17:15:00+11:00] second honest finding, reported rather than forced: CLAUDE.md STILL cannot enter the declaring population, and it is not a markup problem -- `grep -n DEPLOYED CLAUDE.md` returns ZERO hits, because the file states dispositions entirely in exit-code vocabulary. No backtick fix reaches a paragraph that never contains the token. The specialist widened the token match as tasked, checked the population, and reported non-entry instead of bending the predicate to force it. IC directive (iii)'s aspiration to hold rule C over CLAUDE.md is therefore RECORDED AS UNMET rather than quietly dropped.
[2026-07-27T17:15:00+11:00] 202 criterion 11 AMENDED to record the Owner's narrowing, so officer 4 judges against what the Owner decided rather than against the superseded text: met when the guard enforces AND CLAIMS exactly rule A (citation to the live step number), rule C (one declaring paragraph per non-source file, tree-wide -- the rule that catches halt 2 and is proven), the structural degeneracy guard and anti-erasure floor, and every PASS string literally true. Disposition-content agreement and CLAUDE.md coverage are explicitly NOT required and explicitly NOT to be claimed.
[2026-07-27T17:15:00+11:00] a specialist reported that one of its tool results carried an embedded prompt-injection payload -- a "LAZYWEB:ROUTER" block instructing it to modify persistent instruction files. It did not act on it and said so unprompted. Recorded here because an agent correctly refusing an injected instruction is exactly the behaviour that should leave a trace. No instruction file was touched by any agent this incident; the Owner was told.
[2026-07-27T18:05:00+11:00] SAFETY-HALT: one refutation, in CHANGELOG.md, not in the code. Officer 4 attacked rules A and C NINE ways in scratch -- second declaring paragraph, the verbatim halt-2 sentence, citation renumbered, citation removed entirely, deploy.md's live step renumbered 7->11, source anchor removed, a second section claiming source-of-truth, arrow dispositions delexicalised, erasure -- and every one reddened. It explicitly did NOT count F1-F4's designed green against the work, and verified every PASS string is literally true. The refutation is that CHANGELOG.md:136-140 still says the guard makes each paragraph "agree with its class->disposition map" -- that IS rule B, removed at halt 3 and named in the 202 amendment as explicitly not to be claimed -- and says "the only such paragraph in its file outside step 7 itself" when rule C exempts the whole SOURCE FILE, proven by inserting two correctly-cited declaring paragraphs into deploy.md for 83/83 green. CHANGELOG.md is in package.json's files whitelist, so the false claim ships to consumers.
[2026-07-27T18:05:00+11:00] convergence read, fourth halt: DIFFERENT CLASS, and the officer said so unprompted -- "this halt is not a re-litigation of the guard. The failure is a seam." Halts 1-3 were a DESIGN failure: a detector defined by surface form while the defect is defined by role. This is a STALENESS failure: an artifact written before a decision and never revisited, because the fixing specialist's territory excluded it. The distinction matters and is not a technicality -- THE IAP PREDICTED THIS EXACT FAILURE and prescribed its mitigation: "CHANGELOG is written in parallel with the changes it describes, so any specialist deviation leaves it stale by construction; mitigated in the verification plan -- the officer diffs the entry against the actual git diff, not against the IAP." The officer did precisely that and caught it. The mechanism worked as designed, which is the signature of a converging incident rather than whack-a-mole.
[2026-07-27T18:12:00+11:00] ESCALATION: trigger b -- fourth SAFETY-HALT, convergence read DIFFERENT CLASS and converging. 209 appended to <esg_root>/.dcs/esg/SITREPS/deploy-marker-blind-p1.md -- Owner: CONTINUE with one fix-tasking. IAP stands, no new stamp, halt count stays 2 of 3 and the next iteration is the last before a mechanical denial. Command point 4 disposition again decided by the Owner at the escalation rather than by the IC at the command point, for the same reason as last time: the Owner's answer named the disposition and the fix precisely.
[2026-07-27T18:45:00+11:00] S3-FIX2 returned done, no deviation. CHANGELOG's two false conjuncts corrected AND the entry made honest about the halt in the same breath: rule B was built and withdrawn, the reason stated (a contradiction can cite step 7 correctly while naming none of its classes, which a vocabulary-anchored comparator can never see by construction), and what the guard DOES enforce named without implying more. It verified its text against the code -- quoting rule A's live PASS string and rule C's `if r != _DEP_SOURCE_REL` condition beside its own sentence -- which is the discipline that was missing the round before. `grep -rniE 'agree with|class.{0,3}(->|arrow) disposition' CHANGELOG.md` returns nothing.
[2026-07-27T18:45:00+11:00] IC fixed officer 4's four remaining advisories itself rather than spending an execute-and-verify cycle on wording (execute.md 9b's rule for advisories, applied early because halt count is 2 of 3 and ALL FOUR are the same class officer 4 halted on -- a claim outranking what the check does. Leaving them would have risked a fifth halt, which is a mechanical denial). Fixed in tests/test_doctrine_integrity.py: the check-15 docstring no longer claims "a rewording can't dodge it" (it says a rule shape is still required and a rewording outside the three recognised shapes does dodge it, and that the source file is exempt WHOLE rather than just its step-7 section); the class-map PASS line now says "parsed as a degeneracy tripwire only -- no rule compares a declaring paragraph against it since Rule B's removal"; the population PASS line no longer calls two named root files "walked". And in 202-OBJECTIVES.md, criterion 5a now cites the SENTENCE rather than REGISTER.md:59-60, because a line number is a derived fact about one tree and that one silently pointed at the attempt-1 worktree while instructing a measurement on ba6019e, where the text sits at 55-56.
[2026-07-27T18:45:00+11:00] suites after the IC's advisory fixes, read from their own lines: test_doctrine_integrity.py 82/82 exit 0, test_dcs_gate.py 100/100, test_dcs_intake.py 10/10. Encoding of the edited file: no BOM, zero CRLF, 3 non-ascii bytes -- all three PRE-EXISTING at HEAD (line 344, an em-dash in an unrelated check's message), confirmed by decoding `git show HEAD:tests/test_doctrine_integrity.py`. Not introduced by any agent this incident.
[2026-07-27T20:05:00+11:00] SAFETY-PASS: period 1 complete
[2026-07-27T20:30:00+11:00] SAFETY-PASS verdict 5 transcribed verbatim to SAFETY.md, with all five verdicts of the period tabled -- the four halts are kept because the history is the point. All FIVE advisories fixed by the IC before the integration commit (execute.md step 9's advisory rule), and three of them were this incident's own defect class: a population PASS line that printed "plus the named root files" while both were deleted; a docstring gloss that merged rule A's and rule C's different scopes; and a CHANGELOG sentence stating one rule two ways in one paragraph. Advisory 1's fix is PROVEN, not asserted: deleting README.md in a scratch copy now reds the population check where it stayed green at 82/82 before. Also fixed: a line-range citation that was wrong in the tree it was written in (now cited by symbol), and "a human read at merge/close time" -- which names a control that does not exist, since close.md step 1a runs the suite and stops -- reworded to "not checked mechanically anywhere; it relies on review".
[2026-07-27T20:30:00+11:00] integration commit 916bebc (9 files) -- staged explicitly by path, never git add -A. Suites after the advisory fixes, read from their own lines: 82/82 exit 0, 100/100, 10/10.
[2026-07-27T20:50:00+11:00] Owner-UAT APPROVED (criterion 9) against the question "is this the shape you were reaching for when you authorised the substituted check three times by hand?", offered with `python tests/payload_check.py --repo C:/DCS` plus deploy.md steps 4 and 7 as the concrete check. Close-time re-measurement, not carried from plan time: `npm view dcs-command-system version` -> 0.6.9 while dcs/VERSION reads 0.6.10, so 0.6.10 is still unpublished, the CHANGELOG entry stays in that section and no bump was needed.
[2026-07-27T20:50:00+11:00] AAR.md and SAFETY.md written. Memory routed to the vault per CLAUDE.md: Backlog item 12 CLOSED with the chosen fix and, deliberately, with what the item asked for and did NOT get; two new items registered (17 -- check 15 holds citations not content and cannot reach CLAUDE.md; 18 -- the ~250-line workflow budget is a rule no suite enforces, which needed an IC ruling twice in one incident); two meta-lessons appended to vault/Meta/building-dcs-lessons.md (14 -- a detector keyed on surface form cannot enumerate a population defined by role, with the vocabulary/token/markup table and the "under-claims truthfully" rule; 15 -- split one contract across two owners and the defect lands on the seam).
[2026-07-27T21:15:00+11:00] merge-time guards, all three runs the Logistics Chief specified, in order. R1 in the worktree: 82/82 exit 0. R2, THE MERGE-CONTENT RUN -- `git archive dcs/deploy-marker-blind` (227 entries) extracted OUTSIDE both checkouts and the suite run from there: 82/82 exit 0, 100/100, 10/10, and it confirmed tests/payload_check.py IS carried by the archive, which was the specific risk (it was untracked until the integration commit, so R1 alone would have passed on a file the merge might not carry). R3 on the merge result: 82/82 exit 0, 100/100, 10/10.
[2026-07-27T21:15:00+11:00] merged into main at 0843816 (--no-ff). Integration branch confirmed still `main` at ba6019e before the merge and never switched; primary checkout was clean, so no file overlap to assess. No conflict -- the territory promise held.
[2026-07-27T21:15:00+11:00] register row ACTIVE -> MERGED (deploy pending); the territory lock is released, so esg-artifact-bloat and the tests/test_doctrine_integrity.py cluster may open, the cluster still one at a time. Worktree C:\DCS-wt\deploy-marker-blind removed and verified off disk and out of `git worktree list`. Branch dcs/deploy-marker-blind KEPT as the rollback reference until /dcs-deploy confirms the ship.
[2026-07-27T21:15:00+11:00] hygiene, attempted and NOT completed -- recorded as a not-removed per the facts-only rule. The empty orphaned directory C:\DCS-wt\schema-citation-guard, whose removal the Owner chose at the 2026-07-27 ESG and which failed there because that session's shell was rooted inside it, was retried from a session rooted in C:\DCS: `rmdir` returned "Device or resource busy". It is verifiably empty (0 entries) and absent from `git worktree list`, so removal is still zero-loss; something other than this session holds it. Not forced. Still owed, still a one-liner from any terminal that does not hold it: rmdir "C:\DCS-wt\schema-citation-guard".
[2026-07-27T21:15:00+11:00] run.md step 7a checked: Delegation v3 has deploy.auto_after_close FALSE, so no deploy train runs in-line. Reporting deploy-pending, which is what the Owner's revealed preference at Delegation v2 established -- close and ship are separate acts.
[2026-07-27T21:15:00+11:00] incident closed, archived
