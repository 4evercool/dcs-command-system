# 204 — Tasking S1

**Incident:** register-field-repair-path
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Declare the field-repair convention in `dcs/templates/REGISTER.md`. This is
the SINGLE declaring site for the whole package (202 criterion 3), and this
file is check 15's anti-erasure floor -- read the mechanical constraints at
the bottom of this tasking BEFORE editing.

Two edits, both inside HTML comments (the file's existing convention --
guidance never renders in a project's register):

**(a)** In the existing state-enum comment block, extend the `RESOLVED`
description by one clause: a field repair -- an Owner-authorized fix applied
entirely outside the incident lifecycle -- is recorded with this state
qualified, State cell reading exactly `RESOLVED (field repair)`, pointing at
the block added in (b). Do NOT add a new state token: the enum line must
still list exactly the seven existing tokens. Do NOT introduce a blank line
anywhere inside this comment block (202 criterion 4 / IAP risk: check 15
rule A's citation must stay in the same paragraph).

**(b)** Add ONE new comment block (placed after the FACTS-ONLY block, before
the table) that declares the convention in full and is the only place in
`dcs/`, `agents/` or `skills/` that does so (202 criteria 1 and 2):

- **WHAT:** an Owner-authorized fix applied entirely outside the DCS
  lifecycle -- no 201, no typing decision, no worktree, no IAP, no Safety
  review. Distinguish it in one clause from `RESOLVED`'s existing inline
  case, which was a typed incident completed inline; a field repair never
  enters DCS's process at all.
- **STATE CELL:** the exact literal `RESOLVED (field repair)` -- a qualifier
  on an existing terminal state, the same shape as `MERGED (deploy
  pending)` above, never a new enum value.
- **CELLS**, as one generative rule rather than a per-cell list: every cell
  recording a DCS act that never happened reads the template's existing
  em-dash -- Type (never typed), Priority (never ranked), Worktree and
  Branch (never opened) -- while `Closed` carries the date the fix was
  applied and is filled at the same time as the state and Outcome, exactly
  as `RESOLVED` already requires.
- **OUTCOME MINIMUM**, all three mandatory (criterion 1): a one-line
  statement of what was fixed; a commit/diff reference given as a
  regenerating command, not a bare sha (principle 15); and explicitly
  whether a retroactive Safety look was done -- "none" written out, never
  omitted.
- **WRITER** (criterion 2): the Chief of Staff during `/dcs-esg`, on the
  Owner reporting a fix applied outside the lifecycle. State what makes it
  unlike every existing writer: it ORIGINATES a row directly in a terminal
  state with no prior `QUEUED`/`ACTIVE` phase -- every other writer either
  transitions a row that already exists or originates one into
  `QUEUED`/`ACTIVE`. Add the one rule this writer needs that no other does:
  because the row's facts are REPORTED rather than observed by the writing
  session, FACTS-ONLY applies with the commit reference verified first
  (`git show <sha> --stat`) before the row is written.

## File territory (may edit only within these globs)

- `dcs/templates/REGISTER.md`

## Forbidden zones (explicitly, even if it seems related)

- `tests/**`
- `dcs/references/**`
- `dcs/workflows/**`
- `dcs/hooks/**`
- `dcs/templates/STRATEGY.md`
- `dcs/templates/DELEGATION.md`
- `dcs/templates/209-SITREP.md`
- `agents/**`
- `skills/**`
- `CHANGELOG.md`
- `package.json`
- `bin/**`
- `install.*`
- `.dcs/**`
- `vault/**`
- `docs/**`
- `README.md`
- `CLAUDE.md`

## Mechanical constraints (check 14/15 -- read before editing, not after)

Check 15 pins this file twice: it is the named anti-erasure floor (must
stay in the declaring population) and it is capped by rule C at ONE
declaring paragraph. A paragraph declares if it carries the literal token
`DEPLOYED` AND a proof word (verified/read/confirmed/proof/ancestor/
witness/evidence) AND a rule shape (`DEPLOYED --`, `DEPLOYED only after|
once|when|if`, or U+2192 immediately before `DEPLOYED`).

Pre-edit baselines, verified by the IC directly before this tasking was
issued (not merely asserted): `DEPLOYED` literal count **7**, U+2192 count
**0**, `python tests/test_doctrine_integrity.py` **82/82 passed** with
check 15's declaring-population line reading exactly `1 paragraphs across 1
files: ['dcs/templates/REGISTER.md']` and both rule A and rule C PASS.
Neither count may move, and the suite must still report N/N (N >= 82) with
those same two check-15 PASS lines intact.

**IC command-point-2 carry (IAP review), binding, not advisory:** the
writer-map header paragraph you are editing in (b) ALREADY holds two
`DEPLOYED` tokens plus the proof word "read" -- two of the predicate's
three conditions are already present in that paragraph before you touch
it. Beyond keeping the literal token `DEPLOYED` and the U+2192 arrow out of
everything you WRITE, you must also not introduce a `--` or a `:`
immediately after any EXISTING `DEPLOYED` token while editing nearby text --
either would complete the third condition (rule shape) on text that is
already there and turn rule C red on a sentence you did not intend to
declare. If in doubt, leave existing `DEPLOYED` occurrences' surrounding
punctuation untouched and add your new material as clearly separate
sentences.

Secondary: do not use the words advisory/advisories/refutation/halt with a
bar count anywhere in the new text (check 14's declaring predicate; check
14 cannot actually reach this file today per the IC's verification, but
write clean of it anyway rather than relying on that gap).

If a check goes red, the fix is to reword THIS file -- `tests/**` is
forbidden to you, and editing the check to accommodate the prose is the
exact failure the sibling incident's Safety pass exists to catch. That is a
deviation to report, not a workaround to take.

## Evidence required in the return

- `python tests/test_doctrine_integrity.py` -- quote the final `N/M passed`
  line verbatim, plus these two check-15 lines verbatim: the
  `deploy-evidence: declaring population ...` line (must still read `1
  paragraphs across 1 files: ['dcs/templates/REGISTER.md']`), and the
  `deploy-evidence rule C ...` PASS line. Quote the `deploy-evidence rule
  A: dcs/templates/REGISTER.md` line too -- it proves the step 7 citation
  survived your edit.
- `python -c "t=open('dcs/templates/REGISTER.md',encoding='utf-8').read(); print('DEPLOYED', t.count('DEPLOYED')); print('U+2192', t.count(chr(0x2192)))"`
  -- report the output and state it equals the pre-edit baseline (DEPLOYED
  7, U+2192 0). A change in either number is a stop, not a note.
- Quote the state-enum line verbatim after your edit and state the token
  count -- it must still be exactly the seven existing tokens, no eighth.
- `grep -n "field repair" dcs/templates/REGISTER.md` -- paste the output
  and classify each hit as DECLARING or POINTER. Exactly one block may be
  declaring.
- `git diff --stat` -- exactly one file changed, `dcs/templates/REGISTER.md`.
- Quote the new declaring block in full in your return, so the IC and
  Safety Officer can check criteria 1 and 2 against it without re-reading
  the file.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
