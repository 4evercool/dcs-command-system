# 204 — Tasking S2

**Incident:** register-field-repair-path
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Give the convention a live writer and a release note. **Runs AFTER S1**
(sequential execution mode, see `203-ORG.md`) so your evidence grep sees
the integrated tree, not the pre-S1 tree.

**(a) `dcs/workflows/esg.md`** -- the channel-1 carrier that reaches
existing projects, whose registers will never receive S1's template
header. Two small insertions, both CITING the register template's
field-repair block, never restating its cell rules or Outcome minimums
(202 criterion 3 caps the package at one declaring site, and a restatement
here would break it):

- Step 2 "Draft agenda": add item `(g)` -- field repairs the Owner reports
  since the last sweep, each needing a post-hoc register row.
- Step 4 "Record": add one bullet -- originate a post-hoc row for each, per
  the register template's `RESOLVED (field repair)` convention, verifying
  the commit reference (`git show <sha> --stat`) before writing the row
  since its facts are reported rather than observed.

DO NOT renumber, relabel or reorder the existing agenda items (a)-(f): the
letter `(f)` is cited by four other files -- `dcs/references/doctrine.md`
line 153, `dcs/workflows/deploy.md` line 43, `dcs/workflows/loop.md` line
23, and `esg.md`'s own step 1 -- and none of them is in anyone's territory
this period. Append `(g)`, touch nothing above it. Keep esg.md's existing
voice and its ~250-line workflow budget (currently 128 lines; your addition
should be a handful).

**(b) `CHANGELOG.md`** -- one entry in the EXISTING open section `## 0.6.10
- 2026-07-26`, under its existing `### Added` subsection, matching the
surrounding entries' style (bold lead sentence, then the concrete
file-level detail). Name the label `RESOLVED (field repair)`, say it
qualifies an existing state rather than adding one, and name both changed
payload files (`dcs/templates/REGISTER.md`, `dcs/workflows/esg.md`). NO
version bump, NO new version heading, NO edit to `dcs/VERSION` or
`package.json` -- 0.6.10 is unpublished and this rides in it, exactly as
`direct-resolution-lane` did.

Write with the Write/Edit tools only, never PowerShell
`Set-Content`/`Out-File` (BOM). English only. If your grep shows your own
`esg.md` wording reading as a second declaration, fix YOUR wording -- the
template is S1's territory and is forbidden to you.

## File territory (may edit only within these globs)

- `dcs/workflows/esg.md`
- `CHANGELOG.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/templates/**`
- `dcs/references/**`
- `dcs/workflows/new.md`
- `dcs/workflows/close.md`
- `dcs/workflows/deploy.md`
- `dcs/workflows/loop.md`
- `dcs/workflows/plan.md`
- `dcs/workflows/execute.md`
- `dcs/workflows/run.md`
- `dcs/workflows/status.md`
- `dcs/workflows/init.md`
- `tests/**`
- `dcs/hooks/**`
- `dcs/VERSION`
- `agents/**`
- `skills/**`
- `package.json`
- `bin/**`
- `install.*`
- `.dcs/**`
- `vault/**`
- `docs/**`
- `README.md`
- `CLAUDE.md`

## Mechanical constraints (check 14/15 -- IC command-point-2 carry)

`dcs/workflows/esg.md` sits inside check 15's walked population
(`dcs/`). The chief's mitigation for the check-15 collision risk (no
`DEPLOYED` literal token, no U+2192 arrow, in any new prose) was written
into S1's tasking only -- the IC's IAP-review carry extends it to you
explicitly: **your `esg.md` insertions must not contain the literal token
`DEPLOYED` or the U+2192 character**, in any rule shape. Cite S1's
convention by name (`RESOLVED (field repair)`, or "the register template's
field-repair convention") rather than restating what it means -- a
sentence like "never reaches DEPLOYED -- verified in main" would both
violate this constraint and recreate the two-sites-of-truth defect
criterion 3 exists to prevent. If your own wording reads as a second
declaring site when you run the evidence grep below, fix your wording; you
may not edit `dcs/templates/REGISTER.md` to resolve it.

## Evidence required in the return

- `grep -rn "field repair" dcs/ agents/ skills/` over the INTEGRATED tree
  (S1's edit already in place) -- paste the full output and classify every
  hit as DECLARING or CITING. Exactly one declaring site, in
  `dcs/templates/REGISTER.md`. This is 202 criterion 3's evidence; record
  the command and its output, do not assert the count.
- `python tests/test_doctrine_integrity.py` -- quote the final `N/M passed`
  line verbatim (must be N/N, N >= 82) plus the `deploy-evidence: declaring
  population ...` and `deploy-evidence rule C ...` lines, confirming S1's
  edit plus yours left the declaring population at 1 paragraph in 1 file.
- Quote the `esg.md` agenda items showing (a) through (f) unmoved in their
  original order with `(g)` appended.
- Quote your `esg.md` insertions verbatim, and state in one line how each
  CITES rather than restates the template's definition.
- Quote your `CHANGELOG.md` entry verbatim and the heading it sits under --
  it must be `## 0.6.10 - 2026-07-26` / `### Added`, with no new version
  heading created.
- `git diff --stat` -- your two files changed and nothing else; and
  confirm `dcs/VERSION`, `package.json`, `dcs/references/doctrine.md` and
  `dcs/references/schemas.md` appear nowhere in the full-period diff.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
