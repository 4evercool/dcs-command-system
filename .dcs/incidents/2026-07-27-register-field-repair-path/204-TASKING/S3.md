# 204 — Tasking S3

**Incident:** register-field-repair-path
**Period:** 1 (revision 2)
**Specialist:** dcs-ops-specialist (S3)

## Task

Satisfy 202 criterion 6 (revision 2) in `CHANGELOG.md` alone. Numbered S3,
not S1/S2, so the IC does not overwrite `204-TASKING/S1.md` and `S2.md` --
those are the record of revision 1's Safety-verified work.

Steps, in order:

1. Save the current bullet before destroying it: `git diff -- CHANGELOG.md
   > <scratch>/s2-bullet.patch`, and confirm the diff is exactly ONE hunk
   of 12 added lines with zero deletions -- if it is anything else, STOP
   and return a deviation, because step 2 discards whatever is there.
2. `git checkout HEAD -- CHANGELOG.md`. This restores the file to
   byte-for-byte equality with the published 0.6.10 tarball (proven at
   plan time, sha256 `9ae04cccee1304ca9d5181e6742a177b0788004c425e7598723f053f9ce46c82`
   on both sides) and satisfies criterion 6's revert clause completely --
   do not additionally hand-edit the `## 0.6.10` section, and do not
   retype its heading.
3. Insert a new `## Unreleased` section above `## 0.6.10 — 2026-07-26`,
   after the existing `---` at line 21, with a `---` separator between
   the new section and `## 0.6.10` to match every other section boundary
   in this file. Give it an `### Added` subsection carrying the saved
   bullet.
4. Reproduce the bullet verbatim from the patch file with ONE required
   correction: its sentence "the same qualifier shape as `MERGED (deploy
   pending)` above" is false once the bullet sits above 0.6.10 -- both
   occurrences of that token (lines 119, 133 of the reverted HEAD file)
   are in 0.6.10's `### Changed`, now below you. Make it direction-free or
   version-anchored. Fix nothing else in the bullet's wording.
5. Re-run the integrity suite and, if its count differs from the
   bullet's embedded "**82/82 passed**, unchanged", correct the bullet to
   the suite's actual output rather than leaving a stale number
   (principle 15).

**Constraints:** no version bump, no `## 0.6.11` or any dated heading --
`## Unreleased` carries no date; do not touch `dcs/VERSION` or
`package.json`; do not write a "Shipped by incident ..." closing line for
this section (the three most recent sections carry one, but nothing has
shipped, so it would be a false claim). Write with the Write/Edit tools
only, never PowerShell `Set-Content`/`Out-File` -- `.gitattributes` is `*
text=auto eol=lf` and the file is currently pure LF with no BOM (20112
bytes, 0 CRLF). English only.

If reality does not match this plan, STOP and return `status:
"deviation"` per `references/schemas.md` #4 -- do not improvise.

## File territory (may edit only within these globs)

- `CHANGELOG.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/templates/**`
- `dcs/workflows/**`
- `dcs/references/**`
- `dcs/hooks/**`
- `dcs/VERSION`
- `tests/**`
- `agents/**`
- `skills/**`
- `package.json`
- `bin/**`
- `install.*`
- `.dcs/**`
- `vault/**`
- `docs/**`
- `README.md`
- `CLAUDE.md`

## Territory enforcement note

`CHANGELOG.md` matches neither `guarded_paths` nor `unguarded_paths` in
`.dcs/config.json` (`dcs_gate.py` line 705 exempts any path matching
neither list), so the gate does not mechanically defend the forbidden
zones above for this tasking. The pinned hash below is the real
enforcement for the criteria-1-5 work sharing this worktree -- treat it as
load-bearing, not a formality.

## Evidence required in the return

- `npm view dcs-command-system version` and `npm view dcs-command-system
  time --json` -- paste both outputs. Must show `0.6.10` and its
  `"0.6.10"` publish timestamp. Re-measured by you this period, not
  quoted from 202.
- The tarball comparison criterion 6 names literally. In a scratch dir:
  `npm pack dcs-command-system@0.6.10 && tar -xzf
  dcs-command-system-0.6.10.tgz`, then `awk '/^## 0\.6\.10 /{f=1}
  f' <scratch>/package/CHANGELOG.md > <scratch>/pub-tail.md` and the same
  awk over your edited `CHANGELOG.md` into `new-tail.md`, then `diff
  <scratch>/pub-tail.md <scratch>/new-tail.md; echo "exit=$?"`. Paste the
  command and its output; must be empty with `exit=0`, proving everything
  from `## 0.6.10` to EOF is byte-identical to the published release. If
  and only if the registry is unreachable, paste the failure, then
  substitute `git show HEAD:CHANGELOG.md` as the comparison base, say
  plainly that you did so, and cite the plan-time equality (sha256
  `9ae04cccee1304ca9d5181e6742a177b0788004c425e7598723f053f9ce46c82` on
  both sides).
- `git diff HEAD -- CHANGELOG.md` -- paste in full. Must be a single
  pure-insertion hunk positioned ABOVE `## 0.6.10 — 2026-07-26`, with zero
  deleted lines anywhere and no changed line at or below that heading.
  This is simultaneously the revert proof and the EOL proof: a CRLF or
  BOM write would surface here as a whole-file rewrite.
- `git diff -- dcs/templates/REGISTER.md dcs/workflows/esg.md | sha256sum`
  (full paths, run from the worktree root -- a bare-name pathspec matches
  neither file and hashes an empty diff) -- must print
  `b2450322f3a1bb848c474bab1285ea772cf1a65cc0e2608a935f31ffb5e81f8a`, the
  value pinned at plan time. This proves the Safety-verified criteria 1-5
  work is byte-for-byte untouched. A mismatch is a halt, not a note.
- `git diff --stat` -- must show exactly three files: `CHANGELOG.md`
  (your insertion), `dcs/templates/REGISTER.md` at 39 changed lines
  (38 insertions, 1 deletion), `dcs/workflows/esg.md` at 9 changed lines
  (9 insertions, 0 deletions) -- both unchanged from the plan-time
  baseline. Confirm `dcs/VERSION` and `package.json` appear nowhere.
- `python tests/test_doctrine_integrity.py` -- quote the final `N/M
  passed` line verbatim. Must be N/N with N >= 82. State whether it
  matches the number written inside your CHANGELOG bullet, and if not,
  that you corrected the bullet to this output.
- Quote your new section verbatim, from `## Unreleased` through the
  `---` that precedes `## 0.6.10 — 2026-07-26`, including the separator
  lines. State in one line what the old `MERGED (deploy pending)`
  sentence said and what you replaced it with.
- `python -c "d=open('CHANGELOG.md','rb').read(); print('BOM:',
  d[:3]==b'\xef\xbb\xbf', 'CRLF:', d.count(b'\r\n'))"` -- must print
  `BOM: False CRLF: 0`.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
