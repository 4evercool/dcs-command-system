<!--
204-TASKING/S1.md -- transcribed verbatim by the IC from the Planning
Chief's `taskings[]` (schemas.md #2). The specialist receives exactly this
file's content plus the relevant IAP excerpt -- nothing else.
-->

# 204 — Tasking S1

**Incident:** criterion-unmeasured-fact
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Критерии 202 №1, №2 (и обязательная гигиена в той же зоне). В
`dcs/workflows/plan.md`, в списке проверок раздела 4a, добавить НОВЫЙ
соседний пункт `3b` сразу после пункта 3a (после текущей строки 152, перед
пунктом «4. **Territory disjointness**»), перенеся дословный черновик из
тактики №2 плана (приведён ниже). НЕ перенумеровывать существующие пункты
1–8 и не менять текст 3a. Ярлык версии «(vX.Y.Z)» у 3b НЕ ставить (см.
IAP, тактика №4). Разрешено только сжатие прозы последнего предложения
черновика; формулировка триггера (первое предложение) и требования
(i)(ii)(iii) переносятся без изменения смысла — на них проверяется
критерий 2.

Дословный черновик 3b для переноса (вставка после строки 152):

> 3b. **Claims about state outside the tree must be measured.** A
> criterion — or a waiver inside one — asserting anything the working
> tree does not contain (a registry version, whether something is
> published, an installed or deployed copy, another repository, a remote
> ref, a live service) is a **measured claim**; size is irrelevant, one
> external boolean counts as much as a population. Mirror 3a: **(i)** the
> 202 names the **command that establishes the fact** (`npm view <pkg>
> version`, `git ls-remote`, a witness script), not prose and not a
> reading carried over from an earlier artifact; **(ii)** the criterion
> is phrased as *that command's result* ("`npm view <pkg> version`
> returns < X"), never as a bare assertion ("X is unpublished");
> **(iii)** you run it yourself **now** and record the actual output in
> `214-LOG.md`. A fact settled by reading or grepping this project's own
> files is not in scope — that is ordinary evidence (check 5), and a
> tree-wide sweep is 3a. A label on an old reading ("measured during the
> last deploy") is provenance, not truth. Field lesson 2026-07-26: a
> criterion waived a version bump on "0.6.9 is unpublished" — the Owner
> had published it 75 minutes before this lint ran, nothing was required
> to look, and the version shipped twice with different contents.

Вторая, обязательная правка: в строке 116 заменить «These five checks are
arithmetic» на «These checks are arithmetic» (счёт уже был ложен — девять
пунктов, — и принцип 15 запрещает производный факт без команды
регенерации). Это часть постановки задачи, а не самодеятельность.

Третье, критерий 2 — не правка, а вывод в возврате: прочитать
`.dcs/incidents/2026-07-26-schemas-md-trim/202-OBJECTIVES.md:115-118`
(ТОЛЬКО чтение, `.dcs/**` запрещён для записи), процитировать дословно
исторический критерий 10 и рядом дословно формулировку триггера 3b, и
показать в двух строках, по каким именно токенам триггер его ловит и
почему команды рядом с ним нет.

Файл plan.md пишется по-английски (пакет — только английский, кириллица в
payload роняет guard). Правки только Write/Edit, не PowerShell Set-Content
(BOM). Все хунки обязаны лежать в зоне 4a (~строки 114–160); зона шага 7
(331–340, pre-stamp checklist) принадлежит другому инциденту —
прикосновение к ней = deviation, а не решение на месте.

## File territory (may edit only within these globs)

- dcs/workflows/plan.md

## Forbidden zones (explicitly, even if it seems related)

- dcs/templates/**
- agents/**
- CHANGELOG.md
- dcs/references/**
- dcs/hooks/**
- tests/**
- skills/**
- bin/**
- install.ps1
- install.sh
- package.json
- dcs/VERSION
- .dcs/** (запись; чтение исторического 202 разрешено)

## Evidence required in the return

- `git diff -- dcs/workflows/plan.md` (полный diff в возврате, без сокращений)
- `grep -n "^3b\." dcs/workflows/plan.md` (ровно одно совпадение)
- `grep -n "^[0-9]\+[a-z]\?\. \*\*" dcs/workflows/plan.md` (вывод по зоне 4a должен читаться как 1,2,3,3a,3b,4,5,6,7,8 — доказательство, что перенумерации не было)
- `git diff -U0 -- dcs/workflows/plan.md | grep "^@@"` (все заголовки хунков внутри ~114–160)
- `git diff -- dcs/workflows/plan.md | grep -c "Pre-stamp checklist"` (ожидается 0 — зона 331–340 не тронута)
- `git diff -- dcs/workflows/plan.md | grep -c "These five checks"` (ожидается 1: удалённая строка)
- `wc -l dcs/workflows/plan.md` (ожидается <= 425; было 403)
- `grep -n "0\.6\.9" .dcs/incidents/2026-07-26-schemas-md-trim/202-OBJECTIVES.md` (чтение; дословная цитата исторического критерия 10 в возврате, рядом — дословная цитата триггера 3b и вывод по критерию 2)
- `python -c "import re,pathlib; t=pathlib.Path('dcs/workflows/plan.md').read_text(encoding='utf-8'); print('cyrillic:', bool(re.search('[А-Яа-яЁё]', t)))"` (ожидается False)
- `python tests/test_doctrine_integrity.py` (собственная строка N/M passed и код возврата)
- `npm test` (все три сьюта; собственные строки N/M passed и код возврата)
- `git status --short` (в возврате: изменён только dcs/workflows/plan.md)

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
