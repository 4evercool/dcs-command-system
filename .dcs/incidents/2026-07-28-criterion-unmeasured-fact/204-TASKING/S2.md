<!--
204-TASKING/S2.md -- transcribed verbatim by the IC from the Planning
Chief's `taskings[]` (schemas.md #2), with one Dispatcher transcription
fix logged in 214-LOG.md: the BOM/cyrillic evidence command rewritten as a
runnable one-liner (the chief's draft embedded literal \n in `python -c`).
-->

# 204 — Tasking S2

**Incident:** criterion-unmeasured-fact
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Критерии 202 №3 и №4 плюс правка устава, которую план берёт в партицию.

(а) `dcs/templates/202-OBJECTIVES.md`: внутрь существующего HTML-комментария
блока критериев (строки 24–46), сразу после абзаца OWNERSHIP TAG, добавить
блок-правило MEASURED CLAIM того же жанра и той же ширины отступа —
дословный черновик ниже. Комментарий не разрывать, существующие абзацы
STAGING RULE и OWNERSHIP TAG не переписывать.

Черновик MEASURED CLAIM для переноса:

> MEASURED CLAIM: if a criterion asserts anything outside this working
> tree -- a registry version, whether something is published, an
> installed or deployed copy, another repository, a remote ref, a live
> service -- write the command that establishes it inside the criterion,
> and phrase the criterion as that command's result ("`npm view <pkg>
> version` returns < X"), never as a bare claim ("X is unpublished").
> Classify while writing: plan.md lint 4a check 3b is the second line of
> defence, not the first, and it will run the command and record the
> output. An in-tree fact needs no such command -- `grep -n "^##
> Unreleased" CHANGELOG.md` settles itself. Field lesson 2026-07-26: a
> criterion waived a version bump on "0.6.9 is unpublished"; it had been
> published 75 minutes earlier, and the version shipped twice with
> different contents.

(б) `agents/dcs-planning-chief.md`: одна фраза в шаге 2 (строки 50–54),
сразу после предложения про `objectives_feedback`. Черновик для переноса:

> Also classify volatility: a criterion asserting state outside the
> project's working tree (a registry version, a published or deployed
> artifact, another repository, a remote ref) that carries no command
> establishing it goes into `objectives_feedback` as an unmeasured claim.
> You read the 202 before any lint does -- plan.md lint 4a check 3b is
> the second line of defence, you are the first.

(в) `CHANGELOG.md`: запись под УЖЕ существующим заголовком `## Unreleased`,
в подраздел `### Added`, в стиле соседней записи (что изменилось, в каком
файле, зачем, с полевым уроком 2026-07-26 и ценой — 0.6.9 опубликована
дважды разным содержимым). Новый заголовок версии НЕ создавать,
`dcs/VERSION` и `package.json` не трогать: бамп вне периода. Запись
описывает и правку S1 (новая проверка `3b` в plan.md, зона 4a), и свои
две — пиши по зафиксированным в этом плане формулировкам, файл S1 не
читать и не редактировать. Счёт сьютов в записи — только из собственного
прогона (принцип 15), не из этого файла и не из соседней записи.

Строка перекрёстной ссылки в шаблоне и уставе закреплена: `plan.md lint 4a
check 3b` — переноси дословно. Все три файла — по-английски. Только
Write/Edit, не PowerShell Set-Content (BOM).

## File territory (may edit only within these globs)

- dcs/templates/202-OBJECTIVES.md
- agents/dcs-planning-chief.md
- CHANGELOG.md

## Forbidden zones (explicitly, even if it seems related)

- dcs/workflows/**
- dcs/references/**
- dcs/hooks/**
- tests/**
- skills/**
- bin/**
- install.ps1
- install.sh
- package.json
- dcs/VERSION
- .dcs/**

## Evidence required in the return

- `git diff -- dcs/templates/202-OBJECTIVES.md agents/dcs-planning-chief.md CHANGELOG.md` (полный diff в возврате)
- `grep -n "^## Unreleased" CHANGELOG.md` (команда, названная критерием 4; заголовок существовал до правки)
- `git diff -- CHANGELOG.md | grep -c "^+## "` (ожидается 0 — нового заголовка версии не создано)
- `sed -n '/^## Unreleased/,/^## [0-9]/p' CHANGELOG.md | head -60` (новая запись видна внутри Unreleased)
- `grep -n "MEASURED CLAIM" dcs/templates/202-OBJECTIVES.md`
- `grep -n "check 3b" dcs/templates/202-OBJECTIVES.md agents/dcs-planning-chief.md` (перекрёстная ссылка есть в обоих)
- `grep -n "OWNERSHIP TAG" -A 3 dcs/templates/202-OBJECTIVES.md` (новый блок соседствует с ним внутри того же комментария)
- `python -c "import re,pathlib; files=['dcs/templates/202-OBJECTIVES.md','agents/dcs-planning-chief.md','CHANGELOG.md']; [print(p,'bom:',pathlib.Path(p).read_bytes().startswith(b'\xef\xbb\xbf'),'cyrillic:',bool(re.search('[А-Яа-яЁё]',pathlib.Path(p).read_text(encoding='utf-8')))) for p in files]"` (ожидается bom False, cyrillic False на всех трёх)
- `python tests/test_doctrine_integrity.py` (собственная строка N/M passed и код возврата)
- `npm test` (все три сьюта; собственные строки N/M passed и код возврата)
- `git status --short` (в возврате: изменены ровно три файла этой территории)

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
