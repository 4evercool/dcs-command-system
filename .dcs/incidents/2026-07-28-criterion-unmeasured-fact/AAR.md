<!--
AAR.md -- After Action Report, written by the IC during /dcs-close. Requires
a green (pass) Safety Officer verdict to exist before this file is written
-- close.md enforces this, do not write an AAR to paper over a halt.
-->

# AAR — After Action Report

**Incident:** criterion-unmeasured-fact
**Type:** 3
**Opened:** 2026-07-28
**Closed:** 2026-07-28
**Operational periods:** 1

## Outcome

Все пять критериев приёмки периода 1 выполнены — установлено вердиктом
Safety Officer'а (pass, ноль опровержений, его собственные прогоны;
дословно ниже). В `dcs/workflows/plan.md` зона линта 4a получила проверку
3b «Claims about state outside the tree must be measured» (строка 153,
без перенумерации 1–8; ложный счёт «These five checks» на строке 116
снят); шаблон `dcs/templates/202-OBJECTIVES.md` — блок MEASURED CLAIM
рядом с OWNERSHIP TAG (классификация при авторстве);
`agents/dcs-planning-chief.md` шаг 2 — классификация изменчивости в
`objectives_feedback` (Chief — первая линия, линт — вторая);
`CHANGELOG.md` — запись под открытым `## Unreleased`, без бампа.
Интеграционный коммит `623582f`, 4 файла (+58/−3), проверен
`git show --stat` при закрытии. Ретро-проверка: исторический критерий 10
`schemas-md-trim` попадает под новый триггер по собственному тексту —
выведено офицером независимо от S1. Ложные срабатывания: 0 из 5 на
собственном 202 этого инцидента.

## What worked

- **Соседний пункт 3b вместо расширения 3a** — решение Chief'а, опёртое
  на измерение (ноль внешних ссылок на «lint 3a»), пережило Safety без
  единого замечания; нумерация и единственная внешняя ссылка
  («plan.md lint 4a check 6» в шаблоне) целы.
- **Дословный черновик в тактиках, а не пересказ в таскинге**: триггер и
  (i)(ii)(iii) объявлены непереписываемыми, и офицер сверил их difflib'ом
  с планом — единственная дельта оказалась заявленным сжатием прозы.
- **Тест на ложные срабатывания в плане проверки** (приложить новый
  триггер к 202 самого инцидента): дал измеримую границу «не шире, чем
  нужно» — 0/5 при допуске ≤1.
- **Догфуд собственного правила до его посадки**: 202 этого инцидента не
  содержит ни одного неизмеренного внешнего факта; единственный внешне
  похожий критерий (№4) сам называет свою команду
  (`grep -n "^## Unreleased" CHANGELOG.md`).

## Lessons

- **Команда регенерации обязана уметь установить факт, рядом с которым
  стоит.** Четвёртое полевое измерение класса нашёл сам Safety Officer в
  этом же закрытии: `vault/Decisions/fable-review-roadmap.md:24-25`
  несла «0.6.10 remains unpublished» с «командой регенерации», читающей
  **внутридревесное** состояние (State-ячейку регистра и `git log`) — при
  том что утверждение о **внешнем** (npm). Внутридревесная команда рядом с
  внешним фактом выглядит соблюдением принципа 15, но установить факт не
  может. Проверка: `npm view dcs-command-system versions` → 0.6.10
  опубликована. Развёрнуто: `vault/Meta/building-dcs-lessons.md` §19.
- **Линт охраняет только путь жизненного цикла.** Check 3b видит критерии
  202 на линте; vault, регистр и ситрепы остаются неохраняемыми — их
  устаревшие внешние утверждения чинятся только при следующем живом
  замере. Границу зафиксировать честно, а не считать закрытой.
- Открытый вопрос 2 из 201 (метка 16:50 позже времени публикации
  16:39:23) остаётся неразрешённым из артефактов; учтён как данные о
  ненадёжности чтения реестра сразу после публикации.

## Deviations this incident

none — executed as planned. По `214-LOG.md`: ноль возвратов
`status: "deviation"`, ноль `SAFETY-HALT:`, ноль эскалаций; один линт-фикс
транскрипции до точки командования 2 (неисполнимая команда evidence у S2 —
литеральные `\n` в `python -c`), одна правка advisory 5 до
интеграционного коммита (тире в хартии Chief'а).

## Memory routing

Проект документирует три хранилища (CLAUDE.md «Where lessons go»):
- **doctrine.md** — не тронута: правило легло в workflow (plan.md 3b) и
  шаблон; принцип 15 уже покрывает доктринальный уровень.
- **doctrine-appendix.md** — не тронута: провенанс несёт сам текст 3b
  (полевой урок 2026-07-26 внутри пункта).
- **vault/** — `vault/Meta/building-dcs-lessons.md` §19 (+1 раздел,
  закоммичен на ветке инцидента, едет в main с merge); при закрытии в
  основном чекауте (незащищённые, некоммитящиеся правки для Owner'а):
  `vault/Decisions/fable-review-roadmap.md:24-25` переписана результатом
  команды (advisory 1), `vault/Backlog.md` item 13 — пометка о закрытии и
  исправление ссылок «§10/§11» → «Meta §10/§11» (открытый вопрос 1 из
  201), item 18 — заметка advisory 3 (plan.md теперь 422 строки при
  ненаблюдаемом потолке ~250).

## Intake source closure

`.dcs/esg/REGISTER.md`, строка `criterion-unmeasured-fact` → `MERGED
(deploy pending)` на шаге 5a.3 закрытия (внутренний источник, правится
самим close). Первоисточник `vault/Backlog.md` item 13 — пометка о
закрытии этим инцидентом (см. Memory routing). Внешних систем нет.

**Owner-UAT:** IAP периода 1 не определяет UAT-шага («ручной проверки,
требующей Owner'а, в этом периоде нет») — гейта нет.
**Deploy status:** не развёрнуто на момент написания AAR — payload-файлы
вступают в силу только после `/dcs-deploy` (установленная копия);
Delegation v3 `deploy.auto_after_close: false`, поезд — отдельный акт
Owner'а. Свидетель поставки: `python tests/payload_check.py` (маркер
версии не сдвинется — бампа не было; это ожидаемо и задокументировано в
`deploy.md` шаге 7).

## Safety Officer's final verdict (verbatim, from SAFETY.md)

См. `SAFETY.md` этого каталога — вердикт периода 1 хранится там дословно
целиком (JSON: `verdict: "pass"`, `refutations: []`, 5 advisories с
резолюциями IC, 15 позиций `checked[]`). Дублирование полного блока здесь
опущено сознательно: SAFETY.md — канонический носитель, этот AAR
ссылается на него, а не копирует (единственный писатель на артефакт).
Ключевые строки: pass при нуле опровержений; все пять критериев MET
собственными прогонами офицера; сьюты на результате — 82/82 и
100/100+10/10+82/82, exit 0 (счёт из вывода прогонов офицера).
