# 204 — Tasking T3

**Incident:** safety-halt-functional-scope
**Period:** 1
**Specialist:** dcs-ops-specialist (T3)

## Task

Критерий **7**: дописать `CHANGELOG.md` в **уже существующий
неопубликованный раздел 0.6.10**. Нового раздела версии не заводить, версию не
поднимать.

**Исполнять последним, после T1 и T2** — запись описывает то, что реально
легло в дерево, и цитирует итоговые счета наборов.

Запись говорит, что изменилось и почему:
- планка «опровержение против замечания», существовавшая с v0.6.5 только как
  проза внутри промпта одного агента, получила проверку в наборе целостности;
- принцип 15 в `doctrine.md` перестал противоречить уставу офицера;
- из планки убрано число, которое нечем регенерировать.

Маршрутизация по `CLAUDE.md` (раздел о трёх хранилищах): правило живёт в
`doctrine.md`, рассказ — в `doctrine-appendix.md`, а `CHANGELOG.md` говорит о
поставке. Не пересказывать здесь ни правило целиком, ни историю.

**Принцип 15 применяется к самой записи.** Любое производное число в ней —
счёт набора, размер горячего пути, численность популяции — несёт рядом
команду, которая его регенерирует. Если команду поставить некуда, число из
записи убрать. Преамбула `CHANGELOG.md` прямо требует писать счета из
собственного вывода набора, а не по памяти: перезапустить и процитировать.

`dcs/VERSION` и `package.json` вне территории по решению Владельца при
типизации: репозиторий на 0.6.10, в npm опубликована 0.6.9, поэтому раздел
0.6.10 ещё открыт.

## File territory (may edit only within these globs)

- `CHANGELOG.md`

## Forbidden zones (explicitly, even if it seems related)

- `agents/**`
- `dcs/**`
- `tests/**`
- `skills/**`
- `bin/**`
- `install.*`
- `package.json`
- `.dcs/**`
- `vault/**`
- `docs/**`
- `README.md`
- `CLAUDE.md`

## Evidence required in the return

- `git diff -- CHANGELOG.md` — дословно
- `awk '/^## 0\.6\.10/{f=1} /^## 0\.6\.9/{f=0} f' CHANGELOG.md` — запись лежит внутри существующего раздела 0.6.10, нового заголовка версии не появилось
- `python tests/test_doctrine_integrity.py`, `python tests/test_dcs_gate.py`, `python tests/test_dcs_intake.py` — три собственных `N/M passed` дословно, если запись их цитирует (критерий 6 и требование преамбулы `CHANGELOG.md`)
- Разбор по принципу 15: перечислить каждое число из новой записи и команду, стоящую рядом с ним; чисел без команды быть не должно
- `git status --porcelain` и `git diff --stat` — изменён только `CHANGELOG.md`; `dcs/VERSION` и `package.json` в выводе отсутствуют

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC
will re-enter planning around your finding.

**Отдельно для этого задания:** `CHANGELOG.md` не входит в охраняемый набор
гейта (`.dcs/config.json` → `guarded_paths`). Территориальная дисциплина здесь
держится только этим планом, механической опоры под ней нет — выход за
`CHANGELOG.md` никто не остановит, кроме самого исполнителя.
