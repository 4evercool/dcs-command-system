# 204 — Tasking T2

**Incident:** safety-halt-functional-scope
**Period:** 1
**Specialist:** dcs-ops-specialist (T2)

## Task

Дать планке механический носитель: новая **проверка 14** в
`tests/test_doctrine_integrity.py`, которая краснеет, когда правило
«опровержение против замечания» рассогласовано между местами, где оно
объявлено. Закрывает критерии **1**, **2**, **6** и механическую половину
**3** и **5**.

**Исполнять ПОСЛЕ того, как T1 вернул `done`** — проверка разбирает прозу,
которую пишет T1, и доказательства имеют смысл только на итоговом дереве.

**(а) Источник истины — `agents/dcs-safety-officer.md`, разбираемый во время
исполнения.** Из него берутся: порядковый номер шага, содержащего маркерное
предложение перед списком планок; число пунктов этого списка; токен вердикта
по умолчанию из предложения того же шага. **Никакой копии текста планки в теле
проверки.** Импортировать нечего: у этого правила нет исполнителя в коде, в
отличие от проверки 12, импортирующей грамматику из `dcs_gate.py` — поэтому
применяется приём проверки 13 (разбор файла-источника).

**(б) Популяция строится тем же способом, что и команда критерия 2:** `*.md`
под `dcs/references/`, `dcs/workflows/`, `agents/`, отобранные по
`advisor(y|ies)|refutation` без учёта регистра. **Объявляющее место**
определяется во время исполнения — ограниченным окном нормализованного текста,
в котором встречаются токен `advisory` **и** токен `refutation`/halt-вердикта.
В теле проверки не должно быть ни перечня объявляющих файлов, ни номеров
строк; единственный допустимый файловый литерал — путь к уставу, потому что он
и есть источник истины.

**(в) Инвариант, по одному именованному случаю на объявляющее место**, чтобы
красный называл файл (идиома проверок 12(c) и 13(d)):
1. ссылка на устав несёт **живой** номер шага (перенумерование шагов краснеет,
   пока ссылки не переехали);
2. любое число планок, названное рядом со ссылкой, равно числу пунктов,
   разобранных из устава (сегодня `schemas.md:101` говорит «three bars»);
3. токен вердикта, который место называет умолчанием для этого класса находок,
   совпадает с уставным — ни одно место не вправе объявлять умолчанием `halt`.

**(г) Защита от вырождения** (критерий 1, внесена в 202 явно): набор
объявляющих мест обязан быть непустым, содержать сам устав и охватывать не
менее двух из трёх сканируемых поверхностей (`agents/`, `dcs/references/`,
`dcs/workflows/`). Условие **структурное**, не перечнем файлов — перечень
запрещён критерием 2. Без этого проверка зеленеет стиранием: убрали прозу
вместо согласования — популяция пуста — «всё сошлось».

**(д) Негативное доказательство внутри набора** (образец 13(f)): тот же
компаратор прогоняется против **подделанного разбора устава** — номер шага
сдвинут на соседний либо один пункт планки отброшен, — и **отсутствие**
расхождения есть красный. Компаратор, принимающий и подделку, ничего не
читает.

**(е) Носитель критерия 3:** в пределах файла-устава голое
`[0-9]+ of [0-9]+` без команды в кодовой разметке **в том же абзаце** — красный.
Узко по области намеренно: общедревесный вариант даст ложные срабатывания.

**(ж) Подделка для критерия 1 обязана быть применима к дереву `6ef9c47`,**
иначе контрольный выстрел по архиву ничего не доказывает — зелёный получится
тривиально, по отсутствию текста, а не по отсутствию способности. Годятся:
«three bars» → «two bars» в §5 `schemas.md`; удаление одного пункта планки из
устава; сдвиг номера шага в ссылке §5. Весь этот текст существует и в HEAD.
Подделка не должна задевать другие проверки (кодировка, CRLF, бюджет,
нумерация принципов).

**(з) Запреты.** Не менять `HOT_PATH_BUDGET_KB`. Не ослаблять существующие
проверки 1-13. Не заводить фикстуры: снимок дерева в `tests/fixtures/` — это
ровно тот перечень, который запрещает критерий 2.

**Если предикат ошибочно признаёт объявляющим местом `dcs/references/forms.md`,
`dcs/workflows/close.md`, `dcs/workflows/init.md` или
`agents/dcs-commander.md` — возвращать отклонение с предложением, а не править
прозу.** Эти файлы вне территории у всех троих намеренно. Замер на входе:
`forms.md` advisory=0/refutation=1, `close.md` 0/2, `init.md` 2/0,
`dcs-commander.md` 0/3 — ни один не несёт обоих токенов, так что правильный
предикат их не захватывает. Правится предикат, а не текст: проверка, зеленеющая
подгонкой текста под себя, учит именно подгонке.

## File territory (may edit only within these globs)

- `tests/test_doctrine_integrity.py`

## Forbidden zones (explicitly, even if it seems related)

- `agents/**`
- `dcs/references/**`
- `dcs/workflows/**`
- `dcs/templates/**`
- `dcs/hooks/**`
- `dcs/VERSION`
- `tests/test_dcs_gate.py`
- `tests/test_dcs_intake.py`
- `tests/fixtures/**`
- `CHANGELOG.md`
- `package.json`
- `skills/**`
- `bin/**`
- `install.*`
- `.dcs/**`
- `vault/**`
- `docs/**`
- `README.md`
- `CLAUDE.md`

## Evidence required in the return

- `python tests/test_doctrine_integrity.py` — зелёный, собственный `N/M passed` дословно, плюс дословно строки PASS новой проверки: по одной на объявляющее место, чтобы решение проверки читалось из вывода, а не из тела
- **Критерий 1, «до»:** вывод набора на итоговом дереве до подделки (тот же, что выше)
- **Критерий 1, подделка:** `git diff` подделки дословно + вывод набора — красный, с именем файла и причиной
- **Критерий 1, восстановление:** `git checkout -- <файл>`, затем `python tests/test_doctrine_integrity.py` — снова зелёный, вывод дословно; `git status --porcelain` чист по этому файлу
- **Критерий 1, контрольный выстрел:** `git archive 6ef9c47 | tar -x -C <каталог вне рабочего дерева>`, применить ТУ ЖЕ подделку там, запустить тамошний `python tests/test_doctrine_integrity.py` — зелёный; привести и вывод, и точную последовательность команд (счёт `N/M` у архива свой и меньше — это ожидаемо и должно быть названо)
- **Критерий 2, самопроверка:** `grep -nE "doctrine\.md|schemas\.md|execute\.md|forms\.md|close\.md|init\.md|dcs-commander" tests/test_doctrine_integrity.py` — показать, что в теле проверки 14 нет ни одного файлового литерала, кроме пути к уставу; совпадения в проверках 1-13 отделить явно
- **Критерий 2, воспроизводимость:** `grep -rniE "advisor(y|ies)|refutation" dcs/references/ dcs/workflows/ agents/ --include=*.md | wc -l` рядом с популяцией-кандидатом, которую печатает сама проверка — числа совпадают друг с другом; с числом 39 из 202 сверять запрещено
- **Критерий 5:** `grep -n "HOT_PATH_BUDGET_KB" tests/test_doctrine_integrity.py` — значение 37, и `git diff`, показывающий, что строка не тронута
- **Критерий 6:** `python tests/test_dcs_gate.py` и `python tests/test_dcs_intake.py` — хвосты с собственными `N/M passed` дословно (вход: `100/100` и `10/10`)
- `git status --porcelain` — изменён только `tests/test_doctrine_integrity.py`

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC
will re-enter planning around your finding.

**Отдельно для этого задания:** ложное срабатывание предиката на четырёх
исключённых файлах — отклонение. Невозможность разобрать устав по договору
о разметке (в) из T1 — отклонение, а не повод править прозу самому: она чужая
территория.
