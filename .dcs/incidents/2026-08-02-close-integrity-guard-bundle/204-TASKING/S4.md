<!--
204-TASKING/S4.md -- transcribed verbatim by the IC from the Planning
Chief's `taskings[]` array (schemas.md #2, chief plan; second, accepted
return). Version number (0.8.0) is the IC's own ratification at command
point 2, replacing the chief's flagged-for-IC placeholder.
-->

# 204 — Tasking S4

**Incident:** close-integrity-guard-bundle
**Period:** 1
**Specialist:** dcs-ops-specialist (S4)

## Task

Wire the tool into the close path and ship the version — criteria 8, 11, 12, and close.md's half of criterion 5's ordering concern.

**CRITERION 8 + HARD STRUCTURAL CONSTRAINT** — add the new step to `dcs/workflows/close.md` as `5a.1b`, NOT as a renumbered `5a.2`. Four files outside close.md cite its sub-step numbers by number and nothing mechanical checks them: `dcs/templates/REGISTER.md:147` and `dcs/workflows/deploy.md:216` cite step 5a.3; `dcs/workflows/deploy.md:223` and `dcs/workflows/esg.md:120` cite step 5a.4. Renumbering breaks all four silently. `5a.1b` sits after 5a.1 and 5a.1a and before 5a.2 — exactly the position criterion 8 specifies — and renumbers nothing. Verify those four citations yourself before you edit.

Leave step 5a.1a COMPLETELY untouched (criterion 8 says so explicitly): it remains the opt-in slot for project-declared merge-time guards, and it is how this repo's own `test_doctrine_integrity.py` (criterion 6's English-only + term-census checks) keeps running.

The new 5a.1b must:
1. Ensure the incident's own artifact set is COMMITTED before the check runs. This is not optional bookkeeping — step 5a.1 as written commits only "the AAR (step 3) and any memory-routing writes (step 4)", so the rest of the artifact set can still be untracked when 5a.1b runs (verified: this very incident's own directory is currently `?? .dcs/incidents/2026-08-02-close-integrity-guard-bundle/`, wholly untracked at the time this tasking was written), and criterion 2's tracked-check would go red on a compliant close otherwise. Handle it inside 5a.1b itself (e.g. `git add` the incident directory if any artifact is untracked, per the existing "never `-A`/`.`" discipline) rather than by editing step 5a.1's own file list — stay inside this tasking's own step, not another step's stated scope.
2. Invoke S1's tool UNCONDITIONALLY — no project opt-in, no `CLAUDE.md` declaration — quoting S1's `INVOCATION` constant verbatim (S2's carrier case compares `INVOCATION` against your file whitespace-collapsed, so get the string from S1's module, do not retype it from this tasking).
3. Pass `--commit-range` with the real merge range close.md already computes at 5a.2 (`<base>..dcs/<slug>`), because the tool's default under-covers — measured: `git log -- .dcs/incidents/2026-08-02-record-integrity-corrections/` returns only 2 archive commits and misses the integration commit `7fcab05`.
4. Pass `--also-clean <path>` once per memory-routing destination step 4 actually wrote, and say so in prose so the workflow, not the tool, carries the project-specific paths.
5. State the disposition explicitly: exit 0 proceeds to 5a.2; exit 1 is FAIL-CLOSED — stop, do not merge, file a 209, escalation trigger (a), never silently resolved; exit 2 is an environment error that also stops, since a check that could not run is not a check that passed.

**CRITERION 11 + BUDGET** — close.md measures 244 lines against `WORKFLOW_BUDGET_LINES = 250`, with no grandfather. Six lines of headroom for a step that needs roughly 12-18. Fund the trim WITHIN close.md by compressing parenthetical field-lesson prose (steps 1, 3 and 5 carry the most). NEVER touch `WORKFLOW_BUDGET_LINES` — it is not in your territory. NEVER touch step 5a.4's lock-holder-diagnostic prose — that is the separately queued `close-md-lock-diagnostic-inert`'s territory and the 202 forbids disturbing it. Compress, do not delete: every instruction and every stated condition must survive the trim, only its wordiness goes.

Also check, and if it now reads stale, correct, the line "An incident is NOT closed until every one of these five steps succeeds" near the top of section 5a. Under the existing convention lettered sub-steps are not counted (1a already exists and "five" still held), so 1b may leave it correct — but state your reading explicitly in your return rather than leaving a derived count unexamined (doctrine principle 15).

**CRITERION 12** — bump `dcs/VERSION` and `package.json` to **`0.8.0`**, atomically, in the same commit. This is IC-ratified at command point 2: minor, not patch, because this ships unconditional new close-time behaviour to every installing project, which is a behaviour change and not a bugfix. Add a `CHANGELOG.md` entry using the repo's exact heading convention — `## 0.8.0 — <date>` with a literal U+2014 em dash, NOT a hyphen or en dash — which `tests/release_provenance_check.py` enforces at `prepublishOnly`. That check is NOT part of `npm test`, so you must run it yourself; criterion 13's green does not cover criterion 12. The entry's content must follow the file's own stated norm ("written from the repository's own artifacts... not from recollection"): describe the new unconditional close step, the new tool, and the doctrine rule, each traceable to a file that exists.

## File territory (may edit only within these globs)

- `dcs/workflows/close.md`
- `dcs/VERSION`
- `package.json`
- `CHANGELOG.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/deploy.md`
- `dcs/workflows/esg.md`
- `dcs/workflows/plan.md`
- `dcs/workflows/execute.md`
- `dcs/workflows/new.md`
- `dcs/workflows/init.md`
- `dcs/workflows/loop.md`
- `dcs/workflows/run.md`
- `dcs/workflows/status.md`
- `dcs/templates/**`
- `dcs/references/**`
- `dcs/tools/**`
- `dcs/hooks/**`
- `dcs/esg/**`
- `dcs/README.md`
- `tests/**`
- `README.md` (root)
- `CLAUDE.md`
- `agents/**`
- `skills/**`
- `bin/**`
- `install.ps1`
- `install.sh`
- `vault/**`
- `.dcs/**`

## Worktree root

`C:\DCS-wt\close-integrity-guard-bundle`

## Evidence required in the return

- `python -c "from pathlib import Path; raw=Path('dcs/workflows/close.md').read_bytes().replace(b'\r\n',b'\n').replace(b'\r',b'\n'); n=raw.count(b'\n')+(1 if raw and not raw.endswith(b'\n') else 0); print(n,'of 250')"` — run BEFORE and AFTER, paste both. Final must be <= 250.
- `grep -n '^1\.\|^1a\.\|^1b\.\|^2\.\|^3\.\|^4\.\|^5\.' dcs/workflows/close.md` — paste, proving 2/3/4/5 kept their numbers and 1b is the only insertion.
- `grep -rn '5a\.' dcs/templates/REGISTER.md dcs/workflows/deploy.md dcs/workflows/esg.md` — paste. All four external citations must still name the same steps they named before your edit; you must not have touched those files.
- The full text of the new 5a.1b step, pasted verbatim.
- `git diff dcs/workflows/close.md` limited to the 5a.1a hunk — must show ZERO change there. Paste the hunk or the empty result.
- `cat dcs/VERSION` and `grep -n '"version"' package.json` — paste both, must both read 0.8.0.
- `python tests/release_provenance_check.py` then `echo "exit=$?"` — paste the output and exit code.
- Proof the CHANGELOG heading's dash is a literal U+2014 em dash, not a hyphen or en dash (e.g. inspect the codepoint of the character between "0.8.0" and the date).
- State your reading of the "five steps" sentence: still correct with 1b added, or corrected — and why.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
