<!--
204-TASKING/S2.md -- REPLACED for period 1, attempt 2 (post-halt replan).
Transcribed verbatim from the re-spawned Planning Chief's `taskings[]`
array, accepted at command point 2's second pass. Spawn only AFTER S1's
return is validated -- sequential, not parallel, per this attempt's own
risk analysis and dcs-commander's ratification.
-->

# 204 — Tasking S2 (period 1, attempt 2)

**Incident:** close-integrity-guard-bundle
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Fix `tests/test_doctrine_integrity.py` and `tests/fixtures/record-integrity/**` — criteria 6, 13 and the test half of criteria 1 and 3 of the revised 202. Baseline: the suite passes 152/152 today; every case you add must be genuinely capable of failing.

**(A) TERM CENSUS TAUTOLOGY — criterion 6, refutation 1.** `_term_census_texts` (lines 553-562) is built from `candidates`, which includes `tests/test_doctrine_integrity.py` itself because `SHIPPED_DIRS` (line 364) contains `"tests"`; every census term is a literal string inside that same file, so `_term_missing` is provably always `[]`. Exclude the census's own defining file from the SATISFYING population by PATH IDENTITY (resolve each candidate, compare against this file's own resolved path) rather than by name-matching or list surgery — the fix must be structural, not one refactor away from re-breaking. Add the project's root `CLAUDE.md` to the census's scanned population — census population only; do NOT add it to `candidates`, which check 10 (CRLF, line 573) iterates unmodified and which the existing comment at lines 376-386 explicitly warns must not be widened.

**(B) CENSUS NON-VACUITY AND HONEST CURATION.** Add a named case proving a synthetic term known to exist nowhere in the scanned population IS reported as a finding — not merely the existing empty-list degeneracy guard at line 549. Then resolve a measured fact: after the exclusion in (A) plus `CLAUDE.md`, `HOT_PATH_BUDGET_KB` has ZERO occurrences outside the census file (re-verified independently at command point 2), so the corrected census goes RED and blocks the merge-time guard. Its stated reason is also false — it claims `CLAUDE.md` cites it by name; `CLAUDE.md` names `WORKFLOW_BUDGET_LINES` and `WORKFLOW_GRANDFATHERED_LINES` (lines 135-136, 140) and never `HOT_PATH_BUDGET_KB`. Resolve inside your own territory: replace the entry with `WORKFLOW_GRANDFATHERED_LINES` (genuinely cited, and the reason becomes true) or drop it — record the measurement that justifies the choice. Do not "fix" it by editing `CLAUDE.md`; that is outside every territory.

**(C) DATE PIN — criterion 6.** `_NE_EFFECTIVE_DATE` (line 395) must be LITERALLY `"2026-08-02"`. Confirm `_ne_dir_in_scope` already implements strict greater-than and say so rather than changing it blindly. Keep the cross-reference comment accurate: the two constants still share a value and are still deliberately uncoupled. Measured expectation: the change is a no-op today — no `.dcs/incidents/` directory is dated after 2026-08-02 — so the printed in-scope/excluded counts must be IDENTICAL before and after; prove it with the printed line, do not assert it.

**(D) FIXTURE PAIR — criteria 1(b) and 13.** `suppressed-correction/214-LOG.md` currently has a single entry that is both the correction and the only occurrence of its token, so it tests nothing about clearing an EARLIER occurrence, and it passed under the old substring rule for reasons the new rule discards. Build a genuine pair whose `214-LOG.md` entries are identical except for the appended correction: (a) an UNCORRECTED half — an earlier entry citing an unresolvable token in citation position, PLUS, in a different entry, a mid-line prose mention of the literal `RECORD-CORRECTION:` that must suppress nothing; (b) a CORRECTED half — byte-identical plus one appended, correctly-formed `RECORD-CORRECTION:` entry naming that exact token. Assert CRITERION-1-SCOPED, not whole-tool exit code (every fixture directory exits 1 today on criteria 2/4 alone, since `tests/fixtures/` is untracked — a state the test cannot control): the uncorrected half must yield at least one `criterion 1: FINDING` naming the token; the corrected half must yield ZERO `criterion 1: FINDING` lines plus a SUPPRESSED line naming the token. The whole-tool exit-0 claim stays on the REAL clean incident (`.dcs/incidents/2026-08-02-record-integrity-corrections`), which section 23 already asserts and which genuinely exits 0.

**(E) BOUNDARY FIXTURE — criterion 3.** Add a fixture directory whose name begins with EXACTLY `2026-08-03` (one day after the corrected pin — the boundary the previous attempt never tested), carrying a prose-only `SAFETY.md`, and a named case proving it is in scope and flagged. It must live under `tests/fixtures/record-integrity/`; a `2026-08-03`-dated directory under `.dcs/incidents/` would enter check 9's newly-widened Cyrillic population and the informational field-half count, silently changing measured numbers this plan requires be proven unchanged. Keep or repoint the existing `2026-08-10-prose-fence` fixture as you judge best, but the `_RI_SCENARIOS` degeneracy guard must cover every scenario directory the suite actually exercises, and no scenario directory may be orphaned.

**(F) SECTION 23 HYGIENE.** Update case (iv) and the surrounding comments to describe the mechanism that now exists; the current comment describes an entry-body substring test. Keep the guarded-import + red-by-name convention exactly as it is.

Do NOT touch `dcs/**` (S1's file and all already-done work), `tests/test_dcs_gate.py`, `tests/test_dcs_intake.py`, `tests/test_dcs_cli.py`, `tests/payload_check.py`, `tests/release_provenance_check.py`, `tests/fixtures/halt-ceiling/**`, `tests/fixtures/preservation-map/**`, or `CLAUDE.md`.

## File territory (may edit only within these globs)

- `tests/test_doctrine_integrity.py`
- `tests/fixtures/record-integrity/**`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/**`, `tests/test_dcs_gate.py`, `tests/test_dcs_intake.py`, `tests/test_dcs_cli.py`, `tests/payload_check.py`, `tests/release_provenance_check.py`, `tests/fixtures/halt-ceiling/**`, `tests/fixtures/preservation-map/**`, `agents/**`, `skills/**`, `docs/**`, `bin/**`, `install.ps1`, `install.sh`, `package.json`, `CHANGELOG.md`, `README.md`, `CLAUDE.md`, `vault/**`, `.dcs/**`

## Worktree root

`C:\DCS-wt\close-integrity-guard-bundle`

## Evidence required in the return

- Suite tail lines for all three suites; baseline 152/152 on the doctrine suite.
- Census self-exclusion: `file:line` + the ~6-line population construction showing path-identity exclusion and `CLAUDE.md` added to the census population only; one sentence on why `candidates` was NOT widened.
- Census non-vacuity: `file:line` of the synthetic-absent-term case and its PASS line; per-term occurrence counts over the corrected population.
- `HOT_PATH_BUDGET_KB` disposition: which action taken, plus the measuring command and output.
- Pin: `grep -n "_NE_EFFECTIVE_DATE"` showing `"2026-08-02"`, the comparison line, and the before/after in-scope/excluded counts (must be identical).
- Fixture pair, both directions: the two `criterion 1` blocks (uncorrected has a finding, corrected doesn't + shows suppression), plus a diff showing the fixtures differ only by the appended correction entry.
- Boundary fixture: path plus the `criterion 3` output showing it's in scope and flagged.
- Degeneracy guard: `_RI_SCENARIOS` covers every fixture directory, no orphan.
- `git status --short` — only `tests/test_doctrine_integrity.py` and paths under `tests/fixtures/record-integrity/`.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4, with `found`, `why_plan_wrong`, and a `proposal`. The IC will re-enter planning around your finding.
