<!--
204-TASKING/S1.md -- REPLACED for period 1, attempt 2 (post-halt replan).
Transcribed verbatim from the re-spawned Planning Chief's `taskings[]`
array, accepted at command point 2's second pass. The attempt-1 S1
tasking (build the tool from scratch) is superseded by this one (fix two
defects in the already-built tool) -- see 214-LOG.md for the full history
of both.
-->

# 204 — Tasking S1 (period 1, attempt 2)

**Incident:** close-integrity-guard-bundle
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Fix `dcs/tools/record_integrity.py` — criteria 1(b) and 3 of the revised 202. Nothing else in the file's behavior may change; criteria 2, 4 and 5 are already correct and must not regress.

**(A) REDESIGN SUPPRESSION (b) — criterion 1(b), both defects together.** Today the rule is `elif "RECORD-CORRECTION:" in body` at line 313: a substring test over an entry's whole body. Replace it with a two-pass, file-scoped mechanism. Pass 1: walk `split_log_entries()`'s output; an entry is a CORRECTION ENTRY only when its FIRST line matches a pattern built by concatenation from the imported `dcs_gate.ENTRY_PREFIX` — never a locally re-typed prefix, never a body-anywhere test. Collect from each correction entry's body the set of NAMED TARGET tokens (hex runs of the same shape class `CITATION_RE` captures, 7-40 hex chars, word-bounded); document your chosen naming rule in one place and state it in your return. Pass 2: a citation-position token is suppressed iff its value is in that set. Required observable behavior, all four points: (i) a mid-line, non-entry-initial prose mention of the literal `RECORD-CORRECTION:` suppresses NOTHING; (ii) a genuine correction entry naming a token suppresses **every occurrence of that token anywhere in that same 214-LOG.md** (deliberately wider than "anywhere earlier" — a strict superset, ratified at command point 2, because "earlier" recreates the same permanent-block class one entry later); (iii) each suppression prints what it suppressed, why, and the LINE NUMBER of the correction entry that authorized it; (iv) suppression (a) (the `IAP-APPROVED:` stamp digest, via `dcs_gate.sentinel_of`) is unchanged and still takes precedence. **Bound the Safety Officer will check:** suppression must stay token-scoped to the correction's named targets — `halt-enumeration-grammar-drift/214-LOG.md:36`'s separate `integration commit 48ea59a` must still print `resolves to 'commit'`, never get silently swept into the suppression.

**(B) THE REMEDY IN THE FINDING TEXT.** Criterion 1's FINDING message for an unresolvable token must name the `RECORD-CORRECTION:` remedy and its required shape (column-zero bracketed timestamp, sentinel immediately after it, the token named in the body). This is the only surface a blocked operator sees — nothing in `doctrine.md`, `forms.md` or `close.md` documents this convention (measured: zero prose hits package-wide; the IC is registering that as a separate follow-up, not this incident's job to fix).

**(C) DATE PIN — criterion 3.** `SAFETY_FENCE_EFFECTIVE_DATE` must be LITERALLY `"2026-08-02"` (currently `"2026-08-03"`, line 515). Verify the comparison implements STRICT GREATER-THAN scoping: a directory dated strictly after the pin is IN scope; the pin date itself is OUT. Confirm the existing out-of-scope branch's sense is already correct and say so rather than changing it blindly. Post-condition: `2026-08-02-record-integrity-corrections` stays out of scope; a directory dated `2026-08-03` enters scope.

**(D) VERDICT-SHAPED FENCE SCOPING — criterion 3, revised.** `safety_fence_findings()` currently validates EVERY JSON fence it finds (lines 686-705), so a non-verdict JSON fence in a `SAFETY.md` produces a fail-closed "unrecognized key" finding. Scope field validation to fences that carry a `verdict` key; a non-verdict JSON fence becomes a PRINTED NOTE, never a finding. Do not lose the other half: an in-scope `SAFETY.md` whose fences are all non-verdict-shaped must STILL produce the "no genuine verdict fence" finding. Split the field comparison into a pure comparator plus the IO collector, mirroring `clean_tree_findings`/`collect_clean_tree_findings` and `degenerate_message_findings`/`collect_commit_messages` — without that split this behavior is untestable, since no file on disk carries a non-verdict fence.

**(E) REGENERATE THE DERIVED FACTS — principle 15.** Rerun the corpus command and rewrite the docstring's "Measured facts" block (lines 98-119) with the new numbers beside the command that produced them. Correct the `CITATION_KEYWORDS` comment (lines 172-186), which still offers "drop `sha` from this tuple" as an available remedy — keep the keyword (5 of 8 historical `sha`-keyword tokens are true commit citations across 4 incidents; the remedy is now the correction entry, not the keyword). Update the `SAFETY_FENCE_EFFECTIVE_DATE` comment block (lines 505-515) so its description of the pin and the deliberate non-coupling with `_NE_EFFECTIVE_DATE` stays true.

Do NOT touch tests, fixtures, doctrine, workflows, `dcs/README.md`, `CHANGELOG.md`, `package.json` or `dcs/VERSION` — all are another party's territory or already-done work this revision does not reopen. Your suite run may legitimately show red cases owned by S2; report them RED BY NAME, never as "expected to pass later."

**Sequencing note:** you run FIRST, alone. S2 will not be spawned until your return is validated (S2's own assertions hard-read your finding-line and suppression-line formats — this avoids the concurrent-snapshot confusion that happened once already in attempt 1).

## File territory (may edit only within these globs)

- `dcs/tools/record_integrity.py`

## Forbidden zones (explicitly, even if it seems related)

- `tests/**`, `dcs/hooks/**`, `dcs/references/**`, `dcs/workflows/**`, `dcs/templates/**`, `dcs/README.md`, `dcs/VERSION`, `agents/**`, `skills/**`, `docs/**`, `bin/**`, `install.ps1`, `install.sh`, `package.json`, `CHANGELOG.md`, `README.md`, `CLAUDE.md`, `vault/**`, `.dcs/**`

## Worktree root

`C:\DCS-wt\close-integrity-guard-bundle`

## Evidence required in the return

- **Defect (ii) fixed, real corpus:** `python dcs/tools/record_integrity.py .dcs/incidents/2026-07-30-halt-enumeration-grammar-drift 2>&1 | grep "criterion 1"` — paste verbatim. Required: ZERO `criterion 1: FINDING` lines; both `214-LOG.md:37` and `:38` present as SUPPRESSED, each naming token `b4af6e4` and citing the correcting entry at line 38.
- **Defect (i) fixed, real corpus:** `python dcs/tools/record_integrity.py .dcs/incidents/2026-08-02-close-integrity-guard-bundle 2>&1 | grep "214-LOG.md:3[09]"` — paste both lines. Required: `:30` is a FINDING (token `3df43fc8`) AND `:39` is now ALSO a FINDING (currently suppressed only because that entry happens to mention the sentinel mid-line).
- **No regression on the clean incident:** `python dcs/tools/record_integrity.py .dcs/incidents/2026-08-02-record-integrity-corrections; echo "exit=$?"` — required exit 0.
- **Pin + comparison:** `grep -n "SAFETY_FENCE_EFFECTIVE_DATE" dcs/tools/record_integrity.py` showing `"2026-08-02"`, plus the comparison expression quoted with `file:line` and one sentence on why it's strict greater-than.
- **Verdict-shaped scoping, both directions:** a scratch script calling your new pure comparator directly with a non-verdict object (must return empty) and a verdict-shaped object with a bad key (must return a finding) — paste both results and the comparator's `file:line`.
- **Remedy text:** `file:line` and verbatim text of the FINDING format string showing it names `RECORD-CORRECTION:` and the required entry shape.
- **Regenerated derived facts:** the corpus re-run counts plus the `file:line` where the docstring now records them beside the regenerating command; the corrected `CITATION_KEYWORDS` comment's `file:line`.
- **Suites:** `python tests/test_dcs_gate.py`, `python tests/test_dcs_intake.py`, `python tests/test_doctrine_integrity.py` tail lines. Baseline: 152/152 on the doctrine suite. Name every red case; S2-owned cases are red-by-name.
- `git status --short` — only `dcs/tools/record_integrity.py`.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4, with `found`, `why_plan_wrong`, and a `proposal`. The IC will re-enter planning around your finding.
