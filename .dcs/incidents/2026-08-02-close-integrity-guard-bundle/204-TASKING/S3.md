<!--
204-TASKING/S3.md -- transcribed verbatim by the IC from the Planning
Chief's `taskings[]` array (schemas.md #2, chief plan; second, accepted
return).
-->

# 204 — Tasking S3

**Incident:** close-integrity-guard-bundle
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Land the doctrine rule and the artifact-set correction — criteria 9 (prose half) and 10 — inside a 50-byte hot-path budget.

MEASURED STARTING STATE, re-verify it yourself before editing: `doctrine.md` is 23,876 bytes LF-normalised, `schemas.md` is 13,962, total 37,838 against `HOT_PATH_BUDGET_KB = 37` (= 37,888). Fifty bytes of headroom. The principles list currently runs 1-15 contiguously, and the heading is a bare `## The working principles` with no stated count — so check 2 requires the next principle to be exactly 16, and no count needs updating.

**CRITERION 10** — state the close-time fail-closed rule in `dcs/references/doctrine.md`. Principle 11 ("Gate is mechanical, not behavioral") is textually scoped to the PreToolUse edit gate; the new rule is a close-time record-integrity guard, a different mechanism at a different moment. Your call whether that is principle 16 or an explicit extension of 11 — the 202 permits either — but state your reasoning in your return, because the choice is load-bearing for how future readers find the rule. The rule must say, in the core: a close runs a mechanical record-integrity check over the incident's own artifacts before the merge; a red result is fail-closed — it blocks the merge, is escalation trigger (a), and is never silently resolved; and it runs unconditionally, not behind a project's opt-in. Cite the mechanism by path (`dcs/tools/record_integrity.py`) so the rule points at its enforcement, per the "rule ships with its guard" norm.

FUND YOUR OWN TRIM. You will exceed 37,888 bytes if you only add. Pay for it by relocating PROVENANCE — the field lesson, the story, the worked mechanics — from `doctrine.md` into `dcs/references/doctrine-appendix.md`, which is shipped but never `@`-included and therefore costs nothing on the hot path. That split is `CLAUDE.md`'s stated rule ("keep the rule in the core and the story in the appendix"). NEVER touch `HOT_PATH_BUDGET_KB` — the budget constant is not yours and is not in your territory. Relocated text must land in the appendix intact, not summarised away — state in your return exactly what moved and confirm no load-bearing condition was dropped in the move.

**CRITERION 9 (prose half)** — `dcs/references/forms.md` must state the canonical artifact set as NINE files, listed explicitly: `201-BRIEF.md`, `202-OBJECTIVES.md`, `203-ORG.md` (conditional — Type 3 default activation may doctrine-compliantly skip it and log the skip; Type 1 never does), `204-TASKING/` counted as ONE directory entry, `IAP.md`, `IAP-APPROVED`, `214-LOG.md`, `SAFETY.md`, `AAR.md`. Note: forms.md today states NO count at all — this is an addition, not a correction of a written "seven" (the stale "seven-artifact set" text lives only in `vault/Decisions/non-anthropic-hardening.md`, unshipped and outside this incident's territory — not yours to touch). Its existing per-file table already names all nine; add the count and the conditional-203 rule without duplicating the table. S2 owns a carrier case that reads S1's 9-entry constant from the imported module and asserts every entry appears in your file and that it states 9 — so your wording must contain each filename literally, and the numeral 9 must be findable.

Do not touch `dcs/references/schemas.md`: it is on the hot path with your own `doctrine.md`, but nothing in this period's criteria requires editing it, and every byte you spend there comes out of the same 50.

## File territory (may edit only within these globs)

- `dcs/references/doctrine.md`
- `dcs/references/doctrine-appendix.md`
- `dcs/references/forms.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/references/schemas.md`
- `dcs/tools/**`
- `dcs/workflows/**`
- `dcs/hooks/**`
- `dcs/templates/**`
- `dcs/esg/**`
- `dcs/README.md`
- `dcs/VERSION`
- `tests/**`
- `package.json`
- `CHANGELOG.md`
- `README.md`
- `CLAUDE.md`
- `agents/**`
- `skills/**`
- `bin/**`
- `install.ps1`
- `install.sh`
- `vault/**`
- `.dcs/**`

## Worktree root

`C:\DCS-wt\close-integrity-guard-bundle`

## Evidence required in the return

- `python -c "from pathlib import Path; d=Path('dcs/references/doctrine.md').read_bytes().replace(b'\r\n',b'\n'); s=Path('dcs/references/schemas.md').read_bytes().replace(b'\r\n',b'\n'); print(len(d), len(s), len(d)+len(s), 'budget 37888', 'headroom', 37888-(len(d)+len(s)))"` — paste the output. Total must be <= 37888. Run it BEFORE and AFTER your edit and paste both.
- `python tests/test_doctrine_integrity.py 2>&1 | grep -i 'hot-path\|principle'` — paste those lines. Principles must be contiguous from 1 with no duplicates; hot-path budget must pass.
- The new/extended principle text itself, pasted in full.
- `git diff --stat dcs/references/` plus a line-level summary of WHAT moved to the appendix: for each relocated block, its old `doctrine.md` line range and its new `doctrine-appendix.md` location. Confirm explicitly that no rule, condition, or trigger was dropped rather than moved.
- `grep -n '9\|201-BRIEF\|IAP-APPROVED\|204-TASKING' dcs/references/forms.md | head -20` — paste, showing the count and the enumerated set are both present and findable by S2's carrier case.
- State in prose which option you chose (new principle 16 vs. extension of principle 11) and why — one paragraph, with the reasoning, not just the choice.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
