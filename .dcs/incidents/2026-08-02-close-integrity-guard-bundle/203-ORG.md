<!--
203-ORG.md -- written by the IC after the chief(s) return, during
/dcs-plan. Records which positions are activated THIS period -- doctrine
principle 4, scalable activation. Re-written since activation changed:
period 1's first attempt used 4 specialists (build); attempt 2 (this
revision, post-halt replan) uses 2 (fix two defects in already-built
code) -- S3/S4's territory needs no further work this period.
-->

# 203 — Organization (Operational Period 1, attempt 2)

**Incident:** close-integrity-guard-bundle
**Type:** 1
**Period:** 1

## Activated positions

| Position | Activated? | Who / model |
|---|---|---|
| Incident Commander | always | dcs-commander (Fable), transfer of command -- main session runs Sonnet 5, not Fable |
| Dispatcher | always | main session (Sonnet 5) -- intake, spawning, transcription, Owner relay; no command judgment |
| Planning Chief | yes | dcs-planning-chief, opus (re-spawned fresh for this attempt, targeting only the two implicated files) |
| Logistics Chief | no this attempt | attempt 1's Logistics Chief plan is unaffected by the replan (verified: neither defect touches deploy path, env/deps, migration ordering, or rollback) and stands as-is |
| Safety Officer | yes | dcs-safety-officer, opus |
| Ops Specialists | 2 | dcs-ops-specialist x2, sonnet -- S1 (fix `dcs/tools/record_integrity.py`), S2 (fix `tests/test_doctrine_integrity.py` + fixtures). S3 and S4's attempt-1 work (doctrine principle 16, forms.md's 9-artifact count, close.md's step 5a.1b wiring, the 0.8.0 version bump) is verified unaffected and stands without re-tasking. |

## Specialist roster this attempt

| ID | Territory (summary) | Tasking file |
|---|---|---|
| S1 | `dcs/tools/record_integrity.py` (fix suppression (b), date pin, fence-field scoping) | `204-TASKING/S1.md` |
| S2 | `tests/test_doctrine_integrity.py` + `tests/fixtures/record-integrity/**` (fix census tautology, date pin, fixture pair) | `204-TASKING/S2.md` |

## Execution mode

**Sequential, not parallel** — S2 hard-reads S1's finding-line and suppression-line output formats, and this shared (non-isolated) worktree already produced a real concurrent-snapshot confusion in attempt 1 (S4's first spawn saw a transient 150/152 result). S1 is fanned out first and its return validated; S2 is spawned only after. Territories remain disjoint by path (verified independently by the Planning Chief and `dcs-commander`), so this is a sequencing choice for reliability, not a partition-overlap requirement.
