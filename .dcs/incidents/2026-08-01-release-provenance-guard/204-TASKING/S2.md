# 204 — Tasking S2

**Incident:** release-provenance-guard
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Reorder `docs/publishing.md`'s release steps so tag creation precedes
`npm publish`, and keep the file internally true afterward (202
criterion 4).

The file currently has 9 numbered top-level steps (verified at planning
time, 2026-08-01): step 6 is "Publish" (line 136), step 8 is "Tag and push
it" (line 166). Move the tag-and-push step ahead of the publish step; the
tarball dry-run review (current step 3) and the tip gate (current step 5)
stay before publish as they are now; the content witness (current step 7)
and the GitHub Release step (current step 9) stay after publish. Renumber
the full list.

Exactly three internal by-number citations exist today (established at
planning time as the sweep this criterion must fully resolve — if your
returned evidence resolves fewer than three, that is an incomplete sweep,
not a pass):
- line 47: "see step 2 below"
- line 73: "The tip gate ... in step 5"
- line 78: "step 8" (inside the 0.7.1 bullet)

Two passages become **false** after the reorder, not just mis-numbered —
rewrite their claims, not only their numbers:
- The 0.7.1 bullet's narrative that "no step below catches that yet" /
  "Tagging happens at step 8, after publishing" must be rewritten to
  state the new, true situation: `package.json`'s `scripts.prepublishOnly`
  now runs `python tests/release_provenance_check.py` (the frozen
  interface — use exactly this filename, this trigger name, and this
  invocation string; do not paraphrase or respell any of the three), and
  it fails `npm publish` — including `npm publish --dry-run` — before any
  registry contact when the tag or `CHANGELOG.md` entry is missing.
  `npm pack` and `npm pack --dry-run` are unaffected; they never run
  `prepublishOnly`.
- The tag step's own contingency, "If the tag already exists (someone
  tagged ahead of the publish), do not create it — verify it instead" —
  under tag-first ordering, tagging ahead of publish is now the normal
  case, not an anomaly to note specially. Invert or remove this caveat so
  it no longer reads as describing an unusual situation.

You may add at most one short sentence noting the gate now runs
automatically. Do not restructure the document beyond the reorder and
these two rewrites — this is a reorder-and-repair task, not a rewrite of
the runbook.

`README.md:259` is the only reference to `docs/publishing.md` from outside
the file itself, and it is a bare markdown link with no step-number
citation — verified at planning time, nothing there to chase.

**Hard constraint:** write with the Write/Edit tools only, never
PowerShell `Set-Content`/`Out-File` (BOM). English, LF.

## File territory (may edit only within these globs)

- `docs/publishing.md`

## Forbidden zones (explicitly, even if it seems related)

- `tests/**`
- `package.json`
- `CHANGELOG.md`
- `dcs/**`
- `agents/**`
- `skills/**`
- `bin/**`
- `install.*`
- `README.md`
- Every other file under `docs/` besides `publishing.md`

## Worktree root

`C:\DCS-wt\release-provenance-guard`

## Evidence required in the return

- `git diff docs/publishing.md` — the complete diff: the moved step, the
  renumbering, and both rewritten passages.
- `grep -nE "^[0-9]+\." docs/publishing.md` paired with `grep -noE "step
  [0-9]+" docs/publishing.md`, plus one line per citation showing it now
  resolves to the correct renumbered step (three citations to account
  for — see Task above).
- The rewritten 0.7.1-bullet passage and the rewritten tag-step caveat,
  quoted verbatim in the return — not paraphrased.
- The exact prose sentence(s) naming the gate script and its trigger,
  quoted verbatim — the Safety Officer will check this string equals
  `tests/release_provenance_check.py` / `prepublishOnly` /
  `python tests/release_provenance_check.py` exactly, with no respelling.
- `python tests/test_doctrine_integrity.py` — the full `N/M passed` line
  (`docs/` ships in `package.json`'s `files:` whitelist, so this run's
  BOM/Cyrillic/CRLF walks cover your edit to `publishing.md`).

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
