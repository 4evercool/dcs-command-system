# 204 — Tasking S3

**Incident:** revision-preservation-map
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Document the shape and the provenance, inside the hot-path budget (202
criteria 2, 5, 6). Files: `dcs/references/schemas.md`,
`dcs/references/doctrine.md`, `dcs/references/doctrine-appendix.md`.

A. SCHEMAS.MD — new section, heading pinned EXACTLY as
`## 9. Preservation map (6c amendment pre-stamp proof)`. S2 cites it as
`schemas.md #9 (preservation map)` and check 13 truncates the title at the
first " (" to build its anchor, so the words "Preservation map" before the
parenthesis are load-bearing. The section documents: what the block is
(the JSON appended to `214-LOG.md` beside a `## 6c.` re-stamp), one
worked JSON example, and a `| Field | Type | Notes |` table for
`amendment_entry`, `amended_criteria`, `preserved[]` and the nested
pairing keys `criterion` / `artifact` / `section` / `anchor` /
`verified_by` / `output`, with the note on `output` saying plainly that it
is a self-report the validator re-derives and never trusts. Name
`dcs/tools/preservation_map.py` as the checker inline (in backticks, not
in a fence).

THREE MECHANICAL TRAPS, all named because existing checks assert them:
  - check 19 requires EVERY fenced block in `schemas.md` to parse with
    `json.loads`. Your worked example must be valid JSON; a fenced
    shell/bash example anywhere in this file turns the guard red. Put
    commands in inline backticks.
  - checks 18 and 20 enrol a section into the agent-contract population
    by the presence of the phrase "Returned by". This artifact has no
    producing agent — the IC writes it — so section 9 must NOT contain
    that phrase anywhere in its body (it runs to end of file), exactly as
    sections 7 and 8 do not. Getting this wrong makes the suite demand a
    matching `<output_contract>` table in some agent charter that does
    not and should not exist.
  - the section number 9 is next; section 8 explicitly reserves the
    number 8, do not reuse or renumber anything.

B. DOCTRINE.MD — the default is NO EDIT. Add a pointer near principle 8
only if you can show the rule is unreachable without it; hot-path bytes
are the scarcest resource in this incident. If you do add one, do not add
or renumber a principle (check 2 enforces unique, contiguous numbering
matching the stated count).

C. HOT-PATH BUDGET, HARD. Regenerated at planning time with the guard's
own normalised counter (`tests/test_doctrine_integrity.py` around line
211): `doctrine.md` 23876 + `schemas.md` 13610 = 37486 B against a 37888 B
ceiling. Your final pair total must land inside the band
**36865..37888 B**. Above it the guard is red; below 36865 the file's own
documented ratchet would re-derive `HOT_PATH_BUDGET_KB` to 36, and that
constant lives in `tests/test_doctrine_integrity.py`, which is S1's
territory this period and not yours to edit. Section 9 will cost roughly
700-1000 B, so expect to fund most of it by compressing existing
`schemas.md` notes in place. Report the arithmetic.

D. DOCTRINE-APPENDIX.MD — provenance entry, following the file's existing
`### W<N>:` convention under "## Workflow field lessons" (W1-W3 exist;
yours is W4). It must cite `register-field-repair-path`'s `AAR.md` (the
criterion-5 drop at AAR.md:67-72 and the unverified "restored verbatim"
claim at 82-89) and `vault/Meta/building-dcs-lessons.md` section 18, and
record that the 2026-07-31 attempt at branch
`dcs/revision-preservation-map-abandoned-2026-07-31` @ `497dcd4` shipped
prose plus a phrase-grep check and is prior art only. NAMED TRAP: check
20's field-lesson citation guard scans this file and fails any line
matching "field lesson" near a YYYY-MM-DD date unless that line or the
next carries an incident identifier — an incident slug in backticks, a
`vN.N.N`, or "predates self-hosting". Follow W1-W3's heading form. The
appendix is never `@`-included, so it costs no hot-path bytes — put the
story here and keep the rule terse in `schemas.md`.

E. This is the only tasking permitted to move prose into the appendix. If
S2 needs to divest a `plan.md` passage rather than compress it, that
comes back through the IC as a deviation, not as a quiet edit by either of
you.

## File territory (may edit only within these globs)

- `dcs/references/schemas.md`
- `dcs/references/doctrine.md`
- `dcs/references/doctrine-appendix.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/references/forms.md`
- `dcs/references/typing.md`
- `dcs/workflows/**`
- `dcs/tools/**`
- `dcs/hooks/**`
- `dcs/templates/**`
- `dcs/esg/**`
- `dcs/README.md`
- `dcs/VERSION`
- `tests/**`
- `agents/**`
- `skills/**`
- `bin/**`
- `package.json`
- `install.ps1`
- `install.sh`
- `.dcs/**`
- `vault/**`

## Worktree root

C:\DCS-wt\revision-preservation-map

## Evidence required in the return

- `grep -n "preservation" dcs/references/schemas.md` — criterion 2's own
  verification command, run verbatim; paste the matches
- `grep -n '^## 9\.' dcs/references/schemas.md` — must print the heading
  exactly as `## 9. Preservation map (6c amendment pre-stamp proof)`
- `grep -c 'Returned by' dcs/references/schemas.md` — report the count and
  confirm by line numbers (`grep -n`) that none of them falls inside
  section 9
- `python -c "d=open('dcs/references/doctrine.md','rb').read().replace(b'\r\n',b'\n'); s=open('dcs/references/schemas.md','rb').read().replace(b'\r\n',b'\n'); print(len(d), len(s), len(d)+len(s))"`
  — the guard's own regenerator; report before and after totals and
  confirm the after total is within 36865..37888
- `git diff dcs/references/schemas.md dcs/references/doctrine.md` — paste
  the new section in full and every compression hunk, with a before/after
  ledger for each trim showing no rule lost content
- `grep -n 'W4\|revision-preservation-map\|register-field-repair-path' dcs/references/doctrine-appendix.md`
  — paste the provenance entry's heading and citation lines
- `python tests/test_doctrine_integrity.py` — paste the final "N/M passed"
  line plus, by name, the PASS/FAIL lines for the hot-path budget case,
  every schemas.md fenced-block JSON case, the field-lesson citation
  case, and the schema citation cases

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
