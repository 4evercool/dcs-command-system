# 204 — Tasking S2

**Incident:** revision-preservation-map
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Make the bounded-amendment path require the map before it re-stamps (202
criterion 1). File: `dcs/workflows/plan.md` only.

A. Add one labelled paragraph inside `## 6c.`, immediately before the
closing "Then: proceed to step 7 and step 8 exactly as written below"
line — NOT a fifth boundary condition (the boundary decides admissibility;
this is a pre-stamp duty) and NOT an edit to step 7's shared pre-stamp
checklist (that checklist governs the full path too, and this duty is
6c-only). It must state: before the marker is re-stamped, every 202
acceptance criterion the triggering amendment does not name is paired
with the artifact section that satisfies it in the artifact AS IT NOW
STANDS; each pairing carries a literal anchor from that section plus the
command output showing it present; the map is appended to `214-LOG.md` as
a fenced JSON block indented off column zero (`dcs_gate.py`'s grammar: a
line at column zero without a bracketed timestamp is not an entry, so the
fence must be indented); and it is verified by running, verbatim and on
one line inside backticks:

`python "$HOME/.claude/dcs/tools/preservation_map.py" <incident_dir>`

A non-zero exit is a hard stop — no re-stamp until the artifact or the
map is repaired. This exact string is shared with S1, whose merge-time
carrier case reads it back out of this file after whitespace collapsing;
write it as one unbroken line, do not reflow it across a line break, do
not paraphrase the path.

B. Cite the shape as `schemas.md #9 (preservation map)`. Check 13 requires
every `schemas.md #N` citation to carry N's real title within 80
whitespace-collapsed characters; S3 is creating that section with the
heading pinned as `## 9. Preservation map (6c amendment pre-stamp proof)`,
so the parenthetical `(preservation map)` in your citation is load-bearing,
not decoration.

C. LINE BUDGET, HARD. `plan.md` is 247 lines against check 17's 250-line
ceiling; it is not grandfathered. Your addition must be funded by
compression in place WITHIN `plan.md`. You may not move prose out to
`dcs/references/doctrine-appendix.md` — that file is S3's territory this
period. If you conclude prose genuinely must be divested rather than
compressed, return `status: "deviation"` and say so; do not touch the
appendix and do not ship over budget.

D. TRIM LEDGER. For every passage you compress or remove, report the
before text, the after text, and where its content now lives. An incident
about a narrow revision silently dropping content does not get to drop
content while fixing it. Two named traps in this file: check 20 requires
`plan.md` to contain, in backtick context, EVERY field declared in
`schemas.md` sections 2 and 3 (`objectives_feedback`, `tactics`,
`taskings`, `partition_ok`, `risks`, `verification_plan`, `deploy_path`,
`env_deps`, `migration_ordering`, `rollback_plan`, and the tasking-object
fields) — a trim that drops one backticked field name turns the merge
guard red; and check 6 flags any new `doctrine "..."` reference whose
quoted text is not a real doctrine heading.

E. Do not touch lint 4a check 6, the four boundary conditions, the
halt-ceiling clamp, or step 7/8's mechanics. The gap this incident closes
is the one `## 6c.` leaves open after the boundary already held.

## File territory (may edit only within these globs)

- `dcs/workflows/plan.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/close.md`
- `dcs/workflows/deploy.md`
- `dcs/workflows/esg.md`
- `dcs/workflows/execute.md`
- `dcs/workflows/init.md`
- `dcs/workflows/loop.md`
- `dcs/workflows/new.md`
- `dcs/workflows/run.md`
- `dcs/workflows/status.md`
- `dcs/references/**`
- `dcs/tools/**`
- `dcs/hooks/**`
- `dcs/templates/**`
- `dcs/esg/**`
- `dcs/README.md`
- `dcs/VERSION`
- `tests/**`
- `agents/**`
- `skills/**`
- `bin/**`
- `package.json`
- `install.ps1`
- `install.sh`
- `.dcs/**`
- `vault/**`

## Worktree root

C:\DCS-wt\revision-preservation-map

## Evidence required in the return

- `python -c "raw=open('dcs/workflows/plan.md','rb').read().replace(b'\r\n',b'\n').replace(b'\r',b'\n'); n=raw.count(b'\n')+(0 if raw.endswith(b'\n') else 1); print(n)"`
  — check 17's own counter, must be <= 250
- `grep -n -F 'python "$HOME/.claude/dcs/tools/preservation_map.py" <incident_dir>' dcs/workflows/plan.md`
  — one match, on one line
- `grep -n 'schemas.md #9' dcs/workflows/plan.md` — shows the citation
  carries `(preservation map)`
- `git diff dcs/workflows/plan.md` — paste the added paragraph in full
  and every trim hunk; alongside it, the trim ledger (before / after /
  where the content now lives), one row per compressed passage
- `python tests/test_doctrine_integrity.py` — paste the final "N/M passed"
  line plus, by name, the PASS/FAIL lines for the workflow line-count
  budget case, the inbound field guard case for `plan.md` against
  `schemas.md` #2 and #3, the schema citation case for
  `dcs/workflows/plan.md`, and the doctrine-section-reference case. If the
  `schemas.md #9` citation case is red because S3 has not landed section 9
  yet, report it red by name with its exact text

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
