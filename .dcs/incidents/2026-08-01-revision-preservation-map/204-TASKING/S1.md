# 204 — Tasking S1

**Incident:** revision-preservation-map
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Build the mechanism and its proof (202 criteria 3, 4, and the mechanical
half of 7).

A. NEW FILE `dcs/tools/preservation_map.py` — stdlib-only Python 3, no
third-party imports, `main()` behind `if __name__ == '__main__'` so
importing it is side-effect-free. Public surface (the merge guard imports
these names; do not rename them):
  - `INVOCATION` — the exact string
    `python "$HOME/.claude/dcs/tools/preservation_map.py" <incident_dir>`.
    This literal is shared verbatim with S2, which writes it into
    `plan.md`; do not paraphrase it.
  - `MAP_KEY = "preservation_map"` — the top-level JSON key.
  - `criteria(objectives_text) -> list[int]` — the numbered acceptance
    criteria parsed out of a `202-OBJECTIVES.md` (the numbered list under
    its acceptance-criteria heading; `[IC]`/`[Owner]`/`[deploy period]`
    tags do not exclude a criterion).
  - `find_map(log_text) -> dict | None` — the last fenced JSON block in a
    `214-LOG.md` whose parse carries `MAP_KEY`.
  - `verify(incident_dir) -> list[str]` — the findings list; EMPTY means
    admissible. It must report, each as its own finding string naming the
    criterion number: a criterion in neither `amended_criteria` nor
    `preserved`; a `preserved` entry whose `artifact` is outside the
    incident directory or is not one of the artifacts `## 6c.`'s boundary
    condition 1 screens (`IAP.md`, `203-ORG.md`, `204-TASKING/*.md`); a
    `preserved` entry whose `anchor` does not occur in that artifact's
    current bytes; and a `preserved` entry whose self-reported `output`
    disagrees with what the tool itself finds. `output` is NEVER the
    proof — re-derive from the file on disk every time (this is the
    AAR.md:82-89 false-fidelity defect).
  - `prefix_coverage(incident_dir) -> list[int]` — the PRE-FIX comparator,
    deliberately preserved as a named, importable function: the criteria
    that fail lint 4a check 6's rule (a criterion mapping to no
    `204-TASKING/*.md` tasking id and carrying no
    `[IC]`/`[Owner]`/`[deploy period]` tag). This exists so criterion 4's
    contrast is provable in-suite without consulting any git ref.
  - exit codes: 0 clean, 1 findings (print each), 2 environment error
    (missing incident dir / unreadable 202) — `tests/payload_check.py`'s
    convention.

B. NEW FIXTURES under `tests/fixtures/preservation-map/`, modeled on the
existing `tests/fixtures/halt-ceiling/<scenario>/` layout (one directory
per scenario holding synthetic incident artifacts). Three scenarios, each
with `202-OBJECTIVES.md` (6 numbered criteria), `IAP.md`,
`204-TASKING/S1.md` + `S2.md`, `214-LOG.md`:
  - `clean/` — a `## 6c.` re-stamp entry plus a map covering criteria 1-5
    with true anchors, amendment naming criterion 6. `verify()` returns
    `[]`.
  - `dropped-criterion/` — THE REGRESSION FIXTURE, reproducing
    `register-field-repair-path`'s defect shape: the amendment names
    criterion 6, the map still claims criterion 5 preserved AND carries a
    plausible self-reported `output` for it, but `IAP.md`'s criterion-5
    section is gone. Every criterion still maps to a tasking id, so
    `prefix_coverage()` returns `[]` on this same fixture. `verify()` must
    return a finding naming criterion 5.
  - `no-map/` — a `## 6c.` re-stamp entry with no map block at all.
    `verify()` returns a finding.
  Fixture CONTENT must be neutral fiction (`src/`, `docs/`), never any
  real project's paths or prose (this project's own rule: "Ship no
  project facts"); `tests/` is in `package.json`'s `files` whitelist, so
  these fixtures ship. Fixtures must contain NO `schemas.md #N` citation
  of any kind: check 13 scans every `*.md` outside
  `.git`/`node_modules`/`__pycache__`/`.dcs`/`vault`, which includes
  `tests/fixtures/**`, and would demand a title anchor. UTF-8, no BOM, LF
  only, no Cyrillic (checks 8/9/10 cover `tests/`).

C. APPEND ONE SECTION to `tests/test_doctrine_integrity.py`. Number it by
regenerating at execution time with
`grep -n '^# --- [0-9]' tests/test_doctrine_integrity.py` and taking
highest+1 (that is **22** as independently confirmed by the IC at command
point 2). Append AFTER the last existing section; do not renumber,
reorder, edit, or reflow ANY existing section, and specifically leave the
two `# --- 20.` headings and the mislabeled `# --- 21.` exactly as found
— they belong to `field-lesson-guard-vacuity`. Import the module via the
`importlib.util.spec_from_file_location` idiom already used for
`dcs_gate.py` (around line 440), never re-implement its rules. The
section carries, as separately named `check(...)` cases:
  (i) fixture population non-empty (degeneracy guard — without it every
      case below passes vacuously);
  (ii) `clean/` accepted — `verify()` returns `[]`;
  (iii) `no-map/` rejected;
  (iv) THE REGRESSION PAIR on `dropped-criterion/`: `verify()` returns a
       finding naming criterion 5, AND `prefix_coverage()` returns `[]`
       for the same fixture — one case each, so the contrast fails by
       name;
  (v) an in-memory forgery proof in the idiom of check 18(f): take the
      `clean/` fixture's text in memory, delete one preserved anchor's
      line, and confirm the SAME comparator now reports it — proving the
      validator reads content rather than passing whatever it is handed.
      No file on disk is modified;
  (vi) the carrier case: `INVOCATION`, read from the imported module
       (never typed as a literal in the test), appears in
       `dcs/workflows/plan.md` compared on whitespace-collapsed text
       (markdown hard-wraps; reuse the existing `norm()`/`_ws_norm()`
       helper rather than adding a third normaliser);
  (vii) the field half: for every `.dcs/incidents/*/214-LOG.md` `## 6c.`
        re-stamp entry (recognised via the imported `dcs_gate.py`
        grammar, not a re-derived regex) whose timestamp is on or after
        a pinned effective-date constant, `verify()` on that incident
        directory returns `[]`. Pin the constant as a module-level
        assignment with a comment giving its rationale and this
        incident's slug (effective date **2026-08-02**, the day after
        this incident opened); entries before it are out of scope, not
        silently skipped — print how many were in scope so a zero is
        visible.

D. Add one line for `dcs/tools/` to `dcs/README.md`'s payload tree so the
payload map does not go stale.

Ownership note: this tasking satisfies 202 criteria 3 and 4 in full, and
is the mechanical half of criterion 7 (this new section must be part of
`python tests/test_doctrine_integrity.py`'s green run).

## File territory (may edit only within these globs)

- `dcs/tools/**`
- `tests/test_doctrine_integrity.py`
- `tests/fixtures/preservation-map/**`
- `dcs/README.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/**`
- `dcs/references/**`
- `dcs/hooks/**`
- `dcs/templates/**`
- `dcs/esg/**`
- `dcs/VERSION`
- `agents/**`
- `skills/**`
- `bin/**`
- `tests/payload_check.py`
- `tests/test_dcs_gate.py`
- `tests/test_dcs_intake.py`
- `tests/test_dcs_cli.py`
- `tests/fixtures/halt-ceiling/**`
- `package.json`
- `install.ps1`
- `install.sh`
- `.dcs/**`
- `vault/**`

## Worktree root

C:\DCS-wt\revision-preservation-map

## Evidence required in the return

- `grep -n '^# --- [0-9]' tests/test_doctrine_integrity.py` — paste the
  full output; it must show every pre-existing label unchanged (including
  both 20s and the mislabeled 21) with exactly one new label appended last
- `git diff -U0 tests/test_doctrine_integrity.py | grep '^@@'` — paste all
  hunk headers; a single trailing append hunk is the proof that no
  existing section was touched. Any hunk that is not at the end of the
  file must be explained line by line
- `python dcs/tools/preservation_map.py tests/fixtures/preservation-map/clean; echo "exit=$?"`
  — expect `exit=0`
- `python dcs/tools/preservation_map.py tests/fixtures/preservation-map/dropped-criterion; echo "exit=$?"`
  — expect `exit=1` and a finding naming criterion 5; paste the finding
  line verbatim
- `python dcs/tools/preservation_map.py tests/fixtures/preservation-map/no-map; echo "exit=$?"`
  — expect `exit=1`; paste the finding line
- `python -c "import importlib.util,sys; s=importlib.util.spec_from_file_location('pm','dcs/tools/preservation_map.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); print(m.prefix_coverage('tests/fixtures/preservation-map/dropped-criterion'))"`
  — expect `[]`, the pre-fix comparator's blindness, printed beside the
  finding above
- `python tests/test_doctrine_integrity.py` — paste the final "N/M passed"
  line plus the PASS/FAIL line of every case in the new section by name.
  If the carrier case (vi) is red because S2 has not landed the
  invocation string yet, report it as red by name with the exact case
  text; do not describe it as expected to pass later
- `npm test` — paste the final line of each of the three suites
  (criterion 7)

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
