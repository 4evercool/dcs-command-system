# 204 — Tasking S3

**Incident:** workflow-budget-enforcement
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Satisfy 202 criterion 8: version sync + changelog entry.

1. Bump `dcs/VERSION` from `0.6.11` to `0.6.12`.
2. Bump `package.json`'s `"version"` field from `0.6.11` to `0.6.12`, in the same return as step 1 (CLAUDE.md's "version sync is atomic" coding rule — the guard's check 1 fails if these two ever move apart).
3. Add a `CHANGELOG.md` entry for this incident under a new `## 0.6.12 — <today's date, ISO YYYY-MM-DD>` heading, placed above the existing `## 0.6.11` entry (newest first, matching the file's existing order), in the same voice and structure as the existing entries (dated heading, `### Added` or `### Fixed` subsection, prose bullets explaining what changed and why — not a changelog of file diffs). Base the entry's content on the 202 objectives text below — describe the mechanism at the same level of detail 202 does (a per-workflow-file line-count budget check, grandfathered ceilings for four already-over-budget files, a corrected CLAUDE.md rule) — do **not** invent specific line numbers, exact function names, or a check count beyond what's given here, since S1 and S2 are working in parallel and you cannot see their actual diffs. Do not state a bare, unregenerable case count (principle 15 — this project's CHANGELOG has been burned by exactly that before).

### 202 objectives, for the changelog entry's content (this period's goal + criteria, Owner-confirmed)

Goal: A `dcs/workflows/*.md` file exceeding its allowed line-count ceiling is caught mechanically at merge time, every time. The four files already over CLAUDE.md's ~250-line policy (`plan.md`, `execute.md`, `deploy.md`, `close.md`) get a deliberate, documented, finite grandfather ceiling instead of either silently passing forever or immediately reddening the merge-time guard; the six currently-compliant files hold the policy ceiling itself.

Mechanism: a new check in `tests/test_doctrine_integrity.py` enumerates every `dcs/workflows/*.md` file and compares its line count against a per-file ceiling — 250 lines for compliant files, an explicit documented grandfather ceiling (set at each file's own current size) for the four already over budget. `CLAUDE.md`'s "File size" rule is corrected to name the mechanism explicitly instead of reading as advisory prose.

Out of scope this period: actually trimming the four grandfathered files' content back toward 250 lines (deferred to a future incident).

## File territory (may edit only within these globs)

- `dcs/VERSION`
- `package.json`
- `CHANGELOG.md`

## Forbidden zones (explicitly, even if it seems related)

- `tests/**`
- `CLAUDE.md`
- `dcs/workflows/**`
- `dcs/references/**`
- `dcs/hooks/**`
- `dcs/templates/**`
- `agents/**`
- `skills/**`
- `bin/**`
- `install.ps1`
- `install.sh`
- `vault/**`
- `README.md`

## Evidence required in the return

- `git diff dcs/VERSION package.json` — full diff (small; expected: `0.6.11` -> `0.6.12` in both, nothing else).
- `git diff CHANGELOG.md` — full diff showing the new entry, placed above the `0.6.11` heading.
- `python tests/test_doctrine_integrity.py; echo EXIT=$?` — final `N/M passed` line and EXIT value, confirming check 1 (version sync) still passes and nothing else regressed. (This tasking touches files outside S1/S2's territory, so this is a real regression check, not ceremony — `package.json` in particular is read by check 16.)

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
