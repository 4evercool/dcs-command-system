# 204 — Tasking S2

**Incident:** workflow-budget-enforcement
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Correct `CLAUDE.md` per 202 criterion 5. The "File size" bullet at CLAUDE.md:132-133 currently reads `- **File size**: workflows <= ~250 lines. doctrine.md is the exception (it is the constitution) but is budgeted -- see the guard.` — its "see the guard" clause attaches to doctrine.md's hot-path budget only, while scanning as though it covered the workflow rule too, which is the ambiguity criterion 5 names.

Rewrite it so it:
(a) names the new check and its literal constant identifiers `WORKFLOW_BUDGET_LINES` and `WORKFLOW_GRANDFATHERED_LINES` in `tests/test_doctrine_integrity.py`, so the sentence is greppable against the mechanism rather than merely descriptive of it;
(b) states plainly that four files — `plan.md`, `execute.md`, `deploy.md`, `close.md` — currently run under a documented, temporary grandfather ceiling rather than full ~250-line compliance, and that this is recorded debt, not a new normal;
(c) makes clear the ceiling is now hard rather than advisory, since `new.md` sits at 242 with 8 lines of headroom and the next incident to edit it will meet a red merge guard.

Second surface, same file: the "Merge-time guard" section at CLAUDE.md:53-57 enumerates what the suite checks ("version sync, principle numbering..., the hot-path size budget, and encoding") — add the workflow line budget to that list so the two surfaces do not disagree.

Do NOT restate a case count anywhere (CLAUDE.md:69-72 already forbids it, and those counts have rotted once already).

Use the Write/Edit tools only, never PowerShell `Set-Content` or `Out-File` — that emits a BOM and check 8 walks the whole repo for one.

## File territory (may edit only within these globs)

- `CLAUDE.md`

## Forbidden zones (explicitly, even if it seems related)

- `tests/**`
- `dcs/**`
- `agents/**`
- `skills/**`
- `bin/**`
- `package.json`
- `install.ps1`
- `install.sh`
- `vault/**`
- `README.md`

## Evidence required in the return

- Criterion 5, the edit: `git diff CLAUDE.md` in full — it is a few lines, so the full diff is the decisive excerpt.
- Criterion 5, the binding made mechanical: `grep -n 'WORKFLOW_BUDGET_LINES\|WORKFLOW_GRANDFATHERED_LINES' CLAUDE.md tests/test_doctrine_integrity.py` — paste the output. Hits on BOTH sides with identical spelling is the proof. **If the `tests/` side has no hit because S1 has not landed yet, report that verbatim and STOP there** — do not adjust your own wording to match a mechanism you cannot see, and do not invent an identifier.
- Criterion 5, the four grandfathered files and their ceilings as stated in your prose must match the check's dict exactly: paste the two sources side by side (your CLAUDE.md sentence, and the grep or `file:line` of `WORKFLOW_GRANDFATHERED_LINES`). Same STOP rule if S1 has not landed.
- No regression from the prose edit: `python tests/test_doctrine_integrity.py; echo EXIT=$?` — final `N/M passed` line and EXIT value. CLAUDE.md sits inside check 8's repo-wide BOM/U+FFFD walk and inside check 15's declaring-paragraph population, so a prose edit here can turn the suite red on its own.
- Out-of-scope honoured: `git diff --name-only -- dcs/ tests/` from your own work must be empty.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
