# 204 — Tasking S1

**Incident:** workflow-budget-enforcement
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Add check 17 to `tests/test_doctrine_integrity.py` enforcing a per-file line-count ceiling on `dcs/workflows/*.md`, satisfying 202 criteria 1, 2, 3 (the mechanism half), 4 and 6.

Reuse the existing `workflows()` helper at lines 175-176 — do not write a second enumerator. Define `WORKFLOW_BUDGET_LINES = 250` and `WORKFLOW_GRANDFATHERED_LINES = {'close.md': 273, 'deploy.md': 282, 'execute.md': 424, 'plan.md': 666}`; effective ceiling = the grandfather entry if present, else the policy constant.

Measure by: `read_bytes()`, collapse CRLF to LF, then collapse any surviving lone CR to LF, count LF, and add 1 if the result is non-empty and does not end in LF (this deliberately diverges from `wc -l`, which counts newlines not lines — say so in the comment, or a hand cross-check reads the one-line disagreement as a bug).

Emit **exactly one** `check()` call whose name states all four conditions it enforces: (i) every file is within its effective ceiling; (ii) no grandfather entry names a file that no longer exists; (iii) no grandfather entry is now slack — i.e. its file has fallen to or below 250 (the debt is discharged, delete the entry, or the table silently accumulates dead exemptions); (iv) the population (`workflows()`'s result) is non-empty. Detail string enumerates ALL offenders via the `'; '.join` idiom checks 3/4/5 already use — never just the first.

Give each grandfather value an inline comment naming it as documented, temporary technical debt with its derivation. `plan.md`'s comment must record that it was 422 as recently as commit `623582f` (2026-07-28) and reached 666 via `e285108` and `807edb8` the same day — 666 is not a settled state being formalised, it is a same-day 244-line growth being recorded as accepted debt.

Add item 17 to the module docstring's numbered list (lines 13-78) in the same voice as items 1-16.

**Do NOT touch `EXCLUDED_DIRS` or `BYTECODE_SUFFIXES` (lines 156-157)** even though they're nearby: check 16 asserts they stay textually identical to `tests/payload_check.py`'s copies, and changing either turns criterion 6 red for a reason unrelated to this task.

## File territory (may edit only within these globs)

- `tests/test_doctrine_integrity.py`

## Forbidden zones (explicitly, even if it seems related)

- `CLAUDE.md`
- `dcs/**`
- `tests/payload_check.py`
- `tests/test_dcs_gate.py`
- `tests/test_dcs_intake.py`
- `tests/fixtures/**`
- `agents/**`
- `skills/**`
- `package.json`
- `install.ps1`
- `install.sh`
- `vault/**`

## Evidence required in the return

- Criterion 2 + 6, green run: `python tests/test_doctrine_integrity.py; echo EXIT=$?` — paste the new check's PASS line verbatim and the final `N/M passed` line and the EXIT value. Baseline for comparison is 85/85, exit 0 (reproduced fresh on this worktree at `58d9833` — see 214-LOG.md). State the delta as a number; anything other than baseline + the new check's own case count is a criterion-6 regression and must be reported as a deviation, not absorbed.
- Criterion 1, red path on the budget rule: (i) change ONLY the `close.md` grandfather value from 273 to 272; (ii) `python tests/test_doctrine_integrity.py; echo EXIT=$?` and paste the FAIL line, its detail string naming `close.md`, and the non-zero EXIT; (iii) restore 273; (iv) `git diff --stat tests/test_doctrine_integrity.py` plus a re-run showing the green tail and EXIT=0, proving the revert landed.
- Criterion 1, red path on a tripwire: same four-step shape, adding a phantom entry (`'nonexistent.md': 300`) to `WORKFLOW_GRANDFATHERED_LINES`: paste the FAIL line naming the phantom, the non-zero exit, and the proof of revert.
- Criterion 4, the measurement: the `file:line` of the counting expression, then the verbatim output of a one-off command printing the check's own count beside `wc -l`'s for all ten workflow files (must agree on all ten today), plus one synthetic case written to the **scratch directory only, never a repo file** — a file containing a lone CR and no trailing newline — showing the naive newline count would be wrong there and the idiom is right.
- Criterion 3, the derivation: `wc -l dcs/workflows/*.md` verbatim, and the `file:line` range of the constants-plus-comment block.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
