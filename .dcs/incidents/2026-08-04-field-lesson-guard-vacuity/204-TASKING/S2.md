# 204-TASKING — S2: Bring doctrine-appendix.md field-lesson mentions into compliance

**Assigned:** dcs-ops-specialist
**Territory:** `dcs/references/doctrine-appendix.md`
**Forbidden:** `tests/**`, `dcs/workflows/**`, `dcs/templates/**`, `dcs/references/doctrine.md`, `dcs/references/schemas.md`

## Task

Bring every field-lesson mention in `dcs/references/doctrine-appendix.md` into compliance with the repaired guard (S1), per acceptance criterion 7.

### Procedure

1. **Re-derive the full population** — run the enumerating command and record the output:
   ```
   grep -n '[Ff]ield[ -]lesson' dcs/references/doctrine-appendix.md
   ```
   The 202's pre-computed census (lines 1, 13, 669, 731) is approximate — line 731 already carries an incident slug; line 414 is a known additional undated mention. **Do not trust the census; trust the grep output.**

2. **For every line that lacks a verifiable identifier on the SAME physical line** — add one. Recognised identifier forms (per the repaired `_FL_ID_RE`):
   - YYYY-MM-DD date (e.g., `2026-07-23`)
   - Incident slug in backticks (e.g., `` `field-lesson-citations` ``)
   - Version number (e.g., `v0.5.10`)
   - "predates self-hosting"

3. **Blockquote handling** — if a "field lesson" mention appears inside a `>` blockquote and the quoting context provides the identifier (section heading, preceding paragraph), add the identifier inline within the blockquote rather than relying on ambient context that the line-by-line guard cannot see.

4. **Cross-references** — a mention like "per the field lesson below" (line 414) that refers to another lesson needs the referenced lesson's identifier, e.g., `"per the field lesson 2026-07-24 below"`.

5. **Re-run the guard** after all changes: `python tests/test_doctrine_integrity.py` — the field-lesson citation check (now 20a) must pass with zero violations from `doctrine-appendix.md`.

### Evidence required

1. `grep -n '[Ff]ield[ -]lesson' dcs/references/doctrine-appendix.md` — the full census BEFORE changes
2. `grep -n '[Ff]ield[ -]lesson' dcs/references/doctrine-appendix.md` — the full census AFTER changes, with every line now carrying an identifier on the same line
3. `python tests/test_doctrine_integrity.py 2>&1 | Select-String -Pattern 'field-lesson citations'` — the guard check (20a) must pass (no violations reported from doctrine-appendix.md)
