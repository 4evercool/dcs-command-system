# 204-TASKING — S1: Fix the field-lesson citation guard and add self-tests

**Assigned:** dcs-ops-specialist
**Territory:** `tests/test_doctrine_integrity.py`, `tests/fixtures/field-lesson-guard/**`
**Forbidden:** `dcs/**`, `agents/**`, `skills/**`

## Task

Fix the field-lesson citation guard in `tests/test_doctrine_integrity.py` lines 2032-2073 and add permanent test fixtures with self-tests per acceptance criteria 1, 2, 3, 4, and 5.

### Specific changes

1. **Broaden `_FL_LINE_RE`** (line 2048): replace `r'[Ff]ield lesson.*\d{4}-\d{2}-\d{2}'` with a pattern that matches any line containing "field lesson" / "field-lesson" / "field lessons" — no date requirement. Every such mention must enter the guard regardless of line layout.

2. **Broaden `_FL_ID_RE`** (line 2049-2050): add bare YYYY-MM-DD date recognition. The current regex recognises incident slugs (\`…\`), version numbers (vX.Y.Z), and "predates self-hosting" — but NOT bare dates. Multi-line citations where the date is on line N+1 are invisible to the current `_FL_ID_RE` even after `_FL_LINE_RE` is broadened. Add `\d{4}-\d{2}-\d{2}` as a recognised identifier form.

3. **Expand `_FL_FILES`** (lines 2037-2044): add `dcs/workflows/plan.md` and `dcs/workflows/execute.md` to the hard-coded file list. The current population is: `doctrine-appendix.md`, `deploy.md`, `new.md`, `close.md`, `202-OBJECTIVES.md`, `REGISTER.md`.

4. **Renumber the duplicate section** (line 2032): change `--- 20.` to `--- 20a.` (the field-lesson citation guard). The inbound field-presence guard at line 1975 stays `--- 20.`. No subsequent section numbers need to shift.

5. **Add permanent test fixtures** in `tests/fixtures/field-lesson-guard/`:
   - `undated-claim.md` — a file containing "field lesson" on a line with no YYYY-MM-DD date on the same line (the v0.5.10 shape)
   - `multiline-claim.md` — a file containing "field lesson," on line N and "2026-07-22, predates self-hosting" on line N+1 (the `202-OBJECTIVES.md:33-34` shape)
   
   Add the fixtures directory to `_FL_FILES` within a test-scoped guard call, NOT to the production file list — the production guard scans shipped `dcs/` files only; the self-tests call the guard function directly on fixture paths.

6. **Add two self-test assertions** (new `check_*` functions or inline assertions in the test runner):
   - `test_field_lesson_guard_catches_undated_claim`: runs the guard logic on `undated-claim.md`; asserts the guard found a violation (the undated claim is flagged)
   - `test_field_lesson_guard_processes_multiline`: runs the guard logic on `multiline-claim.md`; asserts the guard processed the multi-line form (the split-line pair is checked, and the date on line N+1 satisfies the identifier check)

### Evidence required

1. `python tests/test_doctrine_integrity.py 2>&1 | Select-String -Pattern 'field-lesson|citation guard|20a\.'` — show the guard check output and the two self-test assertions
2. `python tests/test_dcs_gate.py` — show exit 0, no regressions
3. `python tests/test_dcs_intake.py` — show exit 0, no regressions
4. `grep -n 'field.lesson' tests/fixtures/field-lesson-guard/undated-claim.md tests/fixtures/field-lesson-guard/multiline-claim.md` — show fixture contents at the lines the guard reads
5. Show the changed lines: `_FL_LINE_RE` (old and new), `_FL_ID_RE` (old and new), `_FL_FILES` (old and new), section header (`20` → `20a`), and the two self-test `check_*()` calls
