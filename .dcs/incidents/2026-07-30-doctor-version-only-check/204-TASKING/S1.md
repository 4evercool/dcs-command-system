# 204-TASKING — S1

**Assigned to:** dcs-ops-specialist (S1)
**Period:** 1

## Task

Переписать `doctor()` на content-aware проверку через вызов `tests/payload_check.py` и добавить команду `bump <version>` — критерии 1, 2, 3, 4 из 202.

## Territory

- `bin/dcs.js`

## Forbidden

- `docs/**`
- `dcs/**`
- `tests/**`
- `agents/**`
- `skills/**`

## Evidence required

1. `node bin/dcs.js doctor` — вывод должен содержать строки `identical:`, `differing:`, `repo-only:`, `installed-only:` из payload_check.py (или degraded-mode fallback, если `~/.claude/dcs` отсутствует). Проверка Python на PATH по-прежнему в выводе.
2. `node bin/dcs.js bump` — вывод: текущая версия (0.7.0) и usage-строка с `<version>`.
3. `node bin/dcs.js bump 0.7.0` — оба файла не изменились (текущая версия уже 0.7.0); grep `0.7.0` в `dcs/VERSION` и `package.json` подтверждает идентичность.
4. `node bin/dcs.js bump 0.7.0-test && node bin/dcs.js bump 0.7.0` — атомарный цикл: bump на тестовую версию, проверка что оба файла содержат `0.7.0-test`, откат на `0.7.0`, проверка что оба файла снова содержат `0.7.0`.
5. `npm test` — все три набора тестов зелёные (`test_dcs_gate.py`, `test_dcs_intake.py`, `test_doctrine_integrity.py`).
