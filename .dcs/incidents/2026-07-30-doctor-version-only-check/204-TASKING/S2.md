# 204-TASKING — S2

**Assigned to:** dcs-ops-specialist (S2)
**Period:** 1

## Task

Обновить документацию: `docs/publishing.md` и `README.md` — отразить content-aware `doctor` и команду `bump` — критерий 5 из 202.

## Territory

- `docs/publishing.md`
- `README.md`

## Forbidden

- `bin/**`
- `dcs/**`
- `tests/**`
- `agents/**`
- `skills/**`

## Evidence required

1. `grep -n 'content-aware\|payload_check\|sha256' docs/publishing.md README.md` — показать строки, где doctor описан как content-aware проверка (не строковое сравнение).
2. `grep -n 'bump' docs/publishing.md README.md` — показать строки, документирующие `dcs bump <version>` как способ смены версии.
3. `grep -n 'атомарно\|atomic\|both files\|dcs/VERSION.*package.json' docs/publishing.md` — показать, что bump описан как операция над двумя файлами.
4. `npm test` — зелёный (документация не ломает тесты).
