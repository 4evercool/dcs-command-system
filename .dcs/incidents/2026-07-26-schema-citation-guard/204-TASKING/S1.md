# 204 — Tasking S1

**Incident:** schema-citation-guard
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Закрывает критерии 202 **№1**, **№7**, **№8**.

Поставить якорь-название рядом с каждой из **15** ссылок `schemas.md #N`, у
которых его нет. Замер на `c7e42aa`, перепроверен IC — 15 ссылок в 9 файлах:

| Файл | Ссылки без якоря |
|---|---|
| `agents/dcs-commander.md` | `#6` |
| `dcs/references/doctrine.md` | `#6` |
| `dcs/templates/201-BRIEF.md` | `#1` |
| `dcs/templates/204-TASKING.md` | `#2`, `#4` |
| `dcs/templates/DELEGATION.md` | `#7` |
| `dcs/workflows/deploy.md` | `#7` |
| `dcs/workflows/execute.md` | `#4`, `#6`, `#6` |
| `dcs/workflows/new.md` | `#1`, `#6` |
| `dcs/workflows/plan.md` | `#2`, `#6`, `#7` |

**Форма якоря — по уже сложившемуся в пакете обычаю, не изобретать своё.**
Пять уставов из шести уже несут её: `` `references/schemas.md` #1
(situation-analyst findings) ``. Эти пять файлов в твою территорию не входят
и служат образцом — прочитай их, прежде чем писать.

**Правило совпадения задаёт команда-перечислитель из 202 и никто больше:**
заголовок раздела из `dcs/references/schemas.md`, обрезанный по первой `(`
или тире, в нижнем регистре, обязан встретиться **в пределах 80 символов
после цифры** в тексте, нормализованном `re.sub(r"\s+", " ", text)`. Регистр
не важен, скобки не обязательны — важно попадание в окно после нормализации.

**Особый случай:** ссылку в `agents/dcs-commander.md:101-102` разрывает
перенос markdown (`(schemas.md\n#6)`). Якорь ставить так, чтобы он читался
**после** нормализации переносов. Это тот самый файл, из-за которого прошлый
инцидент насчитал 19 вместо 20.

**Критерий 8, `CHANGELOG.md:41`.** Строка «because 19 `schemas.md #N`
citations across six charters, four workflows and three templates depend on
positional numbering and nothing resolves them» неверна дважды: цитат **20**,
и `dcs/references/doctrine.md` в перечне поверхностей отсутствует вовсе.
Предпочтительная правка по принципу 15 — **формулировка без числа** со
ссылкой на новую проверку целостности, потому что число 19 уехало в три
места именно оттого, что стояло в прозе без способа пересчёта. Допустима и
вторая форма: верное число рядом с командой, которая его восстанавливает.
Прежняя формулировка не должна выжить ни в каком виде. Твоя правка **не
должна сама породить новую неякорёванную ссылку** вида `schemas.md #<цифра>`
— буквальное `schemas.md #N` с буквой N в популяцию не попадает (шаблон
требует цифр), это IC проверил.

## File territory (may edit only within these globs)

- `agents/dcs-commander.md`
- `dcs/references/doctrine.md`
- `dcs/templates/201-BRIEF.md`
- `dcs/templates/204-TASKING.md`
- `dcs/templates/DELEGATION.md`
- `dcs/workflows/deploy.md`
- `dcs/workflows/execute.md`
- `dcs/workflows/new.md`
- `dcs/workflows/plan.md`
- `CHANGELOG.md`

## Forbidden zones (explicitly, even if it seems related)

- `tests/**`
- `dcs/references/schemas.md` — **в том числе заголовки.** Если якорь не
  сходится, это отклонение, а не повод поправить заголовок под ссылку
- `dcs/hooks/**`
- `agents/dcs-situation-analyst.md`, `agents/dcs-planning-chief.md`,
  `agents/dcs-logistics-chief.md`, `agents/dcs-ops-specialist.md`,
  `agents/dcs-safety-officer.md`
- `dcs/workflows/close.md`, `esg.md`, `init.md`, `loop.md`, `run.md`,
  `status.md`
- `skills/**`, `bin/**`, `install.ps1`, `install.sh`, `package.json`,
  `dcs/VERSION`
- `.dcs/**`, `vault/**`

Читать что угодно можно и нужно — запрет только на **правку**.

**Отдельно запрещено:**
- Удалять или переписывать саму ссылку, чтобы «убрать» её из популяции.
  Счёт обязан остаться **20 ссылок в 14 файлах**.
- Чинить `advisories` в уставе офицера или заводить блок выходного контракта
  командиру — это явный out-of-scope 202, отдельные строки очереди.
- Писать через PowerShell `Set-Content`/`Out-File` — они добавляют BOM.
  Только Write/Edit.

Язык правок — английский (пакет англоязычный), LF, без BOM.

## Evidence required in the return

1. **Команда-перечислитель из 202 дословно** (heredoc `python - <<'PY' …
   PY` из раздела «Область и метод»), запущенная из корня worktree.
   Приложить команду и её вывод. **Вывод обязан быть пустым.**

2. **Популяция не сжалась** — доказательство, что якоря поставлены, а не
   ссылки удалены:

```bash
python - <<'PY'
import re, pathlib
EX = {".git", "node_modules", "__pycache__", ".dcs", "vault"}
pat = re.compile(r"schemas\.md`?\s*#\s*(\d+)")
files, refs = set(), 0
for p in sorted(pathlib.Path(".").rglob("*.md")):
    if any(x in p.parts for x in EX):
        continue
    t = re.sub(r"\s+", " ", p.read_text(encoding="utf-8"))
    n = len(pat.findall(t))
    if n:
        files.add(p.as_posix()); refs += n
print("refs:", refs, "files:", len(files))
for f in sorted(files): print(" ", f)
PY
```

   Ожидается `refs: 20 files: 14` и тот же перечень файлов, что на `c7e42aa`.

3. `python tests/test_doctrine_integrity.py` — приложить **напечатанную**
   строку `N/M passed` и код возврата; зелёный.
4. `python tests/test_dcs_gate.py` — напечатанная `N/M passed`; зелёный.
5. `python tests/test_dcs_intake.py` — напечатанная `N/M passed`; зелёный.

6. **Критерий 7, бюджет горячего пути:**

```bash
python -c "d=open('dcs/references/doctrine.md','rb').read().replace(b'\r\n', b'\n'); s=open('dcs/references/schemas.md','rb').read().replace(b'\r\n', b'\n'); print(len(d), len(s), len(d)+len(s), 37*1024-(len(d)+len(s)))"
```

   Приложить вывод; последнее число (запас) обязано быть положительным. На
   `c7e42aa` было 1327.

7. **Границы территории:** `git status --porcelain` и `git diff --stat` —
   приложить вывод. Изменённых файлов не более десяти, все из территории;
   `dcs/references/schemas.md` и `tests/**` среди них отсутствуют.

8. **Кодировка изменённых файлов** (`CHANGELOG.md` не входит в набор
   проверок 9 и 10, поэтому смотрим явно):

```bash
python - <<'PY'
import subprocess, pathlib
out = subprocess.run(["git","status","--porcelain"],capture_output=True,text=True).stdout
for line in out.splitlines():
    p = pathlib.Path(line[3:].strip().strip('"'))
    if p.is_file():
        raw = p.read_bytes()
        print(p.as_posix(), "CRLF" if b"\r\n" in raw else "LF",
              "BOM" if raw[:3] == b"\xef\xbb\xbf" else "no-bom")
PY
```

   У всех строк ожидается `LF` и `no-bom`.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC
will re-enter planning around your finding.
