# 204 — Tasking S2

**Incident:** schema-citation-guard
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Закрывает критерии 202 **№2**, **№3**, **№4**, **№5**.

> **НАЧИНАТЬ ТОЛЬКО ПОСЛЕ ТОГО, КАК РАБОТА S1 ПРИНЯТА И ЛЕЖИТ В ВЕТКЕ.**
> До этого команда-перечислитель из 202 даёт непустой вывод, и твоя проверка
> будет красной не по своей вине. Если ты видишь красный результат на
> настоящих якорях — это **не** повод ослаблять сравниватель. Ослабление
> проверки до зелёной есть прямой отказ задания: вакуумно-зелёный сторож —
> ровно тот дефект, ради устранения которого инцидент открыт.

Добавить в `tests/test_doctrine_integrity.py` проверку **13**, «schema
citation anchors», сторожащую **привязку номера раздела к его идентичности**
(не разрешимость номера: проверка «номер существует» ловит 0 из 14
молчаливых сносов смысла).

Требования:

**(а) Источник истины — сам `schemas.md` на прогоне.** Соответствие
«номер → заголовок» строится разбором `^##\s+(\d+)\.\s+(.+)$` из
`dcs/references/schemas.md` во время выполнения; заголовок обрезается по
первой `(` или тире. В теле проверки **нет ни одного номера раздела и ни
одного заголовка раздела строковым литералом**. Приём проверки 12
(«импортировать правило из механизма» — она берёт грамматику из
`dcs_gate.py`) здесь **неприменим**: у цитат схем исполнителя нет.

**(б) Популяция обнаруживается обходом, а не перечисляется.** Обход `*.md`
по дереву за вычетом `.git`, `node_modules`, `__pycache__`, `.dcs`, `vault`.
Никаких списков файлов в коде. Сравнение идёт по тексту, нормализованному по
переносам — **переиспользовать существующую `norm()`** (строка 204), не
заводить третью копию нормализатора рядом с `norm()` и `_ws_norm()` (строка
334). Третья рукописная копия одного правила без арбитра — буквально корень
этого инцидента. Если переиспользование невозможно, назвать причину в
возврате.

**(в) Семантика совпадения обязана совпадать с командой-перечислителем 202**
(окно 80 символов после цифры, нижний регистр, обрезка заголовка).
Расхождение семантики **не выбирается молча** — оно возвращается как
отклонение.

**(г) Один именованный случай на файл популяции**, как в проверке 12(c), с
общим префиксом имени `schema citation:`. Пропавший якорь в **новом** файле
обязан падать по имени, а не растворяться в общем счётчике.

**(д) Страховка от вырождения (критерий 4).** Красное, если:
- популяция пуста;
- из `schemas.md` не разобрано ни одного заголовка;
- в популяции нет `agents/dcs-commander.md` (файл, видимый только после
  нормализации переносов);
- не представлена хотя бы одна из четырёх поверхностей: `agents/`,
  `dcs/workflows/`, `dcs/templates/`, `dcs/references/doctrine.md`.

Последний пункт закрывает лазейку «удалить ссылку вместо якоря»: без него
пустой вывод перечислителя достижим удалением цитат — файл выпадает из
популяции вместе со своим случаем, и сторож зеленеет. IC на командной точке
2 сохранил это требование явно.

**(е) Критерий 3, отрицательное доказательство.** Именованный случай, который
прогоняет **тот же** сравниватель по подделанному входу и обязан сообщить о
несовпадении. Подделка строится **из разобранного `schemas.md`** — сдвигом
нумерации или подменой заголовка на заведомо синтетический, которого в
`schemas.md` нет. Литералом настоящего заголовка подделка не набирается
(иначе столкновение с пунктом (а)).

Проверка stdlib-only, английская, **ASCII** (проверка 9 сканирует `tests/`),
LF, без BOM. Писать Write/Edit, не `Set-Content`/`Out-File`. Обновить
перечень проверок в докстроке файла (строки 13-36) пунктом 13.

## File territory (may edit only within these globs)

- `tests/test_doctrine_integrity.py`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/**` — включая `dcs/references/schemas.md`
- `agents/**`
- `CHANGELOG.md`
- `tests/test_dcs_gate.py`, `tests/test_dcs_intake.py`
- `skills/**`, `bin/**`, `install.ps1`, `install.sh`, `package.json`
- `.dcs/**`, `vault/**`, `README.md`

Читать что угодно можно и нужно — запрет только на **правку**. Разбирать
`schemas.md` на прогоне ты обязан; редактировать его нельзя.

**Отдельно запрещено:** править прозу под сторожа. Ни один файл с якорями в
твою территорию не входит. Если сторож краснеет на настоящем якоре — это
отклонение (либо якорь неверен и правка возвращается к S1 через IC, либо
неверен сравниватель и правится сравниватель), но **не** тихая правка текста.

## Evidence required in the return

1. `python tests/test_doctrine_integrity.py` на настоящем дереве — приложить
   **напечатанную** строку `N/M passed` целиком и код возврата; зелёный.

2. **Именованные случаи видны и покрывают популяцию пофайлово:**
   `python tests/test_doctrine_integrity.py | grep "schema citation:"` —
   приложить вывод. В нём обязана быть строка про `agents/dcs-commander.md`,
   по строке на каждый файл популяции, а также случаи страховки от
   вырождения и случай отрицания.

3. **Критерий 2, отсутствие вписанных заголовков** (перечень титулов берётся
   из `schemas.md`, а не набирается руками):

```bash
python - <<'PY'
import re, pathlib
schemas = pathlib.Path("dcs/references/schemas.md").read_text(encoding="utf-8")
test = pathlib.Path("tests/test_doctrine_integrity.py").read_text(encoding="utf-8").lower()
titles = [re.split(r"\s*[(\u2014]", t.strip())[0].strip().lower()
          for _, t in re.findall(r"^##\s+(\d+)\.\s+(.+)$", schemas, re.M)]
print("titles found in test body:", [t for t in titles if t in test])
PY
```

   Ожидается пустой список.

4. **Критерий 5, подделанное дерево.** Копия строится из **рабочего** дерева
   (не `git archive HEAD`: в `HEAD` нет ни якорей, ни этой проверки) и лежит
   **вне репозитория**, иначе проверки 8, 9 и 10 начнут сканировать копию:

```bash
SCRATCH="$LOCALAPPDATA/Temp/claude/schema-citation-forge"; rm -rf "$SCRATCH"; mkdir -p "$SCRATCH"
SCRATCH="$SCRATCH" python - <<'PY'
import os, pathlib, shutil
dst = pathlib.Path(os.environ["SCRATCH"]) / "tree"
shutil.copytree(pathlib.Path(".").resolve(), dst,
                ignore=shutil.ignore_patterns(".git", "node_modules", "__pycache__", ".dcs", "vault"))
print("copied to", dst)
PY
```

   В копии удалить раздел `## 3.` из `dcs/references/schemas.md` и
   перенумеровать `## 4.`–`## 8.` в `## 3.`–`## 7.`, затем прогнать
   `python tests/test_doctrine_integrity.py` с рабочим каталогом = копия.
   Приложить полный вывод: он обязан быть **КРАСНЫМ**, и в списке FAIL
   обязаны стоять именно случаи новой проверки.

5. **Вторая, грубая подделка** (шаг 6 пути воспроизведения из 201): в свежей
   копии того же вида заменить `#2` на `#42` в `dcs/workflows/plan.md` и
   прогнать проверку в копии — приложить вывод; красный, падает именованный
   случай по `plan.md`.

6. Копии удалить (`rm -rf "$SCRATCH"`) и приложить `git status --porcelain`
   из worktree как подтверждение, что ни `C:\DCS`, ни worktree не тронуты.

7. `python tests/test_dcs_gate.py` и `python tests/test_dcs_intake.py` —
   приложить напечатанные `N/M passed` обоих; зелёные (доказательство, что
   новая проверка не задела соседние наборы).

8. **Границы территории:** `git status --porcelain` — единственный
   изменённый этим заданием файл `tests/test_doctrine_integrity.py` (плюс уже
   принятые файлы S1, если они ещё не закоммичены).

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC
will re-enter planning around your finding.
