<!--
204-TASKING/S1.md -- one file per specialist, transcribed verbatim by
the IC from the Planning Chief's `taskings[]` array (schemas.md #2, chief
plan). The
specialist receives exactly this file's content plus the relevant IAP
excerpt -- nothing else. If the tasking is ambiguous, that's a planning
defect to fix during /dcs-plan, not something for the specialist to
resolve by guessing.
-->

# 204 — Tasking S1

**Incident:** 2026-07-30-hot-path-budget-emergency-trim
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Execute the hot-path cut registry on dcs/references/doctrine.md (primary, 7 positions) and dcs/references/schemas.md (secondary, 2 positions), appending divested provenance to dcs/references/doctrine-appendix.md, then update vault/Metrics/incident-metrics.md with the new hot-path size row. Satisfies 202 acceptance criteria 1 (combined size ≤36,864 B) and 6 (vault metrics updated).

---

## Cut Registry — doctrine.md (primary, 7 positions)

Each position: measured 'before', expected 'after' (forecast, not contract — deviation only if the specialist cannot hit the described transformation, not on byte disagreement), KEEP constraint, and disposition (compress-in-place / move-to-appendix / reference-primary-source).

| id | what | before (B) | after (B, est.) | delta | disposition |
|---|---|---|---|---|---|
| **D1** | 'A command point is never a silent wait' sub-section (lines 33-36): compress the two bullet points. KEEP: the header line '### A command point is never a silent wait', the announcement rule (bullet 1 core), the 'empty return = dead spawn' rule (bullet 2 core). Compress: the liveness-proxy enumeration ('not transcript size, not file mtimes, not silence, all harness artifacts'), the explicit instruction 'Ask the session...; never infer it from the filesystem' — these are implementation detail, not doctrine. Move the full original text to appendix. | 875 | ~450 | **~-425** | compress-in-place; full original prose → appendix |
| **D2** | Principle 13 (line 65): implementation-detail migration to appendix. KEEP: the core trigger enumeration (a) scope, (b) two-halt rule with convergence-read obligation (v0.5.9), (c) attempt counting (v0.5.12), (d) Delegation bound, (f) third-reject rule (v0.5.1), and the closing sentence 'Continue / pivot / demobilize is always the Owner's decision, never the IC's alone.' Move to appendix: the v0.6.9 per-attempt-ceiling mechanics — sentinel grammar definition, 'dcs_gate.py is the counter', 'Exactly two sentinels reset the tally' enumeration, the ceiling-promotion rule, the anchor-only-while clause, the sentinel-quoting caveat. These are implementation details of dcs_gate.py, not doctrine rules — the principle's obligation (halt twice = escalate) survives intact. Replace the moved text with a one-sentence reference: 'The per-attempt ceiling and sentinel-anchoring mechanics live in dcs_gate.py's halt_cycles() and GRAMMAR_LINE; the full text is in the appendix.' | 2418 | ~1550 | **~-868** | move-to-appendix (v0.6.9 mechanics only); core triggers stay |
| **D3** | v0.1 constraints — superseded bullet (line 106, 'One incident active at a time *(superseded by v0.3)*'). KEEP: the current-state summary (one incident per worktree, per-worktree ACTIVE file, territory partition governs parallelism). Move to appendix: the historical evolution narrative ('superseded by v0.3... the no-two-incidents-anywhere constraint moves to the register'). | 478 | ~200 | **~-278** | historical narrative → appendix; rule summary stays |
| **D4** | Parallel operation — worktree audit step 5 (line 151, 'Removal refused → diagnose before escalating'). KEEP: the core rule (diagnose before escalating, cd to esg_root to avoid cwd self-conflict), the .dcs/CLOSED zombie-rule fallback instruction. Move to appendix: the platform-specific diagnostic commands (POSIX lsof/fuser, Windows Get-Process/Get-Process with path filter/Sysinternals handle). The appendix entry cites the incident that produced this rule (worktree-removal-self-conflict). | 817 | ~470 | **~-347** | platform diagnostic commands → appendix; core rule stays |
| **D5** | Principle 15 (line 67) — tighten sub-clause prose. KEEP: all three sub-clauses (v0.5.2 derived facts, v0.5.4 tests and immutable evidence, v0.6.11 transfer between seats) and their operative rules. Compress: the v0.5.4 sub-clause's detailed examples ('a fixture, a frozen blob, a commit SHA — never a moving ref (a branch name, HEAD, "the integration tip")') can be shortened while keeping the rule's substance. | 1455 | ~1260 | **~-195** | compress-in-place; examples stay but shorter |
| **D6** | Automation layers — three hard rules (lines 127-131). KEEP: the three numbered rules. Compress: the explanatory prose after each rule ('.dcs/esg/REGISTER.md's QUEUED items', 'deploys batch for the Owner', 'write pause state to disk'). The substance of each rule is the first sentence; the second-sentence explanations can be shortened or merged. | 622 | ~420 | **~-202** | compress-in-place; rule text stays, explanations shortened |
| **D7** | Model availability paragraph (line 31). KEEP: the core rule ('Fable = strongest tier available'), the try-preferred-tier-first rule, the 'Never cache the fallback' rule, the 'separate spawn preserves a fresh context' justification. Compress: the detailed tier-fallback enumeration ('opus, then sonnet') — already stated in the same paragraph — and the log-format example can be shortened. | 698 | ~540 | **~-158** | compress-in-place |

**Primary registry subtotal:** ~2,473 B estimated reduction (target: 1,555 B). The estimate has ~30% uncertainty — the 'after' column is a forecast (prior art lesson: 'After is a forecast, not a contract'), so the primary overtargets deliberately.

---

## Cut Registry — schemas.md (secondary, 2 positions)

| id | what | before (B) | after (B, est.) | delta | disposition |
|---|---|---|---|---|---|
| **S1** | Section 5 prose paragraph (lines 97-100, 'pass with advisories is the normal healthy verdict'). Already partially trimmed (R3 in schemas-md-trim). KEEP: the normative rule (advisories fixed by IC, never block merge; only halt refutations do). Compress: the bar-definition sentence ('Bar for what counts as a refutation instead of an advisory: agents/dcs-safety-officer.md step 6') can be one clause, not a full sentence — the charter is the primary source. | ~130 | ~70 | **~-60** | reference-primary-source (charter already cited) |
| **S2** | Section 2 — tighten verbose Notes cells in the Chief plan and Tasking tables (lines 33, 36, 37, 48). KEEP: every field name and every Type. Tighten: Notes cells that are multi-sentence explanations where one sentence suffices (e.g. partition_ok's three-clause note, evidence_required's full-sentence instruction). The Field/Type columns are immutable — only the Notes column prose compresses. | ~450 | ~290 | **~-160** | compress-in-place; Field+Type immutable |

**Secondary registry subtotal:** ~220 B.

---

## Reserve (pre-authorised, taken ONLY if primary+secondary falls short of 36,864 B, in strict order)

| id | what | delta (est.) | note |
|---|---|---|---|
| **V1** | doctrine.md — Principle 4 sub-clause (line 55): move v0.6.13 'below the bar' routing detail and STRATEGY-item clause to appendix. Core rule (typing sets ceremony, one defect per incident) stays. | ~-200 | |
| **V2** | doctrine.md — Principle 6 sub-clause (line 57): compress v0.3 and v0.6.2 sub-clauses about portfolio-wide territory and project scoping. | ~-150 | |
| **V3** | doctrine.md — Lifecycle one-period prose (line 102): compress the v0.5.12 'exception, not the shape' paragraph. Core lifecycle stays. | ~-150 | |
| **V4** | schemas.md — Section 1 (Situation-analyst): tighten the Notes column in the Field table (lines 17-21). | ~-100 | |

---

## Execution rules

1. **Measure before you touch.** Run the combined-size command (same as the REGISTER.md regeneration command), save the output.
2. **Execute positions D1-D7 and S1-S2 in order.** After each position: verify the text matches the KEEP constraint before proceeding.
3. **After all primary+secondary positions, measure the combined size.** If ≤36,864 B: stop, report. If >36,864 B: apply V1-V4 in strict order, re-measuring after each until the target is reached.
4. **If even V4 exhausted and still >36,864 B:** that is a deviation — return deviation shape with `found` (final measured size), `why_plan_wrong` (which positions under-delivered vs. estimate), `proposal` (which additional positions to consider — but do NOT execute un-registered cuts).
5. **Append provenance to doctrine-appendix.md at the END of the file.** Each entry gets its own `###` header under the matching core section name (e.g., `### A command point is never a silent wait — full text`, `### Principle 13 — v0.6.9 counting mechanics`, etc.). The entry cites the incident slug (`hot-path-budget-emergency-trim`) and states this is the original prose moved to keep the hot path below budget — the core still carries the rule, just compressed.
6. **Do not renumber any sections.** Not in doctrine.md, not in schemas.md. The section numbering in schemas.md (`## 1.` through `## 8.`) is load-bearing for 19 live citations.
7. **Write only with Edit tool.** Never Write (full-file rewrite), never Set-Content/Out-File (BOM hazard). The diff must show targeted edits, not a full-file replacement.
8. **English only in dcs/ and tests/.** No Cyrillic characters in shipped files (doctrine.md, schemas.md, doctrine-appendix.md) — check 9 of the guard will redden.

## File territory (may edit only within these globs)

- dcs/references/doctrine.md
- dcs/references/schemas.md
- dcs/references/doctrine-appendix.md
- vault/Metrics/incident-metrics.md

## Forbidden zones (explicitly, even if it seems related)

- dcs/workflows/**
- dcs/templates/**
- dcs/hooks/**
- dcs/**
- agents/**
- skills/**
- tests/**
- bin/**
- install.ps1
- install.sh
- package.json
- dcs/VERSION
- .dcs/**
- CLAUDE.md
- docs/**
- vault/Post-mortems/**
- vault/Decisions/**
- vault/Meta/**
- vault/Backlog.md
- vault/00-Navigation.md
- vault/_scripts/**

## Worktree root

C:\DCS-wt\hot-path-budget-emergency-trim

## Evidence required in the return

- Combined normalized size after all cuts: `python -c "import pathlib; d=pathlib.Path('dcs/references/doctrine.md').read_bytes().replace(b'\r\n',b'\n'); s=pathlib.Path('dcs/references/schemas.md').read_bytes().replace(b'\r\n',b'\n'); print(f'doctrine: {len(d)}, schemas: {len(s)}, total: {len(d)+len(s)}, target: {36*1024}, headroom: {36*1024 - (len(d)+len(s))}')"` — full output. Third number must be ≤36864, fourth ≥0.
- `git diff --stat` for doctrine.md and schemas.md — must show targeted edits, not full-file rewrites.
- `git diff dcs/references/doctrine.md` — full diff, with context.
- `git diff dcs/references/schemas.md` — full diff, with context.
- `git diff dcs/references/doctrine-appendix.md` — full diff, showing appended provenance entries at end of file only.
- Section numbering preserved: `python -c "import pathlib,re; print(re.findall(r'^## (\d+)\.', pathlib.Path('dcs/references/schemas.md').read_bytes().replace(b'\r\n',b'\n').decode(), re.M))"` — must return ['1','2','3','4','5','6','7','8'].
- JSON blocks parseable: extract all fenced ```json blocks from schemas.md, run each through `python -c 'import json,sys; json.load(sys.stdin)'` — all must parse without exception. Name which blocks were modified.
- Registry execution table: for each position D1-D7, S1-S2, and any reserve positions taken: (a) whether executed, (b) measured before bytes, (c) measured after bytes, (d) actual delta, (e) confirmation that KEEP constraint was met. For positions moved to appendix: name the exact appendix heading and line range where the text landed.
- vault/Metrics/incident-metrics.md: git diff showing the new hot-path size row added.
- Two suites green: `python tests/test_dcs_gate.py` (read own N/M), `python tests/test_dcs_intake.py` (read own N/M).
- Explicit statement: which reserve positions were taken (if any), in what order, and why the primary+secondary fell short.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a
`proposal` (a recommendation, not an action). The IC will re-enter
planning around your finding.
