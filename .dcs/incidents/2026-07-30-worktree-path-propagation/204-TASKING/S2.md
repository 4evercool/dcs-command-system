# 204 — Tasking S2

**Incident:** worktree-path-propagation
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Wire `worktree_root` into `dcs/workflows/execute.md` step 4 per 202 acceptance
criterion 2. Before the fan-out prose, add a compact paragraph that:
(a) instructs computing `worktree_root` from `git worktree list --porcelain`, and
(b) states it must be passed to each specialist.

The addition must fit within 5 lines total so execute.md stays at or under the
450-line grandfathered ceiling (currently 445). Place it directly before or within
the existing step-4 fan-out instructions so the path is available at spawn time.

## File territory (may edit only within these globs)

- dcs/workflows/execute.md

## Forbidden zones (explicitly, even if it seems related)

- agents/**
- dcs/templates/**
- dcs/references/**
- tests/**

## Evidence required in the return

- `grep -n 'worktree_root' C:/dcs/dcs/workflows/execute.md` — cite every matching line with its line number
- `grep -n 'git worktree list' C:/dcs/dcs/workflows/execute.md` — confirm the `--porcelain` computation command is referenced; cite the matching line
- `python -c "raw = open('C:/dcs/dcs/workflows/execute.md', 'rb').read().replace(b'\r\n', b'\n').replace(b'\r', b'\n'); n = raw.count(b'\n'); lines = n + 1 if raw and not raw.endswith(b'\n') else n; print(f'lines={lines} ceiling=450 ok={lines <= 450}')"` — confirm line count is ≤ 450
- `python C:/dcs/tests/test_doctrine_integrity.py 2>&1 | Select-String -Pattern 'workflow budget|passed|FAIL'` — confirm the workflow line-count budget check passes

## Worktree root

C:\DCS-wt\worktree-path-propagation

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4, with `found`, `why_plan_wrong`, and a
`proposal` (a recommendation, not an action).
