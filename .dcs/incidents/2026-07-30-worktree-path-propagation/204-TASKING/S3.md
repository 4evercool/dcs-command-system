# 204 — Tasking S3

**Incident:** worktree-path-propagation
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Add `worktree_root` field to `dcs/templates/204-TASKING.md` per 202 acceptance
criterion 3. Add a new section `## Worktree root` with the placeholder
`{{worktree_root}}` so the execute workflow has a defined, structurally coherent
field to write the computed path into. Place it after the existing metadata
sections (Task, File territory, Forbidden zones) and before Evidence required —
consistent with the template's existing structure.

## File territory (may edit only within these globs)

- dcs/templates/204-TASKING.md

## Forbidden zones (explicitly, even if it seems related)

- agents/**
- dcs/workflows/**
- dcs/references/**
- tests/**

## Evidence required in the return

- `grep -n 'worktree_root' C:/dcs/dcs/templates/204-TASKING.md` — cite the matching line with its line number
- Read the full modified template — confirm the `worktree_root` field is structurally coherent (has its own heading, uses the `{{placeholder}}` convention, and is not a dangling orphan); cite the line range
- `python C:/dcs/tests/test_doctrine_integrity.py 2>&1 | Select-String -Pattern 'passed|FAIL'` — confirm no new failures

## Worktree root

C:\DCS-wt\worktree-path-propagation

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4, with `found`, `why_plan_wrong`, and a
`proposal` (a recommendation, not an action).
