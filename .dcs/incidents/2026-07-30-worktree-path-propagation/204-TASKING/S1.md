# 204 — Tasking S1

**Incident:** worktree-path-propagation
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Add `worktree_root` to `agents/dcs-ops-specialist.md` per 202 acceptance criterion 1:
(a) In the `<inputs>` block, add `worktree_root` as an optional absolute path to
the incident worktree — alongside the existing three inputs (204-TASKING content,
IAP excerpt, project root + CLAUDE.md).
(b) In `<process>` step 3 ("Edit only inside your file territory"), add a sentence:
territory and forbidden globs resolve relative to `worktree_root` when it is
provided; when absent or empty, resolve relative to the project root.

## File territory (may edit only within these globs)

- agents/dcs-ops-specialist.md

## Forbidden zones (explicitly, even if it seems related)

- dcs/workflows/**
- dcs/templates/**
- dcs/references/**
- tests/**

## Evidence required in the return

- `grep -n 'worktree_root' C:/dcs/agents/dcs-ops-specialist.md` — cite every matching line with its line number
- Read lines covering `<inputs>` through `</inputs>` — confirm `worktree_root` appears as an input with its 'optional' qualifier; cite the decisive line range
- Read lines covering `<process>` step 3 — confirm the territory-resolution rule explicitly mentions `worktree_root`; cite the line number
- `python C:/dcs/tests/test_doctrine_integrity.py 2>&1 | Select-String -Pattern 'passed|FAIL'` — confirm no new failures

## Worktree root

C:\DCS-wt\worktree-path-propagation

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4, with `found`, `why_plan_wrong`, and a
`proposal` (a recommendation, not an action).
