# 204 — Tasking S3

**Incident:** field-lesson-citations
**Period:** 1
**Specialist:** S3
**Territory:** `dcs/templates/202-OBJECTIVES.md`, `dcs/templates/REGISTER.md`
**Forbidden:** `dcs/references/**`, `dcs/workflows/**`, `tests/**`

## Task

Fix all field lesson citations in the two template files so every mention carries an incident identifier (criteria 4, 6).

Target locations (from baseline census):
- `dcs/templates/202-OBJECTIVES.md`: line ~33-34 ("field lesson, 2026-07-22") and line ~58 ("Field lesson 2026-07-26")
- `dcs/templates/REGISTER.md`: line ~77 ("Field lesson 2026-07-23")

### For each mention

1. **202-OBJECTIVES.md line 33-34 (2026-07-22):** Pre-self-hosting. Add version if identifiable from context, otherwise "(predates self-hosting)".

2. **202-OBJECTIVES.md line 58 (2026-07-26):** POST-self-hosting — this one needs a real incident slug. Search `.dcs/incidents/` for incidents opened on or around 2026-07-26 whose 201 or AAR describes the scenario: a criterion waived a version bump because "0.6.9 is unpublished," but it had been published 75 minutes earlier. The incident is likely `criterion-unmeasured-fact` or a nearby one. Use `ls -d .dcs/incidents/2026-07-2*` to list candidates and `grep -l "0.6.9\|unpublished\|version bump" .dcs/incidents/*/201-BRIEF.md` to find the match. If found, add the slug. **Important:** do NOT use "(predates self-hosting)" for a post-2026-07-25 date — that would be factually wrong. If the incident is genuinely unfindable, flag this as a deviation.

3. **REGISTER.md line 77 (2026-07-23):** Pre-self-hosting. Add version if identifiable, otherwise "(predates self-hosting)".

### Verify

For each file:
```bash
grep -ni "[Ff]ield[- ]lesson" dcs/templates/<file>.md | grep -viE "incident \x60[\w-]+\x60|v[0-9]+\.[0-9]+\.[0-9]+|predates self-hosting"
```
Must return zero lines per file.

## Evidence required

- For each template file: confirming grep showing zero unidentified field lessons
- Per changed line: what identifier was added
- For the 2026-07-26 lesson: the incident slug found, the `ls` and `grep` commands used to identify it, and their output
- If the 2026-07-26 incident cannot be identified: deviation report instead of a fix
