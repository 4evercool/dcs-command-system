<!--
204-TASKING/S1.md -- one file per specialist, transcribed verbatim by
the IC from the Planning Chief's `taskings[]` array.
-->

# 204 — Tasking S1

**Incident:** halt-enumeration-grammar-drift
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Fix the halt-counting regex in vault/_scripts/incident_metrics.py and extract an importable count_halts() function. Satisfies 202 criteria 1, 2, 3, and 5.

## File territory (may edit only within these globs)

- vault/_scripts/incident_metrics.py

## Forbidden zones (explicitly, even if it seems related)

- vault/Metrics/**
- dcs/**
- .dcs/**
- tests/**

## Worktree root

C:/DCS-wt/halt-enumeration-grammar-drift

## Evidence required in the return

- Show the new regex compiles and finds both grammar forms on real incident logs: `python -c "from vault._scripts.incident_metrics import count_halts; import io; log=io.open('.dcs/incidents/2026-07-26-schema-citation-guard/214-LOG.md',encoding='utf-8').read(); print('schema-citation-guard halts:', count_halts(log))"` — expected output >= 1, proving the under-count-to-zero defect is fixed.
- Show the over-count from unanchored matching is fixed on a pre-v0.6.9 incident: `python -c "from vault._scripts.incident_metrics import count_halts; import io; log=io.open('.dcs/incidents/2026-07-25-halt-loop-unbounded/214-LOG.md',encoding='utf-8').read(); print('halt-loop-unbounded halts:', count_halts(log))"` — expected output 2 (the two SAFETY: halt sentinels at lines 39 and 64), not 3 (which the unanchored regex reports by also matching the narrative mention at line 66).
- Show count_halts() is importable and callable: `python -c "from vault._scripts.incident_metrics import count_halts; print('importable:', callable(count_halts))"` — must print True.
- Show the CLI path still works end-to-end: `python vault/_scripts/incident_metrics.py C:\dcs` — confirm the halt column for schema-citation-guard is >= 1 and halt-loop-unbounded is 2. Report the decisive lines of output, not the full table.
- Show the regex pattern used is anchored and spans both forms: `grep -n 'HALT_RE\|count_halts' vault/_scripts/incident_metrics.py` — cite the line(s) showing the compiled regex and function definition.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
