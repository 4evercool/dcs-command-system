# 204 — Tasking S1

**Incident:** hot-path-budget-eol-sensitivity
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)
**Wave:** 1 — you run **alone**, before the IC's re-materialisation step

## Task

Create `.gitattributes` at the worktree root. Satisfies 202 criterion 1 and
enables criteria 2, 6 and 7.

**Exact content — one line, then a single newline, nothing else:**

```
* text=auto eol=lf
```

No `.dcs/` exception (the 202 rejects that shape, and the IC ruled it *wrong*
rather than merely worse: 8 of main's 15 stale files are `.dcs/incidents/…`, so
excluding them makes criteria 2 and 6 unreachable by construction). No binary
overrides — the Planning Chief measured that `text=auto` content detection
already leaves `vault/.obsidian/app.json` and `appearance.json` as
`i/none w/none`. No comment header.

**Write it with the Write tool** so it lands as LF with no BOM.

## What you must NOT do

**Do not run any command that re-materialises the working tree.**
`git add --renormalize`, `git rm --cached`, `git reset --hard`,
`git checkout -- .` are all **forbidden to you**. That is a single IC step which
runs after your return, and running it yourself would destroy tree state you
cannot see.

**Your tasking will look like it did nothing, and that is the correct result.**
Creating the attributes file changes zero bytes on disk — the Logistics Chief
measured this in a faithful clone. Report that plainly as the finding it is; it
is the evidence that justifies the IC step, not a failure of your work.

## File territory (may edit only within these globs)

- `.gitattributes`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/**`, `tests/**`, `.claude/**`, `agents/**`, `skills/**`, `bin/**`
- `install.ps1`, `install.sh`, `package.json`, `.gitignore`
- `.dcs/**`, `vault/**`, `docs/**`, `CLAUDE.md`, `README.md`

## Evidence required in the return

- `python -c "print(repr(open('.gitattributes','rb').read()))"` — must print
  exactly `b'* text=auto eol=lf\n'` (proves LF, no BOM, no CRLF, no extra lines)
- `git check-attr text eol -- dcs/references/doctrine.md dcs/hooks/dcs_gate.py .dcs/incidents/2026-07-25-doctrine-hot-path-trim/IAP.md`
  — all six lines must read `text: auto` / `eol: lf`, none `unspecified`
  (202 criterion 1, verbatim command)
- `git check-attr text -- vault/.obsidian/app.json vault/.obsidian/appearance.json`
  then `git ls-files --eol | grep 'i/none'` — confirm both are still
  binary-detected and were **not** forced to text
- `git ls-files --eol | awk '{print $1, $2}' | sort | uniq -c | sort -rn` — run
  **before** creating the file and **after**, paste both. **Expected and
  correct: unchanged at `83 i/lf w/crlf` + `2 i/none w/none`.** State in your
  return that this is expected and that clearing `w/crlf` requires the IC step.
- `git status --porcelain` — must show only `?? .gitattributes` plus the
  untracked incident directory. Anything else means you left the tree dirty and
  the IC's re-materialisation would destroy it.

## On discovering the plan doesn't fit reality

STOP. Return `status: "deviation"` per `references/schemas.md` #4 with `found`,
`why_plan_wrong`, and a `proposal`. Do not improvise.
