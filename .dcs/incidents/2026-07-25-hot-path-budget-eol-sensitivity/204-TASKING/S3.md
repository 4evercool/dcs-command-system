# 204 — Tasking S3

**Incident:** hot-path-budget-eol-sensitivity
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)
**Wave:** 2 — runs after the IC's re-materialisation, in parallel with S2 and S4

## Task

Make the hot-path measurement tree-independent (202 criterion 3a) and add the
enforcement of the policy **that actually ships** — without moving the budget
number (202 criterion 9).

### (1) Normalise the measurement

Replace the two `os.path.getsize` calls at
`tests/test_doctrine_integrity.py:163-164` with a normalised byte count: read the
file's bytes, `.replace(b'\r\n', b'\n')`, take `len`.

**Pure stdlib, no git dependency.** `tests/` ships in `package.json`'s `files`
whitelist and must run in a tree with no repository at all.

Expected result in this tree after the IC's re-materialisation:
`21966 + 15613 = 37579`.

### (2) Fix the comment block

Update the comment above `HOT_PATH_BUDGET_KB` to describe the new basis and carry
a regenerate command that matches **what the check now computes** (principle 15:
a derived number ships with the command that reproduces it).

Its current sentence — *"Sizes are on-disk bytes, so a CRLF checkout measures one
byte per line more than an LF one — see vault/Backlog.md item 8"* — **is now
false and must be replaced.**

**`HOT_PATH_BUDGET_KB` stays 38.** The value is register rank 2's defect
(`schemas-md-trim`) and moving it is a deviation. The normalised measure is
37,579 B against a 38,912 B ceiling, so the check has margin either way.

### (3) Add ONE NEW check — do not widen check 8

**No file in the shipped set contains `b'\r\n'`.**

Scope it with the **same** `SHIPPED_DIRS` / `SHIPPED_FILES` / `TEXT_SUFFIXES`
lists check 9 already uses, so `vault/` and `.dcs/` are out of its reach. Add
`.gitattributes` to **neither** list — it does not ship, and asserting its
presence would go red in an npm-installed tree.

Mention in the check's comment that the policy itself lives in `.gitattributes`
and that **this check is the half of it that reaches a user** — `.gitattributes`
is absent from `package.json`'s `files` whitelist and npm performs no git
checkout, so the attributes file protects a clone of this repo and nothing
downstream.

This follows `0428ac4`'s precedent verbatim: **a new check, not a broadened old
one.** That commit's own lesson was that a guard checks the failure it was born
from, so ask what neighbouring corruption it would miss.

### (4) Update the module docstring

Its numbered list at `:13-22` must include the new check.

### (5) Prove the new check can see a violation

**A check nobody has watched fail is not evidence.** Build a temp directory
**outside the worktree** mirroring the shipped layout, put one CRLF-ified copy of
a `.md` file in it, run the same scan predicate over it, paste the hit, and
delete the directory. **Do not create a CRLF file anywhere inside the worktree.**

## File territory (may edit only within these globs)

- `tests/test_doctrine_integrity.py`

## Forbidden zones (explicitly, even if it seems related)

- `tests/test_dcs_gate.py`, `tests/test_dcs_intake.py` — S2's
- `dcs/hooks/**`, `.claude/**` — S2's
- `.gitattributes` — S1's
- `dcs/workflows/**`, `dcs/references/**` — S4's
- `dcs/templates/**`, `dcs/VERSION`
- `agents/**`, `skills/**`, `bin/**`, `install.ps1`, `install.sh`, `package.json`
- `.dcs/**`, `vault/**`, `docs/**`

## Evidence required in the return

- `python -c "import os; d=os.path.getsize('dcs/references/doctrine.md'); s=os.path.getsize('dcs/references/schemas.md'); print('raw', d, s, d+s)"`
  **and** the same pair computed the normalised way — paste both, showing the
  normalised sum is `37579` in this tree. *(202 criterion 3a; the raw numbers are
  the tree-dependent ones the fix removes.)*
- `python tests/test_doctrine_integrity.py` — full green run, 15/15 (14 existing
  + your new check). Paste the whole output. *(202 criteria 3a, 8, 9)*
- The **non-destructive demonstration** from step 5: the temp-directory scan
  showing your new check detects a CRLF violation, then its deletion
- `git diff -- tests/test_doctrine_integrity.py | grep -E "^[-+].*HOT_PATH_BUDGET_KB"`
  — must return **nothing** *(202 criterion 9 — the value is unchanged)*
- `python tests/test_dcs_gate.py && python tests/test_dcs_intake.py` — both green
  *(202 criterion 8)*
- `git diff --stat` — must list exactly `tests/test_doctrine_integrity.py` and
  nothing else

## On discovering the plan doesn't fit reality

STOP. Return `status: "deviation"` with `found`, `why_plan_wrong`, and a
`proposal`. In particular: if the normalised measurement does **not** come to
37,579 in this tree, do not adjust the budget to accommodate it — that number is
out of your territory and the discrepancy is a deviation.
