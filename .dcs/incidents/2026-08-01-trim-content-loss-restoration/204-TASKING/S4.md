# 204 — Tasking S4 (revised after IAP-review reject)

**Incident:** trim-content-loss-restoration
**Period:** 1
**Specialist:** dcs-ops-specialist (S4)

## Task

Restore `doctrine.md` hard rule 3's dropped clause (202 criterion 8) and write the three deleted field-lesson stories into a new section of `doctrine-appendix.md` (criteria 5, 6, 7). Two files.

**PART 1 — `dcs/references/doctrine.md`, criterion 8.** Line 131 currently reads:
`3. **At any Owner gate the Delegation does not cover:** write pause state to disk (resumable via \`/dcs-status\`), end the loop turn — never busy-wait, never self-approve out of bounds.`

Replace with:
`3. **At any Owner gate the Delegation does not cover:** notify if a tool is available, write pause state to disk (resumable via \`/dcs-status\`), end the loop turn — never busy-wait, never self-approve out of bounds.`

Source: `git show e3d4bcc^:dcs/references/doctrine.md`. That is +33 bytes on a hot path with 433 B free; it is the ONLY change permitted in `doctrine.md`. Do not touch `dcs/workflows/loop.md` (criterion 11) — the equivalent instruction already exists there independently since `a3fb60b` and there is no defect to fix.

**PART 2 — `dcs/references/doctrine-appendix.md`, criteria 5/6/7.** Append at the END of the file (one blank line after the current last line, which is D4's blockquote) the section dictated below. Every line of it is dictated because `doctrine-appendix.md` is in merge-guard check 20's `_FL_FILES` population (`tests/test_doctrine_integrity.py:1734-1775`) and the obvious phrasing fails it. Write the text EXACTLY as given, including the "> — predates self-hosting; regenerate with ..." attribution line under each blockquote.

**Why each line is shaped this way** — read this before writing, and do not "improve" the wording: check 20's `_FL_LINE_RE` (`[Ff]ield lesson.*\d{4}-\d{2}-\d{2}`, re.I) flags every line where a YYYY-MM-DD date follows the words "field lesson" ON THAT SAME LINE; each flagged line must then match `_FL_ID_RE` (`incident \`<slug>\`` | vX.Y.Z | `predates self-hosting`) either on itself or on the IMMEDIATELY following line — a match two lines later does not count. The three `###` headings and each of the three verbatim blockquotes are flagged lines. The headings carry the identifier inline; the blockquotes cannot be altered (they are verbatim restorations), so each is followed immediately by an attribution line carrying it. All three lessons date before the 2026-07-25 self-hosting boundary and no version is recorded for them, so `predates self-hosting` is the correct identifier per this file's own citation convention at lines 13-26 — do NOT substitute an incident slug: the slug form names the ORIGINATING incident, and `workflow-file-trim-grandfathered` is the incident that deleted these lessons, not the one that produced them.

Write exactly this:

`## Workflow field lessons (restored, incident \`trim-content-loss-restoration\`)`

(blank line)

PREAMBLE — write it as ONE unwrapped line, mirroring the D1-D4 prose style at the foot of this file. It must state, in this order and in one line: that incident `workflow-file-trim-grandfathered` (commit `bca0b56`, 2026-07-31) trimmed four workflow files to the 250-line ceiling and deleted the field lessons below outright instead of routing them here, which is where CLAUDE.md's "Where lessons go" convention sends the provenance of a rule; that the rules themselves stayed in the workflows and only the stories were lost; that each entry quotes the deleted prose verbatim and regenerates with `git show bca0b56^:dcs/workflows/<file>`; that all three lessons predate self-hosting with no recorded version, so each carries the `predates self-hosting` identifier per the citation convention above; and — this clause is mandatory, principle 15 — that the figures inside the quotations are historical counts from the incidents that produced them, not numbers this file recomputes, to be read as of that incident's own close, because it moves. Keeping it on one line is what makes it check-20-safe: the single line carries both `incident \`workflow-file-trim-grandfathered\`` and `predates self-hosting`, so whatever dates it contains, `_FL_ID_RE` matches.

(blank line)

`### W1: plan.md step 4a preamble — the tasking lint is not a command point (field lesson 2026-07-23, predates self-hosting)`

(blank line)

`> Field lesson 2026-07-23: an entire IAP review cycle (the IC's second REJECT on one incident) was consumed by defects in this list, and the IC's own verdict was that both were *Dispatcher transcription errors, not the chief's*.`
`> — predates self-hosting; regenerate with \`git show bca0b56^:dcs/workflows/plan.md\`, old lines 139-142.`

(blank line)

`### W2: plan.md lint check 3a — a census is not an enumeration (field lesson 2026-07-24, predates self-hosting)`

(blank line)

`> Field lesson 2026-07-24: an incident took **four Safety halts** on one objective, each closing the named instances and revealing another of the same class, because the criterion enumerated sites instead of asserting an invariant.`
`> — predates self-hosting; regenerate with \`git show bca0b56^:dcs/workflows/plan.md\`, old lines 170-173.`

(blank line)

`### W3: execute.md step 9b — a fix that is not shipped fixes nothing (field lesson 2026-07-24, predates self-hosting)`

(blank line)

`> Field lesson 2026-07-24, in the incident's own AAR: period 1 produced a Safety-passed fix for a bug that was actively corrupting production data, and *"that fix then sat in a branch. A fix that is not shipped fixes nothing, and the defect kept corrupting production data the whole time"* — a second period existed only to make the first one shippable.`
`> — predates self-hosting; regenerate with \`git show bca0b56^:dcs/workflows/execute.md\`, old lines 430-435.`

(blank line)

CLOSING LINE — one line, naming where each compressed rule still lives in the core: W1 at `plan.md` step 4a's preamble, W2 at `plan.md` lint check 3a, W3 at `execute.md` step 9b and in this appendix's "## The lifecycle (Planning P mapped to software)" section above (that section exists and does carry the close/merge/ship rule — the cross-reference is checked, not assumed). This line must contain NO YYYY-MM-DD date, which keeps it outside check 20 entirely.

Each blockquote's three verbatim quotations were re-verified against source at planning time: W1 = `git show bca0b56^:dcs/workflows/plan.md` old lines 139-142; W2 = same file, old lines 170-173; W3 = `git show bca0b56^:dcs/workflows/execute.md` old lines 430-435. Do not paraphrase a word of them.

**HARD CONSTRAINTS:** (i) `doctrine-appendix.md` is never `@`-included and does not count against the hot path — do not move any of this into `doctrine.md`. (ii) Do not introduce any bare `N of M` digit pattern inside double quotes anywhere in the appendix without a regenerating command in the same blank-line-delimited paragraph; the check `bar carrier (criterion 3): doctrine-appendix.md has no bare 'N of M' census inside quotation marks without a regenerating command in the same paragraph` enforces that. W2's "four Safety halts" is spelled out, not digits, and is safe as written — keep it that way; and each blockquote's attribution line supplies a `git show` command inside the same paragraph as the quote, which is the other half of that check's requirement, so do not separate a blockquote from its attribution line with a blank line. (iii) Do not add or restore any story beyond W1/W2/W3 — two further losses are confirmed real (the 2026-07-26 version-bump-waiver story and the 2026-07-24 IAP.md-edit-criterion story) but are explicitly out of this period's scope pending an Owner gate.

## File territory (may edit only within these globs)

- `dcs/references/doctrine.md`
- `dcs/references/doctrine-appendix.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/references/schemas.md`
- `dcs/references/forms.md`
- `dcs/workflows/**`
- `tests/**`
- `dcs/hooks/**`
- `dcs/templates/**`
- `agents/**`
- `skills/**`
- `.dcs/**`
- `CLAUDE.md`
- `vault/**`

## Worktree root

`C:\DCS-wt\trim-content-loss-restoration`

## Evidence required in the return

- `grep -n notify dcs/references/doctrine.md` — must return >= 1 match, inside the "Hard rules for unattended operation" block (criterion 8). Paste the full line.
- `python tests/test_doctrine_integrity.py` — quote explicitly, verbatim and stating it green, the result of the named case `field-lesson citations: every field lesson mention in shipped package carries an incident identifier (slug, version, or 'predates self-hosting')`. `doctrine-appendix.md` is in that check's `_FL_FILES` population and the new section is the single largest new source of flagged lines in this incident.
- `python tests/test_doctrine_integrity.py` — quote explicitly the result of the named case `bar carrier (criterion 3): doctrine-appendix.md has no bare 'N of M' census inside quotation marks without a regenerating command in the same paragraph`, stating it green.
- `grep -n -iE "field lesson.*[0-9]{4}-[0-9]{2}-[0-9]{2}" dcs/references/doctrine-appendix.md` — paste every hit that falls inside your new section, together with the line immediately following each, so the reviewer can confirm by eye that every flagged line carries an identifier within check 20's one-line window.
- `grep -rn "an entire IAP review cycle" dcs/references/doctrine-appendix.md` — >= 1 match (criterion 5).
- `grep -rn "four Safety halts" dcs/references/doctrine-appendix.md` — >= 1 match (criterion 6).
- `grep -rn "sat in a branch\|fixes nothing" dcs/references/doctrine-appendix.md` — >= 1 match (criterion 7).
- `grep -n 'Workflow field lessons' dcs/references/doctrine-appendix.md` — must return exactly 1 line; paste it verbatim so S1's and S2's pointer strings can be checked against it character-for-character.
- Hot-path measurement (criterion 14), the guard's own command from `tests/test_doctrine_integrity.py:197-198`: `python -c "d=open('dcs/references/doctrine.md','rb').read().replace(b'\r\n', b'\n'); s=open('dcs/references/schemas.md','rb').read().replace(b'\r\n', b'\n'); print(len(d), len(s), len(d)+len(s))"` — the third number must be <= 37888; expected ~37488 (HEAD is 37455). Use this, not `wc -c`: the guard normalises CRLF before counting and `wc -c` does not.
- `git diff -- dcs/references/doctrine.md` — paste the full diff; it must be a one-line change and must NOT touch `schemas.md`.
- `python tests/test_doctrine_integrity.py` — paste the final `N/M passed` line. Run it last; a failure naming a file outside your territory is reported, not fixed.
- `git diff --stat -- dcs/references/doctrine.md dcs/references/doctrine-appendix.md` — exactly two files changed.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
