# 204 — Tasking S2 (revised twice after IAP-review rejects — lines now dictated verbatim, no wrapping decisions left)

**Incident:** trim-content-loss-restoration
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Restore the `escalate_owner` handling instruction (202 criterion 2), narrow the over-broadened worktree-isolation clause (criterion 3), add the W3 pointer, and buy the required lines by a content-preserving reflow. `execute.md` is at 250/250 — zero headroom — so do EDIT A FIRST and confirm the line count drops before adding anything. All edits in `dcs/workflows/execute.md`.

**LINE NUMBERS BELOW ARE HEAD NUMBERS.** Edit A deletes 6 lines at lines 59-67, so after it every anchor later in the file shifts UP BY 6 (Edit B's 89-90 becomes 83-84; Edit C's 110-120 becomes 104-114; Edit D's 238-243 becomes 232-237). Anchor on the quoted text, never on the number.

**TRANSCRIPTION RULE, applies to every dictated line in this tasking:** the only non-ASCII character anywhere in them is U+2014 EM DASH (—). Every apostrophe is ASCII U+0027 ('), not a typographic one. Do not substitute characters, do not re-flow a dictated line to a different width, do not add or remove a trailing space. **The parenthesised per-line numbers below are CHARACTER counts. The `awk` evidence commands report BYTES.** On this host `awk`'s `length()` counts bytes (`LANG` unset), and U+2014 is 3 bytes / 1 character, so a dictated line containing it reads 2 higher in `awk` than its character count. This is expected, not a transcription failure.

**EDIT A (buy 6 lines, no content change)** — lines 59-67 are one paragraph hard-wrapped over 9 lines. Reflow it to exactly three lines, ONE SENTENCE PER LINE, changing not a single word, not a single character inside the quotation:

line 1 (136 chars): `Before spawning any specialist, check trigger (c): read \`esg.max_periods_before_review\` from \`<project>/.dcs/config.json\` (default \`3\`).`
line 2 (329 chars): the sentence beginning `Count ATTEMPTS: the number of \`IAP-APPROVED:\` sentinel entries in \`214-LOG.md\` (recognized per ...` through `... off column zero.").` — the whole sentence including the full grammar quotation, on one line, unchanged from HEAD.
line 3 (111 chars): `If the attempt about to run exceeds the threshold, skip to "On any escalation trigger" below instead of step 4.`

**HARD CONSTRAINT:** the grammar quotation "An entry begins at column zero with a mandatory bracketed timestamp; any other line is a continuation, never a sentinel, and quoting a whole prior entry inside a body requires indenting it off column zero." MUST survive verbatim. `tests/test_doctrine_integrity.py` check 12(c) discovers every `dcs/**/*.md` mentioning `SAFETY-HALT:` / `SAFETY-PASS:` / `IAP-APPROVED:` and requires each to quote `dcs_gate.py`'s `GRAMMAR_LINE` verbatim; `execute.md` is in that population. The comparison is whitespace-normalised, so joining wrapped lines is safe — deleting or paraphrasing any word of it is not. **CHECKPOINT after Edit A:** `wc -l` must read 244.

**EDIT B (criterion 3)** — lines 89-90 currently read:
`- **Worktree-isolated:** set up the worktree per new.md step 7b before`
`  spawning, and merge/reconcile after.`

Replace with exactly these three lines (+1, running total 245):
`- **Worktree-isolated:** only when the IAP declares \`isolation: worktree\``
`  for a specialist — set up that specialist's worktree per new.md step 7b`
`  before spawning it, and merge/reconcile after it returns.`

This restores the scoping `bca0b56` dropped (`git show bca0b56^:dcs/workflows/execute.md` old lines 134-138). The clause must be conditional, not unconditional.

**EDIT C (criterion 2)** — step 6's paragraph is HEAD lines 110-120, eleven lines. Replace all eleven with exactly these THIRTEEN lines (+2, running total 247). This is the whole paragraph with `Use \`AskUserQuestion\` when the disposition is \`escalate_owner\` — the right call is genuinely the Owner's judgment, not just a mechanical correction.` inserted immediately after `**If this session is Fable**, make the call yourself.` and the result greedily wrapped at EXACTLY 78 columns. This wrap was performed and counted by the Planning Chief; do not re-derive it — the previous instruction ("<= 78 columns") was rejected because at 73-77 columns the same paragraph lands at 14 lines and puts execute.md at 249. Transcribe these lines:

```
1 (74) **Any `status: "deviation"`:** stop the execution phase. This is a command
2 (70) point. **If not running Fable**: spawn `dcs-commander` via Task (model
3 (78) `fable`) with the triggering specialist's full return, the current 202 and its
4 (76) 204, and execution state — for established facts, pass their source, never a
5 (70) summary from memory. Its decision (schemas.md #6, commander decisions)
6 (76) governs: `replan` / `amend_tasking` / `escalate_owner`. **If this session is
7 (78) Fable**, make the call yourself. Use `AskUserQuestion` when the disposition is
8 (77) `escalate_owner` — the right call is genuinely the Owner's judgment, not just
9 (75) a mechanical correction. Record in `214-LOG.md`. Update `202-OBJECTIVES.md`
10 (76) and/or the relevant `204-TASKING/*.md` to reflect what was learned — editing
11 (75) the plan voids the approval mechanically (the deviation doctrine working as
12 (76) intended). Append to `214-LOG.md`: `deviation reported by <ID>: <summary> --
13 (64) returning to planning`. Tell the Owner to run `/dcs-plan` again.
```

The parenthesised number before each line is its exact character count — use it to verify your transcription, and do not write it into the file. Source of the inserted sentence: `git show bca0b56^:dcs/workflows/execute.md` old lines 187-189. No existing word of the paragraph is dropped: HEAD lines 114 and 117 were over-wide (116 and 84 chars) because earlier edits inserted text without rewrapping, and this dictation absorbs that.

**EDIT D (criterion 7 pointer)** — step 9b's `Partially met` bullet is HEAD lines 238-243, six lines. Replace all six with exactly these SEVEN lines (+1, running total 248 — the final count). This is the existing bullet with the W3 pointer appended and the whole thing greedily rewrapped at 78 columns, two-space continuation indent preserved; the pointer is ~97 chars and could never have been a bare append onto a 55-char last line. Not one word of the existing bullet text changes:

```
1 (74) - **Partially met: CLOSE AND REQUEUE is the default; another period is the
2 (76)   exception that must be argued.** A Safety-passed period holds proven work.
3 (75)   Keeping the incident open keeps it in a branch — unmerged, unshipped, and
4 (69)   fixing nothing. Only keep open when the remaining work is genuinely
5 (72)   inseparable (schema change whose readers are not yet updated, contract
6 (73)   half-migrated). State which in `214-LOG.md`. (field lesson 2026-07-24 —
7 (70)   `dcs/references/doctrine-appendix.md`, "Workflow field lessons", W3)
```

**DETERMINISTIC FALLBACK**, use only if `wc -l dcs/workflows/execute.md` reads > 250 after Edits A-D: unwrap onto ONE line the two-line paragraph at HEAD lines 122-123, `**Any \`status: "blocked"\`:** report the blocker to the Owner — this is an` / `external obstacle, not necessarily a planning defect.` That saves exactly one line, changes no word, and is safe by inspection — it contains no fragment of `GRAMMAR_LINE` and no `grep -c`, and `execute.md` already carries longer unwrapped lines. Apply this fallback **at most once**. If the count is still > 250 afterwards, STOP and return a deviation — do not pick a different paragraph on your own judgment, do not delete content, and never raise a budget constant. With every wrap above dictated rather than derived, this fallback should not fire; if it does, that means a dictated line was re-flowed, so re-check the transcription before applying it.

**HARD CONSTRAINT, SEPARATE FROM THE ABOVE:** do not introduce any new `grep -c <identifier>` text anywhere in `execute.md`. Check 12(h) does `re.search(r"grep -c (\w+)", execute_text)` and asserts the FIRST match's identifier is an attribute of `dcs_gate.py`; a new `grep -c` earlier in the file than step 9's `grep -c halt_cycles` turns the guard red. None of the dictated lines above contains one. Expected final: 248 lines.

**Note:** `execute.md` is NOT in merge-guard check 20's `_FL_FILES` population (`tests/test_doctrine_integrity.py:1739-1746`), so Edit D's bare "field lesson 2026-07-24" pointer on line 6 cannot turn that check red even though it carries no identifier. Leave the dictated string exactly as given — S4's appendix and the cross-string verification grep expect it character-for-character.

## File territory (may edit only within these globs)

- `dcs/workflows/execute.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/plan.md`
- `dcs/workflows/close.md`
- `dcs/workflows/loop.md`
- `dcs/references/**`
- `tests/**`
- `dcs/hooks/**`
- `dcs/templates/**`
- `agents/**`
- `skills/**`
- `.dcs/**`
- `CLAUDE.md`
- `vault/**`

## Worktree root

`C:\DCS-wt\trim-content-loss-restoration`

## Evidence required in the return

- `grep -n -B1 escalate_owner dcs/workflows/execute.md` — paste every matching line plus the line immediately before each. `escalate_owner` appears twice in the dictated text (once inside the `replan`/`amend_tasking`/`escalate_owner` enum, once in the handling sentence); for the SECOND occurrence, the `-B1` context line is `Fable**, make the call yourself. Use \`AskUserQuestion\` when the disposition is` — the handling text and that occurrence of the enum name are on adjacent lines, not the same line, by design of the 78-column wrap. Also run `grep -c "Use \`AskUserQuestion\` when the disposition is" dcs/workflows/execute.md` — must return 1 (criterion 2's actual, unambiguous witness).
- `grep -n 'isolation: worktree' dcs/workflows/execute.md` — must return >= 1 (returns 0 at HEAD). This is the mechanical witness for criterion 3, whose own 202 wording is a human read.
- `grep -n 'Worktree-isolated' dcs/workflows/execute.md` and the two lines following it — paste all three, so the reviewer can confirm the clause is conditioned on the IAP declaring `isolation: worktree` and is no longer a blanket instruction (criterion 3).
- `grep -c 'An entry begins at column zero' dcs/workflows/execute.md` — must return 1, proving the grammar quotation survived Edit A.
- Paste the rewrapped step-6 paragraph in full and state its first and last line numbers. It must be EXACTLY 13 lines. Then run `awk -v a=<first> -v b=<last> 'NR>=a && NR<=b {print NR": "length($0)}' dcs/workflows/execute.md` and paste the output — the sequence (bytes, per the transcription-rule note above) must read `74 70 78 78 70 76 78 79 75 78 75 76 64`. No line exceeds 78 CHARACTERS; the three em-dash lines (positions 4, 8, 10) read 78/79/78 in bytes for that reason. A different sequence means a line was re-flowed instead of transcribed; report it rather than adjusting.
- Paste the rewrapped `Partially met` bullet in full — EXACTLY 7 lines — with the same per-line length dump; the sequence (bytes) must read `74 76 77 69 72 75 70` (positions 3 and 6 carry the em dash).
- State explicitly whether the deterministic fallback (unwrapping the `status: "blocked"` paragraph) was applied or not.
- `grep -n 'Workflow field lessons' dcs/workflows/execute.md` — must return exactly 1 line; paste it in full so it can be compared character-for-character with S4's appendix heading and entry id W3.
- `python tests/test_doctrine_integrity.py` — paste the final `N/M passed` line, AND explicitly quote the result of the named case `log grammar: dcs/workflows/execute.md quotes GRAMMAR_LINE verbatim` and of `log grammar: Channel A's grep -c identifier is defined in dcs_gate.py`. Run it last. A failure naming a file outside your territory is reported, not fixed.
- `wc -l dcs/workflows/execute.md` — must be <= 250; expected exactly 248 (250 -6 +1 +2 +1). If it reads 249 or 247, a dictated block was transcribed at the wrong line count — say so and stop, do not compensate elsewhere. Also confirm the file still ends with a newline (`tail -c 1 dcs/workflows/execute.md | xxd`).
- `git diff --stat -- dcs/workflows/execute.md` — exactly one file changed.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
