# 204 — Tasking S3 (revised twice after IAP-review rejects — Part 1's lines now dictated verbatim, no wrapping decisions left)

**Incident:** trim-content-loss-restoration
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Two independent repairs in two files: `close.md`'s dropped field-lesson citation (202 criterion 4) and the corrupted budget-history comment chain in the merge-time guard (202 criterion 9).

**TRANSCRIPTION RULE for Part 1:** the only non-ASCII character anywhere in the dictated lines is U+2014 EM DASH (—). Every apostrophe is ASCII U+0027. Do not substitute characters and do not re-flow any line. **The parenthesised per-line numbers below are CHARACTER counts. The `awk` evidence command below reports BYTES.** On this host `awk`'s `length()` counts bytes (`LANG` unset), and U+2014 is 3 bytes / 1 character, so a line containing it reads 2 higher in `awk` than its dictated character count. This is expected and is not a transcription failure — the two number sets differing by 2 on em-dash lines is normal, not a sign anything was re-flowed.

**PART 1 — `dcs/workflows/close.md`, criterion 4.** The rule sentence survived `bca0b56`; only its citation was dropped, which is why the 202's own criterion-4 grep is already green at HEAD and cannot witness this work. The paragraph is lines 103-112 — TEN lines, beginning `**First check who owns closure.**` and ending `silently-never-closed finding is a leak).`; line 113 is blank and stays blank. Replace all ten lines with exactly these ELEVEN lines (+1, close.md 243 -> 244):

```
1 (68) **First check who owns closure.** Read the project's `CLAUDE.md` and
2 (71) the 201: if a documented routine closes items itself upon observing the
3 (69) fix (e.g. a daily validate-findings routine that stamps rows when the
4 (70) fixing commit is deployed), then the correct action is **no action** —
5 (70) record in AAR.md that closure is DELEGATED, name the routine, and note
6 (70) what it needs to observe the fix. Manually closing what a routine owns
7 (65) creates a race and duplicates its write (field lesson 2026-07-22,
8 (63) predates self-hosting). Delegate only when the documentation is
9 (70) explicit — a vaguely-mentioned routine is NOT an owner; when in doubt,
10 (64) use the flag-for-Owner fallback (a duplicate flag is annoying; a
11 (41) silently-never-closed finding is a leak).
```

The parenthesised number before each line is its exact character count — use it to verify your transcription, and do not write it into the file. The only content change is that the sentence which read `Manually closing what a routine owns creates a race and duplicates its write.` now reads `Manually closing what a routine owns creates a race and duplicates its write (field lesson 2026-07-22, predates self-hosting).`; every other word of the paragraph is unchanged and merely re-wrapped. Original: `git show bca0b56^:dcs/workflows/close.md` old lines 113-115. The citation form follows `doctrine-appendix.md`'s stated convention (lines 13-26) for a lesson predating the 2026-07-25 self-hosting boundary with no known version, and matches the shape `close.md` already uses at line 61. No appendix entry is needed for this one — there is no narrative text to relocate.

**WHY THIS EXACT WRAP, AND WHAT IT IS PROTECTING** — two constraints, both satisfied by the lines above, both broken by a nearby width. (i) Line 7 must carry `race and duplicates its write (field lesson 2026-07-22,` UNBROKEN on one line, because the binding criterion-4 evidence is `grep -c "race and duplicates its write (field lesson 2026-07-22" dcs/workflows/close.md` == 1. (ii) `predates self-hosting` must fall UNBROKEN on that same line or the IMMEDIATELY following one — it is on line 8 above. Merge-guard check 20 (`tests/test_doctrine_integrity.py:1734-1775`) puts `close.md` in its `_FL_FILES` population: `_FL_LINE_RE` flags any line where a YYYY-MM-DD date follows the words "field lesson" on that same line, and `_FL_ID_RE` must then match `incident \`<slug>\`` / vX.Y.Z / `predates self-hosting` on that line or the next — nowhere further. This wrap is 71 columns, and 71 is the only width in the 60-78 range that satisfies (i) and (ii) at once while landing the paragraph at 11 lines: at 64 columns (ii) fails, at 68-70 (i) fails, at 72-73 the text splits as `...2026-07-22, predates` / `self-hosting).` which matches NEITHER `_FL_ID_RE` alternative and turns check 20 red, and at 74+ the paragraph collapses to 10 lines and close.md lands at 243 instead of 244. Dropping the "predates self-hosting" note entirely also turns the guard red.

**PART 2 — `tests/test_doctrine_integrity.py`, criterion 9.** (unchanged from the prior revision) The 2026-07-26 (schemas-md-trim) paragraph's trailing figure was overwritten in place twice: first by `2e15682` (2026-07-29), which replaced the true `math.ceil(36547/1024) + 1 = 37` with its own `math.ceil(38361/1024) = 38` and deleted an explanatory sentence; then by `e3d4bcc` (2026-07-30), which replaced that with `math.ceil(36539/1024) = 36` and appended a NEW paragraph carrying the identical figure. Result: line 180 and line 187 state the same figure for two different incidents. Three edits:

(2a) Line 180 currently reads `#   budget = math.ceil(36539/1024) = 36`. Replace with `#   budget = math.ceil(36547/1024) + 1 = 37`, and restore the deleted sentence immediately after it as its own comment paragraph:
`#`
`# Still a ratchet: it bites 1 kB sooner than the 38 kB it replaces.`

Regenerate both from `git show 2e15682^:tests/test_doctrine_integrity.py`.

(2b) Append one sentence to the end of that same 2026-07-26 paragraph (position within the paragraph is your choice; wording is fixed):
`# This paragraph's figure was overwritten in place twice, by 2e15682 and`
`# again by e3d4bcc, and reconstructed at incident`
`# trim-content-loss-restoration; regenerate the original with`
`# \`git show 2e15682^:tests/test_doctrine_integrity.py\`.`

(2c) Insert a new paragraph for the incident `2e15682` should have appended, immediately BEFORE the existing `# Incident hot-path-budget-emergency-trim (2026-07-30)` paragraph (currently line 182), so the chain reads 37 -> 38 -> 36 -> 37 in date order:
`# Incident worktree-removal-self-conflict (2026-07-29) then grew`
`# doctrine.md by 479 B (audit step 5's three-tier removal behaviour) and`
`# re-derived the ratchet from the resulting, final, normalised size`
`# (regenerate with \`git show 2e15682 -- tests/test_doctrine_integrity.py\`):`
`#`
`#   budget = math.ceil(38361/1024) = 38`
`#`

**HARD CONSTRAINT (criterion 10, Owner ruling):** do NOT change `HOT_PATH_BUDGET_KB = 37` (line 199) or `WORKFLOW_BUDGET_LINES = 250` (line 1353), or any other constant. These are comment-only edits plus the `close.md` prose fix. Do not add or remove any `check(...)` call.

## File territory (may edit only within these globs)

- `dcs/workflows/close.md`
- `tests/test_doctrine_integrity.py`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/plan.md`
- `dcs/workflows/execute.md`
- `dcs/workflows/loop.md`
- `dcs/references/**`
- `dcs/hooks/**`
- `dcs/templates/**`
- `tests/test_dcs_gate.py`
- `tests/test_dcs_intake.py`
- `tests/payload_check.py`
- `agents/**`
- `skills/**`
- `.dcs/**`
- `CLAUDE.md`
- `vault/**`

## Worktree root

`C:\DCS-wt\trim-content-loss-restoration`

## Evidence required in the return

- `grep -c "race and duplicates its write (field lesson 2026-07-22" dcs/workflows/close.md` — must return 1 (0 at HEAD). This is the binding criterion-4 evidence; the 202's own broader grep is already green at HEAD and does not discriminate. Quote the count.
- `awk 'NR>=103 && NR<=114 {print NR": ["length($0)"] "$0}' dcs/workflows/close.md` — paste the output. Lines 103-113 are the 11 rewritten lines, line 114 is the blank line after them (11 lines from 103 end at 113, so the blank is 114, not 113). The length sequence (bytes, per the transcription-rule note above) must read `68 71 69 72 70 70 65 63 72 64 41 0`. A different sequence means a line was re-flowed instead of transcribed; report it rather than adjusting.
- `python tests/test_doctrine_integrity.py` — quote explicitly, verbatim and stating it green, the result of the named case `field-lesson citations: every field lesson mention in shipped package carries an incident identifier (slug, version, or 'predates self-hosting')`. `close.md` is in that check's `_FL_FILES` population, so this rewrap can turn it red.
- `grep -n -iE "field lesson.*[0-9]{4}-[0-9]{2}-[0-9]{2}" dcs/workflows/close.md` — paste every hit plus the line immediately after each, so the reviewer can confirm each carries an identifier within check 20's one-line window.
- `grep -c "36539/1024" tests/test_doctrine_integrity.py` — must return 1 (returns 2 at HEAD; this is the duplication criterion 9 names).
- `grep -c "36547/1024" tests/test_doctrine_integrity.py` — must return 1 (returns 0 at HEAD).
- `grep -n "budget = math.ceil" tests/test_doctrine_integrity.py` — paste all matches in order; the chain must read 37, 38, 36, 37 with each figure distinct from its neighbours and attributed to a distinct dated incident.
- `git diff -- tests/test_doctrine_integrity.py | grep -E '^[+-].*(HOT_PATH_BUDGET_KB|WORKFLOW_BUDGET_LINES)\s*='` — must return NOTHING (criterion 10). State explicitly that it returned empty.
- `python -c "import ast,subprocess; a=ast.dump(ast.parse(subprocess.run(['git','show','main:tests/test_doctrine_integrity.py'],capture_output=True,text=True).stdout)); b=ast.dump(ast.parse(open('tests/test_doctrine_integrity.py',encoding='utf-8').read())); print('AST IDENTICAL' if a==b else 'AST DIFFERS')"` — must print `AST IDENTICAL`, proving Part 2 changed comments only and no executable statement. This is the Logistics plan's named rollback precondition; run it, do not eyeball the diff.
- `wc -l dcs/workflows/close.md` — must be <= 250; expected exactly 244 (243 at HEAD, +1 from the 10-to-11-line rewrap). Confirm the file still ends with a newline.
- `python tests/test_doctrine_integrity.py` — paste the final `N/M passed` line. Run it last; a failure naming a file outside your territory is reported, not fixed.
- `git diff --stat -- dcs/workflows/close.md tests/test_doctrine_integrity.py` — exactly two files changed.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
