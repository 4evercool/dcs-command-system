# 204 — Tasking S1

**Incident:** trim-content-loss-restoration
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Restore the `guarded_paths` auto-approval condition to `plan.md`'s no-DELEGATION fallback (202 criterion 1) and add two zero-to-one-line appendix pointers for the field lessons S4 restores (202 criteria 5, 6). Three edits, all in `dcs/workflows/plan.md`.

**EDIT A (criterion 1)** — replace line 129 in full. It is currently a single unwrapped 170-char paragraph. New text, also one single line (+0 lines):

`**Any bound fails, no Delegation, or Type 1:** fall through to 6b — name failed bound(s). No DELEGATION.md → fallback to \`config.json\` (conservative, no per-bound audit): on that path auto-approve **only** if Type == 3 AND \`auto_approve_type3: true\` AND the IAP touches no file matching any \`guarded_paths\` glob outside the ordinary source tree — i.e. nothing that already looks unusual for a routine change. Anything else falls to 6b.`

Source of the restored condition: `git show bca0b56^:dcs/workflows/plan.md`, old lines 339-349.

**EDIT B (criterion 5 pointer)** — lines 56-57 currently read, wrapped:
`These checks are arithmetic, not judgment. Run them yourself; a failure`
`is yours to fix, never a reason to spend a command point.`

Replace with three lines (+1 line):
`These checks are arithmetic, not judgment. Run them yourself; a failure`
`is yours to fix, never a reason to spend a command point (field lesson`
`2026-07-23 — \`dcs/references/doctrine-appendix.md\`, "Workflow field lessons", W1).`

**EDIT C (criterion 6 pointer)** — line 62 is lint check 3a, a single unwrapped 429-char line. Append to its end, on the same line (+0 lines):
` Field lesson 2026-07-24 — \`dcs/references/doctrine-appendix.md\`, "Workflow field lessons", W2.`

The heading and entry ids quoted in Edits B and C are written by S4 in the same period and must match character-for-character; do not invent a different heading. Change nothing else in `plan.md` — do not rewrap unrelated paragraphs, do not touch step 6c, and do not touch the `GRAMMAR_LINE` quotation at line 242. Expected final: 247 lines.

Note for your own reassurance: `plan.md` is NOT in merge-guard check 20's `_FL_FILES` population (`tests/test_doctrine_integrity.py:1739-1746`), so the bare "field lesson <date>" pointers above cannot turn that check red. Do not add an incident slug or version to them to be safe — the dictated strings are what S4's appendix and the verification plan's cross-string grep expect.

## File territory (may edit only within these globs)

- `dcs/workflows/plan.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/execute.md`
- `dcs/workflows/close.md`
- `dcs/workflows/loop.md`
- `dcs/references/**`
- `tests/**`
- `dcs/hooks/**`
- `dcs/templates/**`
- `agents/**`
- `skills/**`
- `.dcs/**`
- `CLAUDE.md`
- `vault/**`

## Worktree root

`C:\DCS-wt\trim-content-loss-restoration`

## Evidence required in the return

- `grep -c guarded_paths dcs/workflows/plan.md` — must return 1 (it returns 0 at HEAD). Quote the count.
- `grep -n guarded_paths dcs/workflows/plan.md` — paste the single matching line in full, so the reviewer can read the restored condition and confirm it names files outside the ordinary source tree as what voids the fallback (criterion 1).
- `grep -n 'Workflow field lessons' dcs/workflows/plan.md` — must return exactly 2 lines (Edits B and C). Paste both.
- `wc -l dcs/workflows/plan.md` — must be <= 250; expected 247. Also confirm the file still ends with a newline (`tail -c 1 dcs/workflows/plan.md | xxd`), because the guard's own line count adds 1 when it does not.
- `python tests/test_doctrine_integrity.py` — paste only the final `N/M passed` line plus any failing case names. Run it last. If a failure names a file outside `dcs/workflows/plan.md`, report it in `evidence` and do NOT fix it — another specialist is editing that file in the same worktree.
- `git diff --stat -- dcs/workflows/plan.md` — must show exactly one file.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
