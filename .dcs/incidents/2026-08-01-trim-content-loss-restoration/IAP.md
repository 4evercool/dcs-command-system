# IAP — Incident Action Plan

**Incident:** trim-content-loss-restoration
**Type:** 1
**Operational period:** 1
**Links:** `202-OBJECTIVES.md` · `203-ORG.md` · `204-TASKING/*.md` · logistics plan below

## Objectives (summary of 202)

**Goal:** Content dropped by two prior trim commits (`bca0b56`, `e3d4bcc`) is restored to the shipped package — verbatim where it fits, relocated per the core/appendix convention where it doesn't — without exceeding the current `WORKFLOW_BUDGET_LINES` or `HOT_PATH_BUDGET_KB` ceilings, and the corrupted test budget-history comment chain reflects true provenance.

**Acceptance criteria:**
1. `plan.md`'s no-DELEGATION fallback states the `guarded_paths` auto-approval condition explicitly.
2. `execute.md`'s step 6 states the `escalate_owner` handling instruction (`AskUserQuestion`).
3. `execute.md`'s worktree-isolation clause is narrowed back to a scoped, conditional instruction.
4. The 2026-07-22 field-lesson citation is present in `close.md` (or `doctrine-appendix.md`).
5. The 2026-07-23 field lesson is present in `plan.md` or `doctrine-appendix.md`.
6. The 2026-07-24 "four Safety halts" field lesson is present in `plan.md` or `doctrine-appendix.md`.
7. The 2026-07-24 "sat in a branch" field lesson is present in `execute.md` or `doctrine-appendix.md`.
8. `doctrine.md` hard rule 3 states the "notify if a tool is available" clause.
9. `tests/test_doctrine_integrity.py`'s budget-history comment chain no longer duplicates one figure across the 2026-07-26 and 2026-07-30 paragraphs.
10. [IC] Neither `WORKFLOW_BUDGET_LINES` nor `HOT_PATH_BUDGET_KB` changes value anywhere in `tests/test_doctrine_integrity.py`.
11. [IC] `dcs/workflows/loop.md` is unchanged this period.
12. `python tests/test_doctrine_integrity.py` passes (all cases green) after all restorations.
13. `plan.md`, `execute.md`, `close.md` each stay ≤ `WORKFLOW_BUDGET_LINES` (250).
14. `doctrine.md` + `schemas.md` combined stay ≤ `HOT_PATH_BUDGET_KB` (37 KiB).

Full text: `202-OBJECTIVES.md`.

## Tactics (from the Planning Chief, second revision)

1. Budget is the binding constraint and is resolved per file BY THIS PLAN, not left to specialists — every rewrap is now dictated line-by-line rather than by column width, because a column width is a computation a specialist can get right and still land on the wrong line count. Measured at HEAD in the worktree: `plan.md` 246/250, `execute.md` 250/250, `close.md` 243/250, hot path 37455/37888 B (433 B free). Planned landing, each derived from a wrap the chief performed and counted: `plan.md` 247, `execute.md` 248, `close.md` 244, hot path ~37486 B (~402 B free; the restored doctrine.md clause is +31 bytes, not +33 as earlier estimated — immaterial against the ceiling either way). No budget constant is touched (Owner ruling, criterion 10).
2. `close.md`'s rewrap is pinned to exactly 71 columns and dictated verbatim as 11 literal lines. Of every greedy wrap from 60-78 columns, 71 is the ONLY width that simultaneously (i) lands `close.md` at 244, (ii) keeps `race and duplicates its write (field lesson 2026-07-22,` unbroken so the binding criterion-4 grep returns 1, and (iii) puts `predates self-hosting).` on the immediately following line so check 20's one-line identifier window is satisfied.
3. `execute.md`'s two rewraps (step 6, step 9b's bullet) are pinned to exactly 78 columns and dictated verbatim, for the same reason with different numbers — at 73-77 columns step 6 lands at 14 lines instead of 13, and below 76 columns the step-9b bullet lands at 8 lines instead of 7. Arithmetic, every term now measured rather than estimated: 250 - 6 (Edit A, 9→3 lines) + 1 (Edit B, 2→3) + 2 (Edit C, 11→13) + 1 (Edit D, 6→7) = 248, slack 2.
4. Destination rule, applied uniformly: the RULE stays in (or returns to) the workflow file; the STORY goes to `dcs/references/doctrine-appendix.md`, per CLAUDE.md's "Where lessons go" test. This is also the cheapest route under the budget — the appendix is never `@`-included and doesn't count against the hot path.
5. Stories land in one new appendix section, written by exactly one specialist (S4): `## Workflow field lessons (restored, incident \`trim-content-loss-restoration\`)`, holding entries W1/W2/W3, mirroring the existing D1-D4 convention at the foot of that file. Every line of it is dictated to satisfy merge-guard check 20 (`predates self-hosting` on each flagged line or the one immediately after) by construction.
6. Workflow files get a one-clause pointer, not the story. Two of the three pointers cost zero lines (existing single-line unwrapped paragraphs in `plan.md`); the third costs one line each in `plan.md` and `execute.md`. Exact pointer strings and the appendix heading are dictated verbatim in the taskings so S1/S2/S4 cannot drift apart. `plan.md`/`execute.md` are NOT in check 20's `_FL_FILES` population, so their bare "field lesson <date>" pointers are safe without an identifier — only `close.md` and `doctrine-appendix.md` need one.
7. `plan.md`'s `guarded_paths` restoration is an in-place extension of an existing single-line paragraph — 0 net lines.
8. `close.md` is a citation fix, not a relocation — the rule sentence survived the trim intact; only the field-lesson tag was dropped, which is why the 202's own criterion-4 grep is already green and a narrower grep is the binding one.
9. `doctrine.md` is a single restored clause on the hot path (+31 B); `loop.md` is not touched — the equivalent instruction already lives there independently since `a3fb60b`, predating both trims.
10. The test comment chain is reconstructed across both corrupting commits (`2e15682` then `e3d4bcc`), not merely reverted against the more recent one — restoring the true 2026-07-26 figure and its deleted sentence, and inserting the 2026-07-29 paragraph `2e15682` should have appended, so the chain reads 37 → 38 → 36 → 37 in date order with every figure regenerable.
11. Transcription, not composition, is now the residual failure mode in S2 and S3. Both taskings state the only non-ASCII character in any dictated line is U+2014 EM DASH, every apostrophe is ASCII U+0027, no line may be re-wrapped after transcription, and both are evidenced on a per-line character-length dump (`awk`) so a silently re-flowed line is visible to the reviewer without re-deriving the wrap.

**Correction surfaced during this revision:** of the two additional field-lesson losses flagged as "confirmed real" in the prior IAP text, only ONE actually is. The 2026-07-24 IAP.md-edit-criterion lesson has no survivor anywhere under `dcs/` — a genuine second-tier loss, correctly deferred per the 202's out-of-scope clause. The 2026-07-26 version-bump-waiver lesson is NOT lost: it survives verbatim at `dcs/templates/202-OBJECTIVES.md:58-60` — `bca0b56` removed `plan.md`'s copy of a lesson duplicated in a shipped template, the same doctrine.md/loop.md situation as the 201's fourth evidence bullet. The follow-up owed after this period is one lesson, not two. S4's tasking text is unaffected either way (it says "do not restore any story beyond W1/W2/W3", true regardless).

## File-territory partition

| Specialist | Territory | Forbidden |
|---|---|---|
| S1 | `dcs/workflows/plan.md` | `dcs/workflows/execute.md`, `dcs/workflows/close.md`, `dcs/workflows/loop.md`, `dcs/references/**`, `tests/**`, `dcs/hooks/**`, `dcs/templates/**`, `agents/**`, `skills/**`, `.dcs/**`, `CLAUDE.md`, `vault/**` |
| S2 | `dcs/workflows/execute.md` | `dcs/workflows/plan.md`, `dcs/workflows/close.md`, `dcs/workflows/loop.md`, `dcs/references/**`, `tests/**`, `dcs/hooks/**`, `dcs/templates/**`, `agents/**`, `skills/**`, `.dcs/**`, `CLAUDE.md`, `vault/**` |
| S3 | `dcs/workflows/close.md`, `tests/test_doctrine_integrity.py` | `dcs/workflows/plan.md`, `dcs/workflows/execute.md`, `dcs/workflows/loop.md`, `dcs/references/**`, `dcs/hooks/**`, `dcs/templates/**`, `tests/test_dcs_gate.py`, `tests/test_dcs_intake.py`, `tests/payload_check.py`, `agents/**`, `skills/**`, `.dcs/**`, `CLAUDE.md`, `vault/**` |
| S4 | `dcs/references/doctrine.md`, `dcs/references/doctrine-appendix.md` | `dcs/references/schemas.md`, `dcs/references/forms.md`, `dcs/workflows/**`, `tests/**`, `dcs/hooks/**`, `dcs/templates/**`, `agents/**`, `skills/**`, `.dcs/**`, `CLAUDE.md`, `vault/**` |

**Partition status:** disjoint — parallel execution. (Tasking lint check 1 caught and the IC fixed a redundant `dcs/workflows/**` wildcard in S1/S2/S3's `forbidden` lists that also matched each one's own territory — corrected before this table was written; see `214-LOG.md`. Re-verified clean after the period-1 IAP-review reject and revision below.)

**Revision notes (period 1, IAP rejected twice at command point 2):**
- **Reject 1:** S4's dictated field-lesson text would have failed merge-guard check 20 (no incident identifier/version/"predates self-hosting" on 6 lines), plus five secondary tasking-ambiguity defects.
- **Reject 2:** the first revision fixed S4 correctly but replaced S3's dictated-line approach with a column-WIDTH instruction ("<= 76 columns") while S3's `close.md` edit is bound by two interacting mechanical constraints (check 20 vs. the criterion-4 grep) that only one width in the 60-78 range (71) satisfies simultaneously — `dcs-commander` simulated every width and caught it. S2's Edit C had the same latent defect (a "<= 78" bound that permits widths yielding the wrong line count).
- **Resolution:** `dcs-planning-chief` was re-spawned fresh a second time (principle 9b — never resumed) and now dictates every wrapped line verbatim in S2 and S3, with per-line character counts and `awk`-based verification commands, chosen by direct simulation against check 20's regexes and the criterion-4 grep. S1 and S4 were re-verified correct and left unchanged. Full detail: `214-LOG.md` entries for command point 2.

## Deploy / environment plan (Type 1, from the Logistics Chief)

**Deploy path:** Full install, single command, no scoped variant: `powershell -ExecutionPolicy Bypass -File C:\DCS\install.ps1`, run from the clean main checkout via `/dcs-deploy`, never from the worktree. 5 of the 6 territory files ship (`tests/test_doctrine_integrity.py` is not in the payload — it reaches npm only, Owner-only/2FA-gated, out of scope). Deploy delegation: inside Delegation v5's bounds (`deploy.auto: true`, no territory matches `forbidden_globs`, 1-row train against `max_rows_per_train: 3`); `auto_after_close` is `false` so close and ship stay separate acts. Strong recommendation: relocate restored stories into the *existing* `doctrine-appendix.md`, create no new payload file (a new file shows as repo-only in the pre-install witness and becomes permanent installed-only debris if later removed).

**Env/deps:** No new env vars, secrets, config, package dependencies, or version bump (`package.json`/`dcs/VERSION` are out of territory; version bump is a forbidden topic). Acceptance-criteria commands are Git Bash syntax — run through the Bash tool, not PowerShell. Write every edit with the Write/Edit tools, never PowerShell `Set-Content`/`Out-File` (UTF-8 BOM risk).

**Migration ordering:** No database/schema/service — nothing restarts. The ordering that matters: (1) all edits happen in the worktree; (2) budget-bearing edits are serialized — `execute.md` has zero headroom and `doctrine.md` spends from a shared 433 B hot-path pool, each budget-bearing file gets one owner, re-measure after each edit lands; (3) `close.md` step 1a runs the merge-time guard from the worktree, must be green; (4) merge to main, finish close, `.dcs/ACTIVE` clears; (5) only then `/dcs-deploy`. `install.ps1` must never run while this incident is active — it swaps the workflows the running session reads, and this incident edits three of those workflows.

**Rollback plan:** (a) Before merge: abandon by not merging. (b) After merge, before deploy: `git revert -m 1 <merge sha>` on main; the branch stays alive until `deploy.md` step 8 deletes it. (c) After deploy: revert on main, re-run `install.ps1` (only with `.dcs/ACTIVE` clear), re-run `payload_check.py` (must return to 47 identical / exit 0). If the restoration itself introduces a new error: a bad `tests/test_doctrine_integrity.py` edit is repo-local (not in the payload) — plain revert, no deploy involved, but prove the edit is comment-only via `ast.dump` equality between main's and the worktree's parse of the file (ast discards comments, so identical dumps prove no executable statement changed — stronger than eyeballing the diff). A bad `doctrine.md` edit is the one genuinely live failure (`@`-included every invocation) — revert plus reinstall, noting reinstall is blocked while any incident is active, so treat the `doctrine.md` hunk as highest-scrutiny at Safety review.

**Risks (Logistics):** the merge-time guard witnesses its own edit (S3's changes to `tests/test_doctrine_integrity.py` are checked by that same file — mitigated by the AST-equality proof above, run as a named check, not left to the diff reader); two budgets/several editors on one shared pool (serialize, re-measure after each edit, never raise a constant); check 20's field-lesson-citation coverage is asymmetric (`close.md`/`doctrine-appendix.md` covered, `plan.md`/`execute.md` not — restored lessons in covered files must carry a citable date/version or the merge guard blocks the close); territory overlap with four other queued register rows on the same files (keep the merge→deploy window short, start no overlapping incident inside it); the main checkout has pre-existing non-payload dirt (`vault/` changes) — not a stop, one line in the deploy summary; the version marker will not move (by design, not a failed-ship signal — report witness numbers and pinned sha, not the version string).

## Risks

- **Transcription, not composition, is now the dominant failure mode in S2 and S3.** Every rewrap is dictated as literal lines, so a specialist can no longer choose a wrong column count — but can still re-flow a pasted block, substitute a typographic apostrophe for U+0027, or drop a trailing character. Mitigation is mechanical: each dictated block carries its exact per-line CHARACTER count, and each tasking's evidence requires an `awk ... length($0)` dump whose BYTE sequence must match exactly. **Reject 3 caught that this host's `awk` counts bytes, not characters** (`LANG` unset; U+2014 is 3 bytes/1 char) — the expected byte sequences, corrected, are: `close.md` `68 71 69 72 70 70 65 63 72 64 41` (11 dictated lines, positions 4 and 9 carry the em dash); `execute.md` step 6 `74 70 78 78 70 76 78 79 75 78 75 76 64` (positions 4, 8, 10); `execute.md` step 9b `74 76 77 69 72 75 70` (positions 3, 6). Both taskings now say the character/byte distinction explicitly so a correct transcription is not misread as a re-flow.
- **`execute.md` has exactly 2 lines of slack**, now measured rather than projected: 250 -6 (Edit A) +1 (Edit B) +2 (Edit C at exactly 78 columns) +1 (Edit D at exactly 78 columns) = 248. The deterministic single-use fallback (unwrap the `status: "blocked"` paragraph, lines 122-123) is retained, but if it fires that is itself a signal a dictated block was re-flowed — S2 is told to re-check transcription before applying it.
- **`close.md`'s 71-column wrap is load-bearing on two independent checks pulling in opposite directions**, and every neighbouring width fails one of them (64 breaks check 20, 68-70 split the criterion-4 grep string, 72-73 split `2026-07-22,`/`predates` across a gap `_FL_ID_RE` cannot bridge, 74+ collapses to 10 lines / 243). If S3 deviates from the dictated lines at all, it should return a deviation rather than pick a nearby width.
- **Coupled strings across taskings** (S1/S2's pointers quote S4's exact heading and entry ids `W1`/`W2`/`W3`). Cheap check at verification: `grep -rn 'Workflow field lessons' dcs/` returns exactly four lines.
- **The merge-time guard is a whole-tree check run by four specialists in one shared worktree** — every tasking instructs "run it last, report the output, never fix a failure naming a file outside your territory." The binding measurement is the Safety Officer's single re-run after all four returns are in.
- **Two guard checks live in S2's file and can be tripped by an otherwise-reasonable edit** — check 12(c) (`GRAMMAR_LINE` verbatim, inside Edit A's reflowed sentence) and check 12(h) (first `grep -c <ident>` must resolve in `dcs_gate.py`). Both are hard constraints; none of S2's dictated lines introduces a `grep -c`.
- **Check 20 (field-lesson citations) is asymmetric across the territory** — `close.md` (S3) and `doctrine-appendix.md` (S4) are in its `_FL_FILES` population, `plan.md`/`execute.md` (S1/S2) are not. This is why S1's/S2's bare pointers are safe while S3's/S4's dictated lines must carry `predates self-hosting` within the one-line window; both taskings' evidence names the check's own case string explicitly rather than trusting the aggregate pass count.
- **`wc -l` is not the guard's own measure** (`_workflow_line_count()` adds 1 for a missing trailing newline) — every workflow tasking is evidenced on both `wc -l` and an explicit trailing-newline check.
- **S3's territory includes the guard that witnesses S3's own edit** — mitigated by the AST-equality proof in S3's evidence (main's parse vs. the worktree's parse of `tests/test_doctrine_integrity.py` must be identical dumps, since `ast` discards comments), run as a named check at verification, not left to the diff reader.
- **Scope discipline**: only ONE further field-lesson loss is confirmed real (the 2026-07-24 IAP.md-edit-criterion story — the 2026-07-26 version-bump-waiver story turned out to survive at `dcs/templates/202-OBJECTIVES.md:58-60`, see tactics correction above). It is excluded from every tasking per the 202's own out-of-scope clause — folding it in silently would be goal drift; a follow-up incident or a 202 amendment before the stamp are the only sanctioned routes.
- Territory overlaps four other queued register rows on the same files — keep the merge-to-deploy window short and start no overlapping incident inside it.

## Verification plan

DONE means the six files carry the restored content, the merge-time guard is green on the integrated tree, and neither budget constant moved.

1. **Integrated guard run (criterion 12, binding):** `python tests/test_doctrine_integrity.py` from the worktree root, after all four specialists return — every case green. Not a specialist's own run (each ran against a tree peers were still editing). Also run `python tests/test_dcs_gate.py` and `python tests/test_dcs_intake.py` before close; neither should be affected.
2. **Budget ceilings, exact expected landings this plan is pinned to:** `wc -l dcs/workflows/plan.md dcs/workflows/execute.md dcs/workflows/close.md` must read 247, 248, 244 — not merely "≤ 250". A file under the ceiling but off its predicted count means a dictated rewrap was not transcribed as dictated, a finding even though the ceiling holds. Hot path via the guard's own command (`tests/test_doctrine_integrity.py:197-198`, CRLF-normalising — do not substitute `wc -c`), total ≤ 37888 B, expected ~37486.
3. **Budgets did not move (criterion 10, [IC]):** `git diff main -- tests/test_doctrine_integrity.py | grep -E '^[+-].*(HOT_PATH_BUDGET_KB|WORKFLOW_BUDGET_LINES)\s*='` returns empty.
4. **`loop.md` untouched (criterion 11, [IC]):** `git diff main -- dcs/workflows/loop.md` is empty.
5. **Per-criterion greps** — see each tasking's `evidence_required` for exact commands (c1 `guarded_paths`, c2 `escalate_owner`, c3 `isolation: worktree` plus the clause read, c5/c6/c7 appendix greps, c8 `notify` at `doctrine.md:131`, c9 the budget-figure chain). **Criterion 4, binding evidence:** `grep -c "race and duplicates its write (field lesson 2026-07-22" dcs/workflows/close.md` == 1 — the 202's own broader grep is already green at HEAD and must not be accepted as the witness. **Criterion 3, mechanical companion:** `grep -n 'isolation: worktree' dcs/workflows/execute.md` ≥ 1 (0 at HEAD), alongside the human read the 202 asks for.
6. **Named guard cases, quoted individually, never folded into the aggregate:** `field-lesson citations: every field lesson mention in shipped package carries an incident identifier (slug, version, or 'predates self-hosting')` (check 20 — the case this plan is most likely to break, and the one S3's wrap width is chosen to protect) and `bar carrier (criterion 3): doctrine-appendix.md has no bare 'N of M' census inside quotation marks without a regenerating command in the same paragraph`.
7. **Wrap fidelity** — re-run each tasking's per-line length dump and compare against the dictated sequences (see S2/S3 evidence for the exact numbers). Also confirm by eye that `close.md`'s line 7 ends `...(field lesson 2026-07-22,` and line 8 begins `predates self-hosting).` — that adjacency is what keeps check 20 green.
8. **Content-preservation of the one reflow:** guard case `log grammar: dcs/workflows/execute.md quotes GRAMMAR_LINE verbatim` green, plus `grep -c "An entry begins at column zero" dcs/workflows/execute.md` == 1, plus a manual read of the three reflowed lines.
9. **Comment-only proof for the guard's self-witnessing edit:** S3's AST-equality command must print `AST IDENTICAL` — the Logistics plan's stated rollback precondition, run not inferred.
10. **Cross-tasking string integrity:** `grep -rn 'Workflow field lessons' dcs/` returns exactly four lines, agreeing character-for-character on section name and W1/W2/W3.
11. **Blast-radius check (escalation trigger a):** `git diff --name-only main` returns exactly the six declared files and nothing else.
12. **Manual read, not mechanisable:** read `plan.md` step 6's fallback paragraph, `execute.md` step 6, and `close.md` step 5's rewrapped paragraph end to end — a criterion-satisfying grep does not by itself prove the restored prose reads coherently in place, and three of the four taskings hand a specialist pre-wrapped text whose sentence flow no check inspects.

## Deviation history (this period)

none — first IAP of period 1.
