# 204 — Tasking S2

**Incident:** schemas-contract-format
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Критерии 202 №3 и №4, и тестовая часть критерия 5. ЗАПУСКАЕТСЯ ТОЛЬКО
ПОСЛЕ возврата S1 и читает фактическое состояние
`dcs/references/schemas.md` и `agents/dcs-*.md` на диске — описание
формата в вашей постановке задачи и в возврате S1 является ориентиром, а
не источником истины: разбирайте то, что реально лежит в дереве.

Добавить в `tests/test_doctrine_integrity.py` две новые проверки под
следующими свободными номерами (последняя занятая — 17) и завести им
записи в докстроке-указателе модуля тем же стилем, что у 13-17.

Проверка полей: пары «раздел схемы — устав» находятся обходом (разделы
разбором `schemas.md`, уставы обходом `agents/dcs-*.md`), обе стороны
разбираются во время прогона, сверяется ОДНО направление — поле,
объявленное разделом схемы, обязано присутствовать в блоке контракта
соответствующего устава; обратное направление не сверяется, и имя
проверки вместе с записью в докстроке обязаны сказать это прямо, не
обещая двусторонней сверки (снятый на halt 3 компаратор проверки 15
сняли именно за обещание непокрытого). Ни одного литерала имени поля,
имени агента, номера раздела или счёта в теле проверки (принцип 15).
Страховка от вырождения: пустое население разделов, пустое население
уставов и пустой разбор объявления — каждое красное отдельным именованным
случаем. По именованному случаю на пару, чтобы новый устав падал по
имени, а не растворялся в счёте. Отрицательное доказательство —
постоянный именованный случай, прогоняющий ТОТ ЖЕ компаратор против
подделанного В ПАМЯТИ текста устава, из которого изъято одно объявленное
поле (имя берётся из разбора, не пишется литералом), по образцу проверки
13(f) со сдвинутым отображением; файлы при этом не редактируются —
`agents/**` вам запрещён, и подделка на диске была бы нарушением
территории.

Проверка JSON-примеров: каждый огороженный блок
`dcs/references/schemas.md` разбирается `json.loads`; население блоков —
во время прогона, ни списком, ни числом; ноль блоков — красное.

Не регрессировать проверки 13/14/15/16/17 и не трогать `EXCLUDED_DIRS` /
`BYTECODE_SUFFIXES` (проверка 16 сверяет их текстуально с
`tests/payload_check.py`, который вам запрещён).

ЕСЛИ формат, который оставил S1, не разбирается без эвристик, или сверка
на реальном дереве даёт настоящее расхождение схемы и устава —
возвращайте `status: "deviation"` с находкой. Ослаблять проверку,
подгонять компаратор под расхождение или заводить исключение, чтобы
прогон позеленел, запрещено: сторож здесь и есть инструмент сверки шва
между двумя сторонами контракта.

Содержимое пакета — на английском; тест — stdlib-only Python.

## File territory (may edit only within these globs)

- `tests/test_doctrine_integrity.py`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/**`
- `agents/**`
- `tests/payload_check.py`
- `tests/test_dcs_gate.py`
- `tests/test_dcs_intake.py`
- `package.json`
- `.dcs/**`

## Evidence required in the return

- `python tests/test_doctrine_integrity.py` — собственная строка N/M плюс ВСЕ строки новых именованных случаев (по паре «раздел — устав», случаи вырождения, случай подделки, случай разбора JSON-блоков)
- `python tests/test_doctrine_integrity.py` — строки именованных случаев проверок 13 и 14 из того же прогона, как доказательство отсутствия регресса
- `python tests/test_dcs_gate.py` и `python tests/test_dcs_intake.py` — строки N/M обоих
- прогон временной подделки для критерия 3: одна команда `python -c`, которая читает устав, изымает из его текста одно объявленное поле и прогоняет тот же компаратор, с выводом, показывающим красное; привести саму команду дословно и её вывод — IC перенесёт факт и команду в `214-LOG.md`
- прогон вырождения: команда, показывающая красное на пустом населении (пустой список разделов или пустой список уставов, подставленный в компаратор), с выводом
- `git diff tests/test_doctrine_integrity.py` — решающие выдержки, доказывающие отсутствие литералов имён полей, имён агентов, номеров разделов и счётов в теле новых проверок; без сплошной вставки диффа
- `grep -n 'schemas.md #' tests/test_doctrine_integrity.py` и запись новых номеров в докстроке-указателе модуля — привести с file:line

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
