# 204 — Tasking S1

**Incident:** schemas-contract-format
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Критерии 202 №1 и №2 целиком, вместе с бюджетной частью критерия 5.
Ввести единый машиночитаемый формат объявления контракта и применить его
на ОБЕИХ сторонах: (а) в `dcs/references/schemas.md` — каждому разделу,
объявляющему контракт возврата агента; §3 получает полный контракт
(таблицу полей и строку `Returned by`), §6 привязывает каждое поле к
своей командной точке так, чтобы слэш-ячеек, склеивающих несколько имён
полей, не осталось, а привязка производителя в §2 перестала быть
двусмысленной (её строка `Returned by` называет двух агентов); (б) в
`agents/dcs-*.md` — блок контракта того же формата в каждом уставе, у
`agents/dcs-commander.md` впервые (поля привязаны к четырём командным
точкам, включая `esg_activation`), а в `<output_contract>`
`agents/dcs-safety-officer.md` появляется `advisories` (живое расхождение
со `schemas.md:101` с коммита `6a57b97`). Объявлять только поля верхнего
уровня; вложенные ключи оставить прозой.

ЖЁСТКИЕ ГРАНИЦЫ, каждая — известный способ уронить уже существующие
проверки: (1) заголовки разделов `schemas.md` (номер И текст) не менять —
проверка 13 сверяет каждую цитату `schemas.md #N` с заглавием раздела по
всему дереву, включая `dcs/workflows/`, `dcs/templates/` и
`dcs/references/doctrine.md`, которые вам запрещены; (2) не удалять и не
переносить цитату `schemas.md #6` в `agents/dcs-commander.md:108-109` —
проверка 13 держит именно этот файл именованной страховкой от вырождения,
он виден только после нормализации переноса строк; (3) в каждом
переписанном `<output_contract>` цитата вида `` `references/schemas.md`
#N `` обязана и дальше нести заглавие раздела в пределах 80 символов
после цифры; (4) в `agents/dcs-safety-officer.md` не трогать абзацную
структуру и формулировки шага, объявляющего разделение
advisory/refutation — проверка 14 находит его по содержанию и разбирает
счёт полос; (5) блок контракта командира не должен стать вторым
конкурирующим источником рядом с примерами внутри `<command_points>` —
одно объявление на устав; (6) все огороженные блоки `schemas.md` обязаны
остаться разбираемыми `json.loads` (сегодня их 10 и все разбираются).

Бюджет hot path: чистое изменение размера `schemas.md` не более +153 B,
доплата ищется точечным тримом в самом `schemas.md` или переносом
провенанса в `dcs/references/doctrine-appendix.md`;
`dcs/references/doctrine.md` не трогать. Если коридор не сходится после
добросовестного трима — возврат со `status: "deviation"`, а не подъём
`HOT_PATH_BUDGET_KB` (константа не в вашей территории и поднимать её
нельзя вообще).

В возврате отдельно опишите формат: его грамматику, как он выражает
привязку поля к командной точке и что именно в нём разбирается — это
вход следующего специалиста.

Содержимое пакета — на английском (правило проекта: English throughout
the package).

## File territory (may edit only within these globs)

- `dcs/references/schemas.md`
- `dcs/references/doctrine-appendix.md`
- `agents/dcs-*.md`

## Forbidden zones (explicitly, even if it seems related)

- `tests/**`
- `dcs/workflows/**`
- `dcs/templates/**`
- `dcs/references/doctrine.md`
- `dcs/hooks/**`
- `dcs/VERSION`
- `package.json`
- `.dcs/**`

## Evidence required in the return

- `python -c "d=open('dcs/references/doctrine.md','rb').read().replace(b'\r\n',b'\n'); s=open('dcs/references/schemas.md','rb').read().replace(b'\r\n',b'\n'); print(len(d),len(s),len(d)+len(s))"` — привести число ДО правок (37735) и ПОСЛЕ; после обязано быть <= 37888
- `grep -n '^Returned by' dcs/references/schemas.md` — §3 присутствует; привести весь вывод
- `grep -c 'output_contract' agents/*.md` — привести весь вывод, у `agents/dcs-commander.md` должно стать не 0
- `grep -n 'advisories' agents/dcs-safety-officer.md` — привести строку с file:line внутри `<output_contract>`
- `python -c "import re,json,io; t=io.open('dcs/references/schemas.md',encoding='utf-8').read(); b=re.findall(r'^```[a-zA-Z-]*\n(.*?)^```', t, re.M|re.S); print(len(b),'blocks'); [json.loads(x) for x in b]; print('all parse')"` — привести вывод
- `python tests/test_doctrine_integrity.py` — привести собственную строку N/M и ВСЕ строки именованных случаев проверок 13 и 14, плюс строку бюджета hot path (прогон на дереве после правок; новой проверки ещё нет — S2 не начинался)
- `python tests/test_dcs_gate.py` и `python tests/test_dcs_intake.py` — привести строки N/M обоих
- решающие выдержки: объявление §3 и объявление §6 из `dcs/references/schemas.md` с file:line; блок контракта из `agents/dcs-commander.md` с file:line — без сплошной вставки файлов

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
