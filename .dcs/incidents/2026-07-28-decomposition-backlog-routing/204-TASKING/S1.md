<!--
204-TASKING/S1.md -- one file per specialist, transcribed verbatim by
the IC from the Planning Chief's `taskings[]` array (schemas.md #2, chief
plan). The specialist receives exactly this file's content plus the
relevant IAP excerpt -- nothing else. If the tasking is ambiguous, that's
a planning defect to fix during /dcs-plan, not something for the
specialist to resolve by guessing.
-->

# 204 — Tasking S1

**Incident:** decomposition-backlog-routing
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Satisfy 202 criteria 1 and 3 — the bar's canonical statement in the
constitution and its stem-side application.

**A) `dcs/references/doctrine.md`, principle 4 (line 55) ONLY.** Amend
the existing sentence in place with a parenthetical version note. Add NO
new principle, change NO number, touch NO other principle. Draft (tighten
if needed, do not re-scope): replace "A stem that finds several registers
each of them and opens only the one on the critical path (`new.md` step
4a);" with "A stem that finds several defects registers each of them and
opens only the one on the critical path (`new.md` step 4a) — **(v0.6.13)
except below the bar: a defect the stem proposes at the register's lowest
priority tier (`L`) goes to the project's own lightweight backlog-style
surface instead, when its `CLAUDE.md` documents one; documenting none,
the register row stands as before**;". Note the word "defects" — restoring
it repairs a pre-existing missing object in that clause; that repair is
authorised (confirmed in scope at command point 2), nothing else in the
sentence is.

**B) `dcs/workflows/new.md`, step 4a's FIRST bullet (lines 81-86) ONLY.**
Draft replacement, +6 lines net:

```
- **More than one → decompose now.** Give each a proposed Priority
  (`H`/`M`/`L`, the register template's vocabulary). Register every
  defect at `H`/`M` as its own row in `<esg_root>/.dcs/esg/REGISTER.md`
  (`QUEUED`), then open **one** — the one on the critical path, usually
  the one actively causing harm. The others are follow-ups with the
  evidence already gathered; each can ship the day it is fixed.
- **(v0.6.13) `L` is below the bar.** It goes to the project's own
  lightweight backlog-style surface instead of a register row, if its
  `CLAUDE.md` documents one (doctrine's "Relationship to
  project-specific protocols"); documenting none, the row is written as
  before, so no portfolio loses visibility by omission. Harm is never
  `L`.
```

Do NOT add a reporting bullet — step 4a's existing third bullet ("which
defects were split out and where they went") already covers the routing
disclosure.

## File territory (may edit only within these globs)

- `dcs/references/doctrine.md`
- `dcs/workflows/new.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/esg.md`
- `dcs/references/schemas.md`
- `dcs/references/doctrine-appendix.md`
- `dcs/VERSION`
- `package.json`
- `tests/**`
- `dcs/templates/**`
- `dcs/hooks/**`
- `agents/**`
- `skills/**`
- `vault/**`
- `.dcs/**`
- `install.ps1`
- `install.sh`
- `C:/Users/4ever/.claude/**`

## Hard constraints

1. Never write the string `vault`, `Backlog.md`, or any path from this
   repo into either file — the package ships no project facts; the
   surface is only ever "the project's own `CLAUDE.md`".
2. `new.md` net delta <= +7 lines (242 -> <=249; ceiling 250, this file
   is not grandfathered).
3. `doctrine.md` + `schemas.md` normalised bytes must land <= 37,808
   (baseline 37,458; ceiling 37,888) — you may not touch `schemas.md`, so
   this is a cap of +350 bytes on `doctrine.md`.
4. Use the Edit tool, never PowerShell `Set-Content`/`Out-File` (BOM).
5. Do not run `install.ps1` — installing mid-incident is a HARD RULE
   violation in this repo.
6. If the text cannot fit the budgets without editing prose outside step
   4a's first bullet / principle 4's one sentence, STOP and report a
   deviation; do not trim unrelated prose to make room.

## Evidence required in the return

- `python tests/test_doctrine_integrity.py 2>&1 | tail -3` — quote the
  final `N/M passed` line; baseline is 86/86, anything less is yours.
- `python tests/test_dcs_gate.py 2>&1 | tail -3` and `python
  tests/test_dcs_intake.py 2>&1 | tail -3` — quote each `N/M passed`
  line (202 criterion 4).
- Hot-path budget, exact number: `python -c "from pathlib import
  Path;d=Path('dcs/references/doctrine.md').read_bytes().replace(b'\r\n',b'\n');s=Path('dcs/references/schemas.md').read_bytes().replace(b'\r\n',b'\n');print(len(d)+len(s),'of',37*1024)"`
  — report the pair; baseline 37458 of 37888.
- `new.md` line count by the guard's own method: `python -c "from
  pathlib import
  Path;r=Path('dcs/workflows/new.md').read_bytes().replace(b'\r\n',b'\n').replace(b'\r',b'\n');print(r.count(b'\n')+(0
  if r.endswith(b'\n') else 1))"` — report the number; baseline 242,
  ceiling 250.
- No-project-fact proof: `grep -rn "vault\|Backlog" dcs/workflows/new.md
  dcs/references/doctrine.md` — expected: no output. Paste the empty
  result or the offending lines.
- `git diff -U2 -- dcs/references/doctrine.md dcs/workflows/new.md` — the
  decisive hunks only, not the files.
- Numbering proof: state explicitly that principle 4 is still principle
  4, that no principle was added or removed, and cite the integrity
  suite's principle-numbering check as PASS in the run above.
- One line stating which sentence in principle 4 you rewrote and
  confirming the word "defects" was restored.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
