<!--
204-TASKING/S2.md -- one file per specialist, transcribed verbatim by
the IC from the Planning Chief's `taskings[]` array (schemas.md #2, chief
plan). The specialist receives exactly this file's content plus the
relevant IAP excerpt -- nothing else. If the tasking is ambiguous, that's
a planning defect to fix during /dcs-plan, not something for the
specialist to resolve by guessing.
-->

# 204 — Tasking S2

**Incident:** decomposition-backlog-routing
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Satisfy 202 criterion 2 — the ESG sweep's mirror disposition.

Edit `dcs/workflows/esg.md` step 2's decision cluster (b) (line 67) ONLY.
Draft:

```
(b) **New intake found** this sweep, each with a proposed type +
    priority. **(v0.6.13)** An item proposed at the lowest tier (`L`) is
    below the bar (`new.md` step 4a): when the project's own `CLAUDE.md`
    documents a lightweight backlog-style surface (doctrine's
    "Relationship to project-specific protocols"), offer routing it
    there — instead of a `REGISTER.md` row — as one of step 3's options
    alongside queueing it. With none documented, queueing is the only
    option, exactly as today.
```

The bar wording is fixed by the plan and must match S1's: lowest tier is
`L`, the surface is only ever "the project's own `CLAUDE.md`", never a
named path. Do not invent a second tier name, a rank threshold, or a
different condition.

## File territory (may edit only within these globs)

- `dcs/workflows/esg.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/new.md`
- `dcs/references/doctrine.md`
- `dcs/references/schemas.md`
- `dcs/VERSION`
- `package.json`
- `tests/**`
- `dcs/templates/**`
- `dcs/hooks/**`
- `agents/**`
- `skills/**`
- `vault/**`
- `.dcs/**`
- `install.ps1`
- `install.sh`
- `C:/Users/4ever/.claude/**`

## Hard constraints

1. Every diff hunk must fall inside step 2's cluster block (lines 61-94
   as the file stands). Do NOT edit step 3 (lines 96-101) — its existing
   "One `AskUserQuestion` round per decision cluster above" already
   carries criterion 2's `AskUserQuestion` requirement. Do NOT edit step
   4's Record bullets (lines 103-124): those lines are the
   separately-registered incident `esg-intake-writeback-gap` and the 201
   explicitly asks that they be left alone. If you conclude the change is
   incomplete without a step-4 write-back instruction, you are right and
   it is deliberate — say so in your return as a note, do not fix it.
2. Never write `vault`, `Backlog.md`, or any path from this repo.
3. `esg.md` ceiling is 250 lines, baseline 152, so headroom is ample —
   but report the number.
4. Use the Edit tool, never PowerShell `Set-Content`/`Out-File`.
5. Do not run `install.ps1`.

## Evidence required in the return

- `python tests/test_doctrine_integrity.py 2>&1 | tail -3` — quote the
  final `N/M passed` line; baseline 86/86. (This is the check that
  verifies your `doctrine's "..."` citation resolves to a real heading.)
- Scope proof: `git diff -U0 -- dcs/workflows/esg.md | grep '^@@'` —
  paste the hunk headers and state that every line number falls inside
  step 2's cluster block, with step 3 and step 4 untouched.
- `git diff -U2 -- dcs/workflows/esg.md` — the decisive hunk only.
- `esg.md` line count: `python -c "from pathlib import
  Path;r=Path('dcs/workflows/esg.md').read_bytes().replace(b'\r\n',b'\n').replace(b'\r',b'\n');print(r.count(b'\n')+(0
  if r.endswith(b'\n') else 1))"` — baseline 152, ceiling 250.
- No-project-fact proof: `grep -n "vault\|Backlog"
  dcs/workflows/esg.md` — expected: no output.
- One line confirming the tier name you wrote is `L` and the surface is
  referenced only as documented in the project's own `CLAUDE.md`.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
