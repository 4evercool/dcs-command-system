# 204 — Tasking S2

**Incident:** intake-nudge-telemetry
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Add telemetry verification tests to `tests/test_dcs_intake.py` per acceptance criterion 5. Add tests that:
(a) Verify a telemetry line is written to `<testproj>/.dcs/esg/intake-telemetry.log` for the no-active-incident branch with `event_type` `"nudge_offered"`
(b) Verify a telemetry line is written for the active-incident branch with `event_type` `"active_reported"`
(c) Verify the log is appended to (not overwritten) across successive invocations — count lines before and after a second run
(d) Verify each record is valid JSON with keys: `timestamp` (ISO8601 string), `session_id` (12 hex chars), `event_type` (one of the two literals), `project_root` (string matching the test project path)
(e) Verify fail-open: create a directory at `<testproj>/.dcs/esg/intake-telemetry.log` so the `open()` for append raises `IsADirectoryError`, run the hook, confirm it still emits the advisory text to stdout and exits 0

Note: the log path is `.dcs/esg/intake-telemetry.log`, matching S1's implementation. The `.dcs/esg/` directory is already gitignored.

## File territory (may edit only within these globs)

- `tests/test_dcs_intake.py`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/**`
- `agents/**`
- `bin/**`
- `skills/**`
- `vault/**`

## Worktree root

`C:\DCS-wt\intake-nudge-telemetry`

## Evidence required in the return

- `python tests/test_dcs_intake.py` — full test output, all tests including the new telemetry tests must pass (expected: at least 14 passed, up from current 10)
- `grep -n 'intake-telemetry\|telemetry\|event_type\|IsADirectoryError\|json.loads' tests/test_dcs_intake.py` — show the key test logic added (not the full file, just the structural additions)

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
