# 204 — Tasking S1

**Incident:** intake-nudge-telemetry
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Add fail-open telemetry logging to `dcs/hooks/dcs_intake.py` per acceptance criteria 1-4. Create a `record_telemetry()` function that appends one JSON line to `.dcs/esg/intake-telemetry.log` under the project root. The record must contain: ISO8601 UTC timestamp (`datetime.now(timezone.utc).isoformat()`), session-id prefix (first 12 hex chars of SHA-256 over `session_id + '|' + project_root` — the same hash input `already_seen()` computes), `event_type` (`"nudge_offered"` or `"active_reported"`), `project_root` (as a string). Call `record_telemetry()` before `emit()` in both branches of `main()`. Catch `OSError` during the file append — the write must fail open and the hook must still `emit()` its advisory text.

Note: the log path is `.dcs/esg/intake-telemetry.log`, NOT `.dcs/intake-telemetry.log`. The `.dcs/esg/` directory is already gitignored (verified with `git check-ignore`).

## File territory (may edit only within these globs)

- `dcs/hooks/dcs_intake.py`

## Forbidden zones (explicitly, even if it seems related)

- `tests/**`
- `dcs/workflows/**`
- `dcs/references/**`
- `agents/**`
- `bin/**`
- `skills/**`
- `vault/**`

## Worktree root

`C:\DCS-wt\intake-nudge-telemetry`

## Evidence required in the return

- `grep -n 'record_telemetry\|def record_telemetry' dcs/hooks/dcs_intake.py` — show the function definition and both call sites (before `emit()`)
- `grep -A6 'def record_telemetry' dcs/hooks/dcs_intake.py` — show the JSON record construction with all four fields (timestamp, session_id, event_type, project_root)
- `grep -A2 -B1 'OSError\|except.*Error' dcs/hooks/dcs_intake.py` — show the fail-open guard around the file write (must not catch broad `Exception`, only write-path errors)
- `python -c "import ast; ast.parse(open(r'C:\DCS-wt\intake-nudge-telemetry\dcs\hooks\dcs_intake.py').read()); print('syntax OK')"` — confirm the file parses

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
