<!--
204-TASKING/S3.md -- one file per specialist, transcribed verbatim by
the IC from the Planning Chief's `taskings[]` array (schemas.md #2, chief
plan).
-->

# 204 — Tasking S3

**Incident:** prompt-vs-schema-drift
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Добавить две новые механические проверки в `tests/test_doctrine_integrity.py`:

(a) **check 20 — inbound field-presence guard**: для каждого workflow-файла, выполняющего spawn агента, определить секцию схемы этого агента и проверить, что ВСЕ обязательные поля из этой секции присутствуют в тексте workflow (в backtick-контексте, т.е. как \`fieldname\`).

(b) **check 21 — outbound missing-required-fields guard**: обойти `.dcs/incidents/*/` в поисках файлов, содержащих JSON-блоки, похожие на возвраты агентов (SAFETY.md, AAR.md, 214-LOG.md). Для каждого найденного JSON сверить обязательные поля с соответствующей секцией схемы и выдать named case с расхождением. Проверка НЕ должна фейлить suite при найденных расхождениях (informational/advisory) — исторические артефакты содержат задокументированный pre-existing drift (criterion 6).

Дополнительно: прочитать актуальные line counts dcs/workflows/{new,plan,execute}.md (из evidence S1 или самостоятельно) и, если они превысили текущие потолки в WORKFLOW_GRANDFATHERED_LINES, обновить соответствующие записи в test_doctrine_integrity.py (измерить актуальные значения, установить ceiling = измеренное + margin 5). Соблюдать критерии 4, 5, 6 из 202.

## File territory (may edit only within these globs)

- tests/test_doctrine_integrity.py

## Forbidden zones (explicitly, even if it seems related)

- dcs/workflows/**
- agents/**
- dcs/references/**
- dcs/hooks/**

## Evidence required in the return

- `python tests/test_doctrine_integrity.py 2>&1 | tail -3` — показать финальную строку с N/M passed (должно быть все зелёные, новые проверки входят в общее количество)
- `grep -A30 'check 20' tests/test_doctrine_integrity.py | head -40` — подтвердить, что check 20 существует и содержит логику inbound-валидации
- `grep -A30 'check 21' tests/test_doctrine_integrity.py | head -40` — подтвердить, что check 21 существует и содержит логику outbound-валидации на исторических артефактах
- `grep -n 'WORKFLOW_GRANDFATHERED_LINES' tests/test_doctrine_integrity.py` — показать актуальные ceiling-значения после корректировки

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a
`proposal` (a recommendation, not an action). The IC will re-enter
planning around your finding.
