<!--
204-TASKING/S2.md -- one file per specialist, transcribed verbatim by
the IC from the Planning Chief's `taskings[]` array (schemas.md #2, chief
plan).
-->

# 204 — Tasking S2

**Incident:** prompt-vs-schema-drift
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Добавить поле `esg_activation` во все четыре JSON-примера в `<command_points>` устава `agents/dcs-commander.md`:
- typing (строка ~30): добавить после `open_questions`
- iap_review (строка ~42): добавить последним полем перед `}`
- deviation (строка ~53): добавить последним полем перед `}`
- verdict_disposition (строка ~82): добавить последним полем перед `}`

Значение: `null` для примеров без ESG-активации. Формат: отдельное поле с запятой, не ломающее существующий JSON. Соблюдать критерий 3 из 202.

## File territory (may edit only within these globs)

- agents/dcs-commander.md

## Forbidden zones (explicitly, even if it seems related)

- dcs/workflows/**
- tests/**
- dcs/references/**
- agents/dcs-situation-analyst.md
- agents/dcs-ops-specialist.md
- agents/dcs-safety-officer.md
- agents/dcs-planning-chief.md
- agents/dcs-logistics-chief.md

## Evidence required in the return

- `grep -c '"esg_activation"' agents/dcs-commander.md` — должно вернуть минимум 5 (таблица output_contract + 4 JSON-примера; строка prose-упоминания на 108 тоже содержит токен, итого >=5)
- `python -c "import re,json; text=open('agents/dcs-commander.md').read(); blocks=re.findall(r'\x60\x60\x60json\n(.*?)\x60\x60\x60', text, re.S); [json.loads(b) for b in blocks]; print(f'{len(blocks)} JSON blocks parsed OK')"` — все JSON-блоки в commander.md парсятся без ошибок

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a
`proposal` (a recommendation, not an action). The IC will re-enter
planning around your finding.
