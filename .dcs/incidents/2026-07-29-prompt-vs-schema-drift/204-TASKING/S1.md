<!--
204-TASKING/S1.md -- one file per specialist, transcribed verbatim by
the IC from the Planning Chief's `taskings[]` array (schemas.md #2, chief
plan).
-->

# 204 — Tasking S1

**Incident:** prompt-vs-schema-drift
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Modify three workflow files:
(a) In each agent spawn prompt, pass the inline schema contract from schemas.md:
  - new.md step 3 (situation-analyst) — schema #1: summary, evidence[], affected_files[], repro_path, prior_art
  - plan.md step 3 (planning-chief + logistics-chief) — schema #2: objectives_feedback, tactics[], taskings[], partition_ok, risks[], verification_plan; schema #3: deploy_path, env_deps[], migration_ordering, rollback_plan, risks[]
  - execute.md steps 4 and 8 (ops-specialist + safety-officer) — schema #4: status, files_touched[], tests_run[], evidence, deviation; schema #5: verdict, refutations[], advisories[], checked[]
(b) After each agent return collection, add JSON-block validation: check for structural JSON, required fields per schema, no fields outside schema. Missing required field = deviation. Comply with 202 criteria 1 and 2.

## File territory (may edit only within these globs)

- dcs/workflows/new.md
- dcs/workflows/plan.md
- dcs/workflows/execute.md

## Forbidden zones (explicitly, even if it seems related)

- agents/**
- tests/**
- dcs/references/**
- dcs/hooks/**

## Evidence required in the return

- grep -n 'summary.*evidence.*affected_files.*repro_path.*prior_art' dcs/workflows/new.md
- grep -n 'objectives_feedback.*tactics.*taskings.*partition_ok.*risks.*verification_plan' dcs/workflows/plan.md
- grep -n 'deploy_path.*env_deps.*migration_ordering.*rollback_plan.*risks' dcs/workflows/plan.md
- grep -n 'status.*files_touched.*tests_run.*evidence.*deviation' dcs/workflows/execute.md
- grep -n 'verdict.*refutations.*advisories.*checked' dcs/workflows/execute.md
- python -c "raw=open('dcs/workflows/new.md','rb').read().replace(b'\r\n',b'\n'); n=raw.count(b'\n'); print('new.md:', n+1 if raw and not raw.endswith(b'\n') else n, 'lines')"
- python -c "raw=open('dcs/workflows/plan.md','rb').read().replace(b'\r\n',b'\n'); n=raw.count(b'\n'); print('plan.md:', n+1 if raw and not raw.endswith(b'\n') else n, 'lines')"
- python -c "raw=open('dcs/workflows/execute.md','rb').read().replace(b'\r\n',b'\n'); n=raw.count(b'\n'); print('execute.md:', n+1 if raw and not raw.endswith(b'\n') else n, 'lines')"

## On discovering the plan does not fit reality

STOP. Return status: "deviation" per schemas.md #4 with found, why_plan_wrong, and proposal.
