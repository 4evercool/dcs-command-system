<!--
204-TASKING/S4.md -- one file per specialist, transcribed verbatim by
the IC from the Planning Chief's `taskings[]` array (schemas.md #2, chief
plan). Two containment test cases added explicitly, mirroring S1's tasking,
per command point 2's binding ruling that the Safety Officer will probe
both. No path corrections needed -- this tasking never hardcoded dcs_log.py's
directory.
-->

# 204 — Tasking S4

**Incident:** log-append-helper
**Period:** 1
**Specialist:** dcs-ops-specialist (S4)

## Task

Own all new test coverage (202 criteria 8, and the mechanical proof of 4 and 5).

**FOUR DELIVERABLES.**

**(1) `tests/test_dcs_log.py`** — new file, following `tests/test_dcs_gate.py`'s house style (stdlib-only, its own `check()`/counter, prints its own `N/M passed` as the last line, non-zero exit on failure). Import `dcs_log.py` by the same guarded `spec_from_file_location` pattern check 23 uses (`test_doctrine_integrity.py:2355-2364`) so a missing module degrades to a NAMED RED finding instead of crashing before the count prints — S1 lands in parallel with you and you do not control the order. Cover, at minimum:
- stdlib-only imports;
- round-trip through the imported `dcs_gate.sentinel_of()` for all three sentinel kinds and the no-sentinel case;
- the timestamp is offset-aware, second-resolution, and moves between two calls;
- no flag or environment variable can set it (set junk `DCS_TIMESTAMP`/`SOURCE_DATE_EPOCH`/`TZ` and assert the stamp is still real and offset-aware);
- `--sentinel stamp` with a non-hex body is REFUSED; missing `--by` is refused;
- a file with no trailing newline still gets an entry at column zero;
- the appended entry is counted by `dcs_gate.py --halt-count` identically to a hand-written entry of equivalent content (criterion 2's stated proof, as a permanent regression test);
- **containment, two explicit cases (command point 2, binding):** a slug containing a path separator (`/` or `\`) or a `..` component is REJECTED before resolution; and `append` REFUSES when the resolved incident directory or its `214-LOG.md` does not already exist. Both as permanent regression tests, not one-off manual checks.

**(2) Criterion-6 coverage** in `tests/test_doctrine_integrity.py` as a new numbered section, same guarded-import + degeneracy-guard shape as check 23. Test S2's PURE `log_order_findings()` directly (no 9-artifact fixture needed for the decision logic), plus the IO half against on-disk fixtures. READ `ri.DUPLICATE_TIMESTAMP_THRESHOLD` and `ri.LOG_ORDER_EFFECTIVE_DATE` from the imported module and build your expectations from them — never retype 3 or the date. Cover: clean; a run of exactly THRESHOLD identical brackets (red) and THRESHOLD-1 (clean — the boundary is the whole point of picking N); two adjacent entries reversed (red); legacy unparseable brackets (note, never a finding, never a crash); a mixed naive/aware pair (note, never a `TypeError`); date scope, all three dispositions.

**(3) Fixtures** under `tests/fixtures/log-order/`, EXACTLY these names — S2's evidence cites them and the date prefixes drive the date-scope testing: `2026-08-05-clean/`, `2026-08-05-duplicate-stamps/`, `2026-08-05-out-of-order/`, `2026-08-05-legacy-unparseable/`, `2026-08-01-before-effective-date/`, `no-date-prefix/`. Each holds a `214-LOG.md`. Add a degeneracy guard asserting the fixture population is non-empty and covers every named scenario, exactly as check 23(i) does (`test_doctrine_integrity.py:2371-2382`) — without it every case below passes vacuously.

**(4) Two new merge-guard checks** in `tests/test_doctrine_integrity.py`:
- **(4a)** the workflow append-site check that makes criterion 4 mechanical and permanent. Walk `dcs/workflows/*.md`, find every append-instruction line by a published pattern, require each to name `dcs_log.py`, with exactly two published exclusions (`new.md`'s template initialization, `plan.md`'s preservation-map attachment) and a non-empty-population degeneracy guard. Name any offender by file and line.
- **(4b)** the carrier case: `dcs_log.INVOCATION` and `dcs_log.FORMAT_LINE`, READ FROM THE IMPORTED MODULE and never retyped, must appear whitespace-normalised in `dcs/references/forms.md`. Copy the shape from `test_doctrine_integrity.py:2658-2672` (`record_integrity`'s carrier case), including its guarded `hasattr` fallback.

**CONTROL RUNS (criterion 8's "not passing vacuously").** Your first run happens before S1 and S2 land, and that red-by-name output IS your control for the guarded-import cases — record it. For the decision logic, the control is a swap: run each violation case against the CLEAN fixture's log and record that the assertion fails. Do this in the scratchpad; never edit S1's or S2's file to break it.

**DO NOT:** edit `tests/test_dcs_gate.py`, `tests/test_dcs_intake.py`, `tests/payload_check.py`, `tests/release_provenance_check.py`, `tests/test_dcs_cli.py`, or any existing fixture directory. Do not add anything to `WORKFLOW_GRANDFATHERED_LINES` — if a workflow is over budget that is S3's deviation to raise, not a ceiling to raise here.

## File territory (may edit only within these globs)

- `tests/test_dcs_log.py`
- `tests/test_doctrine_integrity.py`
- `tests/fixtures/log-order/**`

## Forbidden zones (explicitly, even if it seems related)

- `tests/test_dcs_gate.py`
- `tests/test_dcs_intake.py`
- `tests/test_dcs_cli.py`
- `tests/payload_check.py`
- `tests/release_provenance_check.py`
- `tests/fixtures/halt-ceiling/**`
- `tests/fixtures/preservation-map/**`
- `tests/fixtures/record-integrity/**`
- `tests/fixtures/verdict-rerun/**`
- `dcs/**`
- `.dcs/**`
- `package.json`
- `dcs/VERSION`
- `CHANGELOG.md`

## Worktree root

`C:\DCS-wt\log-append-helper`

## Evidence required in the return

- `python tests/test_dcs_log.py` final line (`N/M passed`) and, if red, the named failing cases only.
- `python tests/test_doctrine_integrity.py` final line, plus the names of your new checks as they print (one line each).
- The RECORDED CONTROL RUN from before S1/S2 landed: the red-by-name output of your guarded-import cases, three or four lines. This is criterion 8's non-vacuity proof for the import-dependent half.
- The SWAP CONTROL for the decision logic: for each of duplicate-stamps and out-of-order, the failing assertion line produced when the case is pointed at the clean fixture's log. Two lines of output plus one sentence naming what you swapped.
- The boundary pair explicitly: the output of your THRESHOLD and THRESHOLD-1 cases side by side, plus the `file:line` in `dcs/tools/record_integrity.py` where you read `DUPLICATE_TIMESTAMP_THRESHOLD` from.
- For check (4a): its output when you deliberately point it at a scratchpad copy of a workflow with one append instruction reverted to hand-written — the offender named by file and line.
- For check (4b): the `INVOCATION` and `FORMAT_LINE` values as read from the imported module, plus the `file:line` in `dcs/references/forms.md` where each matched.
- `ls tests/fixtures/log-order/` output — the six named directories, and confirmation the degeneracy guard fails if one is missing (show it failing once, in the scratchpad).
- The two containment regression tests' pass output by name (slug rejection, missing-directory refusal).
- `git status --porcelain` — nothing outside `tests/test_dcs_log.py`, `tests/test_doctrine_integrity.py`, `tests/fixtures/log-order/`.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.

---

## Fix-tasking (post-halt, period 1, command point 4: fix_taskings, dcs-commander)

**Wait for S1's fix to land first** (sequenced ahead of you this cycle, along with S2 running in parallel with you) — your new cross-component test (below) needs S1's fixed tool to actually run against.

The Safety Officer halted on two things in your territory:

**(1) Check 25 (the workflow append-site check, criterion 4's enforcement) is half-blind.** Its population scanner gates on `if "214-LOG" not in line: continue` (your `test_doctrine_integrity.py:3580`) — but 17 of 22 real `dcs_log.py` invocation lines don't carry a same-line `214-LOG` token, so `close.md`, `new.md` and `run.md` contribute ZERO population and pass vacuously. The single global degeneracy guard (`_wac_total_hits > 0`, your `:3619`) can't detect this because the other files' hits satisfy it. Proven by mutation: stripping all `dcs_log.py` references from those three files left the guard green (232/232).

**Fix:** rebuild the population detection so it finds an append-instruction site without requiring a `214-LOG` token on the same or immediately adjacent physical line — the actual signal is "this line invokes `dcs_log.py append`" or "this line is one of the two published exclusions," not "this line happens to also mention `214-LOG`." Replace the single global degeneracy guard with a **per-file** non-empty-population guard across all six writer files (`new.md`, `plan.md`, `execute.md`, `close.md`, `run.md`, `loop.md`) — each file must independently prove it was actually scanned and found something, so one file going silently blind can never hide behind another file's hits.

**Acceptance bar (this is what "fixed" means, not your own judgment call):** reproduce the Safety Officer's exact mutation — strip `dcs_log.py` references from `close.md`, `new.md`, and `run.md` in a scratchpad copy (revert those write sites to hand-written-shaped text) — and confirm your rebuilt check now goes RED, naming the offending files/lines. Then confirm the real, current tree (with the actual conversions intact) stays GREEN.

**(2) No test ever fed criterion 6 a log the real tool wrote** — every existing criterion-6 test uses synthetic stamp lists or hand-authored fixtures, which is exactly the gap that let refutation 2 (S1/S2's clock-precision interaction) through undetected. Add the missing cross-component regression test: run 5 or more sequential appends through the real, fixed `dcs_log.py` (after S1's fix lands) into a fixture directory dated after the effective date, then run criterion 6 against that log — must yield **zero findings**. Pair it with a control: a hand-backfilled fixture carrying genuinely identical timestamps (constructed directly, not through the tool) at the new precision must still yield a finding — proving the fix didn't just weaken the criterion into never firing.

**Two more items, both advisories from the verdict, both in your territory:**
1. Check 26 (the `INVOCATION`/`FORMAT_LINE` carrier check) currently binds only `dcs/references/forms.md`. S3 already correctly quoted both constants in `dcs/templates/214-LOG.md` too (verbatim, verified) — extend check 26 to cover both files, so the template can't silently drift from the tool's real constants later.
2. The `("new.md", "template")` entry in `_WAC_EXCLUSIONS` is currently dead — under the old buggy pattern, `new.md` yields zero hits, so the exclusion never fires. Once your rebuilt pattern is in place, re-confirm this exclusion actually fires (i.e. `new.md`'s legitimate template-initialization line is found by the scanner and correctly excused). If it still never fires under the fixed pattern, delete the dead entry rather than ship an untriggered exemption.

**Territory, forbidden zones, and worktree root are unchanged from your original tasking above.**

### Additional evidence required for this fix

- The rebuilt population-detection logic: `file:line`, plus a plain-English description of what signal it actually keys on now.
- The Safety Officer's exact mutation reproduced against your fix: red-with-named-offenders on the stripped scratchpad copy, green on the real tree. Paste both outcomes.
- The new cross-component test: its `file:line`, the fixture(s) it uses, and its pass output — zero findings on the tool-written log, a finding on the hand-backfilled control.
- Check 26 extended: confirm it now checks both `forms.md` and `dcs/templates/214-LOG.md`, with output showing both pass.
- The dead-exclusion resolution: either proof it now fires (a case where `new.md`'s template line is correctly excused) or confirmation it was deleted.
- `python tests/test_doctrine_integrity.py` full tail (`N/M passed`) — must be genuinely 100% green this time, not 231/232 or any other partial count.
- `python tests/test_dcs_log.py` tail, unaffected by your test_doctrine_integrity.py changes.
- `git status --porcelain` — confirm still only your three declared paths.

Return your structured JSON per schemas.md #4: `status`, `files_touched`, `tests_run`, `evidence`, `deviation`.

---

## Advisory fold-in (post PASS, period 1, execute.md step 9's "advisories on a pass" — not a halt, not a command point, folding into the integration commit)

The Safety Officer's third pass passed — the recurring defect class is closed, all four of its historical falsifying mutations now correctly go RED. It then devised 7 of its own further mutations, adversarially: 3 were caught, 5 are yours to fix as forward-hardening (none dispute anything about the current, real 22-site population — these are all about not-yet-existing future hand-written sites).

1. **The negative scan only walks `_WAC_WRITER_FILES` (6 files)**, not all of `dcs/workflows/*.md`, so a future append-shaped line added to `deploy.md`, `esg.md`, `init.md`, or `status.md` would be invisible. Widen the negative scan to iterate all `dcs/workflows/*.md` — keep the count-manifest bound to the six writers (that part is correct as-is), but the negative scan should cover the whole directory. Verify the four non-writer files currently have zero `214-LOG` append-shaped lines (they do, per the officer's own measurement) before and after your change, so you're not accidentally introducing a false positive on files that are legitimately silent.
2. **The negative scan's look-back is exactly one physical line**, so a hand-written instruction wrapped across three or more lines evades it (a two-line wrap IS caught; three or more is not). Widen the look-back to the enclosing markdown paragraph — scan backward to the last blank line — rather than a fixed one-line window.
3. **The compliance shortcut (`if "dcs_log.py" in line: continue`) marks the ENTIRE line compliant**, so a hand-written clause appended onto a line that already invokes the tool is invisible. Scan every `214-LOG` occurrence on a line (not just the first via `line.index()`), and require the `dcs_log.py` mention to fall within the same sentence as the matched keyword, not merely anywhere on the line.
4. **The two published exclusions match as unanchored, case-folded substrings over the whole line**, so any future line merely containing the word "template" (in `new.md`) or "preservation" (in `plan.md`) is silently exempted regardless of context — `new.md` is dense with template prose, so this is a realistic future collision. Anchor each exclusion to its actual published site: match the specific invariant phrase (`new.md`'s "Initialize `214-LOG.md` from the template", `plan.md`'s preservation-map fence) rather than a bare keyword anywhere on the line.
5. **A reverted site can be hidden by editing `_WAC_EXPECTED_SITE_COUNTS` down in the same change** — this folds into the same fix as item 3 above (scanning all occurrences on a line): once the negative scan properly examines every `214-LOG` occurrence rather than just the first, it independently catches the reverted line regardless of what the manifest says, restoring the two-independent-signals property. Confirm this explicitly once item 3 is done — re-run the officer's exact mutation (revert one site AND edit its file's manifest count down by one in the same change) and confirm it now goes red on the negative scan alone.

**Acceptance bar:** re-run ALL of the Safety Officer's mutations from this pass (the four historical ones it re-confirmed, plus its own seven) and report the disposition of each. The four historical ones and the three it already caught must stay caught; the five now-fixed ones (1 through 5 above, noting 3 and 5 are the same fix) must flip from green to red. The real, current tree must stay fully green throughout.

**Territory, forbidden zones, and worktree root are unchanged from your original tasking above.** This fix touches only `tests/test_doctrine_integrity.py`.

### Evidence

- Before/after of the negative scan's file-iteration (item 1), look-back logic (item 2), and occurrence-scanning (items 3/5), each with `file:line`.
- The exclusion-anchoring rewrite (item 4), with `file:line` and the specific invariant phrases you anchored to.
- Each of the officer's own 7 mutations reproduced against your fix, with disposition (still-caught or now-caught) named individually.
- The four historical mutations re-confirmed still caught (do not skip this — regression risk from touching the same functions).
- `python tests/test_doctrine_integrity.py` full tail — genuinely 100% green.
- `python tests/test_dcs_log.py` tail — unaffected, still 100% green.
- `git status --porcelain` — confirm still only your declared paths.

Return your structured JSON per schemas.md #4: `status`, `files_touched`, `tests_run`, `evidence`, `deviation`.

---

## Second fix-tasking (post SECOND halt, period 1, command point 4: fix_taskings, dcs-commander)

Your first fix-tasking (rebuilding check 25 with a two-signal detector, the cross-component test, extending check 26, resolving the dead exclusion) was NOT sufficient — the Safety Officer's second adversarial pass found the rebuild only narrowed the same defect class rather than closing it. **This is a same-class recurrence, and per doctrine the fix must raise the altitude — a third pattern tweak is explicitly not acceptable.**

**What was still wrong, verified independently by dcs-commander in source, not just claimed by the Safety Officer:** signal (A) in your rebuilt detector (`"dcs_log.py" in line`) makes a line population AND automatically compliant in the same step — the compliance window starts at the hit line itself, so an (A) hit can never be flagged as a violation. Only the old signal (B) (214-LOG-token adjacency) can ever produce a violation, and 17 of 22 real converted sites still lack that token. Worse: `_wac_hits_by_file` counts hits BEFORE exclusions are applied, so `new.md`'s and `plan.md`'s per-file degeneracy guards are held up SOLELY by their one published-exclusion line — a line the compliance loop unconditionally skips, meaning those guards are structurally incapable of ever catching a real violation in those two files. Proven by four fresh mutations, ALL of which your rebuilt check missed: stripping every `dcs_log.py` mention from `new.md` — still green; stripping every mention from `plan.md` — still green; reverting ONE site in `plan.md` to hand-written-shaped text — still green; reverting ONE site in `execute.md` — still green, even though `execute.md` is one of the files the check is supposed to actively enforce.

**The raised-altitude fix (three structural changes, not a fourth pattern variant):**

1. **Pin the population OUTSIDE the guarded text.** Add an explicit, hand-maintained manifest — a module-level constant, e.g. `_WAC_EXPECTED_SITE_COUNTS = {"new.md": 1, "plan.md": 8, "execute.md": 9, "close.md": 2, "run.md": 1, "loop.md": 1}` (verify and correct these exact counts yourself against the real, current tree — do not trust these example numbers) — naming exactly how many real `dcs_log.py` invocation sites each file is expected to contain. The check compares the ACTUAL discovered count per file against this manifest and goes red on ANY mismatch, not just zero. A deliberate future edit that legitimately changes a count must update the manifest in the same commit — this is a feature, not friction: it forces conscious attention exactly where automatic detection is what failed twice.
2. **Decouple the compliance predicate from population membership.** Build an independent, corpus-wide NEGATIVE scan: search all six files for the shape of a hand-written append instruction (the original signal-B-style pattern: prose naming `214-LOG.md` in an imperative append/record/log context) and treat ANY match as a violation UNLESS that exact line is one of the two published exclusions — apply the exclusions at scan time, and never let an excluded line count toward satisfying any degeneracy guard (this is what let the old guard hide behind `new.md`'s and `plan.md`'s exclusion lines). This negative scan and the count-manifest from (1) are two independent checks that must BOTH pass — a line can be miscounted by one and still caught by the other.
3. **Encode the Safety Officer's four mutations as permanent, in-memory sensitivity self-tests.** Add test cases that construct each of the four mutated scenarios in memory (never touching the real files) and assert your check's core function returns a violation for each: (a) all `dcs_log.py` mentions stripped from `new.md`, (b) all stripped from `plan.md`, (c) one site in `plan.md` reverted to hand-written shape, (d) one site in `execute.md` reverted to hand-written shape. **This is what makes the defect class unrepresentable going forward** — a future rebuild of this check cannot ship green unless it defeats these four specific, permanently-recorded attacks, not just today's specific implementation.

**Acceptance bar:** all four of the Safety Officer's original mutations, reproduced exactly as before, must now go RED with named offenders. The real, current tree must stay GREEN. Your new sensitivity self-tests (item 3) must themselves be run and shown passing — proving the check catches its own historical failure modes, permanently.

**Also this round — the concurrency stress test.** S1 has now landed a fix for the Safety Officer's second refutation: `dcs_log.py`'s full read-then-write critical section is serialized under a stdlib cross-process lock, with a bounded-retry timeout that exits non-zero on failure to acquire. Add a concurrent-append stress test to `tests/test_dcs_log.py`: **8 or more simultaneous `dcs_log.py append` invocations against one fixture log, asserting every entry lands exactly once and no invocation reports success (exit 0) for bytes that did not actually survive to disk.** This is the permanent test that makes S1's fix "enforced by a test, not asserted by prose," per dcs-commander's own directive. Read `dcs/tools/dcs_log.py` fresh to see exactly what S1 changed (its evidence will state file:line and the lock mechanism chosen) — do not assume the implementation, verify it.

**Also fold in, both deterministic, both cheap:**
1. **Section renumbering.** Your four new `test_doctrine_integrity.py` sections from the first fix-tasking round are numbered 24-27, colliding with four PRE-EXISTING sections also numbered 24-27 (at different line numbers). Renumber your four sections to 28-31 (or whatever the next free block actually is — verify with `grep -n "^# --- [0-9]*\." tests/test_doctrine_integrity.py` first, do not assume 28-31 is correct without checking). Record the old-to-new number mapping in your return so the incident's own paper trail (which has been citing "check 25"/"check 26" throughout) stays traceable.
2. **`_SUBSECOND_RE` flake.** Your assertion at `tests/test_dcs_log.py:277-286` requires exactly 6 fractional digits, but `datetime.isoformat()` omits the fractional field entirely when microseconds happen to be exactly zero — a real, if rare, flake, not yet observed on this machine but not deterministically prevented either. Fix at the SOURCE: have `dcs_log.py` call `.isoformat(timespec="microseconds")` instead of bare `.isoformat()`, so the fractional field is unconditionally present — this is the commander's preferred direction ("makes the assertion unconditional rather than probabilistic") over loosening the test's regex. **This touches `dcs_log.py`, S1's territory, not yours — flag it to the IC rather than editing it yourself if S1's already-landed fix doesn't already do this** (check first; S1's fix-tasking for this round did not explicitly ask for `timespec="microseconds"`, so verify what's actually there before assuming either way).

**Territory, forbidden zones, and worktree root are unchanged from your original tasking above.**

### Evidence required for this fix

- The count-manifest (item 1): its exact values, and confirmation you verified each against the real current tree (not copied from this tasking's example numbers).
- The negative scan (item 2): `file:line`, and confirmation exclusions are applied at scan time and never counted toward any degeneracy guard.
- The four sensitivity self-tests (item 3): `file:line` for each, and their pass output.
- All four of the Safety Officer's original mutations re-run against your new check: red-with-named-offenders on each mutated copy, green on the real tree.
- The concurrent-append stress test: `file:line`, its output (8+ processes, exact-once landing confirmed), and what you found when you read S1's actual lock implementation.
- Section renumbering: the grep output you used to find the real next-free block, the old-to-new mapping, and confirmation nothing else in the file still cites the old numbers.
- The `_SUBSECOND_RE` finding: what you found when you checked S1's landed code, and either confirmation it's already fixed at the source or an explicit flag to the IC that it still needs S1's attention.
- `python tests/test_doctrine_integrity.py` full tail — genuinely 100% green.
- `python tests/test_dcs_log.py` full tail — genuinely 100% green, including the new stress test.
- `git status --porcelain` — confirm still only your three declared paths.

Return your structured JSON per schemas.md #4: `status`, `files_touched`, `tests_run`, `evidence`, `deviation`.
