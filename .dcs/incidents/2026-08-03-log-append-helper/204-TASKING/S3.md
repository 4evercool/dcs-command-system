<!--
204-TASKING/S3.md -- one file per specialist, transcribed verbatim by
the IC from the Planning Chief's `taskings[]` array (schemas.md #2, chief
plan). Two path corrections applied after command point 2 moved the tool's
target directory from dcs/hooks/ to dcs/tools/ -- everything else
unchanged.
-->

# 204 — Tasking S3

**Incident:** log-append-helper
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Convert every hand-written 214-LOG.md append instruction in the workflows to the tool, and document the canonical format (202 criteria 4, 5). This is the tasking with the tightest mechanical constraints in the incident — read all four before editing anything.

**ENUMERATE THE POPULATION YOURSELF, do not trust the plan's list.** Run `grep -nE '(append|Append|record|Record|log it|Log the)[^.]{0,120}214-LOG' dcs/workflows/*.md` and reconcile against `grep -n 214-LOG dcs/workflows/*.md`. Measured at plan time, the genuine WRITE sites are: `execute.md` 54, 112, 115, 156, 169-172, 192-196, 223, 232; `plan.md` 25, 31, 63, 64, 71, 78, 96, 226, 239-242; `close.md` 206, 215; `new.md` 130, 247; `run.md` 177; `loop.md` 53-54. `status.md` has NO write sites (it states "No writes" at :92 and :98) — do not edit it.

**TWO PUBLISHED EXCLUSIONS**, which stay hand-written and which you must name in the prose so the next reader knows they are deliberate, not missed: (a) `new.md`:216 — initializing `214-LOG.md` from the template CREATES the file; the tool appends to an existing one. (b) `plan.md`:216 — the preservation map is a multi-line fenced JSON attachment indented off column zero, not a one-line entry. S4's new merge-guard check will carry exactly these two exclusions; if you need a third, that is a deviation.

**HARD CONSTRAINT 1 — THE LINE BUDGET**, measured at plan time with the guard's own counting semantics: `close.md` 250/250, `execute.md` 250/250, `plan.md` 249/250, `new.md` 263/270 (grandfathered), `run.md` 187, `loop.md` 170. Two files have ZERO margin and one has one line. Every edit must be net-line-neutral or NEGATIVE per file. The conversion naturally helps: replacing a three-line fenced block (`Append to 214-LOG.md:` + fence + line + fence) with one backticked invocation line gains you three. Count with the guard's `_workflow_line_count` semantics (`tests/test_doctrine_integrity.py:1661-1673`), not `wc -l` — they differ by one on a file with no trailing newline.

**HARD CONSTRAINT 2 — CHECK 12 HAS THREE TRAPS HERE.** (i) Its population is DISCOVERED by walking `dcs/**/*.md` for the three sentinel token literals, and every member must quote `GRAMMAR_LINE` verbatim (`test_doctrine_integrity.py:758-780`). Today's members among your files are `execute.md`, `plan.md`, `run.md`, `forms.md`, `templates/214-LOG.md`. Do NOT introduce a sentinel token literal into `close.md`, `new.md` or `loop.md` — it would drag them into the population and force a `GRAMMAR_LINE` quote you have no line budget for. Use `--sentinel halt|pass|stamp` in every invocation; that is why the flag exists. (ii) Every fenced-code line containing a sentinel token must be boundary-valid per `sentinel_of()` (:782-806). An invocation line like `dcs_log.py append <slug> "SAFETY-HALT: ..."` inside a fence is NOT boundary-valid and reddens the guard. (iii) `dcs/references/forms.md`'s fenced example block at `forms.md:43-50` contains the ONLY line in the entire population that `sentinel_of()` classifies as `'stamp'` with a real hex argument — check 12(i) (:842-849) requires that witness to exist. KEEP that fenced block of rendered example entries intact, hex and all; add the tool invocation alongside it, never in place of it.

**DOCUMENTATION (criterion 5).** In `dcs/templates/214-LOG.md` and `dcs/references/forms.md`: state the canonical format including the operator-identity field, quoting S1's exported `FORMAT_LINE` VERBATIM (whitespace-normalised equality is what S4's check will assert, so a markdown hard-wrap is fine but a paraphrase is not), and quote S1's exported `INVOCATION` verbatim in `forms.md` — that is the carrier binding matching what `plan.md` does for `preservation_map.py` and `close.md` does for `record_integrity.py`. Keep `GRAMMAR_LINE` byte-exact in both files. Update `forms.md`'s example block so the entries show the ` -- op: <operator>` tail. Do not touch `dcs/references/doctrine.md` — criterion 5 requires only that its `GRAMMAR_LINE` quotation survive, and the new rule lands in `forms.md`, which is the artifact-authority document.

**INVOCATION PATH IN WORKFLOWS.** Use the installed-copy form, matching `record_integrity.py`'s and `preservation_map.py`'s precedent in `close.md` and `plan.md`: `python "$HOME/.claude/dcs/tools/dcs_log.py" append <slug> --by <operator> "<text>"`. The `--by` value is the session's own identity in the shape the corpus already uses (e.g. `IC=dcs-commander, fable`).

**WAIT FOR S1'S CONSTANTS.** `INVOCATION` and `FORMAT_LINE` are S1's to define; the plan pins `INVOCATION`'s text (see S1's tasking) so you can start immediately, but re-read `dcs/tools/dcs_log.py` before you finish and quote what is actually there. A drifted quote reddens S4's carrier check.

## File territory (may edit only within these globs)

- `dcs/workflows/execute.md`
- `dcs/workflows/plan.md`
- `dcs/workflows/close.md`
- `dcs/workflows/new.md`
- `dcs/workflows/run.md`
- `dcs/workflows/loop.md`
- `dcs/templates/214-LOG.md`
- `dcs/references/forms.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/status.md`
- `dcs/workflows/init.md`
- `dcs/workflows/deploy.md`
- `dcs/workflows/esg.md`
- `dcs/references/doctrine.md`
- `dcs/references/doctrine-appendix.md`
- `dcs/references/schemas.md`
- `dcs/hooks/**`
- `dcs/tools/**`
- `tests/**`
- `.dcs/**`
- `package.json`
- `dcs/VERSION`
- `CHANGELOG.md`

## Worktree root

`C:\DCS-wt\log-append-helper`

## Evidence required in the return

- The enumeration you ran and its reconciliation: the `grep -nE '(append|Append|record|Record|log it|Log the)[^.]{0,120}214-LOG' dcs/workflows/*.md` output BEFORE and AFTER, as two line-number lists per file (not the full text). Every before-line must appear as converted, or as one of the two published exclusions, with no third category.
- Line-budget table, before and after, for all six workflow files, produced with the guard's own counting semantics: `python -c "from pathlib import Path;[print(p.name, (lambda r: r.count(b'\n') + (1 if r and not r.endswith(b'\n') else 0))(p.read_bytes().replace(b'\r\n',b'\n').replace(b'\r',b'\n'))) for p in sorted(Path('dcs/workflows').glob('*.md'))]"`. Every file must be at or under its ceiling (250, or 270 for `new.md`) and no file you touched may have grown.
- Check-12 self-audit BEFORE you hand back: re-run the population walk and paste (a) the population list, (b) every fenced line containing a sentinel token with its `sentinel_of()` classification, (c) confirmation that `forms.md` still yields at least one `'stamp'`. If you cannot produce this, say so rather than guessing.
- The exact `FORMAT_LINE` and `INVOCATION` strings you quoted, plus `file:line` of each quotation site in the template and `forms.md`, plus the `file:line` in `dcs/tools/dcs_log.py` you read them from — three coordinates proving the quote is copied, not reconstructed.
- One before/after excerpt of the highest-risk conversion (`execute.md`'s halt block at 169-172), showing the fenced sentinel line is gone and the replacement carries no sentinel token literal. Six lines maximum.
- `grep -c 'GRAMMAR_LINE\|An entry begins at column zero' dcs/references/forms.md dcs/templates/214-LOG.md dcs/workflows/execute.md dcs/workflows/plan.md dcs/workflows/run.md` — the verbatim quotation must survive in every file that carries it today.
- `python tests/test_doctrine_integrity.py` tail (`N/M passed`) plus, if red, the named failing check lines only. Baseline at plan time: 196/196. Note S4's new checks may legitimately be red until S4 lands — distinguish a check-12/budget failure (yours) from a missing-new-check failure (not yours) by name.
- `git status --porcelain` — nothing outside your eight files.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
