<!--
204-TASKING/S2.md -- one file per specialist, transcribed verbatim by
the IC from the Planning Chief's `taskings[]` array (schemas.md #2, chief
plan). Forbidden list gained one entry (dcs/tools/dcs_log.py) after
command point 2 moved S1's target into the same directory this file lives
in. One clarifying note added on the date-scope proof, per dcs-commander's
binding instruction at command point 2: real time crossed
LOG_ORDER_EFFECTIVE_DATE mid-incident, so this incident's own live log now
carries entries dated on-or-after the effective date without that making
the log in scope -- the mechanism scopes by directory date, never by
individual entry timestamps.
-->

# 204 — Tasking S2

**Incident:** log-append-helper
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Add close-time criterion 6 to `dcs/tools/record_integrity.py` — duplicate and out-of-order 214-LOG.md timestamps (202 criteria 6, 7). Nothing else in this module changes behaviour.

**STRUCTURE (mirror the criterion-3 split this module already documents at `record_integrity.py:820-833`):**
- `log_order_findings(stamps)` — PURE. Takes an ordered list of `(start_line, bracket_text)` and returns a list of finding strings. No filesystem, no git, no `dcs_gate` import inside it. This is the function S4 unit-tests directly.
- `collect_log_order_findings(incident_dir, dcs_gate)` — IO. Date-scopes, reads `214-LOG.md`, derives the stamps using the EXISTING `split_log_entries(text, re.compile(dcs_gate.ENTRY_PREFIX))` (`record_integrity.py:251-276` — already written and already grammar-faithful; do not re-derive an entry regex), then delegates to the pure function.

**DATE SCOPE.** `LOG_ORDER_EFFECTIVE_DATE = "2026-08-04"` — the day the tool lands, so this incident's own hand-typed stem entries are out of scope and the first incident opened after the tool ships is fully in scope. Reuse `_DIR_DATE_RE` (`record_integrity.py:653`) and reproduce criterion 3's three ALWAYS-PRINTED dispositions verbatim in shape (no parseable date → out of scope; date ≤ effective → out of scope; else in scope). Never a silent skip. **Clarification, binding (command point 2):** the scope test reads the INCIDENT DIRECTORY's own date prefix (this incident: `2026-08-03-log-append-helper`, i.e. its opening date) — never an individual entry's own bracketed timestamp. Real wall-clock time crossed `2026-08-04` while this incident was still open, so several of this log's own entries now carry an `08-04` bracket; that does not and must not put the log in scope. If your implementation or your control-run reasoning drifts toward reading entry timestamps for the scope decision rather than the directory name, that is the exact confusion to avoid.

**DECISION RULES:**
- `DUPLICATE_TIMESTAMP_THRESHOLD = 3` — module constant, exported, because S4's tests READ it rather than retype it. Pinned by the plan so two parallel taskings agree; your job is to record the justification in the code comment. The justification to verify and write: 2 identical stamps are reachable by legitimate second-resolution clock collision when a tool appends twice inside one second; 3 is the smallest count that cannot be, and the observed corpus defects are 11 and 75 (201 evidence). Run your own corpus measurement over `.dcs/incidents/*/214-LOG.md` and record the distribution of same-bracket run lengths — if it contradicts 3, that is a deviation to report, not a value to change silently.
- Duplicates: detected on the RAW bracket string, no parsing at all. A finding names the bracket, the count, and the first and last line numbers.
- Out-of-order: parse each bracket; compare only ADJACENT pairs where both parse AND are mutually comparable. A bracket that does not parse, and a naive/aware pair, are printed as notes and are NEVER findings — measured on Python 3.10.0rc2 in this worktree, `datetime.fromisoformat` rejects the `+0000` form that appears in `dcs_gate.py`'s own SPECIMENS (`dcs_gate.py:220-222`) and a bare-date bracket yields a naive datetime whose comparison to an aware one raises `TypeError`. Do not paper over this with a bare `try/except: pass` around the whole loop; classify explicitly.

**WIRING (criterion 7).** Add one `findings.extend(collect_log_order_findings(incident_dir, dcs_gate))` line to `main()`'s existing chain (`record_integrity.py:1105-1110`) — the same place the other five run, not a standalone function. Update the three places that say "five"/"criteria 1-5": the module docstring, `_build_arg_parser()`'s description (`record_integrity.py:1055-1056`), and the clean-exit line (`record_integrity.py:1117-1120`). Do NOT change `INVOCATION` (`record_integrity.py:150`) — it is quoted verbatim in `close.md` and bound by a merge-guard case; changing it reddens the guard from outside your territory.

**PRE-SHIP DRY RUN (binding, Logistics Chief adoption per command point 2).** Before you report done, run your finished criterion against every real, live `.dcs/incidents/*/` directory in this worktree and record what it finds — this is the pre-ship corpus dry-run that catches a false positive before it can strand a future incident behind CLAUDE.md's no-install-mid-incident rule. Report the full disposition list, not just a summary.

**DO NOT:** edit `dcs_gate.py`; edit any test or fixture (S4 owns those — build your own throwaway fixtures in the scratchpad); change any existing criterion's behaviour or finding text; touch `dcs_log.py`.

## File territory (may edit only within these globs)

- `dcs/tools/record_integrity.py`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/hooks/**`
- `dcs/tools/dcs_log.py`
- `dcs/tools/preservation_map.py`
- `dcs/tools/verdict_rerun.py`
- `dcs/workflows/**`
- `dcs/templates/**`
- `dcs/references/**`
- `tests/**`
- `.dcs/**`
- `package.json`
- `dcs/VERSION`
- `CHANGELOG.md`

## Worktree root

`C:\DCS-wt\log-append-helper`

## Evidence required in the return

- `file:line` for `LOG_ORDER_EFFECTIVE_DATE`, `DUPLICATE_TIMESTAMP_THRESHOLD`, `log_order_findings`, `collect_log_order_findings`, and the one added `findings.extend(...)` line in `main()` — criterion 7's proof that it runs where the other five run, not beside them.
- Corpus measurement backing N=3: the command you ran over `.dcs/incidents/*/214-LOG.md` and its output as a distribution of same-bracket run lengths (a table of counts, not the raw scan). State plainly whether it supports 3.
- Three CLI runs against scratchpad fixture dirs named with post-effective dates, each showing the criterion-6 lines only (grep your own output): a clean log (no criterion-6 finding), a log with `DUPLICATE_TIMESTAMP_THRESHOLD` identical brackets (finding, naming the bracket and line span), a log with two adjacent entries reversed (finding, naming both line numbers). Plus the exit code of each.
- Date-scope proof: the same duplicate-stamp fixture placed under a dir dated `2026-08-01` prints the out-of-scope disposition and produces no finding; and a dir with no date prefix prints the no-parseable-date disposition. Two lines of output.
- Legacy-shape proof, and this is the decisive one: run the criterion against the three real logs the 201 names — `.dcs/incidents/2026-07-25-doctrine-hot-path-trim/`, `.dcs/incidents/2026-07-30-worktree-path-propagation/`, `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/` — with the date scope temporarily bypassed (in a scratchpad copy of the module, NOT by editing your file's constant). Paste the criterion-6 output for each. It must not crash on the `+1100` or bare-date brackets, and each unparseable/incomparable case must appear as a note.
- Control run against THIS incident's own live log: `python dcs/tools/record_integrity.py .dcs/incidents/2026-08-03-log-append-helper` — paste the criterion-6 disposition line. It should read out-of-scope, keyed on the directory's `2026-08-03` opening date (see the DATE SCOPE clarification above) — never mind that some entries inside the file are now dated `2026-08-04`. If it reads in-scope or produces a finding, say so loudly, that is a close-blocker.
- The pre-ship corpus dry-run's full disposition list (every `.dcs/incidents/*/` directory, one line each: out-of-scope / no-date / in-scope-clean / in-scope-finding).
- `python tests/test_doctrine_integrity.py` tail (`N/M passed`) — check 23 imports and subprocess-runs your module against the existing `tests/fixtures/record-integrity/*` dirs, which carry no date prefix and must therefore stay unaffected. Baseline at plan time: 196/196.
- `git status --porcelain` showing exactly one modified file.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.

---

## Fix-tasking (post-halt, period 1, command point 4: fix_taskings, dcs-commander)

**Wait for S1's fix to land first** (sequenced ahead of you this cycle) — it changes the fact your own justification comment rests on. S1 is changing `dcs_log.py` from second-precision (`replace(microsecond=0)`) to full sub-second precision, specifically because the Safety Officer proved your `DUPLICATE_TIMESTAMP_THRESHOLD = 3` justification false: 6 real sequential appends through the tool (at the old, second-precision behavior) produced a 5-entry identical-bracket run, honestly, with no backfilling — the opposite of what your comment at `record_integrity.py:1064-1070` claimed was implausible.

**`DUPLICATE_TIMESTAMP_THRESHOLD` stays 3** — the value is not being changed, only the justification and the evidence behind it. Rewrite the comment to rest on the tool's new sub-second precision (cite `dcs_log.py`'s actual line for it) rather than the falsified same-second-collision argument. The corrected reasoning: at sub-second precision, even two rapid honest appends will essentially never share an identical timestamp, so 3 identical timestamps is a legitimate backfill/tampering signal once `dcs_log.py` is the one true writer — cite the actual measurement you take below rather than asserting this from first principles.

**Two more fixes bundled in, both advisories from the Safety Officer's verdict, both in your territory:**
1. `record_integrity.py:1046`'s comment self-contradicts about which date `LOG_ORDER_EFFECTIVE_DATE` represents and why (it names this incident's opening date in one place and a different rationale in another). Reword for internal consistency — the value `2026-08-04` itself is correct and not in question.
2. The corpus run-length distribution comment you wrote (`1:315` at the low end) is already stale — this incident's own live log grew while you were working. Re-run your original corpus command now and either update the figure or note explicitly that it drifts as live logs grow (principle 15 is satisfied either way since the regenerating command sits beside it; just don't leave a number that's already wrong when nothing marks it as approximate).

**Re-run the binding pre-ship dry run** (all live `.dcs/incidents/*/` directories) one more time after your fix, and report the full disposition list again — confirm it's still all-out-of-scope, zero findings, zero crashes.

**Territory, forbidden zones, and worktree root are unchanged from your original tasking above.** This fix touches only `dcs/tools/record_integrity.py`. Do not touch `dcs_log.py` (S1's territory) or any test file (S4's territory).

### Additional evidence required for this fix

- `file:line` of the rewritten justification comment and the two advisory fixes.
- The measurement backing the rewritten justification: run several honest sequential appends through S1's fixed tool and confirm no two share an identical timestamp at the new precision — cite the actual command and output.
- The refreshed corpus distribution figure (or the explicit drift note), with the command that produced it.
- Full pre-ship dry-run disposition list, re-run after your fix.
- `python tests/test_doctrine_integrity.py` tail — must still show your criterion-6 checks passing (S4 is independently fixing checks 25/26 in parallel with you; a red suite at hand-back doesn't necessarily mean your file is at fault — name what's red, don't assume).
- `git status --porcelain` showing exactly one modified file.

Return your structured JSON per schemas.md #4: `status`, `files_touched`, `tests_run`, `evidence`, `deviation`.
