<!--
204-TASKING/F1.md -- fix-tasking issued at command point 4 after attempt
1's Safety halt (see SAFETY.md and the command: verdict -> fix_taskings
entry in 214-LOG.md). IAP.md unchanged -- no re-stamp required (fix
confined to 204-TASKING/* + territory files).
-->

# 204 — Tasking F1 (fix-tasking, period 1, attempt 2)

**Incident:** field-lesson-guard-bare-date-weakening
**Period:** 1
**Specialist:** dcs-ops-specialist (F1)

## Task

Resolve attempt 1's Safety refutation and fold in three of its four
advisories (the fourth is a constraint below). Four edits, nothing else:

(1) REFUTATION FIX — `dcs/references/doctrine-appendix.md:13`: the line
currently reads `Since v0.5.0, every field lesson in this appendix (and
throughout the` (continuing `package) follows one of three citation
forms...`). The v0.5.0 claim is FALSE — the citation convention was
introduced by commit `710cf52` (2026-07-31), whose tree carries
`dcs/VERSION` = `0.7.1`; at v0.5.0 the phrase `predates self-hosting`
existed nowhere in the tree. Change `Since v0.5.0` to `Since v0.7.1`,
keeping the rest of the sentence intact. Verify the truth of the new
claim yourself first:
`git log --format='%h %ad' --date=short -S "one of three citation forms" -- dcs/references/doctrine-appendix.md`
(expect `710cf52 2026-07-31`, single hit) and
`git show 710cf52:dcs/VERSION` (expect `0.7.1`).

(2) ADVISORY 3 FIX — `dcs/references/doctrine-appendix.md:415`: the
clause `per the field lesson below, which predates self-hosting` has an
unnamed referent (five field lessons appear below; two do NOT predate
self-hosting). Name it: `per field lesson W2 below, which predates
self-hosting` — first verify W2 in the "Workflow field lessons" section
is the 2026-07-24 sweep lesson the passage cites and is marked
`predates self-hosting`.

(3) ADVISORY 2 FIX — `dcs/references/forms.md` (the new fourth-sentinel
paragraph, near :64): replace the bare line-number citation
`record_integrity.py:419`-style reference with a regenerating command or
function name, e.g. cite it as
(`grep -n 'RECORD-CORRECTION:' dcs/tools/record_integrity.py`) —
principle 15: a bare line number in a shipped durable artifact rots.
Keep the paragraph's mechanical claims otherwise byte-identical.

(4) ADVISORY 1 — `dcs/references/doctrine-appendix.md:670`: NO EDIT.
The IC's recorded decision (214-LOG.md, command point 4): the `v0.6.9`
there is a TRUE, grammatical, in-sentence identifier; it stays. Do not
touch the line. (Listed so you know it is deliberate, not overlooked.)

CONSTRAINTS: no edit to `dcs/references/doctrine.md` (hot-path slack is
25 B — advisory 4) and none to `dcs/references/schemas.md`, `tests/**`,
or any other file. The two allowlisted heading openings
(`# DCS Doctrine`, `### Field-lesson citation convention`) must remain
untouched from column zero. Use Write/Edit tools only, never PowerShell
Set-Content/Out-File (BOM). English throughout.

## File territory (may edit only within these globs)

- dcs/references/doctrine-appendix.md
- dcs/references/forms.md

## Forbidden zones (explicitly, even if it seems related)

- tests/**
- dcs/references/doctrine.md
- dcs/references/schemas.md
- dcs/workflows/**
- dcs/hooks/**
- dcs/tools/**
- dcs/templates/**
- package.json
- dcs/VERSION
- .dcs/**
- vault/**
- CLAUDE.md

## Worktree root

C:\DCS-wt\field-lesson-guard-bare-date-weakening

## Evidence required in the return

- Truth verification BEFORE the edit:
  `git log --format='%h %ad' --date=short -S "one of three citation forms" -- dcs/references/doctrine-appendix.md`
  and `git show 710cf52:dcs/VERSION` — paste both outputs.
- The three edits as decisive hunks:
  `git diff -U1 -- dcs/references/doctrine-appendix.md dcs/references/forms.md`.
- `grep -n 'Since v0' dcs/references/doctrine-appendix.md` — the :13
  line now reads `Since v0.7.1`.
- `grep -n 'field lesson W2 below' dcs/references/doctrine-appendix.md`
  — one hit at the :415 site.
- `grep -n 'record_integrity' dcs/references/forms.md` — shows the
  regenerating-command form, no bare `:419`.
- Line :670 untouched:
  `git diff -- dcs/references/doctrine-appendix.md | grep -c 'v0.6.9 prose'`
  — must print 0.
- `python tests/test_doctrine_integrity.py 2>&1 | tail -3` and
  `python tests/test_doctrine_integrity.py 2>&1 | grep -i 'field-lesson'`
  — suite green, all field-lesson cases PASS.
- Hot-path unchanged:
  `python -c "import os;p='dcs/references/';a=os.path.getsize(p+'doctrine.md');b=os.path.getsize(p+'schemas.md');print(a,b,a+b,37*1024-(a+b))"`
  — expect `23293 14570 37863 25` (doctrine.md and schemas.md untouched
  by you).
- `git status --porcelain` — your changes confined to the two territory
  files.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action).
The IC will re-enter planning around your finding.
