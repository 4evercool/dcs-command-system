<!--
204-TASKING/S3.md -- transcribed verbatim by the IC from the Planning
Chief's taskings[] (schemas.md #2).
-->

# 204 — Tasking S3

**Incident:** field-lesson-guard-bare-date-weakening
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Document the fourth mechanically-parsed sentinel in shipped prose and
pay for it (criterion 6 prose half, and criterion 7).

(a) `dcs/references/forms.md`: extend the sentinel-convention passage(s)
at :52-81 with running prose introducing `RECORD-CORRECTION:` as a
fourth sentinel that is parsed mechanically but NOT by `sentinel_of()` —
per tactic T6, state that `dcs/tools/record_integrity.py` (:419)
compiles `dcs_gate.ENTRY_PREFIX + r"RECORD-CORRECTION:"`, so it obeys
the same column-zero bracketed-timestamp boundary the other three do;
that `dcs_gate.py`'s `sentinel_of()` does not classify it and
`dcs_log.py --sentinel` offers only halt/pass/stamp, so a correction
entry is written as ordinary entry text; that the entry names its target
token in its own body immediately after a citation keyword; and that its
effect is file-scoped over the whole 214-LOG.md. Read
`dcs/tools/record_integrity.py` (:290-460) before writing — read-only;
you may import or read anything, you may edit only your three files.

(b) HARD constraint from check 12 sub-check (d): the literal token
`RECORD-CORRECTION:` must NOT appear inside any fenced (```) block in
any `dcs/**/*.md` file — `sentinel_of()` would return None for such a
line and the check would go red, and `dcs/hooks/**` is out of scope this
period. Use inline backticks in running prose; in particular do not add
a line to the existing example log block at forms.md:43-49.

(c) Do not alter the verbatim quotations of `GRAMMAR_LINE`,
`dcs_log.py`'s `FORMAT_LINE`, or `INVOCATION` in forms.md — they are
quoted constants that checks 12(c) and the dcs_log format checks
compare; re-wrapping whitespace is safe, rewording is not, and
'SAFETY-HALT:, SAFETY-PASS:, or IAP-APPROVED:' inside the FORMAT_LINE
quote is what `dcs_log.py` actually publishes and stays as it is.

(d) `dcs/references/doctrine.md`: add the literal token
`RECORD-CORRECTION:` in one clause of running prose, outside any fenced
block (doctrine.md's fences are at :84-101 — stay out of them), placed
where it is true and useful (principle 13's sentinel/log-grammar
neighbourhood is the natural home). This satisfies check 12 sub-check
(f), which requires every token in `_SENTINEL_TOKENS` to appear
literally in doctrine.md.

(e) Criterion 7: the hot-path pair measured 23,152 + 14,570 = 37,722 B
against 37,888 B at plan time — 166 B of slack. Fund your doctrine.md
addition with a trim of genuinely redundant prose in doctrine.md or
schemas.md in the same act, and report bytes before and after. Do not
trim by deleting a rule, a threshold, or a section title another file
references by name — the suite's section-name and schema-anchor checks
will catch it, but a red there means you cut the wrong thing.

(f) Use Write/Edit tools only, never PowerShell Set-Content/Out-File.
Do not touch `tests/**` — S1 owns `_SENTINEL_TOKENS`; until S1 lands,
check 12 sub-check (f) will not exercise your doctrine.md token at all,
so a green there proves nothing on its own and you must say so.

## File territory (may edit only within these globs)

- dcs/references/forms.md
- dcs/references/doctrine.md
- dcs/references/schemas.md

## Forbidden zones (explicitly, even if it seems related)

- tests/**
- dcs/references/doctrine-appendix.md
- dcs/hooks/**
- dcs/tools/**
- dcs/workflows/**
- dcs/templates/**
- package.json
- dcs/VERSION
- .dcs/**
- vault/**
- CLAUDE.md

## Worktree root

C:\DCS-wt\field-lesson-guard-bare-date-weakening

## Evidence required in the return

- Hot-path budget, before and after, with the regenerating command:
  `python -c "import os;p='dcs/references/';a=os.path.getsize(p+'doctrine.md');b=os.path.getsize(p+'schemas.md');print(a,b,a+b,37*1024-(a+b))"`
  — plan-time baseline was `23152 14570 37722 166`; the final slack must
  be >= 0. Also
  `python tests/test_doctrine_integrity.py 2>&1 | grep -i 'hot-path budget'`.
- The token is present in prose and absent from fences.
  `grep -n 'RECORD-CORRECTION:' dcs/references/doctrine.md dcs/references/forms.md`
  for presence, plus this fence audit written to your scratchpad and run
  from the worktree root, which must print `CLEAN`:

  ```
  import pathlib
  bad=[]
  for p in pathlib.Path('dcs').rglob('*.md'):
      inb=False
      for i,l in enumerate(p.read_text(encoding='utf-8').splitlines(),1):
          if l.strip().startswith('```'): inb = not inb; continue
          if inb and 'RECORD-CORRECTION:' in l: bad.append(f"{p.as_posix()}:{i}")
  print('CLEAN' if not bad else bad)
  ```

- Quoted constants intact:
  `python tests/test_doctrine_integrity.py 2>&1 | grep -i 'log grammar'`
  — every GRAMMAR_LINE-verbatim and fenced-boundary case green, and the
  population list unchanged from the plan-time seven files.
- The trim that funded the addition, as a decisive diff excerpt:
  `git diff -U1 -- dcs/references/doctrine.md dcs/references/schemas.md`,
  with one sentence in your return naming what was cut and why it was
  redundant (not merely 'shortest to delete').
- The new forms.md prose as a diff excerpt:
  `git diff -U1 -- dcs/references/forms.md`, plus the `file:line` of
  `dcs/tools/record_integrity.py` you took each mechanical claim from.
- `python tests/test_doctrine_integrity.py 2>&1 | tail -3` — report N/M;
  name any case red because of a peer's in-flight file rather than
  repairing it.
- `git status --porcelain` — changes confined to your three files.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action).
The IC will re-enter planning around your finding.
