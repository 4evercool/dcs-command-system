<!--
204-TASKING/S1.md -- transcribed verbatim by the IC from the Planning
Chief's taskings[] (schemas.md #2), one lint repair applied and logged in
214-LOG.md: the forbidden pseudo-glob "tests/fixtures/** (except
tests/fixtures/field-lesson-guard/**)" is expanded to explicit sibling
globs.
-->

# 204 — Tasking S1

**Incident:** field-lesson-guard-bare-date-weakening
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Own both guards' code and their self-tests in
`tests/test_doctrine_integrity.py` (criteria 1, 2, 4, and the code half
of 6).

(a) Check 20a: replace `_FL_ID_RE` with the strict form
``re.compile(r"incident `[a-z0-9-]+`|v\d+\.\d+\.\d+|predates self-hosting", re.I)``
— delete the bare-date alternative and nothing else; leave `_FL_LINE_RE`
broad and the one-line lookahead as-is (tactic T1).

(b) Add the named non-claim exemption per tactic T3:
`_FL_NON_CLAIM_PREFIXES = {"dcs/references/doctrine-appendix.md": ("# DCS Doctrine", "### Field-lesson citation convention")}`,
applied to the stripped start of a line, with an accompanying named check
that each allowlisted prefix still matches at least one line in its file
(a stale exemption must go red, never silently pass). Use ASCII-only
prefixes — the appendix title contains an em dash, so do not key on it.
Do not exempt anything not listed here; S2 handles the rest by rewording.

(c) Refactor per tactic T4 so the live `_FL_FILES` loop and
`_fl_check_file` share ONE implementation.

(d) Criterion 1: create
`tests/fixtures/field-lesson-guard/bare-date-claim.md` whose claim line
carries a bare `YYYY-MM-DD` and nothing else (shape:
`Field lesson 2026-09-01: users reported a crash on save.`), with no
identifier on that line or the following line, and add a permanent
self-test asserting `_fl_check_file` returns a NON-empty result for it.

(e) Criterion 2: leave `multiline-claim.md` and `undated-claim.md`
byte-for-byte unchanged and keep their two assertions' expected outcomes
unchanged — but rewrite the stale comment at :2121-2124, which claims the
multiline fixture is accepted via the bare date when in fact its next
line reads `2026-07-22, predates self-hosting.`.

(f) Criterion 4: rewrite the check 20a docstring (:2032-2058) so it
describes exactly what the code does — the broad entry filter, the
three-form strict identifier set, the explicit statement that a bare
date is NOT an identifier and why (this incident), the one-line
lookahead, the two named non-claim exemptions with their reasons, and
the existing doctrine.md file-level exclusion. No promised strictness
the regex does not keep and no undocumented acceptance branch.

(g) Criterion 6, code half: add `"RECORD-CORRECTION:"` to
`_SENTINEL_TOKENS` (:728) and correct every description that the
widening makes false — the check name at :816 ('names all three
sentinels'), the comment at :715-720 ('any of the three sentinel
tokens'), and the (f) comment at :815. Where remaining prose says 'three
sentinels' meaning the three `sentinel_of()` classifies (e.g. the
`_TERM_CENSUS` entry at :546), scope it explicitly to `sentinel_of()`
rather than deleting it. Optionally add a
`("RECORD-CORRECTION:", <why-it-breaks>)` `_TERM_CENSUS` entry — but
first measure that the token exists in check 9's candidate population
(it is in `dcs/tools/record_integrity.py`); if adding it goes red, drop
it and say so in your return rather than widening the population.

Do NOT edit any file under `dcs/` — check 12 sub-check (f) needs the
literal token in doctrine.md, and S3 is putting it there; if the suite
is red on that case alone mid-flight, report it, do not cross into S3's
territory.

## File territory (may edit only within these globs)

- tests/test_doctrine_integrity.py
- tests/fixtures/field-lesson-guard/**

## Forbidden zones (explicitly, even if it seems related)

- dcs/**
- dcs/hooks/**
- dcs/tools/**
- tests/test_dcs_gate.py
- tests/test_dcs_intake.py
- tests/fixtures/halt-ceiling/**
- tests/fixtures/log-order/**
- tests/fixtures/preservation-map/**
- tests/fixtures/record-integrity/**
- tests/fixtures/verdict-rerun/**
- package.json
- dcs/VERSION
- .dcs/**
- vault/**
- CLAUDE.md

## Worktree root

C:\DCS-wt\field-lesson-guard-bare-date-weakening

## Evidence required in the return

- The decisive source excerpt, not a paste of the file:
  `git diff -- tests/test_doctrine_integrity.py | grep -n -B2 -A6 '_FL_ID_RE\|_FL_NON_CLAIM_PREFIXES\|_SENTINEL_TOKENS'`
  — must show the bare-date alternative deleted and the tuple widened.
- Proof the weakening cannot return by inspection:
  `grep -n 'd{4}' tests/test_doctrine_integrity.py` — report every hit
  and state, per hit, that none is inside `_FL_ID_RE`.
- `python tests/test_doctrine_integrity.py 2>&1 | grep -i 'field-lesson'`
  — every field-lesson case with its PASS/FAIL, including the new
  `bare-date-claim.md` case (must be PASS, i.e. the fixture IS flagged)
  and the two pre-existing fixture cases (must be PASS with their
  expected outcomes unchanged).
- `python tests/test_doctrine_integrity.py 2>&1 | grep -i 'log grammar\|names all'`
  — every check 12 case green after the tuple widening.
- `python tests/test_doctrine_integrity.py 2>&1 | tail -3`,
  `python tests/test_dcs_gate.py 2>&1 | tail -2`,
  `python tests/test_dcs_intake.py 2>&1 | tail -2` — baselines at plan
  time were 266/266, 100/100, 18/18; report the new N/M. If a case is
  red because a peer's file is mid-flight, name the failing case and say
  so; do not repair it.
- `git status --porcelain` — must show changes only under
  tests/test_doctrine_integrity.py and tests/fixtures/field-lesson-guard/.
- The new fixture's full content (it is 4-5 lines) plus the `file:line`
  of the assertion that consumes it.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action).
The IC will re-enter planning around your finding.
