<!--
204-TASKING/S2.md -- transcribed verbatim by the IC from the Planning
Chief's taskings[] (schemas.md #2).
-->

# 204 — Tasking S2

**Incident:** field-lesson-guard-bare-date-weakening
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Make every shipped field-lesson citation site strict-compliant and
remove the parent's identifier-stuffing (criteria 3 and 5), across
`doctrine-appendix.md`, `plan.md` and `execute.md`.

(a) Criterion 5, stated as an invariant rather than three line numbers:
run `git show bcf9468 -- dcs/references/doctrine-appendix.md` and remove
EVERY identifier that commit inserted into non-claim prose — at intake
that is four sites, `(provenance, field lessons, background; v0.5.0)` in
the title (:1), `### Field-lesson citation convention (v0.5.0)` (:11),
`follows (v0.5.0)` mid-sentence (:13), and `narratives (v0.6.9)`
mid-sentence (:669) — restoring each sentence to prose that reads as
English. Operational test for stuffing versus citation: an identifier
that reads naturally as part of the sentence's own claim is a citation
and stays; one that only makes sense as regex food is stuffing and goes.

(b) Two of those four lines (:1 and :11) are headings whose next line is
blank and which therefore cannot carry a natural identifier: S1 is
exempting them by name via prefixes `# DCS Doctrine` and
`### Field-lesson citation convention`. Your rewrite MUST leave those
two line-openings intact, character for character, from column zero —
anything after the prefix is yours.

(c) The other two (:13, :669) get natural rewording, not exemption. For
:13, the convention has applied since v0.5.0, so a clause such as 'Since
v0.5.0, every field lesson in this appendix ... follows one of three
citation forms' carries the identifier as part of the sentence;
alternatively re-wrap the paragraph so the line carrying 'field lesson'
is immediately followed by a line carrying a strict identifier. For
:669, the D2 entry is about prose divested from doctrine.md principle 13
at v0.6.9 — write that fact into the sentence so 'v0.6.9' is the
sentence's own subject rather than a parenthetical.

(d) Criterion 3, the three known citation sites, reworded to the shipped
forms per tactic T2, never by teaching the grammar a new one:
`doctrine-appendix.md:414` ('per the field lesson 2026-07-24 below')
cites W2, which is `predates self-hosting`; `plan.md:57-58` cites W1 and
BOTH physical lines trip the entry filter (line 58 contains the section
name 'Workflow field lessons'), so both need coverage — arrange the wrap
so the line holding 'field lesson' is followed by a line holding a
strict identifier AND the section-name line carries one on itself;
`execute.md:231` is a single long line holding both 'field lesson' and
'Workflow field lessons' and needs a strict identifier on that line. All
three cite W-entries marked `predates self-hosting`
(`doctrine-appendix.md:735`, `:740`, `:745`), so that is the correct
identifier for each — verify against those entries rather than assuming.

(e) Budgets: plan.md is 243/250 lines and execute.md 238/250 — your
rewording must not cross the ceiling.

(f) Use Write/Edit tools only, never PowerShell Set-Content/Out-File
(CLAUDE.md coding rules: BOM). Do not touch `tests/**` — if a site
cannot be made strict-compliant without widening the grammar, that is a
deviation to report, not to fix in the test file.

## File territory (may edit only within these globs)

- dcs/references/doctrine-appendix.md
- dcs/workflows/plan.md
- dcs/workflows/execute.md

## Forbidden zones (explicitly, even if it seems related)

- tests/**
- dcs/references/doctrine.md
- dcs/references/schemas.md
- dcs/references/forms.md
- dcs/hooks/**
- dcs/tools/**
- dcs/templates/**
- dcs/workflows/close.md
- dcs/workflows/new.md
- dcs/workflows/deploy.md
- dcs/workflows/run.md
- dcs/workflows/loop.md
- dcs/workflows/esg.md
- dcs/workflows/init.md
- dcs/workflows/status.md
- package.json
- dcs/VERSION
- .dcs/**
- vault/**
- CLAUDE.md

## Worktree root

C:\DCS-wt\field-lesson-guard-bare-date-weakening

## Evidence required in the return

- Self-contained strict sweep, independent of S1's in-flight edits.
  Write this to your scratchpad as `fl_sweep.py` and run
  `python <scratchpad>/fl_sweep.py` from the worktree root; it must
  print exactly `CLEAN`:

  ```
  import re, pathlib
  R=pathlib.Path('.')
  F=["dcs/references/doctrine-appendix.md","dcs/workflows/deploy.md","dcs/workflows/new.md","dcs/workflows/close.md","dcs/workflows/plan.md","dcs/workflows/execute.md","dcs/templates/202-OBJECTIVES.md","dcs/templates/REGISTER.md"]
  L=re.compile(r"[Ff]ield[- ]lesson", re.I)
  I=re.compile(r"incident `[a-z0-9-]+`|v\d+\.\d+\.\d+|predates self-hosting", re.I)
  X={"dcs/references/doctrine-appendix.md":("# DCS Doctrine","### Field-lesson citation convention")}
  bad=[]
  for f in F:
      ls=(R/f).read_text(encoding='utf-8').splitlines()
      for i,l in enumerate(ls,1):
          if any(l.strip().startswith(p) for p in X.get(f,())): continue
          if L.search(l) and not I.search(l) and not I.search(ls[i] if i<len(ls) else ""):
              bad.append(f"{f}:{i}: {l.strip()[:80]}")
  print("CLEAN" if not bad else "\n".join(bad))
  ```

- Stuffing gone, by the parent's own insertions:
  `grep -n 'background; v0.5.0\|convention (v0.5.0)\|follows (v0.5.0)\|narratives (v0.6.9)' dcs/references/doctrine-appendix.md`
  — must exit 1 with no hits; paste the exit code.
- Allowlisted line-openings preserved:
  `grep -c '^# DCS Doctrine' dcs/references/doctrine-appendix.md` and
  `grep -c '^### Field-lesson citation convention' dcs/references/doctrine-appendix.md`
  — each must print 1.
- The reworded citation sites as decisive excerpts only:
  `git diff -U1 -- dcs/references/doctrine-appendix.md dcs/workflows/plan.md dcs/workflows/execute.md`
  — hunks only, no full-file paste.
- `wc -l dcs/workflows/plan.md dcs/workflows/execute.md` — both at or
  under 250.
- `python tests/test_doctrine_integrity.py 2>&1 | grep -i 'field-lesson citations' ; python tests/test_doctrine_integrity.py 2>&1 | tail -3`
  — report the result; if the field-lesson case is still using the OLD
  lax regex because S1 has not landed, say so explicitly, since a PASS
  there proves nothing until S1's regex is in.
- `git status --porcelain` — changes confined to your three files.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action).
The IC will re-enter planning around your finding.
