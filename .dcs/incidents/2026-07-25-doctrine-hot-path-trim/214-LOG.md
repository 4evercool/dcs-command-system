<!--
214-LOG.md -- append-only. Never edit or delete a past entry (see
references/forms.md's note on why). Every workflow appends here on every
phase transition. This is what /dcs-status reads the tail of, and what a
fresh session reads in full to resume losslessly after a context reset.

Format: [ISO8601 local timestamp] free-text description of the transition.
-->

# 214 — Operational Log

**Incident:** doctrine-hot-path-trim

[2026-07-25] incident opened, type 3, phase=planning
[2026-07-25] command: typed 3 -- bounded prose relocation across two known files whose only real hazard (66 heading/number reference sites) is already mechanically enforced by the merge-time guard; matches CLAUDE.md's convention that doctrine changes are Type 3. Ratchet edit (HOT_PATH_BUDGET_KB constant + comment in tests/test_doctrine_integrity.py) ruled IN scope -- lowering a ratchet only tightens enforcement and is that constant's designed maintenance; CLAUDE.md's Type 1 clause covers the gate (dcs_gate.py, test_dcs_gate.py), not this guard's constant. Any edit to the guard's check logic is out of territory and a deviation. (IC=dcs-commander, fable)
[2026-07-25] command: esg_activation requested by the IC at command point 1 -- principle 14 ESG-absence trigger (worktree incident, no .dcs/esg/ founded, vault/Backlog.md queued candidates with no body to queue into). (IC=dcs-commander, fable)
[2026-07-25] Owner confirmed typing as proposed (Type 3) via AskUserQuestion, and directed that the ESG be founded before this incident opened.
[2026-07-25] ESG founded in the main checkout (C:\DCS) -- REGISTER.md, STRATEGY.md, DELEGATION.md, SITREPS/ created; the IC's esg_activation request is thereby answered. Five rows queued from vault/Backlog.md; this incident is rank 1. DELEGATION v1 granted by the Owner at founding: auto_approve_type3=true, max_files=4, max_specialists=2, require_tests_green=true, forbidden_globs=[dcs/hooks/**, tests/test_dcs_gate.py, install.ps1, install.sh, bin/**, package.json], deploy.auto_after_close=true. Recorded that the grant rests on zero closed incidents of evidence.
[2026-07-25] worktree created: C:\DCS-wt\doctrine-hot-path-trim on branch dcs/doctrine-hot-path-trim (from main @ 51dd073). Incident directory, 201-BRIEF.md and this log written inside it.

## Operational period 1 -- planning

[2026-07-25] period 1 begins. Command-chain entry check passed: `command: typed` entry present above.
[2026-07-25] 202 confirm deferred to IAP approval (Delegation v1) -- Type 3 with auto_approve_type3=true, so plan.md step 2's AskUserQuestion is skipped and the Owner's first look at the objectives is the step 6b prompt, if the bound check fails there.
[2026-07-25] PLAN-TIME DISCOVERY -- hot-path measurement is line-ending-sensitive. `core.autocrlf=true`, no `.gitattributes`: this fresh worktree checked out CRLF while the main checkout holds LF. Same commit 51dd073, measured with `wc -c dcs/references/doctrine.md dcs/references/schemas.md` in each tree: main = 41,444 B (doctrine 27,010 + schemas 14,434), worktree = 41,763 B (doctrine 27,167 + schemas 14,596). 319 B spread against a 43,008 B ceiling; the guard reads raw os.path.getsize so the check's result genuinely depends on checkout line endings. Registered as its own QUEUED row `hot-path-budget-eol-sensitivity` (independent root cause, ships alone -- principle 4 decomposition) rather than absorbed here. This period measures in the WORKTREE, the conservative tree.

### Lint 3a(iii) -- enumeration baselines recorded before planning

[2026-07-25] 202 criterion 1 baseline: `python -c "import os; d=os.path.getsize('dcs/references/doctrine.md'); s=os.path.getsize('dcs/references/schemas.md'); print(d, s, d+s)"` in the worktree => 27167 14596 41763. Target <= 36864.
[2026-07-25] 202 criterion 2 baseline: `grep -n "Field lesson" dcs/references/doctrine.md` => 2 matches, lines 38 and 120. Criterion is this command returning EMPTY. (Line 3's lowercase "field lessons" is the routing rule and does not match.)
[2026-07-25] 202 criterion 3 baseline: `grep -noE "^[0-9]+b?\." dcs/references/doctrine.md` => 28 matches across three numbered lists: command points 24:1. 25:2. 26:3. 27:4. | principles 54:1. 55:2. 56:3. 57:4. 58:5. 59:6. 60:7. 61:8. 62:9. 63:9b. 64:10. 65:11. 66:12. 67:13. 68:14. 69:15. | automation hard rules 131:1. 132:2. 133:3. | worktree audit 149:1. 150:2. 151:3. 152:4. 153:5. Criterion is that this LABEL SEQUENCE is unchanged (line numbers will shift; the labels and their order must not).
[2026-07-25] 202 criterion 4 baseline: `grep -n "^## " dcs/references/doctrine.md` => 12 headings: Concept | Why phases, not nesting | Transfer of command | Hierarchy (chain of command) | The working principles | Incident typing (decided at the stem, recorded in 201) | The lifecycle (Planning P mapped to software) | v0.1 constraints (deliberate, not oversights) | Communication convention: slash commands are chat input, not shell | Relationship to project-specific protocols | Automation layers (v0.2) | Parallel operation (v0.3). Criterion is that this heading set and its text are unchanged.
[2026-07-25] Appendix baseline for criterion 7: `grep -n "^## " dcs/references/doctrine-appendix.md` => 6 headings (Why phases not nesting | Transfer of command @29 | The working principles @46 | Relationship to project-specific protocols @91 | Automation layers @116 | Parallel operation @137); size 11,302 B in this worktree. `grep -n "quota\|transcript" dcs/references/doctrine-appendix.md` => EMPTY, which is why criterion 7 is a write-then-cut.
[2026-07-25] 202 written. Spawning dcs-planning-chief (period 1).
[2026-07-25] dcs-planning-chief returned: 1 tasking (OPS-1), partition_ok=true (trivially -- single seat), 8 tactics, 7 risks, 9-step verification plan. Chief argues explicitly against splitting doctrine.md and doctrine-appendix.md across two seats: the cut and the landing are two ends of one edit, criterion 6's routing ledger needs one seat holding both, and the ratchet must be measured after the trim lands.
[2026-07-25] tasking lint: 2 defects fixed pre-review -- (1) SELF-CONTRADICTION, OPS-1's forbidden[] ended with the catch-all `**/*`, which under fnmatch matches every entry in its own territory[]; the 204's territory is already allowlist-shaped ("may edit only within these globs"), so the catch-all was both redundant and contradictory. Removed. (2) ENUMERATION INCOMPLETENESS in criterion 4's evidence -- the 202 enumerates protected headings with `grep -n "^## "` (12 hits), but the guard's check 6 matches by-name references against `^#{2,3}` headings, and `### A command point is never a silent wait` at doctrine.md:33 IS cited by name from dcs/workflows/plan.md:206 and agents/dcs-commander.md:113. A specialist could delete that sub-heading, pass criterion 4's literal command, and turn check 6 red. Added the superset `grep -n "^#\{2,3\} " dcs/references/doctrine.md` to OPS-1's evidence_required, baseline 13 headings (the 12 plus the `###`). The 202's literal command remains the criterion; the superset is evidence, same pattern the chief applied to criterion 2.
[2026-07-25] lint checks passing without change: territory/forbidden disjointness after fix 1; no orphaned deliverables (all tactics map to OPS-1); criterion-2 population (`grep -n "Field lesson"` => 38, 120) and its dated superset (`grep -noE "20[0-9]{2}-[0-9]{2}-[0-9]{2}"` => 38, 120, verified identical) both fall entirely inside OPS-1's territory; partition trivially disjoint at one seat; every evidence command runnable headless; criterion coverage complete in both directions (1-9 and 11 -> OPS-1; 10 tagged [Owner]; 12 tagged [deploy period]); criterion 11's version bump collides with no committed test -- `grep -rn "0\.6\.[0-9]" tests/` returns nothing and the only 0.6.4 pin outside dcs/VERSION is package.json:3, both of which OPS-1 owns; all territory globs resolve inside this project root (lint 8).
[2026-07-25] Spawning dcs-commander for COMMAND POINT 2 (IAP acceptance). Announced to the Owner; no file changes until the decision returns.
[2026-07-25] command: iap_review ACCEPT (IC=dcs-commander, fable). The IC re-measured the chief's seven largest claimed spans itself rather than relying on the return: 429, 165, 689, 488, 445, 406 and 269 B all matched to the byte, and the one discrepancy found favours the plan (principle 15's line measures 1,901 B against 1,868 claimed). It also spot-checked every "redundant with" citation against the appendix (48-68, 116-135, 139-156, 31-44) and confirmed docs/spec-v0.3-parallel.md:184 really does cite the three bolded Parallel-operation lead-ins. Three binding rulings, transcribed into IAP.md's "IC rulings" section: (A) tier-2 reserve PRE-AUTHORIZED, criterion 1 NOT relaxed at 36,864 B, deviation rather than cuts into rule text if tier 1 + reserve are both exhausted, and the ratchet's +1 kB margin is reserved for criterion-10 rebound repairs and must not be spent to reach criterion 1; (B) the routing ledger is accepted as criterion 6's artifact -- required structured return field, transcribed verbatim into this log by the Dispatcher, carried into the AAR, and refuted row by row by the Safety Officer; (C) criterion 8's formula pinned to math.ceil(total/1024)+1 with the arithmetic pasted, never the constant asserted. No esg_activation requested -- tactical question, and the register already carries the queued follow-ups.
[2026-07-25] 203 skipped (default Type 3 activation) -- 1 specialist matching 1 tasking, plain execution; IAP.md's partition table carries the same information.
[2026-07-25] 204-TASKING/OPS-1.md and IAP.md written.
[2026-07-25] register territory refined (plan.md 5a) from the 201 blast-radius estimate to the IAP partition union: dcs/references/doctrine.md, dcs/references/doctrine-appendix.md, tests/test_doctrine_integrity.py, dcs/VERSION, package.json.
[2026-07-25] DELEGATION v1 bound check (plan.md step 6, Type 3): FAILED on three of five bounds -- (i) max_files=4 but the IAP partitions 5 files; (ii) forbidden_globs contains `package.json`, which is in OPS-1's territory (criterion 11's atomic version sync); (iii) forbidden_topics contains "version bump", which matches both the 201's phrase "twelve version bumps since" and criterion 11 itself. Bounds that HELD: max_specialists=2 >= 1 tasking; require_tests_green=true and the verification plan names concrete automated runs (test_doctrine_integrity.py, test_dcs_gate.py, test_dcs_intake.py). Auto-approval therefore does NOT apply; falling through to step 6b for real Owner approval with the failed bounds named.
[2026-07-25] Owner approved the IAP as written via AskUserQuestion (step 6b), having been shown the three failed bounds, the 202 in full, and the option to strike criterion 11 (which would have cleared every bound at the cost of criterion 12 and a verifiable deploy). Criterion 1 left at 36,864 B; the offered relaxation to ~37,400 B was declined, consistent with the IC's ruling (A).
[2026-07-25] pre-stamp checklist passed: `command: typed` and `command: iap_review` both present in this log.
[2026-07-25] IAP-APPROVED stamped. sha256(IAP.md) = a5eec3b4de96e18be91665daff7d37e11978b568a94ecd169cabe8a4e1d5af85, approved_by: Owner, period 1.
[2026-07-25] phase: planning -> execution (IAP approved, hash=a5eec3b4...)

## Operational period 1 -- execution

[2026-07-25] execute.md entry checks passed: ACTIVE phase=execution; command chain complete (`command: typed` + `command: iap_review`); approval marker re-verified independently -- sha256(IAP.md) recomputed = a5eec3b4de96e18be91665daff7d37e11978b568a94ecd169cabe8a4e1d5af85, matches IAP-APPROVED line 1.
[2026-07-25] escalation-trigger check, period boundary: trigger (c) NOT fired -- this is attempt 1, threshold esg.max_periods_before_review=3 (regenerate the tally with `grep -c "IAP-APPROVED stamped" 214-LOG.md`). Trigger (d) NOT fired -- the IAP's territory does cross DELEGATION v1's forbidden_globs on `package.json`, but that crossing was caught at plan time (step 6 bound check), named to the Owner, and knowingly approved; trigger (d) guards the opposite case, a Delegation tightened AFTER approval.
[2026-07-25] fan-out: 1 dcs-ops-specialist (OPS-1), single seat per IAP tactic T0.
[2026-07-25] OPS-1 returned status=done. Tier 1 (T2+T3+T4) alone reached the target -- doctrine.md 27,167 -> 22,121 B, net -5,046 B against a -4,899 B requirement. NO T5 reserve draws, no deviation. Combined hot path 22,121 + 14,596 = 36,717 B against the 36,864 B target: 147 B of headroom. Ratchet arithmetic pasted, not asserted: total=36717; 36717/1024=35.8564453125; math.ceil(36717/1024)+1 = 37. HOT_PATH_BUDGET_KB 42 -> 37 (strictly < 42, per IC ruling C).
[2026-07-25] territory validation (execute.md step 5): files_touched = dcs/references/doctrine.md, dcs/references/doctrine-appendix.md, tests/test_doctrine_integrity.py, dcs/VERSION, package.json -- all five inside OPS-1's declared territory, none outside. Independently confirmed by `git diff --numstat`: 5 files, and doctrine.md shows 18 insertions / 20 deletions across 155 lines, i.e. surgical edits rather than a wholesale rewrite (which would have flipped CRLF and booked a phantom win). Trigger (a) NOT fired -- files_touched does not exceed the 201's declared blast radius.
[2026-07-25] OPS-1 FLAGGED AN OUT-OF-TERRITORY FINDING and correctly did not act on it: package.json is 6,322,630 B because its `description` field contains 2,942,431 characters of mojibake. Pre-existing at HEAD, not caused by this incident (`git show HEAD:package.json | wc -c` = 6,322,584, the 46 B delta being CRLF normalisation across 46 lines). This is the npm release surface and would ship. Registered as its own QUEUED row `package-json-description-corruption`; NOT absorbed here (principle 4) and NOT a deviation -- the specialist stayed in territory and reported, which is exactly right.

### Routing ledger -- criterion 6's artifact, transcribed verbatim from OPS-1's return (IC ruling B)

| # | doctrine.md:baseline-line "first ~6 words" (gross B) | destination | net B |
|---|---|---|---|
| 1 | `:31` "A tier that failed earlier in this session" (429 B) | -> `doctrine-appendix.md` `## Transfer of command` append (new "Model availability rationale" para, lines 46-60) | -- |
| 2 | `:31` "and if it fails again the cost" (167 B) | -> same append (para above) | 597 (combined 1+2, one Edit) |
| 3 | `:37` "a command-point agent writes nothing" (150 B) | -> same append (announce-rule rationale sentence, within lines 46-60) | 150 |
| 4 | `:38` "Transcript size and file mtimes are" (690 B -- contains "Field lesson") | -> `doctrine-appendix.md` `## Transfer of command` append (new "Field lesson 2026-07-24 (quota vs. transcript)" para, lines 62-73) | -- |
| 5 | `:38` "A spawn is dead only if it" (168 B) | -> same append (para above), replaced in-core by 74 B proxy-list kernel | 781 (combined 4+5, one Edit) |
| 6 | `:35` "(v0.5.10) Two rules, both about" (86 B) | -> dropped -- ceremonial lead-in, no rule content, heading now leads directly into its bullets | 86 |
| 7 | `:57` "and nothing downstream can undo" + "(rethink how X is accounted)" (~162 B) | -> `doctrine-appendix.md` `## The working principles` append ("Principle 4 -- the cost of over-scope" para, lines 120-129) | 168 |
| 8 | `:59` "so a session rooted in repo A" (445 B) + "keeps their git merge --no-ff" (62 B) | -> `doctrine-appendix.md` `## The working principles` append ("Principle 6 -- one session, one project, in full" para, lines 131-145); ~150 B kernel kept in core | 360 |
| 9 | `:63` "Two reasons, both structural:" (488 B) + "this was prose twice before" (84 B) | -> `doctrine-appendix.md` `## The working principles` append ("Principle 9b -- why single-shot, not resumed" para, lines 147-159); ~172 B kernel kept in core | 400 |
| 10 | `:67` "Counting periods alone is a loophole" (309 B) + "each further pass buys one instance" (83 B) + "three rejects means the objectives" (138 B) | -> `doctrine-appendix.md` `## The working principles` append ("Principle 13 -- the four-revisions field lesson" para, lines 161-173) | 530 |
| 11 | `:69` "Two independent forces make this" (406 B) | -> REDUNDANT with `doctrine-appendix.md:48-55` (pre-existing Principle-15 provenance para); replaced with 35 B pointer "Derived facts rot, and they travel." | -- |
| 12 | `:69` "in the incident that produced this" (195 B) | -> REDUNDANT with `doctrine-appendix.md:52-55` and `57-61` (pre-existing) | -- |
| 13 | `:69` "A test asserting that two live branches" (269 B) | -> NOT pre-existing in appendix; write-then-cut -> `doctrine-appendix.md` `## The working principles` append ("Principle 15 -- the test-inversion lesson" para, lines 175-182) | 838 (combined 11+12+13, one Edit) |
| 14 | `:104` "a Safety-passed period holds proven" (166 B) | -> `doctrine-appendix.md` NEW `## The lifecycle (Planning P mapped to software)` section (lines 184-194) | 166 |
| 15 | `:120` "Field lesson 2026-07-24: a project made" (271 B -- contains "Field lesson") | -> `doctrine-appendix.md` `## Relationship to project-specific protocols` append (new "Field lesson 2026-07-24 (charter defect, not agent failure)" para, lines 221-228) | -- |
| 16 | `:120` "An agent that reports the protocol's" (173 B) | -> compressed to 80 B kernel in core, same location | 364 (combined 15+16, one Edit) |
| 17 | `:124` "neither changes who holds command judgment" (145 B) | -> REDUNDANT with `doctrine-appendix.md:116-135` (pre-existing "Automation layers" rationale) | 144 |
| 18 | `:127` "Legitimate only because the Delegation defines" (74 B) | -> REDUNDANT with `doctrine-appendix.md:128-135` (pre-existing) | 65 |
| 19 | `:139-141` three ICS-analogy bullets, full elaboration (571 B) | -> ICS imagery REDUNDANT with `doctrine-appendix.md:158-171` (pre-existing); bolded lead-ins preserved verbatim, compressed to operative rules only (329 B kernel) | 242 |
| 20 | `:149` "(harness worktrees, a deploy script's own, a human's personal one)" (67 B) | -> REDUNDANT with `doctrine-appendix.md:139-156` (pre-existing "Field lesson 2026-07-23" para) | 67 |
| 21 | `:17` "regardless of who first reported the incident (ICS)" (52 B) | -> REDUNDANT with `doctrine-appendix.md:31-44` (pre-existing "The ICS original" para) | 52 |
| 22 | `:114` "which fails and confuses the Owner" (36 B) | -> dropped -- non-substantive tail clause, no rule lost | 36 |

Total net removed from doctrine.md per the ledger: 5,046 B (27,167 -> 22,121). Regenerate the endpoints with `python -c "import os; print(os.path.getsize('dcs/references/doctrine.md'))"` in this worktree.

[2026-07-25] Spawning dcs-safety-officer for the period-1 verdict.
[2026-07-25] SAFETY: halt -- ONE refutation, against criterion 6's artifact rather than against the trim. The routing ledger transcribed above states ~30 line citations in TWO unlabeled coordinate systems: all nine DESTINATION rows (1, 4, 7, 8, 9, 10, 13, 14, 15) cite POST-edit line numbers and were verified exact to the line at both endpoints, but the six REDUNDANCY rows (11, 12, 17, 18, 19, 20) cite PRE-edit HEAD numbers and therefore resolve to unrelated text in the delivered tree -- because this same incident appended 114 lines to doctrine-appendix.md and shifted every line past 44. Verified by the officer: `sed -n '48,55p' dcs/references/doctrine-appendix.md` now returns the NEW "Model availability rationale" paragraph, not principle-15 provenance; `sed -n '116,135p'` returns the pre-existing goal-drift paragraph, not "Automation layers" (whose heading is now at line 230); `sed -n '158,171p'` returns the NEW principle-9b paragraph, not the ICS imagery; `sed -n '139,156p'` returns the NEW principle-6 paragraph, not the 2026-07-23 worktree field lesson. Criterion 6 requires "cited by line"; six citations do not resolve in the tree being shipped. This is a principle-15 violation inside the incident's own durable artifact -- derived facts with no regenerating command, already rotted by the edit they document -- and it is aggravated by 214-LOG.md being append-only and by IC ruling B carrying this ledger into the AAR.
[2026-07-25] DISPATCHER'S OWN DEFECT, recorded plainly: the ledger was authored by OPS-1 but transcribed into this append-only log by the Dispatcher under IC ruling B, without adding the regenerating commands principle 15 requires. The transcription step was the last place this was catchable and it was not caught.
[2026-07-25] Everything else PASSED under independent re-check. Criteria 1 (36,717 B, 147 B headroom, correct to the byte), 2 (grep empty, plus the dated superset empty, plus doctrine.md:3's routing pointer intact), 3 (28 labels, sequence identical, 9b present), 4 (12 headings unchanged, plus the `###` sub-heading cited from plan.md:206 and dcs-commander.md:113 surviving), 5 (12/12), 7 (appendix hits at 47/62/65/68/69/150, verified to the stricter bar as sitting between the `## Transfer of command` heading at 29 and `## The working principles` at 75), 8 (ratchet 37 re-derived independently; comment carries derivation, measured bytes, CRLF caveat, slug and a runnable command), 9 (all 13 rule greps present AS INSTRUCTIONS, read in context), 11 (atomic 0.6.5). Criterion 6's COVERAGE half also met -- a word-level diff enumeration found all 22 rows account for 100% of deletions with no orphan. Territory clean (exactly 5 files; the `hot = os.path.getsize(...)` check logic byte-identical to HEAD; schemas.md untouched). No regression (dcs_gate 25/25, dcs_intake 10/10). CRLF 100% preserved in every touched file -- no phantom byte win.
[2026-07-25] escalation-trigger check after the verdict: trigger (b) NOT fired -- this is the FIRST `SAFETY: halt` entry for this objective (regenerate with `grep -c "SAFETY: halt" 214-LOG.md`), so the mandatory convergence read does not yet apply. Trigger (a) NOT fired -- files_touched stayed inside the 201's declared blast radius. No sitrep owed.
[2026-07-25] NOTE constraining the disposition: the corrective names 214-LOG.md, which lives under `.dcs/**`. Specialists are barred from that tree by construction, so the "fix-taskings" path of execute.md step 9 is NOT available for this refutation -- it is IC/Dispatcher work. Spawning dcs-commander for COMMAND POINT 4 (verdict disposition). Announced; no file changes until the decision returns.
[2026-07-25] command: verdict -> fix_taskings (IC=dcs-commander, fable). The IC re-verified the refutation itself -- delivered doctrine-appendix.md:48-55 is the new model-availability paragraph while 51dd073:48-55 is the principle-15 provenance paragraph, and 51dd073:63-68 is indeed the ROT half of row 11's claim -- and ruled the defect real but confined to a transcription error in an append-only, deliberately unguarded artifact, with every substantive criterion passing to the byte. Disposition: the single fix-tasking is executed by the Dispatcher in its IC capacity (no specialist spawn -- `.dcs/**` is IC work by construction, plan.md lint 6), touching nothing but this log, then the Safety Officer re-verifies per execute.md step 8. Replanning was rejected as voiding a fully proven trim to repair a file no specialist may edit. No esg_activation: the ESG is founded, this is a halt not a pass, and goal drift is not possible at a halt disposition.

## CORRECTION -- routing-ledger transcription (IC work, per command point 4 directives)

**This entry supersedes the original ledger's redundancy citations.** The defect
is the **Dispatcher's own**, per the entry above: OPS-1 authored the ledger, but
the Dispatcher transcribed it into this append-only log under ruling B without
adding the regenerating commands principle 15 requires. Every line citation below
carries its regenerating command, and every command pins the commit **51dd073**
rather than symbolic `HEAD` -- `HEAD` moves at commit and merge, so a `HEAD`-based
command would rot one hop later and recreate this same defect.

**The original ledger mixed two coordinate systems in one unlabeled table:**

- **Destination rows 1, 4, 7, 8, 9, 10, 13, 14, 15** cite **POST-edit** line
  numbers, valid in **the delivered tree of this operational period**.
- **Redundancy rows 11, 12, 17, 18, 19, 20** cite **PRE-edit** line numbers, valid
  **only against commit 51dd073**. They do not resolve in the delivered tree,
  because this incident appended 114 lines to `doctrine-appendix.md` and shifted
  every line past 44.
- Row 21's citation (51dd073:31-44) is the one redundancy citation that resolves
  in **both** trees, because the append landed after line 44.

### Redundancy citations, restated as pinned runnable commands

| Row | Regenerating command (pinned to 51dd073) | Resolves to |
|---|---|---|
| 11 | `git show 51dd073:dcs/references/doctrine-appendix.md \| sed -n '48,55p'` **and** `... sed -n '63,68p'` | facts **TRAVEL** ("an unmeasured assertion travelled `IC prose -> Safety Officer prose -> ...`") **and** facts **ROT** (the `060132b0` -> `f21fdd42` reference-tree rot) |
| 12 | `git show 51dd073:dcs/references/doctrine-appendix.md \| sed -n '52,55p'` and `... sed -n '57,61p'` | "three times in one incident, once by each seat, including the IC and the Dispatcher"; and why the third instance could not be *corrected* |
| 17 | `git show 51dd073:dcs/references/doctrine-appendix.md \| sed -n '128,135p'` | "unattended operation is legitimate specifically *because* the Delegation ... is a written, Owner-signed artifact" |
| 18 | `git show 51dd073:dcs/references/doctrine-appendix.md \| sed -n '128,135p'` | same paragraph as row 17 |
| 19 | `git show 51dd073:dcs/references/doctrine-appendix.md \| sed -n '158,171p'` | "Why 'one worktree per division of the fire line' as the framing" |
| 20 | `git show 51dd073:dcs/references/doctrine-appendix.md \| sed -n '139,156p'` | "Field lesson 2026-07-23 (worktree audit scoping, v0.4.2)" |
| 21 | `git show 51dd073:dcs/references/doctrine-appendix.md \| sed -n '31,44p'` | "The ICS original: the person who first reports ... is never required to be the incident commander" |

**Row 11 coverage corrected.** The cut text asserted **two** forces -- derived
facts **rot** and they **travel**. The original citation (48-55) covers TRAVEL
only; ROT's provenance is at 51dd073:63-68. The corrected citation is **both
ranges**. Verified by running both commands above.

**Row 17 citation corrected.** The original cited 116-135; the claim holds only on
the **full** range **128-135**, not the deploy-delegation paragraph at 118-126
alone.

**Row 20 annotated.** The cited range covers **two of the three** cut examples of
a foreign worktree -- the agent harness's own and a deploy script's -- but never
"a human's personal one". **No rule is lost**: the core's surviving predicate is
total ("everything else is foreign"), so the three were pure illustration.

### Destination citations, with their regenerating commands

Valid in **the delivered tree of this operational period** (run from the worktree
root):

| Rows | Regenerating command | Lands as |
|---|---|---|
| 1, 2, 3 | `sed -n '46,60p' dcs/references/doctrine-appendix.md` | "**Model availability rationale.**" |
| 4, 5 | `sed -n '62,73p' dcs/references/doctrine-appendix.md` | "**Field lesson 2026-07-24 (quota vs. transcript).**" |
| 7 | `sed -n '120,129p' dcs/references/doctrine-appendix.md` | "**Principle 4 -- the cost of over-scope.**" |
| 8 | `sed -n '131,145p' dcs/references/doctrine-appendix.md` | "**Principle 6 -- one session, one project, in full.**" |
| 9 | `sed -n '147,159p' dcs/references/doctrine-appendix.md` | "**Principle 9b -- why single-shot, not resumed.**" |
| 10 | `sed -n '161,173p' dcs/references/doctrine-appendix.md` | "**Principle 13 -- the four-revisions field lesson.**" |
| 13 | `sed -n '175,182p' dcs/references/doctrine-appendix.md` | "**Principle 15 -- the test-inversion lesson.**" |
| 14 | `sed -n '184,194p' dcs/references/doctrine-appendix.md` | new `## The lifecycle (Planning P mapped to software)` section |
| 15 | `sed -n '221,228p' dcs/references/doctrine-appendix.md` | "**Field lesson 2026-07-24 (charter defect, not agent failure).**" |

Delivered appendix headings, for orientation (regenerate:
`grep -n "^## " dcs/references/doctrine-appendix.md`): 14 Why phases not nesting |
29 Transfer of command | 75 The working principles | 184 The lifecycle | 196
Relationship to project-specific protocols | 230 Automation layers | 251 Parallel
operation.

**Ruling B is amended** (IC directive, command point 4): the AAR carries the
original ledger **and this correction entry together**, with this entry
authoritative on all coordinates. The original ledger is never carried alone.

**Scope of this correction:** nothing outside this file changed. Territory,
measured bytes, and every criterion 1-11 result are undisturbed; no doctrine or
appendix edit was authorized by this disposition.

[2026-07-25] Re-spawning dcs-safety-officer (execute.md step 8) scoped to the corrected ledger and to confirming nothing outside `.dcs/**` moved since the halted verification. Fresh spawn, not a resumed agent (principle 9b).
[2026-07-25] SAFETY: halt (SECOND) -- one refutation, against the correction entry's row 17. The corrected citation 51dd073:128-135 homes ONE of the row-17 deletion's three clauses; the other two ("nor what counts as approval", "they only remove the Owner's need to type each phase command by hand") live at 51dd073:118-126 in the sentence "delegation removes a keystroke, not a record", which the correction explicitly excluded. Regenerate: `git show 51dd073:dcs/references/doctrine-appendix.md | grep -n 'keystroke\|hash-bound'` => lines 123, 125, 126 only, none in 128-135. The officer also found the entry internally incoherent -- 118-126 and 128-135 are disjoint, so calling 128-135 "the full range" in contrast to "118-126 alone" cannot be true under any reading; the wording only parses if 118-135 was meant. AGGRAVATING: the corrective DEGRADED this row. The original ledger's 116-135 spanned both paragraphs and homed all three clauses correctly; only its coordinate system was unlabeled. The fix relabeled the coordinates and broke the coverage.
[2026-07-25] Scope item (b) re-passed cleanly: `git diff --numstat` still exactly 5 files with identical counts (VERSION 1/1, doctrine-appendix.md 114/0, doctrine.md 18/20, package.json 1/1, test_doctrine_integrity.py 9/4); nothing untracked outside `.dcs/incidents/`; hot path still 22,121 + 14,596 = 36,717 B; guard 12/12 against the new 37 kB budget. All standing criterion results carry. Rows 11, 12, 18, 19, 20, 21 and all nine destination rows verified SOUND under adversarial re-check; the SHA pin is universal with zero symbolic HEAD; row 11's two-range fix and row 20's annotation are both true as written. The officer also independently confirmed 51dd073 is the true base commit (`git merge-base HEAD 51dd073` == 51dd073).
[2026-07-25] DISPATCHER'S OWN DEFECT, second instance, recorded plainly: officer 1 observed the coverage came from HEAD:128-135 "not the 118-126 paragraph alone"; the IC turned that observation into directive 7; the Dispatcher executed it literally WITHOUT re-reading the deleted clause against both ranges. Three seats, each faithfully transcribing the one before, none re-deriving -- the exact chain principle 15 exists to stop, occurring inside the remedy written for a principle-15 defect.
[2026-07-25] ESCALATION: trigger (b) -- second SAFETY: halt on the same objective (regenerate the tally with `grep -c "SAFETY: halt" 214-LOG.md`). 209 filed at C:\DCS\.dcs\esg\SITREPS\doctrine-hot-path-trim-p1.md (esg_root, the main checkout -- never this worktree's gitignored copy). Incident PAUSED pending the Owner's continue/pivot/demobilize.
[2026-07-25] CONVERGENCE READ (mandatory from the second halt, v0.5.9): SAME CLASS. Both halts are the same defect -- a hand-written line citation into a file this same incident is editing. The population (~30 such citations) was never enumerated by a check that validates each member, so each verify cycle surfaces the next defective one; halt 1 bought six instances, halt 2 bought one, and each further pass costs a full IC-write + Safety-verify cycle to remove exactly one. There is no bounded end, because nothing distinguishes "citations we have checked" from "citations that are correct". Per principle 13(b) the Owner's options therefore LEAD with raising the fix's altitude -- replacing every line-range citation with anchor-based ones (`grep -n "<bolded lead-in>" <file>`), which cannot rot under line shifts and cannot mis-cover because the anchor names the substance -- and NOT with an unqualified "continue". Note the trim itself is untouched by any of this: no doctrine byte has changed since OPS-1 returned `done`.
[2026-07-25] ESCALATION: trigger (b) -- second Safety halt, same class -- Owner: RAISE THE ALTITUDE. Decision recorded in the 209 at C:\DCS\.dcs\esg\SITREPS\doctrine-hot-path-trim-p1.md. Replace every line-range citation in the ledger with anchor-based citations; fix row 17 in the same pass.

## CORRECTION 2 -- ANCHOR-BASED CITATIONS (IC work, per the Owner's escalation decision)

**This entry supersedes BOTH the original ledger's citations and CORRECTION 1's.**
It is the altitude fix the convergence read called for: it does not resolve the
outstanding row-17 defect one instance at a time, it **removes line numbers from
the citation scheme entirely**, so the defect class -- a coordinate that rots, or
a range that under-covers -- stops being representable.

**Why anchors instead of ranges.** A line number is a derived fact about a file's
current shape; it rots the moment anything above it moves, and this incident moved
114 lines (regenerate: `git diff --numstat dcs/references/doctrine-appendix.md`).
A **content anchor** is not derived -- it *is* the substance being cited. It
resolves in any tree, it cannot silently drift, and a citation that stops matching
is loudly broken rather than quietly wrong. Every anchor below was verified to
return **exactly one** hit in its stated tree.

**The anchor scheme's own failure mode, found and handled while writing this:**
markdown hard-wraps prose, so an anchor phrase that spans a line break silently
matches nothing under line-based `grep`. The first draft of row 12's anchor
(`three times in one incident`) returned **0 hits** for exactly that reason. Every
anchor here is therefore chosen to sit **within a single physical line**, and every
one was uniqueness-checked before being written down. Regenerate any of them by
running its command; a hit count other than 1 means the anchor needs re-choosing,
not that the citation is wrong.

### Redundancy rows -- cut text already covered by pre-existing appendix prose

Run against the pre-edit tree, pinned to the SHA (never symbolic `HEAD`, which
moves at commit and merge):

| Row | Regenerating command | Covers |
|---|---|---|
| 11 | `git show 51dd073:dcs/references/doctrine-appendix.md \| grep -n -F "faithfully transcribed at every hop"` | derived facts **TRAVEL** |
| 11 | `git show 51dd073:dcs/references/doctrine-appendix.md \| grep -n -F "060132b0"` | derived facts **ROT** |
| 12 | `git show 51dd073:dcs/references/doctrine-appendix.md \| grep -n -F "once by each seat, including the IC and the Dispatcher"` | "every seat including the IC and the Dispatcher shipped one" |
| 12 | `git show 51dd073:dcs/references/doctrine-appendix.md \| grep -n -F "describe the shape instead"` | "the corrective was to delete the number, not to fix it" |
| **17** | `git show 51dd073:dcs/references/doctrine-appendix.md \| grep -n -F "not DCS deciding for itself what counts as routine"` | clause 1: **who holds command judgment** |
| **17** | `git show 51dd073:dcs/references/doctrine-appendix.md \| grep -n -F "keystroke, not a record"` | clauses 2 and 3: **what counts as approval**, and **removing only the Owner's keystroke** |
| 18 | `git show 51dd073:dcs/references/doctrine-appendix.md \| grep -n -F "not DCS deciding for itself what counts as routine"` | "legitimate only because the Delegation defines routine in writing" |
| 19 | `git show 51dd073:dcs/references/doctrine-appendix.md \| grep -n -F "one worktree per division of the fire line"` | the ICS imagery for all three analogies |
| 20 | `git show 51dd073:dcs/references/doctrine-appendix.md \| grep -n -F "Field lesson 2026-07-23 (worktree audit scoping"` | foreign-worktree examples (see annotation below) |
| 21 | `git show 51dd073:dcs/references/doctrine-appendix.md \| grep -n -F "The ICS original"` | "regardless of who first reported the incident" |

**ROW 17 IS HEREBY FIXED, and this is the refutation's resolution.** The row-17
deletion had **three** clauses: *who holds command judgment*, *what counts as
approval*, and *removing only the Owner's keystroke*. CORRECTION 1's
`51dd073:128-135` homed only the first. The second and third live in the sentence
*"delegation removes a keystroke, not a record"*, which CORRECTION 1 wrongly
excluded. Row 17 now carries **two anchors**, one per home, and coverage is
complete. Note that the anchor scheme made this fix structural rather than
arithmetic: there is no range to get wrong, only substance to name.

**Row 18 keeps its own single anchor.** Rows 17 and 18 genuinely have different
homes; CORRECTION 1 collapsing them onto one identical range is what obscured the
row-17 gap in the first place.

**Row 20 annotation (unchanged, re-verified).** The anchored paragraph names two
of the three cut examples -- the agent harness's own worktrees and a deploy
script's -- and never "a human's personal one". **No rule is lost**: the surviving
core predicate is total ("everything else is foreign -- NEVER an orphan, NEVER
flagged, NEVER handed a removal command"), so the three were illustration.

### Destination rows -- cut text relocated into the appendix

Run against the delivered file from the worktree root:

| Rows | Regenerating command |
|---|---|
| 1, 2, 3 | `grep -n -F "**Model availability rationale.**" dcs/references/doctrine-appendix.md` |
| 4, 5 | `grep -n -F "**Field lesson 2026-07-24 (quota vs. transcript).**" dcs/references/doctrine-appendix.md` |
| 7 | `grep -n -F "**Principle 4 — the cost of over-scope.**" dcs/references/doctrine-appendix.md` |
| 8 | `grep -n -F "**Principle 6 — one session, one project, in full.**" dcs/references/doctrine-appendix.md` |
| 9 | `grep -n -F "**Principle 9b — why single-shot, not resumed.**" dcs/references/doctrine-appendix.md` |
| 10 | `grep -n -F "**Principle 13 — the four-revisions field lesson.**" dcs/references/doctrine-appendix.md` |
| 13 | `grep -n -F "**Principle 15 — the test-inversion lesson.**" dcs/references/doctrine-appendix.md` |
| 14 | `grep -n -F "## The lifecycle (Planning P mapped to software)" dcs/references/doctrine-appendix.md` |
| 15 | `grep -n -F "**Field lesson 2026-07-24 (charter defect, not agent failure).**" dcs/references/doctrine-appendix.md` |

Rows 6 and 22 cite nothing because nothing was relocated: both were dropped as
non-substantive (a ceremonial lead-in, and a tail clause), and the Safety Officer
confirmed at halt 1 that no rule was lost in either.

### Two corrections to CORRECTION 1's own prose, per the officer's observations

- **The bare count.** CORRECTION 1 stated "appended 114 lines" with no regenerating
  command -- a principle-15 residue in the remedy for a principle-15 defect.
  Regenerate: `git diff --numstat dcs/references/doctrine-appendix.md`.
- **Off-by-one.** CORRECTION 1 said the append "shifted every line past 44". The
  first hunk is `@@ -45,0 +46,29 @@`, so it landed after line **45** and line 45
  did not shift. Regenerate: `git diff -U0 dcs/references/doctrine-appendix.md | grep "^@@"`.
  The claim over-stated the shift, so no citation was endangered -- and under the
  anchor scheme the boundary no longer matters at all.

**Ruling B, as amended at command point 4, now reads:** the AAR carries the
original ledger (for its coverage mapping, which passed) together with **this**
entry, which is authoritative on **all** citations. CORRECTION 1 is superseded and
is never carried as authoritative; it remains in this append-only log as the record
of what was tried.

**Scope:** nothing outside this file changed. No doctrine or appendix byte has
moved since OPS-1 returned `done`.

[2026-07-25] Re-spawning dcs-safety-officer (third verification, fresh spawn per principle 9b) scoped to the anchor-based citations and to re-confirming nothing outside `.dcs/**` moved.
[2026-07-25] SAFETY: pass -- period 1 complete. Verdict written verbatim to SAFETY.md. The officer extracted all 21 backtick-quoted commands from CORRECTION 2 PROGRAMMATICALLY rather than retyping them (so a transcription normalisation on its own side could not mask a broken anchor) and ran all 19 anchor commands twice each: 19/19 returned exactly one hit, zero returned 0, zero returned >1. Row 17's three clauses all traced and homed -- the second anchor `keystroke, not a record` resolves to 51dd073:126, the exact sentence halt 2 said CORRECTION 1 had wrongly excluded. Rows 6, 22 and the supersession chain re-derived independently from the diff rather than carried from prior officers. Scope (b) clean: 5 files with identical counts, hot path re-measured at 36,717 B, guard 12/12 against the new 37 kB budget, gate 25/25, intake 10/10, version sync atomic at 0.6.5, guard check logic untouched.
[2026-07-25] escalation-trigger check after the verdict: no NEW trigger. (b) already fired at the second halt and was resolved by the Owner's "raise the altitude" decision, recorded in the 209; this is a pass, not a third halt. (a) not fired -- files_touched still inside the 201's blast radius (5 files, unchanged since OPS-1 returned). No further sitrep owed.
[2026-07-25] Five officer OBSERVATIONS carried forward to the Owner's criterion-10 read rather than treated as refutations, all recorded verbatim in SAFETY.md: (i) row 16 is absent from CORRECTION 2's accounting -- no citation was owed (it was a compression-in-core) and the substance was verified surviving at delivered doctrine.md:118 and appendix:226, but the entry's list of citation-free rows is short by one; (ii) "rows 17 and 18 genuinely have different homes" is imprecise -- row 17's home set is a strict SUPERSET of row 18's, and the causal diagnosis of halt 2 is loose; (iii) "the defect class stops being representable" OVERCLAIMS BY HALF -- anchors eliminate ROT but not UNDER-COVERAGE; what they actually bought is that the population is now enumerable and each member is a one-command binary check, which is why one officer could exercise all 19 in a single script instead of the one-instance-per-cycle grind; (iv) row 17 clause 3's home is generalized rather than literal, with the literal content surviving in delivered core; (v) the new HOT_PATH_BUDGET_KB comment's "~319 B" lacks a command beside that specific number, though it is hedged and its derivation is recorded at this log's line 25.
[2026-07-25] Spawning dcs-commander for COMMAND POINT 4 (verdict disposition, second time this period -- the first disposed a halt, this disposes the pass). Announced; no file changes until the decision returns.
[2026-07-25] command: verdict -> close (IC=dcs-commander, fable). The IC re-verified the hot path (36,717 B), the atomic 0.6.5 version sync and the green guard itself, and additionally checked that `install.ps1` contains no reference to package.json -- so the queued mojibake row cannot contaminate the post-close auto-deploy; it gates only a future npm publish, which is Owner-only regardless. Reasoning on the four weighing points: (i) GOAL FULLY MET against the bar that binds -- the 201's "toward the post-diet size" is direction, not a number, and the number the Owner approved is criterion 1 at 36,864 B, which the Owner explicitly declined to relax, which "cuts both ways: it also fixes it as the bar"; the remaining ~4.7 kB gap to post-diet equivalence is "more to do", which is what the register is for, and nothing delivered depends on a further trim to be coherent. (ii) The 147 B / 1,171 B guard-blind band is the ratchet working as designed -- inherent to any kB-granularity ratchet, and it argues for a FUTURE re-seat to 36 after stability, not for more trimming inside an incident that has passed three verifications unchanged. (iii) The five observations are precision defects in CORRECTION 2's commentary, not its citations; a fourth write+verify cycle for prose polish is exactly the trade doctrine's close-default refuses. (iv) Nothing about this close should pause the deploy. No esg_activation -- the ESG is founded, the register is live, the three discovered rows are already queued, and no next-period objectives are proposed, so goal drift is not in play.
[2026-07-25] integration commit de50257 (5 files, 143 insertions / 26 deletions). Territory files staged explicitly by path, never `git add -A`. NOTE: the first attempt at this commit carried a stray `@` line at both ends of the message -- a PowerShell here-string delimiter leaking into a bash `-m` argument -- and was amended twice (78aae4d -> 391fc75 -> de50257) before the message was clean. Recorded because the DCS environment offers both shells and the two syntaxes are not interchangeable; the fix that worked was writing the message to a file and using `git commit --amend -F <file>`.
[2026-07-25] period 1 objectives assessed against 202-OBJECTIVES.md: criteria 1-9 and 11 MET; criterion 10 is the Owner-UAT close gate; criterion 12 is deploy-period work. Goal fully met at the approved bar. Proceeding to close.md.

## Close-out

[2026-07-25] close.md step 1 passed: SAFETY.md's latest verdict for period 1 is `pass`; integration commit de50257 exists, touches only the five territory files, and its message references the intake source (Owner chat via /dcs-run restating vault/Backlog.md item 1).
[2026-07-25] CRITERION 10 -- OWNER-UAT: DONE. The Owner was shown the complete doctrine.md diff (38 changed lines, `git show de50257 -- dcs/references/doctrine.md`) together with two borderline compressions the Dispatcher flagged on its own read: principle 13(f) lost its diagnosis of what three rejects MEAN (the three offers remain), and Parallel operation bullet 3's explicit "never reaches into a worktree early" is now only implied (the third Safety Officer flagged the same one independently). The Owner approved: no rule lost, no restoration requested. The ~180 B that restoring both would have cost was offered against the 147 B criterion-1 headroom and declined.
[2026-07-25] DEPLOY: the Owner explicitly DECLINED the in-line deploy train, overriding DELEGATION v1's `deploy.auto_after_close: true` on its first use. /dcs-run step 7a's deploy branch is therefore SKIPPED and the register row lands MERGED (deploy pending). ~/.claude/dcs/VERSION stays at the pre-incident version until someone runs /dcs-deploy. Recommend amending the Delegation at the next /dcs-esg so it stops asserting a default the Owner overrode.
[2026-07-25] AAR.md written. Facts-only: every "verified" claim cites the artifact checked at write time; deviation history taken from this log, not memory; Owner-UAT and deploy status both recorded as mandatory fields.
[2026-07-25] memory routing (close.md step 4) -- CLAUDE.md documents the three-store rule and names vault/00-Navigation.md as the entry point. Written to the WORKTREE's copies (v0.3.3), so they ride the merge: vault/Meta/building-dcs-lessons.md (new section 7, "Citations into a file the incident is editing" -- anchors over line ranges, the hard-wrap failure mode, the anchors-kill-rot-not-under-coverage correction, and the corrective-can-be-worse-than-the-defect pattern); vault/Metrics/incident-metrics.md (hot-path table normalised to the guard's 1024 basis -- it had mixed 1000- and 1024-based figures, making the diet look better than it was -- plus raw bytes, both regenerating commands, and this incident's row); vault/Backlog.md (item 1 marked DONE citing de50257, and three new items: 6 the remaining ~4.7 kB with schemas.md named, 7 the EOL sensitivity plus the guard-blind band, 8 the package.json corruption with the note that the encoding check cannot detect well-formed-UTF-8 mojibake). NOTHING was routed to doctrine.md or doctrine-appendix.md: these are lessons about BUILDING DCS, not about how DCS behaves, which is the vault's own stated test.
[2026-07-25] intake source closure (close.md step 5): the source is Owner chat restating vault/Backlog.md item 1 -- repo-local and maintainer-owned, no external system, no curating routine to delegate to, no production write at risk. Item 1 marked done in the close commit rather than flagged for the Owner.
[2026-07-25] close commit 6d70944 in the worktree (11 files: the 8 incident artifacts + 3 vault files).
[2026-07-25] close.md step 1a -- MERGE-TIME GUARD. Ran `python tests/test_doctrine_integrity.py` in the worktree: 12/12. Then checked the integration branch before merging and found MAIN HAD MOVED: HEAD was 0428ac4, not the 51dd073 this worktree branched from. Three commits landed during this incident, none through DCS -- 6a57b97 (v0.6.5), c09f0fd, 0428ac4 (v0.6.6). Not a territory violation: the portfolio partition governs incidents and nothing polices direct commits to main.
[2026-07-25] ESCALATION: trigger (a) -- the merge-time guard FAILS ON THE MERGE RESULT while passing on both branches independently, which is the exact defect class close.md step 1a exists to catch. Three premises invalidated: (1) 6a57b97 added 1,189 B to dcs/references/schemas.md, which this incident's 202 measurement convention, criterion 1 derivation and criterion 8 ratchet all treated as a fixed 14,596 B constant -- merged pair 37,906 B against a 37,888 B ratchet, RED BY 18 B; (2) versions 0.6.5 and 0.6.6 were both taken while this branch held 0.6.5, so dcs/VERSION conflicts; (3) 0428ac4 already fixed the package.json corruption OPS-1 discovered, rebuilding it 13.5 MB -> 1.3 kB, so this branch's edit to the old corrupt file conflicts and main's side must win entirely. Regenerate: `git merge-tree --write-tree --name-only HEAD dcs/doctrine-hot-path-trim`. Second 209 filed at C:\DCS\.dcs\esg\SITREPS\doctrine-hot-path-trim-p1-merge.md. NOT MERGED pending the Owner.
[2026-07-25] ESCALATION: trigger (a) -- Owner: RE-SEAT THE RATCHET AND MERGE. Resolution as decided, none of it taken unilaterally: dcs/VERSION -> 0.6.7 (0.6.5 and 0.6.6 taken); package.json -> main's side wholesale via `git checkout --ours`, then its version field edited 0.6.6 -> 0.6.7 with the Edit tool so guard check 1 holds (NOT with PowerShell -- per CLAUDE.md, and because PowerShell Get-Content/WriteAllText is precisely what caused the corruption 0428ac4 repaired); HOT_PATH_BUDGET_KB 37 -> 38, re-derived on the merge result as ceil(37906/1024)+1, with the comment rewritten to explain why it was re-derived rather than carried. tests/test_doctrine_integrity.py auto-merged cleanly -- this incident's ratchet edit and 0428ac4's two new checks touch different regions.
[2026-07-25] merge-time guard on the RESOLVED merge result: 14/14 passed, including "hot-path budget: doctrine+schemas <= 38 kB" and 0428ac4's new "no Cyrillic anywhere in the shipped package" and "package.json under 8 kB". Full suite green: dcs_gate 25/25, dcs_intake 10/10. Measured merged hot path 22,121 + 15,613 = 37,734 B, 1,178 B under the new budget, a 4,889 B reduction from main's pre-merge 42,623 B. (Note the merged figure is LOWER than the 37,906 B predicted from blobs: main's schemas.md is on disk with LF while the freshly-written doctrine.md is CRLF, so the tree now measures with two line-ending conventions at once -- registered as backlog item 7, and the reason that item now says the metric has no stable definition rather than merely an offset.)
[2026-07-25] stale artifacts corrected before committing the merge, all of them numbers this incident wrote that the three out-of-band commits falsified: AAR.md (both measured-vs-merged tables, the trigger-(a) escalation, three new lessons, the retired register row), vault/Backlog.md (item 1's shipped version and figures, item 6 with schemas.md promoted to strongest candidate, item 7's no-stable-definition finding, item 8 rewritten as FIXED OUT-OF-BAND with 0428ac4's cause analysis), vault/Metrics/incident-metrics.md (two new table rows and the measured-vs-merged lesson).
