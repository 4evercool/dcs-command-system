# 204-TASKING — S1

**Task:** Edit `dcs/workflows/status.md` line 102-103 per 202 acceptance criteria 1-3. Replace the four-state list `(QUEUED / ACTIVE / PARKED / CLOSED)` with the canonical seven-state enum `(QUEUED | ACTIVE | MERGED (deploy pending) | DEPLOYED | PARKED | KILLED | RESOLVED)` matching `dcs/templates/REGISTER.md:26-27` verbatim. Remove the word `CLOSED` as a register state. Re-express the history-at-a-glance clause: the rows that give the Owner history at a glance are MERGED, DEPLOYED, KILLED, and RESOLVED — not the non-existent CLOSED. Preserve line-wrapping consistent with the file's existing style. Do not touch any other file.

**Territory:** `dcs/workflows/status.md`

**Forbidden:** `dcs/templates/**`, `dcs/references/**`, `dcs/workflows/close.md`, `tests/**`, `agents/**`, `skills/**`, `dcs/hooks/**`, `dcs/VERSION`, `package.json`, `install.*`

**Evidence required:**
1. `grep -n 'QUEUED\|ACTIVE\|MERGED\|DEPLOYED\|PARKED\|KILLED\|RESOLVED' dcs/workflows/status.md` — show the corrected lines, confirming all seven states appear with `|` separator
2. `grep -n 'CLOSED' dcs/workflows/status.md` — must return no output (confirm `CLOSED` does not appear as a register state; lowercase "closed" at line 18 is fine)
3. `python tests/test_doctrine_integrity.py 2>&1 | tail -3` — confirm passes, no regression
4. `grep -c 'QUEUED' dcs/workflows/status.md` — confirm returns ≥ 1
