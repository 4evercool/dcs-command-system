# 204 — Tasking S1

**Incident:** provisioning-script-upstreaming
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Add a post-worktree-create provisioning hook to both worktree-creation sites. Satisfies 202 acceptance criterion 2 (mechanism integrated into new.md and execute.md).

**(a)** In `dcs/workflows/new.md` step 7b, after the `git worktree add` command block (line 198) and before "Everything below — the incident directory, 201-BRIEF.md, 214-LOG.md, ACTIVE — is written inside this worktree" (line 200), add a new paragraph: a version-tagged step that checks whether `<project_root>/.dcs/provision` exists. If it does, run it with the new worktree path as the first argument and the main checkout root as the second. Non-zero exit: warn the Owner and proceed (the worktree was created successfully; missing provisioning is a warning, not a stop). Absent file: skip silently.

**(b)** In `dcs/workflows/execute.md` step 4's worktree-isolated specialist clause (line 134-137), expand the parenthetical "set up the worktree (e.g. `git worktree add`)" to reference both the `git worktree add` AND the `.dcs/provision` convention from new.md.

Keep additions concise. `new.md` has 5 lines of headroom (255/260 grandfathered); `execute.md` has 0 (450/450). S2 adjusts the grandfather ceilings, but the additions must still be measured in single-digit line counts.

## File territory (may edit only within these globs)

- `dcs/workflows/new.md`
- `dcs/workflows/execute.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/references/**`
- `tests/**`

## Worktree root

C:\DCS-wt\provisioning-script-upstreaming

## Evidence required in the return

- `git diff dcs/workflows/new.md` — show the new provisioning paragraph positioned after the `git worktree add` block and before the incident-directory creation paragraph
- `git diff dcs/workflows/execute.md` — show the expanded worktree-isolated clause with the provisioning reference
- Read `dcs/workflows/new.md` step 7b from "Create the branch and worktree" through substep 1 (directory creation) — confirm the provisioning step sits between worktree creation and file writes with no gaps or ambiguities

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
