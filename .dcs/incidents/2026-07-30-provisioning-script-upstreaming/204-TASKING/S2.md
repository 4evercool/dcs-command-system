# 204 — Tasking S2

**Incident:** provisioning-script-upstreaming
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Document the `.dcs/provision` convention and adjust test budget ceilings. Satisfies 202 acceptance criterion 2 (documentation) and criterion 4 (tests pass).

**(a)** In `dcs/references/doctrine.md`'s "Parallel operation" section, add a new subsection documenting:
- What the convention is: a project-supplied hook for worktree environment setup
- Where it lives: `<project>/.dcs/provision` (an executable or script)
- The invocation contract: args = (worktree-path, main-checkout-root); exit 0 = success, non-zero = warn-and-proceed; absent file = skip silently
- The idempotency expectation: safe to re-run, every target skipped if already present
- The DCS guarantee: DCS provides only the execution point, never any project-specific provisioning content

**(b)** In `dcs/references/doctrine-appendix.md`, add a provenance paragraph tracing the convention to:
- bread_bot's `.dcs/provision-worktree.ps1` (commit `4ae52377`, 2026-07-27)
- The three motivating bread_bot incidents: `cost-dynamics-labor-toggle`, `cost-dynamics-per-product`, `tools-prod-db-guards`
- The fable-review-roadmap rec-6 chain: Phase 0 field repair → "LOCAL — candidate for upstreaming" → two-day silent drift → register row `provisioning-script-upstreaming`

**(c)** In `tests/test_doctrine_integrity.py`:
- Adjust `WORKFLOW_GRANDFATHERED_LINES`: `new.md` from 260 to 270, `execute.md` from 450 to 460. Update the comments next to each entry to cite this incident as the measurement source.
- Measure the hot-path size after your doctrine.md additions. Current: 36,563 bytes normalized / 36,864 budget (301 bytes headroom). If the additions exceed ~300 bytes, adjust `HOT_PATH_BUDGET_KB` from 36 to 37. Update the ceiling comment to cite this incident.
- Run the test to confirm all checks pass with the adjusted ceilings.

## File territory (may edit only within these globs)

- `dcs/references/doctrine.md`
- `dcs/references/doctrine-appendix.md`
- `tests/test_doctrine_integrity.py`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/**`

## Worktree root

C:\DCS-wt\provisioning-script-upstreaming

## Evidence required in the return

- `python tests/test_doctrine_integrity.py` — full output showing all checks pass (no budget violations, no structural defects, encoding clean)
- `git diff dcs/references/doctrine.md` — excerpt showing the new provisioning subsection under "Parallel operation" with the invocation contract stated
- `git diff dcs/references/doctrine-appendix.md` — excerpt showing the provenance paragraph citing bread_bot's script, the three incidents, and the review-to-register chain
- `git diff tests/test_doctrine_integrity.py` — excerpt showing the adjusted `WORKFLOW_GRANDFATHERED_LINES` entries (new.md: 270, execute.md: 460) and `HOT_PATH_BUDGET_KB` (36→37 if needed) with updated measurement-source comments

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per `references/schemas.md` #4 (ops-specialist return), with `found`, `why_plan_wrong`, and a `proposal` (a recommendation, not an action). The IC will re-enter planning around your finding.
