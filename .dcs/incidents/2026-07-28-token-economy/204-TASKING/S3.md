<!--
204-TASKING/S3.md -- transcribed verbatim from dcs-planning-chief's
taskings[] (schemas.md #2).
-->

# 204 — Tasking S3

**Incident:** 2026-07-28-token-economy
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

202 criterion 2's workflow sites and criterion 4's input half.

**(a) Criterion 2:** rephrase every 214-LOG read instruction in
`dcs/workflows/plan.md` and `dcs/workflows/execute.md` as the bounded
command that answers it — `plan.md:38` (entry-gate command-chain check)
and `plan.md:573` (pre-stamp checklist) both become an existence grep with
the command shown inline (`grep -n "command: typed" <incident_dir>/214-LOG.md`,
likewise `command: iap_review`); `execute.md:25`'s "must contain both a
`command: typed` and a `command: iap_review` entry" becomes the same. Do
not weaken what any check DECIDES — only how it reads the file. Leave
`execute.md:227`'s already-scoped `verdict_disposition` bound exactly as
it is (it is the model the rest copy), and leave every APPEND to 214-LOG
untouched. Find your sites with the enumerator in the evidence list rather
than trusting this list — it is a census and this tasking is the reason it
will rot.

**(b) Criterion 4:** in `execute.md` step 8, add two inputs to the Safety
Officer spawn, needed because a second officer cannot cite what it was
never given — the prior same-period verdict(s) VERBATIM, and a "changed
since" manifest (`git diff --name-only` of what the fix-taskings touched
since that verdict). Both must carry step 8's existing framing — "claims
to verify, not facts". Confirm and state in your return that step 9's
fix-tasking branch routes the re-spawn back through step 8, so the new
inputs actually reach it. S2 writes the charter half that consumes these —
the names you give these two inputs are the contract; state them plainly
in your return so the Safety Officer and the IC can check the two halves
agree.

Neither file may grow materially: `plan.md` is already 663 lines and
`execute.md` 417 against this project's ~250-line workflow budget.

## File territory (may edit only within these globs)

- `dcs/workflows/plan.md`
- `dcs/workflows/execute.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/run.md`
- `dcs/workflows/loop.md`
- `dcs/workflows/new.md`
- `dcs/workflows/close.md`
- `dcs/workflows/esg.md`
- `dcs/workflows/deploy.md`
- `dcs/workflows/status.md`
- `dcs/workflows/init.md`
- `agents/**`
- `dcs/references/**`
- `dcs/templates/**`
- `dcs/hooks/**`
- `skills/**`
- `tests/**`
- `bin/**`
- `.dcs/**`
- `install.ps1`
- `install.sh`
- `package.json`
- `dcs/VERSION`

## Evidence required in the return

- `rg -U -n "(read|reads|reading|contain|contains|history|inspect|full)
  [^.]{0,80}214-LOG\.md|214-LOG\.md[^.]{0,90}(in full|must contain|read|
  history)" dcs/workflows/plan.md dcs/workflows/execute.md` — full output,
  before and after; every hit must show a bounded command or
  `execute.md:227`'s existing period+20-lines bound
- `grep -n -A4 'Command-chain check' dcs/workflows/plan.md
  dcs/workflows/execute.md` and `grep -n -A4 'Pre-stamp checklist'
  dcs/workflows/plan.md` — full output, showing all three sites now name
  the command
- The quoted new text of `execute.md` step 8 listing both new spawn inputs
  by name, plus the quoted line from step 9's fix-tasking branch that
  routes back to step 8 — this pair is the whole evidence that criterion
  4's input half reaches a second officer
- `grep -n 'scoped, v0.5.1' dcs/workflows/execute.md` — full output,
  proving the pre-existing `verdict_disposition` bound is unchanged
- `wc -l dcs/workflows/plan.md dcs/workflows/execute.md` — full output
  (663 / 417 before; report the delta)
- `python tests/test_doctrine_integrity.py` — final `N/M passed` line,
  plus `git diff -- dcs/workflows/plan.md dcs/workflows/execute.md` quoted
  at the decisive hunks only

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
