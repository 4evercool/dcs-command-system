<!--
204-TASKING/S2.md -- transcribed verbatim from dcs-planning-chief's
taskings[] (schemas.md #2), with conditionals resolved by the IC per
dcs-commander's instruction at command point 2: criterion 2's enumerator
WAS widened, so part (c) below applies in full, including the
agents/dcs-commander.md half.
-->

# 204 — Tasking S2

**Incident:** 2026-07-28-token-economy
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

202 criterion 6 in full, plus criterion 4's charter half, plus criterion
2's agent-charter sites.

**(a) Criterion 6:** state the brevity rule — cite the decisive excerpt or
a `file:line` location, never paste a full unabridged transcript — in all
five named locations: `dcs/references/schemas.md`'s `evidence` (#4, line
75), `checked`/`refutations`/`advisories` (#5, lines 100-102) and the
tasking object's `evidence_required` (#2, line 48); `agents/dcs-ops-specialist.md`;
`agents/dcs-safety-officer.md`; `agents/dcs-planning-chief.md`;
`dcs/templates/204-TASKING.md` (its `evidence_required` comment, lines
32-34). Add it as SHORT CLAUSES inside the existing tables/sections, not
new prose blocks: schemas.md is on the hot path with 719 B of measured
slack. It must not read as licence to paraphrase — schemas.md #4's "Real
output, not a paraphrase" and the ops-specialist charter's "Do not
paraphrase" both stay; excerpting is quoting less, not quoting loosely.

**(b) Criterion 4:** in `agents/dcs-safety-officer.md`, add the
by-reference allowance to step 2 (which today says only "Re-run the tests
yourself, independently", unconditionally). A verdict written when
`SAFETY.md` already holds a verdict for THIS period may cite a prior
verdict's `checked[]` entry by reference instead of restating it — but
only for a subject the officer has itself just established is unchanged
with a named command it ran (a scoped `git diff` returning empty or
equivalent), and never for anything in the fix-tasking's `files_touched`,
which is re-derived in full. "Unchanged" asserted by a specialist, the IC,
or the earlier verdict is a claim to check, exactly like `tests_run`; say
so in the charter's existing `<forbidden>` voice. The input this rule
consumes is named by S3 in `execute.md` step 8 — use exactly the names
S3's tasking states, do not invent your own.

**(c) Criterion 2 — this period's enumerator was widened (confirmed by
the Dispatcher and independently re-verified by dcs-commander at command
point 2); perform this part in full, it is not conditional:** give the
Safety Officer charter an explicit bounded 214-LOG statement (it names
none today), reusing `execute.md:227`'s existing bound verbatim —
"entries for the CURRENT period plus the last ~20 lines, never the whole
file"; and bound `agents/dcs-commander.md:63`'s "period history from
214-LOG.md" the same way.

## File territory (may edit only within these globs)

- `agents/dcs-safety-officer.md`
- `agents/dcs-ops-specialist.md`
- `agents/dcs-planning-chief.md`
- `agents/dcs-commander.md`
- `dcs/references/schemas.md`
- `dcs/templates/204-TASKING.md`

## Forbidden zones (explicitly, even if it seems related)

- `agents/dcs-logistics-chief.md`
- `agents/dcs-situation-analyst.md`
- `dcs/references/doctrine.md`
- `dcs/references/doctrine-appendix.md`
- `dcs/references/typing.md`
- `dcs/references/forms.md`
- `dcs/workflows/**`
- `dcs/templates/REGISTER.md`
- `dcs/templates/STRATEGY.md`
- `dcs/templates/214-LOG.md`
- `dcs/templates/201-BRIEF.md`
- `dcs/templates/202-OBJECTIVES.md`
- `dcs/templates/203-ORG.md`
- `dcs/templates/209-SITREP.md`
- `dcs/templates/AAR.md`
- `dcs/templates/DELEGATION.md`
- `dcs/templates/IAP.md`
- `dcs/templates/config.json`
- `dcs/hooks/**`
- `skills/**`
- `tests/**`
- `bin/**`
- `.dcs/**`
- `install.ps1`
- `install.sh`
- `package.json`
- `dcs/VERSION`

## Evidence required in the return

- `grep -niE "verbos|concise|terse|paraphrase|paste|full output|unabridged|
  one line|one paragraph" agents/dcs-*.md dcs/references/schemas.md
  dcs/templates/204-TASKING.md` — full output; the new rule text must be
  visible in all five criterion-6 locations (baseline this period returned
  12 hits, none of which is the rule)
- `python -c "from pathlib import Path; d=Path('dcs/references/doctrine.md').read_bytes().replace(b'\r\n',b'\n'); s=Path('dcs/references/schemas.md').read_bytes().replace(b'\r\n',b'\n'); print(len(d), len(s), len(d)+len(s), 37*1024-(len(d)+len(s)))"` —
  full output; the last number is remaining slack and must stay >= 0 (it
  was 719 B before your change)
- `python tests/test_doctrine_integrity.py` — the hot-path budget line and
  the final `N/M passed` line
- By-reference dry run against the 201's own baseline, all four parts in
  the return: (i) `wc -c .dcs/incidents/2026-07-27-register-field-repair-path/SAFETY.md`
  and `grep -c '"verdict"' <same file>` — real output (expect 21415 / 3;
  regenerate, do not copy); (ii) for each `checked[]` entry in verdicts 2
  and 3, a one-line classification — citable-by-reference under your new
  rule (naming the exact confirming command the rule would force the
  officer to run) or must-re-derive; (iii) the resulting byte figure for
  those two verdicts under the rule, beside the current one; (iv) at
  least one real entry from that artifact that the rule FORBIDS citing
  because the fix-tasking touched its subject — the demonstration that a
  genuine change is still independently caught
- `git diff -- agents/ dcs/references/schemas.md dcs/templates/204-TASKING.md` —
  quote the decisive hunks only (the added rule text in each of the five
  locations, and the charter allowance), applying the very rule this
  tasking adds
- `rg -U -n "(read|reads|reading|contain|contains|history|inspect|full)
  [^.]{0,80}214-LOG\.md|214-LOG\.md[^.]{0,90}(in full|must contain|read|
  history)" agents/*.md` — full output, showing the Safety Officer's new
  bounded statement and `agents/dcs-commander.md:63` bounded

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
