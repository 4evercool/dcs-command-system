<!--
204-TASKING/S1.md -- transcribed verbatim from dcs-planning-chief's
taskings[] (schemas.md #2), with one conditional resolved by the IC per
dcs-commander's instruction at command point 2: criterion 2's enumerator
WAS widened (confirmed, see 214-LOG.md), so the 214-LOG-bounding sentence
below applies -- it is not carried as a conditional.
-->

# 204 — Tasking S1

**Incident:** 2026-07-28-token-economy
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

202 criterion 1 (both halves), plus criterion 2's sites inside these two
files.

In `dcs/workflows/run.md`: delete `@$HOME/.claude/dcs/workflows/{new,plan,
execute,close}.md` from `<required_reading>`, keeping `@doctrine.md`. Then,
at steps 3, 4, 5 and 7, extend each existing "Read `<phase>.md` and execute
its `<process>`" instruction so it ALSO instructs reading the files that
workflow's own `<required_reading>` block names — phrased generically over
the block ("read the files its own `<required_reading>` names"), never as
a hand-listed set of filenames (a census rots when a workflow's block
changes; principle 15). State in-line WHY: a Read-tool read returns those
`@` lines as literal text and does not resolve them, so without this the
phase's references stop loading. Carry the asymmetry rule: where there is
doubt whether `doctrine.md` is still in context, re-read it. Use run.md's
own step 7a (`deploy.md`, read at the point of use, never eagerly
included) as the in-file precedent.

Do NOT remove `@$HOME/.claude/dcs/workflows/run.md` from `loop.md`'s
block — `run.md` is not a phase workflow and every sweep executes it
unconditionally; loop.md's saving is transitive.

**Criterion 2's enumerator was widened this period (confirmed by the
Dispatcher and independently re-verified by dcs-commander at command
point 2) — perform this part, it is not conditional:** bound the 214-LOG
read instructions in your two files: `run.md:101`'s IAP-stamp count
becomes a `grep -c`-shaped count; `loop.md:102`'s pause-state confirmation
becomes a bounded tail/grep.

## File territory (may edit only within these globs)

- `dcs/workflows/run.md`
- `dcs/workflows/loop.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/new.md`
- `dcs/workflows/plan.md`
- `dcs/workflows/execute.md`
- `dcs/workflows/close.md`
- `dcs/workflows/esg.md`
- `dcs/workflows/deploy.md`
- `dcs/workflows/status.md`
- `dcs/workflows/init.md`
- `agents/**`
- `dcs/references/**`
- `dcs/templates/**`
- `dcs/hooks/**`
- `skills/**`
- `tests/**`
- `bin/**`
- `.dcs/**`
- `install.ps1`
- `install.sh`
- `package.json`
- `dcs/VERSION`

## Evidence required in the return

- `grep -n '^@' dcs/workflows/run.md dcs/workflows/loop.md` — full output
  (this is 202 criterion 1's own stated check; no phase-workflow file may
  remain in either unconditional block, and `@doctrine.md` must remain in
  both, `@run.md` in loop.md)
- `grep -n -A6 '<required_reading>' dcs/workflows/new.md dcs/workflows/plan.md
  dcs/workflows/execute.md dcs/workflows/close.md` — full output, paired
  with the quoted new text of run.md steps 3/4/5/7, showing each step's
  instruction covers that workflow's block generically rather than by
  name-list
- `sed -n '1,20p' dcs/workflows/new.md` — full output, as the standing
  demonstration that a file read returns `@` lines literally (the reason
  the second half of this tasking exists)
- `git diff -- dcs/workflows/run.md dcs/workflows/loop.md` — quote the
  decisive hunks (the deleted include lines and each amended read
  instruction), not the whole diff
- `python tests/test_doctrine_integrity.py` — the `@`-include check line
  and the final `N/M passed` line (currently 83/83; regenerate, do not
  trust that number)
- `rg -U -n "(read|reads|reading|contain|contains|history|inspect|full)
  [^.]{0,80}214-LOG\.md|214-LOG\.md[^.]{0,90}(in full|must contain|read|
  history)" dcs/workflows/run.md dcs/workflows/loop.md` — full output,
  every hit showing a bounded command

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
