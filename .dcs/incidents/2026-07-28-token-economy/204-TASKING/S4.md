<!--
204-TASKING/S4.md -- transcribed verbatim from dcs-planning-chief's
taskings[] (schemas.md #2).
-->

# 204 — Tasking S4

**Incident:** 2026-07-28-token-economy
**Period:** 1
**Specialist:** dcs-ops-specialist (S4)

## Task

202 criterion 3's mechanism, in full and in shipped-safe language.

**(a)** In `dcs/templates/REGISTER.md`, state the two-state rule for the
row: while `State` is QUEUED or ACTIVE, `Territory` is a bare glob list —
no rationale prose — because `new.md` step 7b scans that column across
every ACTIVE row; at the transition to any terminal state (MERGED /
DEPLOYED / PARKED / KILLED / RESOLVED), `Territory`, `Outcome` and `Intake
source` each collapse to ONE LINE pointing into the incident's own
authoritative record (`IAP.md`'s partition table, `AAR.md`'s Outcome
section, the original intake citation) instead of restating it. Caps are
numbers, not adjectives: one line each.

**(b)** Place the collapse at the three write points that already touch
the row, as an instruction in each, not as a separate archival pass:
`close.md` step 5a.3 (ACTIVE -> MERGED), `deploy.md`'s MERGED -> DEPLOYED
step, `esg.md` step 4's park/kill handling.

**(c)** In `dcs/templates/STRATEGY.md` and `dcs/workflows/esg.md` step 4,
cap a `## Sessions` entry at <= 5 lines (date, one-line decisions,
Delegation version in force, optional pointer) — the template's own shape
and `esg.md`'s own existing "one-line summary of the decisions made,"
which practice has drifted from at ~64 lines/entry — and route substantial
decision rationale to **the project's own decision store, if its
`CLAUDE.md` documents one** (doctrine's "Relationship to project-specific
protocols"). Do NOT write `vault/` or `vault/Decisions/` into these files:
both ship, and this project's CLAUDE.md bars shipping project facts; the
project-agnostic phrasing is what lets this repo's own vault satisfy the
rule at runtime.

**(d)** `dcs/workflows/esg.md` must state all of (a)-(c) explicitly — it
names none of them today.

Two things you must NOT do: do not edit `new.md` step 7b or `plan.md` step
5a (both already write Territory as globs — verify this read-only and say
so in your return, they are another specialist's territory); and do not
touch any real register or strategy file — `<esg_root>/.dcs/esg/*` is
another working tree AND under `.dcs/`, read-only for you. Compacting this
project's own bloated rows is IC work at close, using the mechanism you
build. `esg.md` is 137 lines; keep it under the ~250-line budget.

## File territory (may edit only within these globs)

- `dcs/workflows/esg.md`
- `dcs/workflows/close.md`
- `dcs/workflows/deploy.md`
- `dcs/templates/REGISTER.md`
- `dcs/templates/STRATEGY.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/new.md`
- `dcs/workflows/plan.md`
- `dcs/workflows/execute.md`
- `dcs/workflows/run.md`
- `dcs/workflows/loop.md`
- `dcs/workflows/status.md`
- `dcs/workflows/init.md`
- `agents/**`
- `dcs/references/**`
- `dcs/templates/204-TASKING.md`
- `dcs/templates/214-LOG.md`
- `dcs/templates/201-BRIEF.md`
- `dcs/templates/202-OBJECTIVES.md`
- `dcs/templates/203-ORG.md`
- `dcs/templates/209-SITREP.md`
- `dcs/templates/AAR.md`
- `dcs/templates/DELEGATION.md`
- `dcs/templates/IAP.md`
- `dcs/templates/config.json`
- `dcs/hooks/**`
- `skills/**`
- `tests/**`
- `bin/**`
- `.dcs/**`
- `install.ps1`
- `install.sh`
- `package.json`
- `dcs/VERSION`

## Evidence required in the return

- `grep -n -i -E "bare glob|one line|pointer|terminal state|ACTIVE"
  dcs/templates/REGISTER.md dcs/workflows/esg.md` — full output, showing
  the two-state Territory rule and the one-line caps present in both the
  template and esg.md
- `grep -n -B2 -A6 'MERGED' dcs/workflows/close.md dcs/workflows/deploy.md`
  and `grep -n -A8 'Update .REGISTER.md. rows' dcs/workflows/esg.md` —
  full output, showing the collapse instruction present at all three
  write points
- `grep -rn -i 'vault' dcs/workflows/esg.md dcs/templates/STRATEGY.md
  dcs/templates/REGISTER.md dcs/workflows/close.md dcs/workflows/deploy.md` —
  must return NOTHING (the shipped-package rule); paste the empty result
- Read-only confirmation, quoted: the line in `new.md` step 7b and the
  line in `plan.md` step 5a that already write Territory as globs —
  proving no edit is owed there and the ACTIVE-row scan still has exactly
  the input it needs
- Baseline for the IC's close-time compaction, run read-only against the
  live files and reported with the command beside it:
  `wc -c <esg_root>/.dcs/esg/REGISTER.md <esg_root>/.dcs/esg/STRATEGY.md`
  and the column-attribution
  `awk -F'|' '/^\| [A-Za-z0-9]/ && NF>=13 {for(i=1;i<=13;i++) len[i]+=length($i)}
  END{for(i=2;i<=13;i++) print i, len[i]}' <esg_root>/.dcs/esg/REGISTER.md` —
  full output of both; resolve `<esg_root>` with `git worktree list
  --porcelain` (first entry)
- A worked example in `dcs/templates/REGISTER.md` showing one ACTIVE row
  and one terminal row side by side under the new rule, quoted in the
  return — the concrete artifact the Safety Officer checks the caps
  against
- `wc -l dcs/workflows/esg.md dcs/workflows/close.md dcs/workflows/deploy.md`
  (137 / 269 / 275 before) and `python tests/test_doctrine_integrity.py` —
  final `N/M passed` line

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
