# 204 — Tasking S1

**Incident:** independence-fail-closed-and-model-floor
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

References, schema and template layer — criteria 3, 5, 6, plus the
doctrine backing criterion 7's model floor rests on.

(a) Criterion 5: state the independence/regenerability rule in
`dcs/references/doctrine.md`, either as an explicit extension of
principle 7 ("Independent safety authority") or as a new principle 17 —
a Safety verdict whose independent-agent origin cannot be established, or
whose `checked` entries do not reproduce, is a HALT, never a silent
pass.

(b) Criterion 3: amend schemas.md #5's EXISTING `checked` field-table row
so it states each entry must be a REGENERABLE command — one whose output
a later reader can re-run and compare — never a description or a
one-off manual observation. DO NOT add a new field to #5: guard checks
18 and 20 would then force edits in `agents/` (yours) AND `execute.md`
(S3's territory), which is a partition break; if you conclude a new
field is unavoidable, that is a deviation, not a judgement call.

(c) Fix BOTH #5 example blocks (the IC's accepted Planning Chief feedback
item 3): `schemas.md:102`'s halt example has two bare `checked` commands
carrying no recorded observation, making them non-conforming under (b)
and giving S4's tool nothing to compare against — give each entry an
observation in the shape line 91's pass example already uses; both
blocks must still parse with `json.loads` (check 19).

(d) Criterion 6: add `approved_models` (string[]) to schemas.md #7's
field table and JSON example, and to `dcs/templates/DELEGATION.md`'s
founding delegation-bounds block, with a documented default and explicit
empty/absent semantics — empty or absent means NO model is approved, so
every site falls back to full v0.1 every-gate-is-an-Owner-gate behaviour
(the fail-closed reading; say it, do not leave it inferred).

(e) Keep `agents/dcs-safety-officer.md`'s `<output_contract>` table and
its step-6 prose consistent with (b) — the charter and schemas.md have
silently disagreed before (appendix records the v0.6.5 `advisories`
drift).

**FUNDING — READ THIS AS WRITTEN:** the hot-path ledger counts
normalised bytes of `dcs/references/doctrine.md` +
`dcs/references/schemas.md` ONLY (`tests/test_doctrine_integrity.py:341`;
regen command verbatim at `:211`). `doctrine-appendix.md` is NOT in the
sum. Therefore every byte of funding must come from COMPRESSING
doctrine.md/schemas.md PROSE ITSELF, and every removed passage must be
MOVED VERBATIM into a new `### D6: <title> (incident
independence-fail-closed-and-model-floor, 2026-08-03)` anchor in
doctrine-appendix.md, following the D5 format exactly: the quoted
original, then an explicit "Removed: ... Preserved: ..." itemisation of
every operative guarantee that survived. A trim applied TO
doctrine-appendix.md frees zero hot-path bytes and must NEVER appear in
your deletion ledger as funding. Nothing is deleted from the repository
— only from the hot path.

Measured menu with existing anchors: doctrine.md `## Parallel operation
(v0.3)` L134-155 = 3068 B (anchor `## Parallel operation`, appendix
L507; L136-140's three ICS analogies already have a full spec at
`docs/spec-v0.3-parallel.md`, and L148/L152 carry long explanatory
tails), `## The lifecycle` = 1482 B (anchor L440), `## Automation layers
(v0.2)` = 1448 B (anchor L486), `## Relationship to project-specific
protocols` = 1094 B (anchor L452); schemas.md `## 2. Chief plan` = 2491 B
incl. a 903 B worked example, `## 6. Commander decisions` = 1810 B
(anchor `## Structured return schemas`, appendix L590).

**DO NOT TOUCH, these look like slack and are guard-pinned:** doctrine.md's
verbatim `GRAMMAR_LINE` quote in principle 13 (check 12c requires it
verbatim; 205 B), all three sentinel tokens in doctrine.md (check 12f),
any doctrine heading referenced by name from a workflow or charter —
"Transfer of command", "Relationship to project-specific protocols",
"Parallel operation", "Automation layers", "never a silent wait" (check
6; compress a BODY, never rename or delete a heading), and principle
numbering contiguity from 1 (check 2 — a new principle must be 17, and
nothing may be renumbered).

**Ledger invariant to land:** `bytes_removed_from(doctrine.md+schemas.md)
>= bytes_added_to(doctrine.md+schemas.md) - 54`.

## File territory (may edit only within these globs)

- `dcs/references/doctrine.md`
- `dcs/references/schemas.md`
- `dcs/references/doctrine-appendix.md`
- `dcs/references/typing.md`
- `dcs/templates/DELEGATION.md`
- `agents/dcs-safety-officer.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/**`
- `dcs/tools/**`
- `tests/**`
- `dcs/hooks/**`
- `dcs/references/forms.md`
- `dcs/templates/214-LOG.md`
- `dcs/templates/config.json`
- `agents/dcs-planning-chief.md`
- `agents/dcs-commander.md`
- `agents/dcs-ops-specialist.md`
- `dcs/VERSION`
- `package.json`
- `.dcs/**`
- `vault/**`

## Worktree root

`C:\DCS-wt\independence-fail-closed-and-model-floor`

## Evidence required in the return

- HOT-PATH LEDGER, before and after: run the regen command recorded
  verbatim at `tests/test_doctrine_integrity.py:211` (do not retype it —
  copy it from that line) on the pre-edit tree via `git stash`/`git
  show <base>:` and again on the final tree. Report both triples
  (doctrine, schemas, sum) and the arithmetic against 37888. Baseline for
  comparison: 23872 13962 37834, headroom 54.
- FUNDING LEDGER, one row per compressed passage: source file, pre-edit
  line range, bytes removed, bytes added, and the `### D6` sub-item now
  carrying the verbatim original. State explicitly, as its own line, that
  no row claims a doctrine-appendix.md deletion as funding. Then `git
  diff --numstat dcs/references/` — doctrine.md and/or schemas.md must
  show a net shrink; doctrine-appendix.md must GROW.
- VERBATIM-PRESERVATION PROOF, mechanical not asserted: for EVERY moved
  passage, a short python one-liner that pulls the pre-edit text out of
  `git show <base>:dcs/references/<file>`, whitespace-normalises it, and
  asserts it is a substring of the whitespace-normalised post-edit
  doctrine-appendix.md. Paste the command and its True/False output per
  passage. Any False is a content-loss defect, not a formatting nit.
- `grep -n "regenerable" dcs/references/schemas.md` — must return at
  least one match, and the reported line number must fall inside section
  #5 (show the `## 5.` and `## 6.` heading line numbers alongside so the
  containment is checkable, not asserted).
- `grep -n "approved_models" dcs/references/schemas.md
  dcs/templates/DELEGATION.md` — before-count is ZERO in the whole
  payload (verified), so every match is new. Report matches for both
  files.
- Decisive excerpt only (not the whole section): the post-edit `checked`
  field-table row from #5, and the post-edit halt example block from
  around `schemas.md:102` showing each `checked` entry now carries a
  recorded observation.
- If a new principle was added: `python tests/test_doctrine_integrity.py
  2>&1 | grep -i principle` showing the numbering cases green, plus the
  new principle's number.
- `python tests/test_doctrine_integrity.py` — report the trailing `N/M
  passed` line (baseline 156/156) AND explicitly confirm green for
  checks 2 (principle numbering), 6 (doctrine sections referenced by
  name), 7 (hot-path budget), 12 (log grammar), 18 (schema field
  contract carrier), 19 (schemas.md JSON blocks parse), 20 (inbound
  field-presence guard). Name any case that changed state.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
