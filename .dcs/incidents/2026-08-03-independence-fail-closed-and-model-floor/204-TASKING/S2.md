# 204 — Tasking S2

**Incident:** independence-fail-closed-and-model-floor
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Criterion 7 — gate all nine bound-read sites across six workflow files
on the new model floor. At each site, the bound (`auto_approve_type3`,
`deploy.auto`, `deploy.auto_after_close`) holds ONLY when the session's
current operating model appears in the latest delegation-bounds block's
`approved_models`; a session on an unlisted model gets full v0.1
every-gate-is-an-Owner-gate behaviour at that site regardless of what the
bound's other fields say. Empty or absent `approved_models` = no model
approved = v0.1 behaviour everywhere (fail-closed; must be stated, not
inferred).

The nine sites, regenerated and confirmed from this worktree: `plan.md:31`
(step 2 Delegation-aware confirm) and `plan.md:124-130` (step 6 IAP
approval bound check); `run.md:70-71` (`auto_approve_type3`) and
`run.md:154-158` (step 7a `auto_after_close`); `loop.md:33`/`36`
(every-run expectation-setting statement) and `loop.md:71` (hard rule
2's `auto_after_close`); `deploy.md:117-124` (step 5 deploy-delegation
check); `status.md:106` (read-only report — here the model floor's
EFFECT must be reported, since status changes no state); `esg.md:33`
(founding-default note).

**NOTE DEPLOY.MD SPECIFICALLY:** its site does not contain any literal
bound key — it reads "If it has a `deploy` object with `auto: true`",
which is why a literal-key grep undercounts this population. Gate it by
its actual phrasing, and use the literal token `approved_models` in the
gate text you write there so S4's coverage check and the criterion-7
greps can both see it.

Read the model identity the same way doctrine's existing
Transfer-of-command mechanics already do when deciding whether to spawn
`dcs-commander` — the CURRENT session's own identity; this must not
depend on the unshipped `log-append-helper` incident.

**OUT OF SCOPE, do not add:** a model check on `plan.md:130`'s "no
DELEGATION.md -> fallback to config.json" branch (that branch has no
DELEGATION.md to read `approved_models` from; the 202 names it a known,
deliberately deferred gap).

**HARD CO-CONSTRAINT:** the per-file line ceiling is 250 under the
guard's own counting semantics (`_workflow_line_count`, NOT `wc -l`).
Measured headroom you are working inside: `plan.md` 249 (1 line),
`deploy.md` 245 (5 lines), `loop.md` 161, `run.md` 181, `status.md` 121,
`esg.md` 164. Workflows have no appendix — fund `plan.md`'s and
`deploy.md`'s additions by compressing prose in the SAME file, in this
tasking, and report the counts.

## File territory (may edit only within these globs)

- `dcs/workflows/plan.md`
- `dcs/workflows/run.md`
- `dcs/workflows/loop.md`
- `dcs/workflows/deploy.md`
- `dcs/workflows/status.md`
- `dcs/workflows/esg.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/execute.md`
- `dcs/workflows/close.md`
- `dcs/workflows/new.md`
- `dcs/workflows/init.md`
- `dcs/references/**`
- `dcs/templates/**`
- `dcs/tools/**`
- `dcs/hooks/**`
- `tests/**`
- `agents/**`
- `skills/**`
- `dcs/VERSION`
- `package.json`
- `.dcs/**`
- `vault/**`

## Worktree root

`C:\DCS-wt\independence-fail-closed-and-model-floor`

## Evidence required in the return

- Site sweep, both phrasing classes, after the edit: (a) `grep -rn
  "auto_approve_type3\|auto_after_close\|deploy\.auto\b"
  dcs/workflows/` for the literal-key sites, and (b) a DIRECT read `sed
  -n '115,133p' dcs/workflows/deploy.md` for the deploy-object site —
  because (a) returns zero hits in deploy.md by construction and cannot
  witness it.
- `grep -c "approved_models" dcs/workflows/plan.md dcs/workflows/run.md
  dcs/workflows/loop.md dcs/workflows/deploy.md dcs/workflows/status.md
  dcs/workflows/esg.md` — plan.md, run.md and loop.md must each be >= 2
  (their two independent sites); deploy.md, status.md and esg.md >= 1.
  Report the six counts.
- One decisive excerpt per site (file:line + the gate sentence, never
  the whole step) showing what an UNLISTED-model session gets there —
  nine excerpts, and each must make the v0.1 fallback explicit rather
  than implying it.
- Post-edit line counts under the guard's own semantics, for all six
  files (or simply re-run the guard's check 17 and report it green).
  Each of the six must be <= 250. If plan.md or deploy.md needed in-file
  compression to fit, name what was compressed.
- `python tests/test_doctrine_integrity.py` — trailing `N/M passed`
  (baseline 156/156), and confirm check 17 (workflow line-count budget)
  and check 12 (log grammar) green.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
