# 204 — Tasking S3

**Incident:** independence-fail-closed-and-model-floor
**Period:** 1
**Specialist:** dcs-ops-specialist (S3)

## Task

Criteria 1 and 2, plus criterion 4's close.md wiring.

(a) Criterion 1: `dcs/workflows/execute.md` step 8 (Safety Officer spawn)
gains an explicit spawn-liveness fallback in its OWN body — a spawn that
returns empty, errored, or with no decision is a FAILED spawn, re-spawn
on the next tier, log BOTH attempts, and never treat it as "Safety
verification happened". Doctrine's existing rule (`doctrine.md:33-36`)
is scoped to the two `dcs-commander` command points; step 8 is not a
command point, so write the fallback into step 8 rather than relying on
a cross-reference. You may cite doctrine's section by name — the heading
"A command point is never a silent wait" exists and guard check 6
validates such citations — but do not edit doctrine.md (S1's territory).

(b) Criterion 2: `dcs/workflows/close.md` refuses to complete an
UNATTENDED close when independent Safety spawn could not be established
after (a)'s retry protocol is exhausted — it either PARKs the incident
(register row state `PARKED`, reason naming the spawn failure) or routes
to the Owner via `AskUserQuestion`, in both cases BEFORE the merge step
(5a step 2). Never proceeding as if verification happened. The word
"unattended" occurs zero times in close.md today — verified.

(c) Criterion 4's wiring: close.md invokes S4's `dcs/tools/verdict_rerun.py`
against the current period's SAFETY.md before the merge, in the shape
and position step 1b already establishes for `record_integrity.py` —
`python "$HOME/.claude/dcs/tools/verdict_rerun.py" <incident_dir> ...`,
exit 0 continues, exit 1 (findings) and exit 2 (environment error) are
FAIL-CLOSED: stop, do not merge, escalation trigger (a).

**MEASURED TRAP, do not discover this at merge time:** guard check 12(c)
requires EVERY `dcs/**/*.md` containing the literal `SAFETY-HALT:`,
`SAFETY-PASS:` or `IAP-APPROVED:` to quote `dcs_gate.py`'s
`GRAMMAR_LINE` verbatim (205 B, ~4 wrapped lines). close.md today
contains NONE of those tokens and no grammar quote, at 248/250 lines. So
writing the literal `SAFETY-HALT:` into close.md turns the merge guard
red and costs you your entire line budget.

**Preferred route (adopted at command point 2's ratification — see
214-LOG.md):** refer to the halt-handling machinery by reference and
reuse (b)'s own PARK / `AskUserQuestion` disposition for a failed
re-run — close.md must gain that branch anyway. `dcs-commander`
explicitly ratified that criterion 4's parenthetical mention of a
`SAFETY-HALT:` sentinel is superseded by 202-OBJECTIVES.md's own Goal
text ("refusal means PARK or an Owner `AskUserQuestion` gate, never a
silent close") for this exact case — you are NOT required to emit a new
sentinel, and the ratification is binding on the Safety Officer's
verification too. If you still judge the literal sentinel genuinely
necessary, you must budget the verbatim `GRAMMAR_LINE` quote AND the
offsetting in-file trim in this tasking, and say so in your return; note
also that `dcs_gate.py`'s `halt_cycles()` counts that token, so emitting
it at close time consumes a principle-13 attempt-ceiling slot — a real
behavioural change in a period where `dcs/hooks/**` is out of scope.
Verified safe if you want a distinct log marker: `sentinel_of()`
classifies only halt/pass/stamp, so a new marker such as `SAFETY-SPAWN:`
returns None and cannot perturb the counter.

**HARD CO-CONSTRAINT:** `execute.md` 248/250 and `close.md` 248/250
under the guard's own line semantics — 2 lines of headroom each. Fund
in-file, in this tasking.

Do not edit `dcs/references/schemas.md`: if you believe criterion 3's
schema change requires a new field named in execute.md (guard check 20),
that is a deviation to report, not a cross-territory edit.

## File territory (may edit only within these globs)

- `dcs/workflows/execute.md`
- `dcs/workflows/close.md`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/plan.md`
- `dcs/workflows/run.md`
- `dcs/workflows/loop.md`
- `dcs/workflows/deploy.md`
- `dcs/workflows/status.md`
- `dcs/workflows/esg.md`
- `dcs/workflows/new.md`
- `dcs/workflows/init.md`
- `dcs/references/**`
- `dcs/templates/**`
- `dcs/tools/**`
- `dcs/hooks/**`
- `tests/**`
- `agents/**`
- `skills/**`
- `dcs/VERSION`
- `package.json`
- `.dcs/**`
- `vault/**`

## Worktree root

`C:\DCS-wt\independence-fail-closed-and-model-floor`

## Evidence required in the return

- Criterion 1, DISCRIMINATING — the 202's own original command (`grep -n
  "step 8\|Safety Officer" dcs/workflows/execute.md`) returns 10 matches
  on the UNMODIFIED tree and proves nothing; the 202 has been corrected
  to require instead: (i) `sed -n '/^## 8\./,/^## 9\./p'
  dcs/workflows/execute.md` — the decisive excerpt showing the fallback
  inside step 8's own body; (ii) a before/after count for the specific
  token you wrote, e.g. `grep -c "FAILED spawn" dcs/workflows/execute.md`
  (measured before-count: 0; "never returns" 0; "re-spawn on the next
  tier" 0).
- Criterion 2: `grep -n "unattended" dcs/workflows/close.md` (before-count
  0, verified) plus the decisive excerpt of the new branch showing BOTH
  sanctioned dispositions — a PARK register state with a reason naming
  the spawn failure, and the `AskUserQuestion` route — and showing it
  sits BEFORE section 5a step 2's merge.
- Criterion 4 wiring: the decisive excerpt of close.md's new step
  showing the `verdict_rerun.py` invocation, its position relative to
  step 1b and step 2, and all three exit-code dispositions (0 continue /
  1 fail-closed / 2 fail-closed) spelled out.
- CHECK-12 POPULATION PROOF, before and after — run the population-proof
  script from the accepted plan (walks `dcs/**/*.md` for the three
  sentinel tokens and checks each carries the verbatim `GRAMMAR_LINE`)
  both times and paste the output. State plainly whether close.md joined
  the population, and if it did, show the verbatim `GRAMMAR_LINE` quote
  you added and the offsetting trim.
- Post-edit line counts for execute.md and close.md under the guard's
  own semantics (or check 17 reported green) — both must be <= 250, from
  a 248 baseline. Name what you compressed to fit.
- `python tests/test_doctrine_integrity.py` — trailing `N/M passed`
  (baseline 156/156), with checks 12 (log grammar), 17 (line budget) and
  20 (inbound field-presence guard) confirmed green by name.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
