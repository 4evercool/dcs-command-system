# 204 — Tasking S4

**Incident:** independence-fail-closed-and-model-floor
**Period:** 1
**Specialist:** dcs-ops-specialist (S4)

## Task

Criterion 4's tool, criterion 8's fixtures, and criterion 7's + criterion
2's merge-guard coverage.

(a) Build `dcs/tools/verdict_rerun.py` in the established
`record_integrity.py` / `preservation_map.py` convention — stdlib only,
no dependencies, invoked as `python
"$HOME/.claude/dcs/tools/verdict_rerun.py" <incident_dir>`, exit 0 clean
/ 1 findings / 2 environment error — the same convention as
`record_integrity.py` and `preservation_map.py`, whose exit contract
`close.md` already knows how to dispose of. It reads the current
period's schema-#5 verdict fence from `<incident_dir>/SAFETY.md`, splits
each `checked[]` entry on the FIRST em-dash into command and observation,
SELECTS an entry whose command is stable across the integration commit,
re-runs it, and asserts the recorded observation is CONTAINED in the
fresh combined output. Three design points that are the whole substance
of the tool, not details: (i) containment, never byte equality — pytest
emits timings and paths that differ every run; (ii) a working-tree `git
diff` entry cannot reproduce at close time, because execute.md step 7
has the Safety Officer verify an UNCOMMITTED diff that step 9b then
commits, so a naive re-run would halt every close — the selection rule
must skip such entries; (iii) "no stable re-runnable entry found" is a
FINDING (exit 1), never a silent pass, or the whole mechanism is vacuous
the moment a verdict lists only diffs. Fence discovery must recognise a
delimiter only when a physical line's STRIPPED content starts with
three backticks — never a substring search; `record_integrity.py:683-692`
documents the live counterexample in this repo's own history where an
inline-code ```json span defeats the naive form. Re-running arbitrary
text out of an artifact is code execution: bound it with a timeout, run
from the repo root, and refuse to shell anything that does not parse as
a command rather than executing prose.

(b) Fixtures under `tests/fixtures/verdict-rerun/`, alongside the
existing record-integrity / preservation-map / halt-ceiling fixture
sets, driving at least three cases: a genuine entry that reproduces
(exit 0), a non-reproducing entry (exit 1, finding names the entry), and
a verdict whose every entry is non-reproducible-by-design (finding,
never exit 0). Follow the shape of `tests/fixtures/record-integrity/`
and `tests/fixtures/preservation-map/`.

(c) **CRITERION 7 COVERAGE CHECK** in `tests/test_doctrine_integrity.py`
— read this as written, it is the part the prior plan got wrong. The
site population is DISCOVERED by walking `dcs/workflows/*.md`, never
hardcoded, and the walker MUST match BOTH phrasing classes, each defined
as its own named constant with its own named cases: **CLASS A, literal
bound key** — the substrings `auto_approve_type3`, `auto_after_close`,
`deploy.auto`; **CLASS B, deploy-object** — `deploy.md:117-124`'s actual
phrasing, "a `deploy` object with `auto: true`", which contains NO
literal bound key and returns ZERO hits for every class-A pattern
(measured, confirmed by direct grep). A walker derived from the three
key names alone covers 8 of the 9 sites and goes green while the
hardest site ships ungated; that is the exact defect this clause exists
to prevent. The check must: (i) assert PER CLASS that its matched-site
population is NON-EMPTY, as its own named case, so an empty class is a
FAILURE rather than a silently skipped loop; (ii) assert per site, one
named case each carrying file:line, that a model gate (the literal
`approved_models`) is co-located with it, so a bound-read site added by
a later incident fails BY NAME; (iii) PRINT a per-class inventory at run
time — class name, matched-site count, and each file:line — so the
evidence discloses WHICH phrasing classes actually matched, not merely
that the check passed; (iv) carry a NON-VACUITY proof calling the SAME
predicates the real walk uses on synthetic input (the guard's own
`_ne_finding` / census idiom): deploy.md's deploy-object sentence must
return True under the class-B matcher AND False under the class-A
matcher, proving class B is load-bearing and not shadowed. **Corpus
scope, explicit:** the walk must be scoped to `dcs/workflows/*.md`
specifically — dcs-commander's second-pass review flagged that a
broader corpus would self-announce red on definitional class-A matches
inside `schemas.md`'s own JSON example and `templates/DELEGATION.md`; if
you see such a red, narrow the corpus, never weaken a class. Do NOT
freeze the number 9 into the check — assert the invariant (both classes
non-empty, every discovered site gated), never the instance (principle
15).

(d) Criterion 2 coverage (Planning Chief's accepted feedback item 2): a
case asserting close.md's unattended branch names BOTH sanctioned
dispositions — a PARK register state and an `AskUserQuestion` route —
and that they sit before the merge step, so a later edit deleting one
fails by name.

(e) Any structural check criteria 3/6's schema additions warrant per
criterion 9.

You own `tests/test_doctrine_integrity.py` exclusively — S1/S2/S3 will
each run it, none will edit it.

**VACUITY RULE, WHICH OVERRIDES YOUR OWN GREEN RUN:** your checks assert
on text S1, S2 and S3 are writing concurrently, so your own run may be
RED until their edits land. That is expected and correct. Report each
new check by name with its live/red state; NEVER weaken, skip-guard, or
soften a check to make your own run green — a check that passes because
the thing it tests does not exist yet is the exact defect the queued
`field-lesson-guard-vacuity` row is about.

## File territory (may edit only within these globs)

- `dcs/tools/verdict_rerun.py`
- `tests/fixtures/verdict-rerun/**`
- `tests/test_doctrine_integrity.py`

## Forbidden zones (explicitly, even if it seems related)

- `dcs/workflows/**`
- `dcs/references/**`
- `dcs/templates/**`
- `dcs/hooks/**`
- `dcs/tools/record_integrity.py`
- `dcs/tools/preservation_map.py`
- `tests/test_dcs_gate.py`
- `tests/test_dcs_intake.py`
- `tests/test_dcs_cli.py`
- `tests/payload_check.py`
- `tests/release_provenance_check.py`
- `tests/fixtures/record-integrity/**`
- `tests/fixtures/preservation-map/**`
- `tests/fixtures/halt-ceiling/**`
- `agents/**`
- `skills/**`
- `dcs/VERSION`
- `package.json`
- `.dcs/**`
- `vault/**`

## Worktree root

`C:\DCS-wt\independence-fail-closed-and-model-floor`

## Evidence required in the return

- `python dcs/tools/verdict_rerun.py --help` — the interface, and the
  documented exit-code table (0/1/2) in the module docstring.
- Three fixture runs, each with the command, the exit code (`echo $?`)
  and the decisive line of output: reproducing entry -> 0;
  non-reproducing entry -> 1 with the entry named; all-entries-non-reproducible-by-design
  -> a finding, never 0. Include the printed stability rule that did the
  skipping, so the selection logic is visible, not asserted.
- PER-CLASS INVENTORY AT RUNTIME — paste the check's actual printed
  output: each phrasing class by name, its matched-site count, and every
  matched file:line. This must show `dcs/workflows/deploy.md` under the
  deploy-object class. An evidence line that reports only "the coverage
  check passed" does NOT satisfy this requirement.
- NON-VACUITY OUTPUT: the two synthetic cases' results — deploy.md's
  deploy-object sentence returns True under the class-B matcher and
  False under the class-A matcher — printed by the same predicates the
  real walk calls. Paste both case lines.
- PER-CLASS LIVENESS PROBE, run by you once (the Safety Officer will
  repeat it independently): for EACH class, remove one site's model
  gate, re-run `python tests/test_doctrine_integrity.py`, confirm it
  goes RED naming that file:line and that class, restore with `git
  checkout -- <file>`, confirm green. Class B's target is MANDATORY
  `dcs/workflows/deploy.md`; class A's target is any literal-key site
  outside deploy.md. Paste the red case name and the restored green
  count for both.
- The per-class non-degeneracy case is itself live: temporarily neuter
  one class's matcher, show the guard goes red on that class's own
  non-empty case (not merely on the per-site cases), restore, confirm
  green.
- `python tests/test_doctrine_integrity.py`, `python
  tests/test_dcs_gate.py`, `python tests/test_dcs_intake.py` — each
  trailing `N/M passed` line. Baselines: 156/156, 100/100, 18/18. Report
  the new doctrine-integrity total and name the cases you added.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a `proposal` (a recommendation, not an action). The
IC will re-enter planning around your finding.
