# 204-TASKING/S1 — REGISTER.md Notes Compaction

**Assigned to:** dcs-ops-specialist
**Tasking ID:** S1

## Task

Compact REGISTER.md Notes: move all 16 `###`-headed historical sections to individual files in `vault/Meta/ESG-sessions/`, replace each with a one-line pointer, and create the vault README. Satisfies acceptance criteria 1, 2, 3, 4, and 6. Criterion 5 (no workflow edits) is a constraint, not an action — any edit to `dcs/workflows/` is a violation.

## Territory

- `C:/DCS/.dcs/esg/REGISTER.md`
- `C:/DCS/vault/Meta/ESG-sessions/**`

## Forbidden

- `C:/DCS/dcs/workflows/**`
- `C:/DCS/dcs/templates/**`
- `C:/DCS/.dcs/esg/STRATEGY.md`
- `C:/DCS/.dcs/esg/REGISTER-LOCK`

## Procedure

1. **Pre-edit snapshot.** Before any edit: `head -144 C:/DCS/.dcs/esg/REGISTER.md > /tmp/register-table-pre-edit.bak` and `wc -c /tmp/register-table-pre-edit.bak` — record the byte count.

2. **Acquire REGISTER-LOCK.** Create-exclusive `C:/DCS/.dcs/esg/REGISTER-LOCK` with contents `register-notes-compaction S1 <ISO8601 timestamp>` before touching REGISTER.md. Delete it when done. If a lock exists and is older than 10 minutes, it is stale — take it over, noting the takeover in REGISTER.md Notes.

3. **Extract each `###` section.** For each `###`-headed section in REGISTER.md Notes (from `## Notes` at line 145 to EOF), extract the full section text (heading + body through the next blank-line terminator, or EOF for the last one) into its own file under `C:/DCS/vault/Meta/ESG-sessions/`. Derive each filename from the heading: kebab-case the heading text, e.g. `fifth-esg-session-2026-07-25.md`, `territory-lock-race-2026-07-26.md`, etc. Create the `vault/Meta/ESG-sessions/` directory first if it doesn't exist.

4. **Replace with one-line pointer.** In REGISTER.md, replace each extracted `###`-headed block with a single line: `see vault/Meta/ESG-sessions/<filename>.md — <one-line summary derived from the heading>`. The pointer replaces the entire section block — heading and all body text.

5. **Create README.** Write `C:/DCS/vault/Meta/ESG-sessions/README.md`. First paragraph must clearly state these are read-only historical records of past ESG sessions and incident-stem bookkeeping, preserved for maintainer reference, not to be edited. Include a brief listing of the files and what each covers.

6. **Verify.** Run the evidence commands below and report real output.

## Evidence required

- Criterion 1: `grep -c '^### ' C:/DCS/.dcs/esg/REGISTER.md` — expected: 0
- Criterion 2a: `grep -c 'see vault/Meta/ESG-sessions/' C:/DCS/.dcs/esg/REGISTER.md` — expected: 16
- Criterion 2b: `for f in $(grep -oP 'vault/Meta/ESG-sessions/[^ )]+' C:/DCS/.dcs/esg/REGISTER.md); do test -f "C:/DCS/$f" || echo "MISSING: $f"; done` — expected: no MISSING lines
- Criterion 3: `test -f C:/DCS/vault/Meta/ESG-sessions/README.md && head -5 C:/DCS/vault/Meta/ESG-sessions/README.md` — show file exists and opening states read-only purpose
- Criterion 4: `diff /tmp/register-table-pre-edit.bak <(head -144 C:/DCS/.dcs/esg/REGISTER.md) && echo 'TABLE IDENTICAL'` — expected: TABLE IDENTICAL (empty diff)
- Criterion 5: `git -C C:/DCS diff --stat -- dcs/workflows/` — expected: empty or no output
- Criterion 6: `python -c "t=open('C:/DCS/.dcs/esg/REGISTER.md',encoding='utf-8').read(); n=t.find('## Notes'); print(len(t)-n)"` — expected: value < 10000
