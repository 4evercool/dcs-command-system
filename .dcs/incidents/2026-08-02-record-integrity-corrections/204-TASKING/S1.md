# 204 — Tasking S1

**Incident:** record-integrity-corrections
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Criterion 1 — append exactly one new entry to the END of `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/214-LOG.md`, correcting the fabricated merge citation at its line 37. Change no existing byte: line 37 and every line above it stay exactly as they are, and the file's existing trailing newline means the append is a clean new final line. The entry follows that file's own grammar — column zero, bracketed local timestamp `[2026-08-02T<HH:MM:SS>+11:00]` (the timezone this session's own log uses), one physical line, then the literal token `RECORD-CORRECTION` immediately after the timestamp, then the correction. The correction must state: that `b4af6e4` is not a git object in this repository and appears in no ref; that the incident's work in fact reached main via two real merge commits, `f7e0cc9` (code plus AAR.md) and `838adea` (the remaining 8 artifacts), both ancestors of main; that `48ea59a`, named on the preceding line 36, is the integration commit on the incident branch and an ancestor of `f7e0cc9`, never itself a merge; and that this entry corrects only the merge citation and asserts nothing about line 37's timestamp or any other line.

**HARD CONSTRAINTS, each mechanical:**
- The token immediately after the timestamp must NOT be `IAP-APPROVED:`, `SAFETY-HALT:` or `SAFETY-PASS:`.
- The entry must NOT contain the literal string `re-stamp`.
- If you quote any prior entry, indent it off column zero.
- Write with the Edit tool, never PowerShell `Set-Content`/`Out-File` (BOM risk).
- Do not create any new file in this directory — the plan sits exactly at Delegation v5's `max_files: 4`.

## File territory (may edit only within these globs)

- `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/214-LOG.md`

## Forbidden zones (explicitly, even if it seems related)

- `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/201-BRIEF.md`
- `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/202-OBJECTIVES.md`
- `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/204-TASKING/**`
- `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/AAR.md`
- `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/IAP.md`
- `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/IAP-APPROVED`
- `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/SAFETY.md`
- `.dcs/incidents/2026-07-30-workflow-file-trim-grandfathered/**`
- `.dcs/incidents/2026-07-29-check-14-hardening/**`
- `.dcs/incidents/2026-07-29-worktree-removal-self-conflict/**`
- `.dcs/incidents/2026-08-02-record-integrity-corrections/**`
- `.dcs/esg/**`
- `CHANGELOG.md`
- `dcs/**`
- `tests/**`
- `agents/**`
- `skills/**`
- `bin/**`
- `install.ps1`
- `install.sh`
- `package.json`
- `vault/**`

## Worktree root

`C:\DCS-wt\record-integrity-corrections`

## Evidence required in the return

- `git cat-file -t b4af6e4` — paste the one-line output; must still be `fatal: Not a valid object name b4af6e4`.
- `git log --all --oneline | grep -c b4af6e4` — must print `0`. Run it standalone, not in an `&&` chain (grep exits 1 on zero matches).
- `git cat-file -t f7e0cc9; git cat-file -t 838adea` — both must print `commit`.
- `git log -1 --format='%h %ci %p %s' f7e0cc9` and the same for `838adea` — paste both lines verbatim. If either disagrees with your entry's wording, fix the entry, not the evidence.
- `git merge-base --is-ancestor 48ea59a f7e0cc9 && echo ANCESTOR` — must print `ANCESTOR`.
- Byte-identity proof of everything above the append: `git show HEAD:.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/214-LOG.md | sha256sum` versus `head -n 37 .dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/214-LOG.md | sha256sum` — the two digests must be identical. Paste both.
- `grep -n "b4af6e4" .dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/214-LOG.md` — must show at least two numbered lines: the original at 37 and your new entry. Paste them.
- `git diff --numstat HEAD -- .dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/` — must be exactly one row with `0` in the deletions (second) column. Paste the row.
- Grammar proof: `grep -n "^\[.*\] SAFETY-HALT:\|^\[.*\] SAFETY-PASS:\|^\[.*\] IAP-APPROVED:" .dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/214-LOG.md` — the line numbers returned must be exactly the pre-existing 32 and 35, with your new line absent. Also `grep -c "re-stamp" <same file>` must print `0`. Paste both results.
- `python tests/test_doctrine_integrity.py 2>&1 | tail -4` — paste the last four lines. The `N/M passed` count must be at least the `133/133` baseline with zero failures, and the `preservation map field guard:` line must still read `0 ... in scope`.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a
`proposal` (a recommendation, not an action). The IC will re-enter
planning around your finding.
