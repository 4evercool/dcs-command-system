# 204 — Tasking S2

**Incident:** record-integrity-corrections
**Period:** 1
**Specialist:** dcs-ops-specialist (S2)

## Task

Criteria 2 and 3 — create exactly three new files, one per sparse incident directory, all named `CORRECTION-2026-08-02.md`, all following the same fixed head-matter and the same rules. Open no pre-existing file for writing in any of the three directories.

Each file contains:
(a) an `# RECORD-CORRECTION — 2026-08-02` H1, a line naming what it corrects and who issued it (`incident record-integrity-corrections`), and a bold line stating that nothing in the directory was edited or deleted and that the original record stands as written;
(b) an ARTIFACT CENSUS section listing all eight standard artifacts (`201-BRIEF.md`, `202-OBJECTIVES.md`, `203-ORG.md`, `204-TASKING/`, `IAP.md`, `SAFETY.md`, `AAR.md`, `214-LOG.md`) as present or missing, with, for every missing one, the exact `git log --all --full-history` command and its actual output (empty), plus the whole-history slug sweep (`git log --all --full-history --pretty=format: --name-only -- "*<slug>*" | sort -u`) as the decisive evidence, plus one line naming the ref set the search covered (`git for-each-ref refs/heads refs/remotes`);
(c) one line noting that `dcs/references/forms.md:11-19` defines a ninth artifact, `IAP-APPROVED`, likewise never committed in this directory and outside criterion 3's fixed eight;
(d) the honesty bound — every missing artifact is confirmed never committed and therefore irrecoverable, stated as annotation, never as restoration or reconstruction. Never write "restored verbatim" or similar — that exact false-restoration shape is the defect this correction must not repeat (see `doctrine-appendix.md:658-670`, field lesson W4).

ADDITIONALLY, in the `2026-07-30-workflow-file-trim-grandfathered` file only, a numbered CORRECTION section for criterion 2: that `AAR.md:44`'s header "Safety Officer's final verdict (verbatim, from SAFETY.md)" is a false attribution, that no `SAFETY.md` ever existed for this incident, that the JSON block at `AAR.md:46-62` therefore cannot be verbatim from anything and its provenance is unknown, and that `AAR.md` is left exactly as written.

**HARD CONSTRAINTS:**
- Do NOT re-paste the AAR's JSON verdict block inside a ` ```json ` fence anywhere — describe or quote it as indented plain text if you must refer to it (a fenced JSON block in `SAFETY.md`/`AAR.md`/`214-LOG.md` gets parsed by the merge-time guard's check 21; a sibling `CORRECTION-*.md` is outside that walk, which is exactly why it must not carry the block instead).
- English throughout, including the file beside the Russian-language `worktree-removal-self-conflict/AAR.md` — you neither translate, rewrite, nor extensively quote that AAR (it is separately registered as `russian-artifacts-translation`; out of scope here).
- Write with the Write tool, never PowerShell `Set-Content`/`Out-File` (BOM risk).

## File territory (may edit only within these globs)

- `.dcs/incidents/2026-07-30-workflow-file-trim-grandfathered/CORRECTION-2026-08-02.md`
- `.dcs/incidents/2026-07-29-check-14-hardening/CORRECTION-2026-08-02.md`
- `.dcs/incidents/2026-07-29-worktree-removal-self-conflict/CORRECTION-2026-08-02.md`

## Forbidden zones (explicitly, even if it seems related)

- `.dcs/incidents/2026-07-30-workflow-file-trim-grandfathered/AAR.md`
- `.dcs/incidents/2026-07-29-check-14-hardening/AAR.md`
- `.dcs/incidents/2026-07-29-check-14-hardening/SAFETY.md`
- `.dcs/incidents/2026-07-29-worktree-removal-self-conflict/AAR.md`
- `.dcs/incidents/2026-07-29-worktree-removal-self-conflict/SAFETY.md`
- `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/**`
- `.dcs/incidents/2026-08-02-record-integrity-corrections/**`
- `.dcs/esg/**`
- `CHANGELOG.md`
- `dcs/**`
- `tests/**`
- `agents/**`
- `skills/**`
- `bin/**`
- `install.ps1`
- `install.sh`
- `package.json`
- `vault/**`

## Worktree root

`C:\DCS-wt\record-integrity-corrections`

## Evidence required in the return

- For each of the three slugs, `git log --all --full-history --pretty=format: --name-only -- "*<slug>*" | sort -u | grep -v '^$'`. Paste all three outputs verbatim. Each must list only the files already on disk in that directory — this is what proves "never committed" rather than "moved or renamed".
- `git log --all --full-history -- "**/workflow-file-trim-grandfathered/SAFETY.md"` — must produce no output. State explicitly that it produced none (criterion 2).
- The per-artifact census command for the missing artifacts, compact form: `for d in <3 dirs>; do for f in <8 artifacts>; do echo "$d/$f -> $(git log --all --full-history --oneline -- ".dcs/incidents/$d/$f" | wc -l) commits"; done; done`. Paste the full output.
- `git for-each-ref --format='%(refname)' refs/heads refs/remotes` — paste the output; this is the ref set your "never committed under any ref" claim covers.
- `git status --porcelain -- .dcs/incidents/2026-07-30-workflow-file-trim-grandfathered .dcs/incidents/2026-07-29-check-14-hardening .dcs/incidents/2026-07-29-worktree-removal-self-conflict` — must show exactly three `??` lines, your three new files, and nothing else.
- `git diff --numstat HEAD -- .dcs/incidents/2026-07-30-workflow-file-trim-grandfathered .dcs/incidents/2026-07-29-check-14-hardening .dcs/incidents/2026-07-29-worktree-removal-self-conflict` — must produce EMPTY output. State explicitly that it produced none.
- `grep -rn "RECORD-CORRECTION" .dcs/incidents/ | cut -c1-100` — paste the matching paths (your three files, and S1's log entry if it has already landed).
- `grep -n '```json' .dcs/incidents/*/CORRECTION-2026-08-02.md` — must return nothing.
- No-BOM proof: `python -c "import glob;[print(f, open(f,'rb').read(3)) for f in sorted(glob.glob('.dcs/incidents/*/CORRECTION-2026-08-02.md'))]"` — none of the three may begin `b'\xef\xbb\xbf'`.
- `python tests/test_doctrine_integrity.py 2>&1 | tail -6` — paste the last six lines. `N/M passed` must be at least the `133/133` baseline with zero failures, and check its outbound-field-guard output against the suite's own actual printed line (do not assume a specific count) — a new parseable JSON block landing in a scanned artifact would change it.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a
`proposal` (a recommendation, not an action). The IC will re-enter
planning around your finding.
