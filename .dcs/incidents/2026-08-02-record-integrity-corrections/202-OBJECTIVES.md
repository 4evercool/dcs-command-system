# 202 — Objectives (Operational Period 1)

**Incident:** record-integrity-corrections
**Period:** 1

## Goal

Every record-integrity failure the sixteenth `/dcs-esg` session named is corrected honestly, by **appending** new, clearly-labelled correction material — never by editing or deleting what a closed incident's own artifacts already say. A future reader of any of the four touched incident directories should see both the original (flawed) record, intact, and a clearly dated correction beside it — never a silently rewritten history.

## Acceptance criteria (the Definition of Done)

1. `.dcs/incidents/2026-07-30-halt-enumeration-grammar-drift/214-LOG.md`'s existing line 37 (citing "merge b4af6e4") is still present, byte-for-byte, and a new entry appended after it states plainly that `b4af6e4` does not exist in this repository and names the two real merge commits instead. Verified by: `git cat-file -t b4af6e4` still fails, `git log --all --oneline | grep b4af6e4` still empty, `git cat-file -t f7e0cc9` and `git cat-file -t 838adea` both return `commit`, and the new entry is present via `grep -n "b4af6e4" 214-LOG.md` showing both the original line and the new correction.
2. `.dcs/incidents/2026-07-30-workflow-file-trim-grandfathered/AAR.md`'s existing content is unchanged, byte-for-byte, and a new, clearly-dated correction (appended to `AAR.md` or a new sibling file — the Planning Chief decides which, since no convention exists yet) states plainly that no `SAFETY.md` ever existed for this incident and that the "verbatim, from SAFETY.md" attribution at line 44 is false. Verified by: `git log --all --full-history -- "**/workflow-file-trim-grandfathered/SAFETY.md"` still returns nothing, and the correction text is present via `grep`.
3. Each of the three artifact-sparse incidents — `2026-07-30-workflow-file-trim-grandfathered`, `2026-07-29-check-14-hardening`, `2026-07-29-worktree-removal-self-conflict` — gains one new, honestly-labelled annotation file listing exactly which of the 8 standard incident artifacts (201-BRIEF.md, 202-OBJECTIVES.md, 203-ORG.md, 204-TASKING/, IAP.md, SAFETY.md, AAR.md, 214-LOG.md) are present versus missing, stating that every missing one is confirmed irrecoverable (never committed to this repository under any branch, not merely moved), with the exact `git log --all --full-history` command and output for each missing artifact. No existing file in any of the three directories is edited.
4. `.dcs/esg/REGISTER.md`'s `token-economy-advisory-fixes` row's Branch cell is corrected from naming `dcs/token-economy-advisory-fixes` to `— (deleted)`, matching every sibling `DEPLOYED` row's convention. Verified by: `grep -n "token-economy-advisory-fixes" C:\DCS\.dcs\esg\REGISTER.md` showing the stale cell *before* the edit and the corrected cell *after*, plus `git branch -a --list "*token-economy*"` returning nothing. **[IC]** — `.dcs/esg/` is git-ignored and lives only in the main checkout (`esg_root`), never in an incident worktree (doctrine "Parallel operation"); no specialist spawned into this incident's worktree can see or edit this file, so this criterion cannot be a tasking (per this template's own ownership-tag guidance: "updating the register is [IC]"). Corrected here after the Planning Chief flagged it as blocking `objectives_feedback` — the original draft left this criterion untagged, which is a lint check 6 (criterion coverage) defect.
5. `CHANGELOG.md`'s `0.7.1` entry is confirmed present — `grep -n "^## 0.7.1" CHANGELOG.md` returns a match. No write performed; this closes the loop on the original (now-stale) intake item honestly rather than silently dropping it. **[IC]**
6. Across all four touched incident directories, every file that existed before this incident opened has zero removed or modified lines — only new files, or purely appended lines, appear in the diff. Verified by: `git diff --numstat HEAD -- .dcs/incidents/2026-07-30-halt-enumeration-grammar-drift .dcs/incidents/2026-07-30-workflow-file-trim-grandfathered .dcs/incidents/2026-07-29-check-14-hardening .dcs/incidents/2026-07-29-worktree-removal-self-conflict` shows `0` in the deletions column for every pre-existing path. This operationalizes the incident's own "corrections, never edits" mandate and is the Safety Officer's primary check. **[IC]**

## Out of scope this period

- Building the general SHA-existence / artifact-completeness enforcement mechanism (that is `close-integrity-guard-bundle`, register rank 2 — explicitly sequenced to start only after this incident closes).
- Correcting the ~7 incidents with substantially Russian-language artifacts (`russian-artifacts-translation`, a separate registered row).
- Building the append-only log-writing tool (`log-append-helper`, a separate registered row) — this incident corrects specific past instances, it does not build prevention tooling.
- Re-timestamping or otherwise annotating the suspected backfilled/+03:00-cluster timestamps in `halt-enumeration-grammar-drift/214-LOG.md` beyond the one line named in criterion 1 — a timestamp's honesty cannot be retroactively established, and this class of finding belongs to `log-append-helper`'s prevention scope, not this incident's correction scope. Do not touch other lines in that file.
- The prose-header (non-schema-#5-JSON) format of `check-14-hardening/SAFETY.md` and `worktree-removal-self-conflict/SAFETY.md` — noted by a situation analyst as a real defect, but not one of the five items this incident's register row names; it belongs with the SAFETY.md schema-conformance work bundled into `close-integrity-guard-bundle`.
- Any register row other than `token-economy-advisory-fixes`'s Branch cell — no sweep of the rest of `REGISTER.md` for similar staleness this period.

## Chief feedback (filled in after /dcs-plan spawns the Planning Chief)

Three flags from `dcs-planning-chief`, one blocking:

1. **Blocking — resolved.** Criterion 4 cannot be a specialist tasking: `.dcs/esg/REGISTER.md` is git-ignored and exists only in the main checkout (confirmed: `git ls-files --error-unmatch .dcs/esg/REGISTER.md` → "did not match any file(s) known to git"; the worktree has no `.dcs/esg/` directory at all). Resolved by retagging criterion 4 `[IC]` above and adding the missing before/after measured-claim command, per lint check 6 (criterion coverage) and check 3b (unmeasured claim) — a lint-level fix, not a scope change, made before the command point per plan.md step 4a's own instruction ("run them yourself... never a reason to spend a command point").
2. **Non-blocking.** The "8 standard artifacts" list in criterion 3 is one short of `forms.md`'s own count (`IAP-APPROVED` is a 9th). Not revising criterion 3's fixed list — the Chief's taskings additionally have each census note `IAP-APPROVED` as a 9th, likewise never committed, explicitly outside criterion 3's own count. No 202 change needed.
3. Criterion 5 independently confirmed still true at planning time: `grep -n "^## 0.7.1" CHANGELOG.md` → `117:## 0.7.1 — 2026-07-30`.
