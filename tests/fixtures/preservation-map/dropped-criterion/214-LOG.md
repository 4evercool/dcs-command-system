<!--
Fixture -- see IAP.md's header comment in this same directory (THE
REGRESSION FIXTURE).
-->

# 214 -- Operational Log (fixture)

[2026-07-27T10:00:00+0000] incident opened, type 1, phase=planning
[2026-07-27T10:05:00+0000] command: iap_review accept (IC=dcs-commander)
[2026-07-27T10:06:00+0000] IAP-APPROVED: aaaa1111bbbb -- phase: planning -> execution (period 1)
[2026-07-27T20:51:00+0000] command: deviation -> amend_tasking (S2: criterion 6's warning line needs a narrower tasking; IC-authored IAP.md rewrite in the same pass also dropped the "Criterion 5" section, uncaught until this map was mechanically checked)
[2026-07-27T20:52:00+0000] IAP-APPROVED: cccc2222dddd -- re-stamp, no phase transition (still execution, period 1)

Preservation map for the above amendment: criterion 6 is named by the
amendment itself; every other criterion is claimed re-proven below --
criterion 5's entry below is stale (carried over unchanged from the
pre-amendment IAP.md), which is exactly what this fixture exists to
catch: its self-reported `output` is plausible, but this directory's own
IAP.md no longer contains the anchor it claims.

```json
{
  "preservation_map": {
    "amended_criteria": [6],
    "preserved": [
      {"criterion": 1, "artifact": "IAP.md", "anchor": "the retry loop wraps each attempt in exponential backoff", "output": "Anchor: the retry loop wraps each attempt in exponential backoff capped by `max_retries`."},
      {"criterion": 2, "artifact": "IAP.md", "anchor": "the retry policy section spells out the backoff schedule", "output": "Anchor: the retry policy section spells out the backoff schedule for operators."},
      {"criterion": 3, "artifact": "IAP.md", "anchor": "test_timeout_path", "output": "Anchor: `test_timeout_path` exercises the sync worker's timeout branch."},
      {"criterion": 4, "artifact": "IAP.md", "anchor": "max_retries` is exposed as a configurable integer", "output": "Anchor: `max_retries` is exposed as a configurable integer default of 5."},
      {"criterion": 5, "artifact": "IAP.md", "anchor": "the rollout checklist enumerates every launch step", "output": "Anchor: the rollout checklist enumerates every launch step end to end."}
    ]
  }
}
```
