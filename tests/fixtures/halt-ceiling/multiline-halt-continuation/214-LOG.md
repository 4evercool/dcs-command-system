<!--
Fixture for tests/test_dcs_gate.py, IC addendum 4 (halt-loop-unbounded
period 1 revision 2, 204-TASKING/S2.md step 7) -- 214-LOG.md's contract
(references/forms.md) is one entry, one line. This fixture's third entry
is a SAFETY-HALT: summary that spans two physical lines; its continuation
line begins at column zero with a sentinel-shaped token (a real
IAP-APPROVED: line, quoted mid-summary) even though it is NOT a new
entry. It must neither count as an extra entry nor act as a reset --
only a line that is itself the start of an entry (opens with a bracketed
timestamp) is ever tested against a sentinel pattern.

The leading and quoted hex (68304eb79661) is deliberately a real prefix of
sha256(b"# IAP\nobjectives...\n") -- see fresh-stamp/214-LOG.md's header
for why, and the companion IAP-APPROVED file below carries the matching
full digest. This makes the first entry (index 1) a REAL, valid anchor,
so the assertion is precise: if the continuation line at index 3 were
wrongly treated as a second, later anchor, the final count would drop
from 2 to 1. Regenerate the digest with:
  python -c "import hashlib; print(hashlib.sha256(b'# IAP\nobjectives...\n').hexdigest())"

halt_cycles() on this file must return 2 (the two real, single-field
SAFETY-HALT: entries after the valid stamp), never 1.

This directory is this period's own uncommitted product, not historical
evidence. Do not edit; add a new fixture directory instead.
-->

# 214 -- Operational Log (fixture)

[2026-01-01T00:00:00+0000] incident opened, type 1, phase=planning
[2026-01-01T00:01:00+0000] IAP-APPROVED: 68304eb79661 -- phase: planning -> execution (period 1)
[2026-01-01T00:02:00+0000] SAFETY-HALT: refutation detail below, quoted for context --
IAP-APPROVED: 68304eb79661 -- this is body text of the halt entry above, not a new entry
[2026-01-01T00:03:00+0000] SAFETY-HALT: second halt, single line
