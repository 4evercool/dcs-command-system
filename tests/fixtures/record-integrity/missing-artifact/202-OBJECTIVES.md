<!-- Fixture -- see 214-LOG.md's header comment in this same directory. -->

# 202 -- Objectives (fixture: missing-artifact)

**Goal:** fixture only -- exercises record_integrity.py's artifact-set
completeness check (criterion 2) against one absent artifact (AAR.md),
with no skip note.

## Acceptance criteria

1. N/A -- fixture, not a real incident.
