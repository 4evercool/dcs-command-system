<!--
Fixture for tests/test_doctrine_integrity.py section 23 case (iv) --
suppressed-correction/, a genuine PAIR of incident directories
(uncorrected/ and corrected/) proving dcs/tools/record_integrity.py's
suppression (b), corrected design (close-integrity-guard-bundle, period
1 attempt 2's fix to the Safety Officer's refutation 2): a genuine,
entry-initial RECORD-CORRECTION: entry (recognized by dcs_gate.py's own
entry grammar -- never a body-anywhere substring match) suppresses every
occurrence of the token it names, anywhere in this same file. This exact
file (214-LOG.md) is byte-identical between uncorrected/ and corrected/
EXCEPT for one appended entry in corrected/ -- diff the two directly to
see that is the only difference. uncorrected/'s citation therefore stays
an unresolved FINDING; corrected/'s is fully suppressed. Both halves also
carry a DIFFERENT entry containing a mere MID-LINE, non-entry-initial
mention of the literal string "RECORD-CORRECTION:", to prove a mention
alone (the discarded old design's trigger) suppresses nothing either way.

"cafef00d1" is deliberately fabricated and not a real git object.

Other files in each directory carry a one-line pointer back to this
comment. These directories are this incident's own uncommitted product,
not historical evidence: do not edit in place, add a new fixture
directory instead (principle 15).
-->

# 214 -- Operational Log (fixture: suppressed-correction)

[2026-08-03T09:45:00+00:00] fixture created for record_integrity.py's suppression (b) corrected mechanism (section 23 case (iv)). See this file's own header comment for the pair design; ../corrected/214-LOG.md is this file plus one appended entry.
[2026-08-03T09:50:00+00:00] measured: a legacy review of this fixture's own history cites sha cafef00d1 as a supposed integration reference, unverified -- fixture citation, deliberately fabricated and not a real git object.
[2026-08-03T09:55:00+00:00] note: only an entry that carries the literal sentinel RECORD-CORRECTION: as the very first thing after its own bracketed timestamp counts as a correction under dcs/tools/record_integrity.py's grammar-recognized suppression (b) -- a mid-line mention like this one, which also happens to cite sha cafef00d1 in passing rather than as a formal correction, must not by itself suppress anything, and uncorrected/ carries no correction entry at all.
[2026-08-03T10:00:00+00:00] RECORD-CORRECTION: the sha cafef00d1 cited above is a fixture-only fabricated token, not a real commit -- this entry names it explicitly so record_integrity.py's suppression (b) clears every earlier occurrence of it in this same file.
