<!-- Fixture -- see SAFETY.md's header comment in this same directory. -->

# 202 -- Objectives (fixture: boundary-pin)

**Goal:** fixture only -- exercises record_integrity.py's criterion 3
date-scope check (SAFETY_FENCE_EFFECTIVE_DATE) at the exact boundary: a
directory dated one day after the pin.

## Acceptance criteria

1. N/A -- fixture, not a real incident.
