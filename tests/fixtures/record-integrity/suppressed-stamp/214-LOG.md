<!--
Fixture for tests/test_doctrine_integrity.py section 23 case (iii) --
suppressed-stamp/. Exercises dcs/tools/record_integrity.py's suppression
(a): a token inside an entry whose FIRST LINE sentinel_of() classifies
'stamp'. The entry below genuinely reaches this branch -- something the
real corpus never does (suppression (a) fires zero times across all 74
real citation-position tokens), so without this fixture the suppression
is a mechanism nothing tests. Line 1 is a real IAP-APPROVED: stamp
(matches dcs_gate.py's STAMP_RE); line 2 is a continuation (no bracketed
timestamp at column zero) that re-mentions the SAME digest, this time
genuinely preceded by the commit keyword -- so citation_tokens() finds
it, and suppression (a) must then hide it from the findings while still
printing a suppression line naming it. Other files in this directory
carry a one-line pointer back to this comment. This directory is this
incident's own uncommitted product, not historical evidence: do not
edit in place, add a new fixture directory instead (principle 15).
-->

# 214 -- Operational Log (fixture: suppressed-stamp)

[2026-08-04T00:00:00+0000] IAP-APPROVED: 35fce4016641deadbeef -- phase: planning -> execution (period 1)
Verified independently: commit 35fce4016641deadbeef matches the stamped digest exactly, no discrepancy found.
