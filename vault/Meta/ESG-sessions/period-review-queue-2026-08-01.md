# Owner-directed queue from the period review — 2026-08-01

Outside any incident and outside an ESG session, on the Owner's explicit
directive ("queue them via the register"), ten rows were appended to
`.dcs/esg/REGISTER.md` from the findings of the external review of the
2026-07-29 → 07-31 development period.

Full findings: `vault/Post-mortems/deepseek-period-review.md` — that
file is the intake source cited by every row queued in this act.

Rows queued (all QUEUED, unranked — ranking deferred to the next
`/dcs-esg`): `at-risk-records-preservation`,
`trim-content-loss-restoration`, `doctor-silent-pass-and-bump-defects`,
`field-lesson-guard-vacuity`, `close-md-lock-diagnostic-inert`,
`russian-artifacts-translation`, `shipped-project-facts-sweep`,
`record-integrity-corrections`, `metrics-pass-counter-blind`,
`run-md-markup-corruption`.

Also done in the same register write: added the missing Notes pointer
for the fourteenth `/dcs-esg` (2026-07-31) — the session file existed in
`vault/Meta/ESG-sessions/` but no pointer line had been added to the
Notes section.

Priorities were set H/M/L by the reviewing session on the evidence, not
ranked against the existing queue — the next `/dcs-esg` owns the
re-rank. The `revision-preservation-map` row was left untouched: it is
ACTIVE mid-execution in its worktree, and its disposition (resume,
restart, or kill) is an Owner decision for the next session, not a new
row.
