# Fourteenth /dcs-esg — 2026-07-31

## Portfolio sweep

9 of 12 QUEUED rows cleared to DEPLOYED since the thirteenth session (~1 day ago).
The Fable roadmap Phase 3 carriers (`doctor-version-only-check`,
`halt-enumeration-grammar-drift`, `provisioning-script-upstreaming`) all shipped,
along with the emergency hot-path trim, workflow file trim, register notes
compaction, field lesson citations, status.md enum fix, and intake nudge telemetry.

Remaining QUEUED after the sweep: `revision-preservation-map`, `register-writer-map-completeness`,
`strategy-compaction-loses-history`.

## Decisions

### Rank 1
`revision-preservation-map` (M) — Owner's pick over two L rows. Highest priority
among survivors, most concrete defect with two field measurements.

### New intake
Two L rows queued from `vault/Backlog.md`:
- `halt-ceiling-re-anchor` (item 22) — Type 1, touches `dcs/hooks/dcs_gate.py`.
  The doctrinal backstop held, so low urgency.
- `independent-fixture-authorship` (item 23) — candidate doctrine change. One
  data point; low urgency.

Both registered at L priority. Queue is short (now 6 rows), no capacity concern.

### Unpark
`type5-express-lane-tuning` unparked → QUEUED (L). Parked 2026-07-25 on a condition
("several self-hosted incidents have closed") now clearly met. No evidence of
systematically conservative typing has surfaced — the row returns on its original
merits, not on new urgency.

### Delegation
v4 unchanged. No bound has failed since 2026-07-27.

### Worktree hygiene
Clean: single worktree on main, no stale branches, no orphans.

### Sitreps
All 8 have Decisions filled. No pending escalations.

### Field repairs
None new since the thirteenth session.

## Portfolio after session

6 QUEUED (ranked 1–6), 1 PARKED (`halt-binding-status`), rest DEPLOYED/KILLED/RESOLVED.
No territory collisions except ranks 1 and 5 sharing `plan.md`.

## Delegation version
v4 (unchanged).
