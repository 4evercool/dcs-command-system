### Thirteenth `/dcs-esg`, 2026-07-30 — roadmap-first re-rank, two new rows, hot-path emergency

Portfolio: **18 DEPLOYED, 1 MERGED (deploy pending), 1 RESOLVED,
12 QUEUED (ranked 1–12), 2 PARKED, 10 KILLED, 0 ACTIVE.** Since the
twelfth session (same day, minutes earlier): `esg-intake-writeback-gap`
resolved by field repair `c50d565` — Owner added the missing cluster (b)
writeback instruction directly, one file, no worktree.

**Roadmap-first re-rank.** `doctor-version-only-check` → rank 1 (Fable
roadmap Phase 3, "the highest-ranked roadmap remainder").
`halt-enumeration-grammar-drift` → rank 2 (gates rec 8 telemetry table).
`provisioning-script-upstreaming` → rank 3 (rec 6 residue). These three
close out the roadmap's Phase 3.

**Two new rows this sweep, both from the Owner's agenda:**
`hot-path-budget-emergency-trim` (rank 4, H) — hot path measured at
38,419 B against 37,888 B ceiling (−531 B), budget violated, next merge
will go red. Regenerate with the command in its Territory cell.
`register-notes-compaction` (rank 6, M) — route historical Notes to
vault, same pointer-not-copy pattern as `token-economy`'s
Territory/Outcome/Intake collapse. Largest remaining single-source token
drain in the ESG read path.

`workflow-file-trim-grandfathered` boosted from rank 8 to rank 5 (token
economy). Remaining rows keep prior relative order, shifted down to
accommodate the two insertions.

**Hygiene:** worktree audit clean (one worktree, one `dcs/*` branch
merged deploy-pending). Both PARKED rows stay parked. No pending
sitreps. No field repairs reported. Delegation unchanged at v4 (sixth
session running). The `token-economy` husk + PID 40876 specimen stands.

