### `token-economy` opened at the `/dcs-new` stem, 2026-07-28 — QUEUED on a territory conflict with an ACTIVE incident, not yet resolved

**Territory check (v0.3, `new.md` step 7b) refused a worktree by default —
this is the mechanical gate working as designed, not a judgment call.**
`token-economy`'s estimated territory includes `dcs/references/doctrine.md`,
`dcs/workflows/plan.md` and `dcs/workflows/execute.md`. `deviation-path-proportionality`
(row above, rank 4) is **ACTIVE**, mid-execution as of this writing (one
Safety halt, fix-tasking just returned, awaiting re-verification — see its
own `214-LOG.md`), and its own refined territory names all three of the
same files. Overlap confirmed by direct comparison of both rows' Territory
cells, not asserted.

**Rank 7 (`safety-officer-incremental-verify`), rank 8
(`esg-artifact-bloat`) and `log-read-scoping-incomplete` are now `KILLED`
and folded into `token-economy`** — the scope decision (combine all six)
was already settled by the Owner; only the *timing* of opening the worktree
was open, and that is decided below. Folded rather than left pending, since
leaving three rows in an indefinite "maybe" state serves no future reader.

**Decided: wait.** Put to the Owner in the same turn this note was first
written — wait for `deviation-path-proportionality` to close, override and
accept merge-conflict risk on the three shared files, or narrow
`token-economy`'s scope to exclude items needing them. **The Owner chose
to wait.** `token-economy` stayed `QUEUED`, territory estimated above,
worktree not yet created.

**Resolved 2026-07-28, same day.** `deviation-path-proportionality`
closed and deployed (merge `f62938b`, integration `e285108`, `/dcs-deploy`
verified marker `0.6.10 → 0.6.11`) — confirmed independently via `git
worktree list --porcelain` (worktree gone) and `git log` (merge commit
present on `main`) before proceeding, not taken on report alone. Territory
re-checked against the full register: no other `ACTIVE` row. Worktree
`C:\DCS-wt\token-economy` and branch `dcs/token-economy` created;
`201-BRIEF.md` and `214-LOG.md` written re-measuring every figure
post-merge (`plan.md` grew 22,996 → 37,634 B from the closed incident's
own `## 6c.` addition, which raised `run.md`'s eager-load figure 94,566 →
111,725 B — folded into the brief as fresh evidence for item 1, not
treated as stale). Row flipped `QUEUED` → `ACTIVE` above.

