### `decomposition-backlog-routing` opened at the `/dcs-new` stem, 2026-07-28 — Type 3, one split, one correction

Territory check clean at open: zero `ACTIVE` rows portfolio-wide. Worktree
`C:\DCS-wt\decomposition-backlog-routing`, branch
`dcs/decomposition-backlog-routing`. **Split at this stem:**
`esg-intake-writeback-gap` (below) — an independent completeness gap in
`esg.md` step 4's Record bullet, found by a situation analyst, kept
separate per principle 4 rather than folded in. **Correction to this
row's own history:** it is not `vault/Backlog.md`-sourced despite the
sibling-row pattern elsewhere in this table — its Intake source
(third-party `direct-resolution-lane` review) was already accurate; only
the assumed Backlog provenance was wrong. **Carried into planning**
(dcs-commander, command point 1): the fix must not hardcode
`vault/Backlog.md` (this project ships no project facts) — route via a
project-documented surface, discovered at runtime, with a
register-as-today fallback; and doctrine principle 4's current wording
("registers each of them") needs amending in the same IAP if a bar ships,
or workflow and constitution contradict.

