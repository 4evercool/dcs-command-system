### `direct-resolution-lane` opened 2026-07-27 — scope SPLIT at the stem, and four things are owed to the next sweep

Worktree `C:\DCS-wt\direct-resolution-lane`, branch `dcs/direct-resolution-lane`,
Type 1. **Territory check clean at open:** zero `ACTIVE` rows portfolio-wide, so
no overlap with a live incident was possible.

**Blocked rows, recomputed at `/dcs-plan` step 5a and now NARROWER than at open:
ranks 6, 8 and 9 only** (`prompt-vs-schema-drift` via `dcs/workflows/**`;
`esg-artifact-bloat` via `dcs/templates/`; `decomposition-backlog-routing` via
`new.md`), plus `register-field-repair-path` and `trivial-work-inline-lane` from
this incident's own split. **Ranks 5, 7 and 12 were blocked at open and are now
FREED** — they collided only through `dcs/references/doctrine.md`, which the IAP
made *forbidden* rather than conditional. Regenerate by intersecting this row's
Territory cell with each `QUEUED` row's; do not read the figure off an earlier
paragraph, which is how this list was wrong once already today.

**The stem was not re-derived.** The brief was already worked by a third-party
review and parked; two analysts were spent only on the queue note's own reopen
condition (*"unless the blast radius changed"*), which was met. Every original
evidence claim held but one, and that one is a hard constraint: **check 15 now
pins `dcs/templates/REGISTER.md`'s `DEPLOYED` paragraph as the sole member of
its declaring population, adjacent to the enum this incident must edit.** The
IC put `tests/test_doctrine_integrity.py` in every tasking's forbidden list as a
result — a red check 15 is fixed by rewording `REGISTER.md`, never by editing the
check.

**Scope narrowed by IC ruling at command point 1** (`new.md` step 4a, doctrine
principle 4): the brief's asserted single root cause is a **model**, not a
defect, so the incident opens on the first concrete defect it causes —
manifestation (a), the one measurably causing harm (3 of 6 Type-5 rows stuck
`QUEUED` in the field census). The keep-together argument was refuted on its own
premise: a split does **not** mean three enum edits, because one incident creates
the state and the others consume it — hence the IC's hard requirement that the
new state be defined **scenario-neutral**.

**Owed to the next `/dcs-esg`, and it must not be discovered as unranked
debris** — the fourth session had to clean up exactly that:

1. **Rank the three rows this split created** — `register-field-repair-path`,
   `trivial-work-inline-lane`, `status-md-enum-drift`. All three carry
   `— (rank owed to the next /dcs-esg)`.
2. **Decide `trivial-work-inline-lane` vs. `decomposition-backlog-routing`
   (rank 9)** — same two sites, same complaint, different axis (triviality vs.
   priority). The IC declined to decide merge-or-sequence and routed it here
   deliberately.
3. **Record the model as a `STRATEGY.md` objective** — *"the register's contract
   for sub-lifecycle resolution"* — which is where principle 4 sends a model.
4. **Escalation trigger (e) is on the record as declined-for-now, not refused.**
   The IC requested ESG activation at command point 1; the Owner chose
   `continue` because the fifth session had run an hour earlier. 209 at
   `.dcs/esg/SITREPS/direct-resolution-lane-p0.md`.

